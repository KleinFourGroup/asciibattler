node_modules/
dist/
.vite/
*.log
.DS_Store
Thumbs.db
.env
.env.local

# Claude Code per-machine local settings
.claude/settings.local.json

# Fuzz harness artifacts — regenerated by `npm run fuzz`
tests/fuzz/output/
output/

# Scratch / experimental work (FE reference data scrape, etc.)
scratch/

# Devlog — local authoring workspace (changelogs, post drafts, shot lists).
# Outward-facing scratchpad; devs cloning the repo don't need it.
devlog/

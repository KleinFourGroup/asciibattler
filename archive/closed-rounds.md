# Closed rounds — the condensed paragraphs (append-only)

Moved here verbatim from HANDOFF.md at the 2026-09-09 trim (user-signed):
the Closed rounds section had grown to 44 % of a file every session reads
cold, duplicating facts whose canonical homes are BALANCE / PATHING /
GOTCHAS / the archived roadmap+worklog pairs. HANDOFF now carries ONE LINE
per round; **each round close appends its condensed paragraph HERE** (newest
first, the same shape) and its one-liner there. Numbers: BALANCE's run log;
blow-by-blow: the round's archived pair + git.


> **🗄️ THE CASUALTY EXPERIMENT — the encounter feel interstitial (Phases 89→94, 2026-09-02 → 2026-09-08): ✅ COMPLETE, every phase user-signed; the project's first experimental round, run under a pre-registered ⛔ keep-or-rollback stop.** **§89 the DATA phase** (criterion 1 pinned — the overkill share 0.61 at pool 20 under survivors) · **§90 the SEAM FLOOR** (kept regardless of the verdict; tag `pre-casualty-experiment`) · **§91 the CASUALTY RULE** (the fallen-power ledger, World v36 · `chipMode` casualties / `capPenalty` survivors · the power table; gotchas #129–130) · **§92 the REBALANCE** (pool max 20 → 40 · the twelve pools at booked burn × the turn targets · the per-encounter power override · the deploy vector re-searched under the rule, `92c2-winner` · prior v4 / roster v2) · **§93 ⛔ KEEP** (criterion 1 0.072 · the feel verdict filed BEFORE the board was shown) · **§94 the ENCOUNTER LIST + the close** (the seven held rows re-scaled + the rule's trims → the config FROZEN at `d9675b6`; the fallen ledger Run v45 + the post-turn screen; "morale" + the chip everywhere + the price book; ⭐ the ARM's DP tail found DEAD since 70e — a constant multiplying zero through two re-pins and two probes — and, wired for the first time, HARMFUL (−0.100 on the walk: the 70e composition mixed the DP's currency with the rollout's) → DELETED byte-identical, gotchas #131–132; the sheet's shape as a fraction + the pacing bands; the 94h signing — every §94 band PASSES on both walk arms, the refs re-pinned, the wall band HELD as a watch, the priest parity breach NAMED, the pacing cost ACCEPTED, prior v5 / roster v3). **Still load-bearing:** the casualty rule SHIPS · the config frozen at `d9675b6` · the sheet lineage AMENDED 2026-09-08 · the burn per turn saturates at the player's kill throughput (the pool is the lever, not the wave) · the named watches + the §92/§94 riders in TODO. Archives: [archive/post-88-roadmap.md](post-88-roadmap.md) + [archive/post-88-worklog.md](post-88-worklog.md) + [archive/encounter-feel-spec.md](encounter-feel-spec.md) + [archive/retro-scratchpad-casualty-experiment.md](retro-scratchpad-casualty-experiment.md); BALANCE 2026-09-03 → 09-08.
>
> **🗄️ Round 6 — Instruments (Phases 84→88, 2026-08-22 → 2026-09-02): ✅ COMPLETE, every phase user-signed.** The balance-instrument round end to end: **§84 the LONG-HORIZON SHADOW** (the §71c shadow at a run-end horizon on the acquisition sites; `decisions.csv` `horizon` rows → `npm run prior:table` → `tests/fuzz/board/prior-table.json`; ⭐ the shadow-only contract byte-identical at batch scale; hops-linearity NO twice-measured; the 84f2 inert-class tripwire) · **§85 the FOLD** (`priorBonus` = λ × the UNSCALED site-conditioned meanΔ [load-time shrinkage `SHRINK_K=80`] on the walk-terminal diff · the ε v2 floor rule · the campRaid site · the 85h twelve amendments [disjoint banks TRAIN 1001–1120 / CHOOSE 2001+ / SIGN 3001+ SPENT; the λ rerun protocol] · ⭐⭐ **λ=0.5 SIGNED into the ARM** 2026-08-27 [SIGN +0.092 z=2.04, pre-specified Δ>0] · finalist-56 DEPLOYED via the hybrid-light arbitrated selection · `pre55ReachRef` RETIRED · gotchas #126–#128) · **§86 PERF** (profile-first: the balancer IS a pathfinding benchmark; four oracle-gated levers; **3.47× scored / 3.32× searcher / 2.95× ARM**; ⭐⭐ the FAIL-CLOSED board split [`manifest.json` provenance · VERDICT/DRIFT/HEALTH · the skill-gradient anchors] = BALANCE §"The board integrity protocol") · **§87 ROSTER REALISM** (`rosters.csv` → `roster-table.json` + `--roster=sampled:<hop>`; the X3 re-read at 41×n=80: the per-encounter spread dwarfs the kind bands · act-2 hot / act-1 soft · the hop bracket a clean null; ⛔ bands HOLD as intent → §89) · **§88 RARITY** (the desk read + the grant cohort → halberdier→rare · rioter→common · janus 40 · ⭐ the miner DAEMONIZED as Dis Pater; BALANCE §"The rarity verification protocol" standing for every new archetype; ⭐⭐ the first fully-MANIFESTED full board [the verdict's maiden-pooled-read catch → `armArgv`]; ⭐⭐ the DERIVED-ARTIFACT TRIPWIRE [`tests/prior-table-coverage.test.ts` + BALANCE §"The derived-artifact registry"] after Dis Pater rode a whole board at prior 0; prior table v3 @`251514d`; the ONE sheet amendment ⭑ 2026-09-02, prose-only). **Load-bearing:** ⭐⭐ the ARM = `--searcher --audition --redraw=level:2 --empower=level:hi --arbitrate --prior-lambda=0.5` (λ≠0 loads the prior table at launch, missing = loud throw) · ⭐⭐ the signed sheet lineage §60e→§68d/f→§72b/f→§83f→§85g6d→⭑88 (seam 15–18 · wall 30–35 · reach 40–50 · WIN DERIVED · act-1 refs at n=120 fold reality · bank 102/70 · tx 0.25/0.775 · fires ~1.7 · forced bosses 0.775/0.675) · every board MANIFESTED at one HEAD (`--allow-unmanifested` = pre-86e archives only; `--only` = a named PARTIAL, never a signing read; amendment boards require a FRESH `--search` derive as the ceiling read) · a catalog/arm change RE-DERIVES the artifacts that measured it (the registry) · site-causal pairs are stream-perturbation-dominated (§85g6c) — paired `--grant` arms at n=80 for thin rows · `scripts/box-drive.sh` + ONE HEAD per cohort (12 boxes / 12 destroyed this round) · named watches → §89: the deploy-walk overperformance on BOTH arms (arb 0.625 / doctrine 0.550) · the walls under band on the 88d3 partial · the gambler shape-flip · the band promote (deferred to the §89 close). Archives (landed 2026-09-02): [post-83-roadmap](post-83-roadmap.md) + [post-83-worklog](post-83-worklog.md) + [round-6-spec](round-6-spec.md); the sweep: [retro-scratchpad-round-6](retro-scratchpad-round-6.md); numbers BALANCE 2026-08-22 → 09-02.
>
> **🗄️ Cluster 5 — Map Content (Phases 73→83, 2026-08-05 → 2026-08-21): ✅ COMPLETE, every phase user-signed.** The content cluster end to end: §73 the hook speedup (`isolate:false`, docs ~45s) · **§74 EVENTS** (page-map grammar → Run phase [**v40→v41**, `eventRng`] → executor → dense placement + every run opens on the starting boon → EventScene → the `eventChoice` arbitration site → the editor → ten user-authored events + prodigy; per-run no-repeat) · **§75 CAMPS** (the neutral third faction: registry [**World v34→v35**] · portal drip · the ~40-site active-neutral widening · leash wander · damage-sourced hostility + kill credit · rewards · TERMINAL_AMBER · both editors · the per-turn enemy PULL; gotchas #122–123) · §76 unit mechanics (gotcha #124) · **§77 the BRAID map** (**v41→v42**; keyed `deriveRng` streams — gotcha #125; the n=500 executable sheet `nodemap-metrics.test.ts`, NEVER relax) · §78 UI/UX (⭐ DESIGN §Input accessibility) · §79 the glyph anchor fix (drift 9.1px→0; self-hosted JBM) · §80 the five C6 `plans/` docs (⭐ ONE store/FOUR consumers) · §81 procedural parity (per-theme tiles + camps; ⭐ dose-independence) · §82 the feel round (single-type reward tables, daemons@1, `rewardOverride` ref list **v42→v43**, ⭐ the catapult RELEASE GATE) · **§83 the closing rebalance** (⭐ `statusImmunities` + healer panic immunity — parity regen INSIDE ±5 · the camp pull's BOSS EXEMPTION **v43→v44** · dose ×1.075 · the §82 flood ACCEPTED · ladder TABULAR · ⭐ the camps probe `sim.campsStartHostile` [forced engagement decisively net-negative; documented] · ⭐ `pre55ReachRef` · the closing board 0 FAIL / 6 WARN at the ⭑ 2026-08-21 sheet). **Load-bearing:** ⭐⭐ the signed sheet now reads §60e→§68d/f→§72b/f→**§83f** (seam 15–18 · wall 30–35 held [floor-hugging 0.265/0.292 ACCEPTED] · reach 40–50 regen / 0.542 ref 55pre · win DERIVED · act-1 refs at n=120 · bank ~111/~90 · fires ~1.7 · fire channel +0.10; parity: the 55pre-shape breaches ride the vector re-derive) · ⭐ the n=120 protocol (decision-feeding arms extend via `--seed-offset=40 --count=80`, SAME HEAD only) · the queue-file box driver + ONE HEAD per cohort · the standing probe tools (`--set` flat-numeric registry + `--event-chance=0`) · ⭐ the two fold riders (campRaid nominator · 55pre re-derive) and the ε-floor re-read wait for the interstitial's fold · the horizon-blindness doctrine now has its sharpest exhibit (K=2 can't tell a daemon from 25 bits). Archives (landed 2026-08-21): [post-72-roadmap](post-72-roadmap.md) + [post-72-worklog](post-72-worklog.md) + [cluster-5-spec](cluster-5-spec.md); the sweep: [retro-scratchpad-cluster-5](retro-scratchpad-cluster-5.md); numbers BALANCE 2026-08-05 → 08-21.
>
> **🗄️ The rollout-arbitration interstitial (Phases 69→72, 2026-07-29 → 2026-08-04): ✅ COMPLETE, all four user-signed.** §69 the arbitration substrate (clone seam · walker · five-site cost model · resolution-4 evaluator · the amended A/A ε methodology) · §70 ALL FIVE DECISION SITES live (`--arbitrate`; ε floors pooled-per-class flat) · §71 decision-grade telemetry end to end (the decisions.csv 18-col sidecar · per-item aggregates + the n=80 floor · the flip instrument · ⭐⭐ THE PARITY TABLE: the arb arm inside paired noise of doctrine — the "crater" was baseline-anchoring · the cheap inner tier validated) · §72 the balance agenda (72a the board twins + run-alongside cycle · 72b ⭐⭐ the wall correction [gotcha #120] + the unified band architecture [the run = a pool-HP budget flow; win DERIVED reach×(1−wall)] · 72c the 2× value buffs [the channels convert] · 72d the cheap tier holds · 72d2 the act-1 ×1.15 one-round hit · 72e draw DISSOLVED under the cap [A/A sha-identical] + elites negative-EV at decision grade + packet cuts · **72f THE SIGNING SESSION: ⭐⭐ the ARBITRATED DEFAULT** [ARM gains `--arbitrate`; the board = 10 arb primaries + 5 doctrine controls; the searcher-tier direct test paired-Δ EXACTLY 0.000 — the cheap tier locked by test, not inference] · the deep-end boss pool split at the ×1.25 dose [walls 0.313/0.348 in the held 30–35; ×1.5 craters the finale — the two-dose bracket caught it] · seam re-signed 15–18 at arb reality · the fire channel re-signed +0.10 [the 72c buffs repaired it; ≈0-by-substitution on the arb arm] · maxHandSize 10 + Surge-3 · closing board GREEN, 0 FAIL / 3 pre-registered WARNs). **Load-bearing:** ⭐⭐ the DEFAULT realistic-bot arm = **`--searcher --audition --redraw=level:2 --empower=level:hi --arbitrate`** (board.ts ARM is the canon) · decisions.csv rides every default-arm batch (per-item decision value is a standing instrument) · the horizon-blindness doctrine (a knob must land INSIDE the rollout horizon to move a decision-grade needle; run-long passives judge at run grade — BALANCE caveat) · probe-shape win rates are SHAPE artifacts · the dose-bracket protocol (unmeasured elasticity → two-point bracket before a magnitude signs) · the §72b/§72f signed sheet supersedes §68d/f (seam 15–18 · reach 40–50 · wall 30–35 · win derived) · gotchas #120 (sector-aware hop reads) + #121 (the sectorAdvanceConfig slice). Archives: [post-68-roadmap](post-68-roadmap.md) + [post-68-worklog](post-68-worklog.md) + [rollout-arbitration-spec](rollout-arbitration-spec.md) + [retro-scratchpad-rollout-arbitration](retro-scratchpad-rollout-arbitration.md); numbers in BALANCE §§69–72f.
>
> **🗄️ Cluster 4 — Drafting & Identity (Phases 61→68, 2026-07-21 → 2026-07-29): ✅ COMPLETE, all eight user-signed.** The C4 chain + the absorbed riders, end to end: §61 rarity (four tiers legendary-top, the 2-draw weighted sampler, the USER-SIGNED 5/3/3/2 assignment, background-tint accents, the 1/1.5/2/3 price seam via `unitPriceFor`, `ranged`→`archer`) · §62 the hcloud box launcher (ON-DEMAND; box-batch.sh the only driver) · §63 the three starting characters (Soldier/Priest/Gambler; **Run v37→v38**) · §64 the three drafting daemons · §65 the draw/packet dials (`--draw-add`; the +2-vs-+4 non-monotonicity flagged → the interstitial) · §66 boss forewarning (**v38→v39**, pre-rolled identity+layout) · §67 the second sector **"The Deep End"** (**v39→v40**, the `sectorCleared` gate; world identity LOCKED: "dark fantasy on a haunted terminal") · **§68 protocol v2 + the balance pass** (the executable board vs signed-sheet.json · ⭐ the §68d/f SIGNED SHEET supersedes §60e · ⭐ character parity ±5 ACHIEVED — a signed design principle, defect repaired live via four buffs · the two-act cliff decomposed into two defective entries → minHop gates 6/3 + trims · prices dispositioned · the fire channel re-signed ≈0, repair → the interstitial · the instrument registry stamps the kit). **Load-bearing:** per-batch character isolation, Soldier = the continuity anchor · the consumption contract + the new-mechanic shipping checklist · `--grant` paired same-seed = realized value, price against it · the n=80 floor for per-item signals · elite reads are censoring-aware (the arrival-pin caveat) · the walk-wall/two-act re-read WAITS for the interstitial's ceiling move (the board's four two-act WARNs = the deliberate marker). Archives: [post-60-roadmap](post-60-roadmap.md) + [post-60-worklog](post-60-worklog.md) + [cluster-4-spec](cluster-4-spec.md); numbers in BALANCE (Protocol v2 + the dated 2026-07-27→29 run-log).
>
> **🗄️ The micro/balance-realism round (Phases 53→60, 2026-07-11 → 2026-07-21): ✅ COMPLETE, all eight user-signed.** The interstitial between Clusters 3 and 4 that closed the §52 calibration gap: §53 the trace recorder + human gauntlet (the ~80% human baseline, §53g) · §54 traffic scripts + the spot-check board · §55 the cell-Goodhart doctrine (probes arbitrate, cells attribute — now a BALANCE caveat) · §56 the generalized swap engine · **§57 the AUDITION SEARCHER** (candidate supply, not arbitration, was the bottleneck; K=2 locked; bot ~60 vs human ~80 — the gap HALVED; VERDICT YES ×3, pre-registered) · §58 the earned no-op (searcher-ate-it) · §59 the economy strategy layer (WASH — expressive but outcome-neutral) · **§60 the REAL balance pass**: fire-ablation (fires real, +5/−0) · the port-toll ladder (the toll IS the policy) · ⭐⭐ the grant-consumer catch → **the EXTENDED realistic-bot arm** (`--searcher --audition --redraw=level:2 --empower=level:hi`, doctrine-grade) · daemon reprice + drop probe · the per-boss split (the King 33.3% IS the wall; the Queen reads softer) · ⭐ **the SIGNED RE-ANCHOR SHEET** (bot 60–67/62 held-out · wall band 30–35 · channels +5/+5 non-stacking · bank idle-high accepted; BALANCE §60e). Held-out verify: both instrument vectors at exactly 62.5%. **Load-bearing:** the extended arm is the default for every skill-anchored read · the §60e sheet = the design anchors until §68 re-anchors · force-isolate rolled encounters before tuning · price against REALIZED value · two mid-round crash recoveries cost zero work (determinism + commit discipline). Archives: [post-52-roadmap](post-52-roadmap.md) + [post-52-worklog](post-52-worklog.md) + [micro-round-spec](micro-round-spec.md); numbers BALANCE §§53g–60e.
>
> **🗄️ Cluster 3 — Economy (Phases 47→52, 2026-07-07 → 2026-07-11): ✅ COMPLETE & user-signed-off.** The earn-store-use-spend loop end to end: §47 the **Rule vocabulary keystone** (`Rule = modifier | hook` over run-stat folds; battleRules compile-in + serialized-tally settle — the two seam crossings; uncapped multi-daemon by id; the bits substrate) · §48 rewards (tables + the serialized reward phase + the bits overlay + `bitsMultiplier`) · §49 packets & cache (the guided fire strip, `passIsFinal` TRUE; cache size DERIVED via fold, never flagged) · §50 ports (`$` node + stock + five transactions; the `removeRosterUnit`/`appendRosterUnit` chokepoint pair — gotcha #118) · §51 cohesion (NOT a no-op; source-labeled declinable tally portions) · §52 the re-scoped docs-only close. Carried **Run v24→v37 / World v32→v34**; gotchas #116–118. **Load-bearing:** sell/refund = raw `addBits` (the mint guard vs Moneta-style `bitsGain` folds — fold at EARN sites only); injected rules carry `{rule, sourceId}` provenance; the reward phase interposes at ANY turn gate; ⚠ the §49 shrink flow has no shipped trigger content. **The §52 calibration finding: bot ~30% vs human ~80%** — economy numbers ship LAUNCH-ROUGH BY DESIGN, the whole tuning agenda + the boss-wall rider live in the micro interstitial round (BALANCE §52 + worklog §52). Riders: boss-wall RE-SCOPED there; renderer "queued"-stance stays in [TODO.md](../TODO.md). Archived 2026-07-11: [post-46-roadmap](post-46-roadmap.md) + [post-46-worklog](post-46-worklog.md) + [cluster-3-spec](cluster-3-spec.md).
>
> **🗄️ The Pathfinding Audit round (Phases 42→46, 2026-07-04 → 2026-07-06): ✅ COMPLETE & USER-CONFIRMED.** The interstitial movement-intelligence audit between Clusters 2 and 3 (the Post-N O→R precedent). All four playtest symptoms addressed (§43 bias fixes — "no drift I can ID at all" · §44 the decision protocol: [positioning.ts](../src/sim/positioning.ts) + the instantaneous, deliberately-unregistered [WaitAction](../src/sim/actions/WaitAction.ts) · §45 cooperation — "dithering WAY down"/👍 · §46a the verdict); **the School-2/3 gate = NO — well-tuned School 1 is enough**, WHCA\*-lite/flow-field seams documented not built; §46b balance = **ACCEPT + RE-BASELINE, no config change** (the ceiling genuinely moved — symmetric cooperation, asymmetric benefit at the boss wall). The whole round is serialized-shape-neutral (no snapshot bump). **Canonical detail: [PATHING.md](../PATHING.md)** (permanent run-log — append at any future movement change) + [BALANCE.md](../BALANCE.md) §46b; blow-by-blow in git + the archived round doc.
>
> **Standing movement rules (permanent):** the [drift.test.ts](../tests/pathing/drift.test.ts) QUALITY GATES survive every re-baseline — **NEVER relax a gate** (drift bounds · dx sign-mixed · riverFork osc ≤ 0.5 · corridor throughput floors · river ttfc · riverFork move mass); `baseline.test.ts` exact pins re-pin on every DELIBERATE movement change; **the round's doctrine: DERIVE-DON'T-CACHE** (vacancy ETAs + route stability derived from `activeAction`/serialized state, never serialized themselves; the §45 dials live in `config/sim.json`); ⚠ gotcha #114 recurrence: `positioning.ts` takes `minRange` as a PARAM — never import archetypes.ts there; the FIXED-VECTOR probe (re-run `best-strategy.json` around engine rounds) = the cheapest ceiling-drift read; **NO RNG in movement — symmetric rules only**; ⚠️ labyrinth stays an intentional slow maze.

> **🗄️ Cluster 2 — Spatial & Movement (Phases 35→41): COMPLETE & user-confirmed.** Full as-built archived → [archive/post-34-roadmap.md](post-34-roadmap.md); per-phase detail in git + [BALANCE.md](../BALANCE.md). In one breath: the §35 occupancy core ([occupancy.ts](../src/sim/occupancy.ts) — every spatial query routes through it; `cellsOccupiedBy` footprint + `OccupancyPlane` seams; `World.shove` the de-overlap/knockback primitive) · §36 non-instant moves (destination CLAIMED on move-start; logical position flips at the 50% mark; claims block pathing — **gotcha #113**) · §37 terrain/palettes (the `TileDef` cost+mod table, [TileGrid.ts](../src/sim/TileGrid.ts)) · the §38 data-driven `UnitDef` keystone ([units.json](../config/units.json), catalog reads CALL-time — **gotcha #114**) · §39 N×N footprints (`position` stays the canonical corner) · §40 destructibles (HP-presence ⇒ destructible; auto-target rubble; **WorldSnapshot v31→v32**) + auto-target-aware connectivity ([layoutConnectivity.ts](../src/sim/layoutConnectivity.ts)) · §41 the closing balance pass = **a documented NO-OP** (the §33 caster-summoner equilibrium HOLDS; run-log in BALANCE.md §41). Flight design LOCKED / build deferred (seams planted §38). ⚠️ `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow).

> **🗄️ Cluster 1 — Combat Depth (Phases Y→34): COMPLETE & user-confirmed.** Full as-built archived → [archive/post-x-roadmap.md](post-x-roadmap.md); per-phase detail in git + [BALANCE.md](../BALANCE.md) + the project memory. In one breath: the data-driven `EffectAbility`/`EffectAction` attack/effect keystone (legacy attack classes + the determinism oracle DELETED) · non-stat statuses (burn/bleed/poison/blind/confusion/panic/frozen, periodic + behavior axes) · new ops (status-on-hit / chain / summon + the 9-archetype demo roster) · the attack & archetype-create editors · effect-scaling (`ScaledValue`) · FX·SFX·status-viz · the §33 balance re-derive (a caster-summoner equilibrium; merc+ranged duopoly dissolved) · §34 polish (double-KO soft-lock + §29 ability-detail rows). Carried World → v30 / Run → v23. Load-bearing Cluster-1 facts still in play: every combat verb is one `AbilityDef` (`config/abilities.json`, [src/sim/effects/](../src/sim/effects/)); statuses are DEF-RESOLVED by key ([statusEffects.ts](../src/sim/statusEffects.ts) — a runtime `StatusEffect` carries only `nextTickAt`/`sourceUnitId`/`magnitude`); the renderer-owned FX registry ([fxRegistry.ts](../src/render/fxRegistry.ts), one `FxKey` = visual+SFX, boot-asserted); `applyDamage`/`dealDamage` ([World.ts](../src/sim/World.ts)) is the single damage chokepoint (`bypassDefense`/`evadable`); ⚠ the action registry holds NON-verb ids only (a verb's `EffectAction.id` == its `AbilityDef` id — registering a colliding id would shadow the snapshot-decode fallback).

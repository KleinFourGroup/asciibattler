# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

> ## 🧭 ACTIVE — **Cluster 1: Combat Depth** ([ROADMAP.md](ROADMAP.md)). **READ ROADMAP.md FIRST.** The encounter round (S→X) is **content-complete** (the X-band re-derivation shipped — a cheap loosen/tighten if the playtest reads off, not a blocker). The post-X meta-roadmap ([META-ROADMAP.md](META-ROADMAP.md)) is locked (6 ordered clusters); **Cluster 1 is the live ROADMAP.md** (Phases **Y→31**); source brief archived at [archive/cluster-one-feedback.md](archive/cluster-one-feedback.md). **✅ DONE & user-confirmed:** **PHASE Y** (the data-driven attack/effect keystone — every combat verb is now the one `EffectAbility`/`EffectAction`; the legacy ability+action classes + the determinism oracle + the legacy `AbilityConfig` config are ALL DELETED; one `config/abilities.json` `AbilityDef` catalog read via `abilityDef`/`damageOpOf`/`healOpOf`; **WorldSnapshot v26**), **PHASE Yb** (`5434d99` — speed-scalable timeline phases + a first-class `AbilityDef.name`), **PHASE Z** (the FX seam — Z1 `d44c2de` renderer FX registry + retire the ad-hoc magic/catapult events, Z2 `f2ebdc8` camera shake, Z3 `33e9791` melee-shove/bow-tracer re-home onto `action:phase`). **PHASE 27 (statuses, periodic axis) IN PROGRESS — 27a (`98b29b4`) + 27b (`940d55e`, the periodic DoT/HoT engine + WorldSnapshot v26→v27) + 27c (the four statuses authored) SHIPPED & green (1257 main + 210 fuzz), all HEADLESS** — the catalog is authored but NOTHING applies it in production yet (tiles→27d, status-on-hit→§29), so there's no observable surface and no playtest yet. **NEXT = the 27d+27e PAIR** (tile unification → burn/rejuvenate + the status viz) = the FIRST observable surface — **PAUSE there for the user's native-browser playtest.** See the **Phase-27 as-built** block.
>
> **The round in one breath:** turn hand-coded attack classes into a **data-driven attack/effect model** (the keystone — the most-referenced schema in the whole meta-roadmap), then build the depth on top: non-stat status effects (burn/bleed/poison/blind/confusion/panic/frozen) + new attack mechanics (chain / status-on-hit / summon), plus the attack editor + archetype-editor "create." **Phase lettering: A→X used; Cluster 1 = Y, Z, then NUMBERS — 27, 28, 29, 30, 31** (Z is the 26th letter; count past it).
>
> **The phase sequence (full Shape/Cost/Tests/Decisions per phase in ROADMAP.md):**
> - **Y — Keystone ✅ DONE:** the `EffectOp`/`TargetSelector`/`AbilityDef` vocabulary (`src/sim/effects/`) + the generic `EffectAction` interpreter; strangler-migrated every combat verb to data (the 6 attack classes + Dash, collapsing the ability `kind` discriminator), each proven byte-identical via the (now-retired) determinism oracle; then Y5 deleted the dead legacy classes + the config (`WorldSnapshot v26`). FX preserved as-is (cleaned in Z). **Y1–Y5 all shipped — see the "Phase-Y as-built" block.**
> - **Z — Renderer comms ✅ DONE (Z1+Z2+Z3):** opaque `FxKey`s on defs + a renderer-owned **FX registry** + a boot assert; retired the ad-hoc `magic:detonated`/`catapult:fired` events (Z1 `d44c2de`); **camera shake** as an fx key (Z2 `f2ebdc8`); **Z3** re-homed the melee shove + bow tracer + their whoosh off `unit:attacked`/`unit:missed` onto `action:phase` fx keys (heal cue stayed on `unit:healed`; the damage-coupled hitsplat/HP/Miss stay on the data events). No bump. See the **Phase-Z as-built** block.
> - **27 — Statuses, periodic axis (IN PROGRESS):** extend K1 with `StatusDef` (DoT/HoT) — burn/bleed/poison/rejuvenate; the `status:applied/ticked/expired` lifecycle events (= the in-battle status display); **retire the D7.B tile-damage pass** (fire→burn, healing→rejuvenate). **WorldSnapshot bump (DONE: v27).** Sub-steps: **27a ✅** schema/registry, **27b ✅** the periodic engine + bump, **27c ✅** the 4 statuses authored (all three headless, green), **27d** tile unification (first observable), **27e** the status viz (renderer subscribes to `status:*` → §Z registry). ⚠️ **Sequencing for the fresh session:** 27d deletes the D7.B pass + retires the `unit:burned` hitsplat, but the replacement `status:ticked` viz is 27e — so do **27e WITH or BEFORE 27d** (or the playtest sees fire damage with NO visual feedback). Recommend pairing 27d+27e into ONE observable commit, then PAUSE for playtest.
> - **28 — Statuses, behavior axis:** blind/confusion/panic/frozen as **decision-hooks** the existing selector/targeting/movement consumers read (the status system holds NO AI logic). Reuses 27's serialized shape (no bump).
> - **29 — New mechanics:** status-on-hit (pure composition), chain (recursive op), summon (caster-anchored placement + per-caster `maxLive`). No bump expected.
> - **30 — Dev tools + content:** the **attack editor** (recursive form builder) + the **archetype-editor "create"** expansion + the demo-consumer roster (§30 table). FontAtlas is FULL (32/32) → a grid resize. Browser-verify glyphs.
> - **31 — SFX + the closing balance pass** (BALANCE.md 5-step loop, scoped to the NEW content — the migration is balance-neutral by construction).
>
> ### Phase-27 as-built (27a+27b+27c SHIPPED & green; the periodic axis) — what 27d+ build on
>
> **The keystone decision — DEF-RESOLVED by key (user-locked):** a runtime `StatusEffect` (K1, [statusEffects.ts](src/sim/statusEffects.ts)) for a status carries ONLY the runtime cursor; the op / interval / duration are resolved from `STATUS_DEFS[effect.key]` at tick time (NOT serialized). So the serialized effect gained just `nextTickAt?` (per-unit tick cursor) + `sourceUnitId?` (attribution; `null` = environmental) — and **§28's behavior axis resolves the SAME way (def by key) → adds no serialized state → no §28 bump** (the roadmap's prediction holds by construction). ⚠️ a status only ticks if its `key` is in `STATUS_DEFS` — so a TEST with a fixture def must inject it into `STATUS_DEFS` (file-isolated; see [statusPeriodic.test.ts](src/sim/statusPeriodic.test.ts)).
> - **27a (`98b29b4`):** `StatusDef` schema [statusSchema.ts](src/sim/effects/statusSchema.ts) `{id, name, durationSeconds, merge, periodic?{everySeconds, op}, fx?}` — `merge` = the brief vocab `refresh`/`add`/`instances`/`ignore`; `periodic.op` restricted to a `damage` DoT / `heal` HoT via `PeriodicOpSchema` (in [schema.ts](src/sim/effects/schema.ts)). **`statMods` + `behavior` DEFERRED to their consuming phases (§28/§31)** — config isn't serialized, so adding them later costs no bump. Catalog `config/statuses.json` ships **EMPTY** (27c authors content) + [src/config/statuses.ts](src/config/statuses.ts) (`STATUS_DEFS` / `statusDef(id)` / `assertStatusRefsResolve` — the `applyStatus` ref boot-check, wired onto the [abilities/registry.ts](src/sim/abilities/registry.ts) boot IIFE, cycle-free via args).
> - **27b (`940d55e`):** the engine. `StatusEffect` + `nextTickAt`/`sourceUnitId`; `MergePolicy` + `'ignore'` (no-op-if-present, reserved); **`mergeEffectInto` PRESERVES `nextTickAt`/`sourceUnitId` on re-apply** (a `refresh` tops up DURATION without shifting the tick phase — so a fire tile re-stamping burn every tick never pushes the next tick away; the first applier keeps credit). `Unit.expireEffects` now RETURNS the removed effects. The bridge [statusRuntime.ts](src/sim/effects/statusRuntime.ts) (`buildStatusEffect` + `statusMergeToPolicy`: refresh→replace/add→add/instances→independent/ignore→ignore). **World:** extracted **`dealDamage`** = the shared mitigation+HP+ledger chokepoint (every HP-reducing source routes through it); `applyDamage` is now its combat wrapper (to-hit + `unit:attacked` + triggers, **byte-identical**) and gained **`bypassDefense`** (wired through the interpreter's `executeDamage` too — false today, byte-identical). New **`applyStatusEffect(target, def, sourceUnitId, magnitude=1)`** = the apply chokepoint (fires `status:applied`); **`applyPeriodicEffects()`** pass in `tick()` (before `applyTileEffects`, reaped by the existing `reapDead`); `status:expired` fires from the expiry sweep. **WorldSnapshot v26→v27, reject-stale** (RunSnapshot unchanged, v23).
> - **Events** [events.ts](src/core/events.ts): `status:applied`/`status:ticked`/`status:expired` `{unitId, statusId, sourceUnitId: number|null}`. ⭐ **`status:ticked` ALSO carries `amount`** (post-mitigation HP delta) — a deliberate refinement over the roadmap's lean sketch: the periodic tick does NOT also emit `unit:attacked`/`unit:healed` (no double-cue, no null-attacker dance), so 27e renders the number + flavor off this ONE event. **Flagged to the user; revisitable.**
> - **27c (SHIPPED) — the four periodic statuses** in [config/statuses.json](config/statuses.json) (catalog now real; was empty): **burn** (`refresh`, 4 s, `damage` 2/s, `bypassDefense`), **bleed** (`add`, 4 s, 2/s), **poison** (`add`, 6 s, 1/s), **rejuvenate** (`refresh` HoT, 4 s, `heal` 1/s). All DoT ops author the LOCKED default (`scaling:'none'` flat `might`, `bypassDefense:true`, `evadable:false`, `critable:false`). **Durations/magnitudes are PLACEHOLDERS → tune §31.** No `fx` keys yet (those + the §Z registry entries + a status-fx boot-check all land in **27e**). **Headless/inert** — nothing applies these until 27d/§29, so fuzz stays byte-identical. Balance-proof ([statuses.test.ts](src/config/statuses.test.ts)) derives from `tiles.json`: burn rate (`might/everySeconds`) == `fire.damagePerSec`, rejuvenate == `healing.amountPerSec`. The `config/statuses.json` → `/__save-config` add stays deferred to §30.
> - **27d notes:** fire tiles apply/refresh `burn`, healing tiles apply/refresh `rejuvenate` (both `refresh` → standing reapplies + lingers after stepping off). **DELETE** `World.applyTileEffects` (the D7.B pass; I left a marker comment in `tick()`) — but **KEEP `reapDead`** (it now reaps periodic-status kills). Retire `unit:burned` + its renderer handler (the burn hitsplat moves to `status:ticked`); **`unit:healed` STAYS** (ability heals still use it). All damage flows through `applyDamage`/`dealDamage`. ⚠️ pair with 27e (see the §27 sequencing note above).
> - **Tests:** 27a (12 schema + 5 registry), 27b (10 periodic — cadence / first-tick-one-interval-after-apply / bypassDefense both ways / source-vs-environmental attribution / DoT-kill-reaped-same-tick / HoT-clamp / add+refresh+ignore merge / snapshot round-trip), 27c (the registry-content + DoT/HoT-shape + tile-rate balance-proof split, replacing the 27a empty-catalog assertion). **1257 main + 210 fuzz:smoke green; typecheck + lint clean.** Fuzz still byte-identical (nothing applies a status ⇒ the periodic pass is a production no-op).
>
> ### Phase-Z as-built (Z1+Z2+Z3 SHIPPED) — the FX seam, now CLOSED
>
> The renderer owns a **pure-data FX registry** ([fxRegistry.ts](src/render/fxRegistry.ts)) — `FxKey → FxDescriptor` (channels `sound`/`projectile`/`burst`/`shake`/`shove`/`tracer`) + **`assertFxKeysResolve(defs)`** (boot check — a typo'd key throws at battle start); headless-tested, pixels eyeballed. **FX driver = `BattleRenderer.onActionPhase`** def-resolves `actionId → def.fx[phase] → registry`; **one `FxKey` carries BOTH visual + SFX** (LOCKED). **27e wires the status-viz the same way** (renderer subscribes `status:*` → registry).
> - **Z1** (`d44c2de`) registry + driver + mage/catapult re-home (`{release:…_launch, impact:…_burst}` in [config/abilities.json](config/abilities.json)) + retired the ad-hoc `magic:detonated`/`catapult:fired` events (catapult dud now ALWAYS fires).
> - **Z2** (`f2ebdc8`) camera shake = Renderer-owned ([Renderer.ts](src/render/Renderer.ts) `shakeCamera`), transient apply/clear around render. Per-key magnitude catapult `0.16/0.35s`, magic `0.06/0.22s` — ⭐ **user-loved, DO NOT CHANGE.**
> - **Z3** (`33e9791`) `shove`+`tracer` channels + `melee_swing`/`ranged_shot` keys on the strike defs (`sword`/`club`/`katana`/`whip`→`{impact:melee_swing}`, `bow`→`{impact:ranged_shot}`, `gambit_strike`→`{windup:…}`); re-homed the melee shove + bow tracer + their whoosh off `unit:attacked`/`unit:missed` onto `action:phase` (fire on **hit AND miss for free**); `onUnitAttacked`/`onUnitMissed` kept **hitsplat+HP only**, heal cue stayed on `unit:healed`. No snapshot bump.
>
> ### Phase-Y as-built (the keystone facts Z+ build on)
>
> **⚠️ THE ID-COLLISION GOTCHA (still load-bearing for Z+):** a migrated verb's `EffectAction.id` == its `AbilityDef` id. [actions/registry.ts](src/sim/actions/registry.ts) registers ONLY the non-verb actions (move/spawn/swap); every other id falls through to `EffectAction.fromData`. If you ever register a new action whose id EQUALS a verb's def-id, that factory would SHADOW the fallback and mis-decode a snapshot — keep the action registry to non-verb ids only.
>
> **The keystone (full per-step detail in git + ROADMAP §Y; Y is fully closed & user-confirmed):** every combat verb is the single generic `EffectAbility`/`EffectAction` interpreting an `AbilityDef`'s `effects:[{phase,op}]` over the F2 `'fill'` timeline — vocabulary in [src/sim/effects/](src/sim/effects/) (`EffectOp`/`TargetSelector`/`AbilityDef` [schema.ts](src/sim/effects/schema.ts) + timeline/targeting/reposition/interpreter/EffectAction + the `propose.ts` bridge). **`scaling` lives on the damage op** (`damageStatFor` SURVIVES for the HUD "ATK"); **`priority` = the proposal score**; gambit = `damage@windup`+`move-retreat@impact`; dash selector = `self`; **`EffectAction.phaseTarget` switches on the SELECTOR** (aoe→targetCell, self→nothing, single-target→targetId, +fizzle also surfaces the cast cell). `affects:'enemies'` = "not the caster's team" (the neutral exclusion is the separate `isCombatTargetable` guard, a Cluster-2 seam). One **`config/abilities.json`** `AbilityDef` catalog ([src/config/abilities.ts](src/config/abilities.ts), `abilityDef`/`damageOpOf`/`healOpOf` — NOT in `/__save-config` until §30); **WorldSnapshot v26** (rejects pre-migration in-flight saves). `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow).
>
> **Decisions LOCKED with the user (2026-06-22, this planning session):**
> - **`move`** = caster-reposition only (ports Dash + GambitStrike in §Y); **`knockback`/`pull` = a reserved-but-unimplemented union seam, deferred to Cluster 2** (touch the occupancy/collision core once, in the round built to harden it — like F2's `re-home`).
> - **Summon** = caster-anchored placement: `at:TargetSelector` names an **anchor** (default `self`); the op places into the **nearest free cell(s)** to it (reuse the [actingPosition.ts](src/sim/actingPosition.ts) BFS) — `at:self`=adjacent-to-caster, a target anchor=flank summon, `aoe` anchor=multi-summon (one resolver). Per-`SummonSpec` **`radiusCells` (default 2)**; **fizzle** if no free cell in radius. **Per-caster `maxLive`** cap (single digits) → bounded unit count → **object-pooling stays parked**.
> - **Enemy/encounter AI** = OUT of scope until Cluster 5 (camps); §29 ships only code-thin `priority`-based selection (no data-driven AI scoring).
> - **Demo consumers** (the §30 content floor): a **Summoner + summoned minion**, a **chain caster**, and **bleed/poison + behavior-trio afflicters** (status-on-hit is their vehicle — no extra units); spread across both teams. Actual stat blocks designed at spec time.
>
> **Still open (resolve at the phase):** §27 content dials (durations/magnitudes → §31); §30 exemplar-content scope + how many "disruptor" archetypes carry blind/confusion/panic; §31 archetype-rebalance fold-in. (Full list: ROADMAP §"Open decisions.")
>
> **Live seams (built in Y, load-bearing for 27+):** `applyDamage`/`dealDamage` ([World.ts](src/sim/World.ts)) = the single damage chokepoint (every HP source routes through it; `bypassDefense`/`evadable` flags); K1 ([statusEffects.ts](src/sim/statusEffects.ts) + [triggers.ts](src/sim/triggers.ts) + `Unit.effects`/`effectiveStats`) = the status base — **extend, don't replace**; the F2 timeline ([Action.ts](src/sim/Action.ts)) = the scheduling spine; AoE→units via `unitsInCells` (the Cluster-2 footprint seam, single-cell today). Full Y-seam + build-pattern detail in git + ROADMAP §Y.
>
> **Conventions:** headless-first (the interpreter, every op, the status fold, the behavior hooks are pure — vitest before the browser); **the determinism trace is the equivalence oracle in Y**; balance-proof tests derive from config (never hardcode); keep DESIGN/ARCHITECTURE honest in-commit; commit per logical change + **pause between commits** for the user's playtest. ⚠️ `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow) — don't delete them alongside the migrated attack classes.
>
> **Tree state:** on `main`. WorldSnapshot **v27** (27b — periodic status fields on `effects[]`) / RunSnapshot **v23**; **1249 main** + **210 fuzz:smoke** green; typecheck + lint clean. HEAD = **27b (`940d55e`)**, on top of 27a (`98b29b4`). **Phase Y is fully closed** — EVERY combat verb is the data-driven `EffectAbility`/`EffectAction`; the legacy ABILITY+ACTION classes + the oracle + the legacy `AbilityConfig` config are ALL DELETED. `actions/registry.ts` registers only move/spawn/swap (every other id falls through to `EffectAction.fromData`); there is one `config/abilities.json` `AbilityDef` catalog read via `abilityDef`/`damageOpOf`/`healOpOf`. **Phase Z COMPLETE & user-confirmed. PHASE 27 IN PROGRESS — 27a + 27b SHIPPED & green (headless, not yet playtested); NEXT = 27c** (author the statuses), then pair 27d (tile unification) + 27e (status viz) into one observable commit and PAUSE for the user's playtest. See the **Phase-27 as-built** block.

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phases A–D complete** — the pre-combat foundation (selectors/commands/fuzz/config/scenes · palette+bloom/bars/scanlines/audio/root `@` · terrain/walls/LOS/prisms/layout-editor · renderer-cap/drag-paint/variable-sizes/fit-scroll-camera/spawn-overflow/half-cover/chasm+fire+healing/theming). Detail: [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).
- **Phase E complete** — combat depth (E1–E7): the stat vocabulary + `deriveStats`, the generic ability layer, per-archetype leveling, `TICK_RATE` 10→20, the XP/promotion loop + half-cover, pathfinding + combat visuals, and the six archetypes (melee/ranged + rogue/healer/mage/catapult). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase F complete** — Phase-E playtest response (F1–F6): draftable E7 archetypes, the first-class action-phase timeline (windup→…→recovery), projectile/impact timing, rogue gambit sequencing, heal VFX + heal/utility XP. Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase G complete** — run progression (G1–G5): `RunConfig` + short-run harness, longer maps + the full-viewport map scene, rest/boss nodes, the enemy level-*budget* model + recruit leveling, and the fuzz-strategy factory + GUI. Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase GP complete** — combat & stat-legibility playtest response (GP1–GP5): the `endurance→mobility`/`speed→agility` rename + universal move base, the `defense` stat + `World.applyDamage` consolidation, recruit-card/HUD legibility, ranged/catapult acting-position pathing, and the healer chokepoint yield (`SwapAction` + centroid snap). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase H complete** — the multi-turn deckbuilder battle trial (H1–H7): the `power` stat, spawn-tile range, the deployment/fatigue hooks, the health-pool encounter→turn loop + pool HUD, the card-drawn hand, rest-heal/recruit-pass, and the search-driven balance tooling (difficulty band `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance). Detail in [ROADMAP.md](ROADMAP.md) §Phase H + [BALANCE.md](BALANCE.md).
- **Phase I complete** — foregone conclusions & combat identity (I1–I6): the `agility→speed` revert + `precision`/`evasion` dodge stats, the subtractive hit/miss roll + "Miss" hitsplat (`hitChanceBase` 0.6, turn caps →150s), 1×/2×/3× fast-forward, the schema-driven archetype editor + `/__save-config`, the melee subclass family (mercenary/Adventurer/Ronin/Bandit), and the per-ability combat profile (`might`/`accuracy`/`critBase` + evadable/critable gates, weapon split sword/club/katana/whip/bow; World v21→v22). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase I + [BALANCE.md](BALANCE.md).
- **Phase J complete** — the in-battle objective system (J1–J4). J1 (the objective MODEL + targeting preemption, sim/headless: one shared player-team objective, `tile` persist / `enemy` auto-clears, engage radius `min(attackRange, rangedLeashCells)` + retaliation; **WorldSnapshot v22→v23**, byte-identical with no objective). J2 (the pathing SEAM: a movement-intent primitive in [movement.ts](src/sim/movement.ts) over the pure `routeToward` wrapper = the cache boundary; **cache DEFERRED seam-first** + a `pathfindingStats` bounded-recompute guard). J3 (objective UI + the runtime-rebindable [Keybindings](src/ui/Keybindings.ts) registry — Phase J's first playtestable surface). J4 (fuzz objective strategy + arena harness + the `--objective` flag — dev-only, zero `src/` change). As-built detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase J + the git log.
- **Phase K complete** — status effects & pre-battle agency (K1–K4). K1 (the generic status-effect system [`StatusEffect`/`foldEffects`/`effectiveStats` + the full trigger set] + fatigue migration; **World v23→v24 / Run v11→v12**, byte-identical at default). K2 (roster/hand decouple 10/6 + the latent H5 wave-size bug fix — `rollEnemyWave` sized off the whole roster not the fielded hand; `enemyArcherRatio`→0.3, band re-swept). K3 (the redraw mechanic, turn-intro gate + `maxCardsPerTurn` dial; **Run v12→v13**) + K3.5 (ONE MAP PER ENCOUNTER; **Run v13→v14**). K4 (the pre-turn empower; **Run v14→v15** — MEASURED **band-moving**, +21…+27 wins/200). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase K + [BALANCE.md](BALANCE.md) (K3c3 / K4c3).
- **Phase L complete** — L1 daemons (relics): the four-idol catalog (Mars/Minerva/Mercury/Janus) + **DAEMON-ONLY gates** (the K3/K4 static enables ship false) + one uniform roll at run start + `chance` as a first-class gate + the PreTurnScreen idol surface + fuzz `--daemon` with per-idol telemetry; **Run v15→v16**. Minerva shipped +4 DEF, **nerfed +4→+2 same-day** (the L1c3 outlier). The `battleTrigger` daemons + multi-daemon economy DEFERRED to the shop/loot round. Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase L + [BALANCE.md](BALANCE.md) §L1c3.
- **Phase M complete** — progression & presentation (M1–M6): the per-turn promotion cadence (**Run v16→v17**, band-neutral), the staggered level-up screen + LOCKED leveling curve `50/1.1`, scene polish, the battle backdrop (apron/fog/mist floor), the layout-editor save-to-config, and the water mechanic + from-scratch procedural-map rework (`?layout=procedural`). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase M.
- **Phase N complete (bar N4)** — rogue mobility & balance closure: N1 the rogue DASH (the ability `kind` union + an aggressive-close `DashAbility` on J2's movement-intent seam + the `unit:dashed` event), N2 the band re-sweep → **LOCKED `1.25 × 1.5`** (a mercenary+ranged duopoly), N3 the leveling check (GREEN at `50/1.1`), + the turn-cap unification (`resolveAsDraw`-at-cap across harness/arena/live). **N4 (overnight out-of-sample verify) DEFERRED INDEFINITELY to a VPS** (the dwm leak). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase N + [BALANCE.md](BALANCE.md).
- **Phase O complete** — the Post-N agency round's typed objective refactor (O1–O5). O1 (always-an-objective TYPED per-team model `atWill`/`engage`/`hold`, enemy inert-but-symmetric; **WorldSnapshot v24→v25**), O2 (`hold`), O3 (`focus` + 3 switchable tile-resolutions), O4 (ranged `minRange` kiting — bow 2 / mage 2 / catapult 4), O5 (dev-only `--objective=coverage` churn bot, kept separate from the measurement proclivity). Band **re-confirmed `1.25 × 1.5`** (no retune). Detail: git log + [BALANCE.md](BALANCE.md) §O5.
- **Phase P complete** — the shared `UnitCard` component ([src/ui/UnitCard.ts](src/ui/UnitCard.ts)). P1 (extract + recruit/promotion parity + the rarity-accent seam), P2 (full-variant XP-to-next bar, config-derived `xpProgress`), P3 (PreTurnScreen adopts the full card; the screen scrolls for a tall hand), + the POW-display rework (its own accented meta row). **UI-only — no snapshot/fuzz change** (WorldSnapshot v25 / RunSnapshot v17 hold). Detail: git log + the top ACTIVE block.
- **Phase Q COMPLETE (Q1–Q6)** — the in-battle HUD overhaul + pacing: the `PlaybackSpeed` set-by-value model (0.5×–3× + pause), the pre-battle countdown (replacing the spawn-anim), the four HUD panes (speed TR / objective BR / player-cards BC / enemy-cards top, on P's `compact`/`full` UnitCard + a `team`-tinted enemy mirror), and dismantling the old monolithic HUD (net −325 lines). **Presentation/UI/input — no snapshot/fuzz change** (v25/v17 hold). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase Q.
- **Phase R COMPLETE (R1–R3)** — auxiliary card surfaces + cleanup; closes the Post-N O→R round. R1 (the shared roster-view modal — a top-right "Roster" button on Map/Recruit/pre-turn opens a dimmed overlay of the roster as `full` UnitCards [a new `roster` skin]; order via the pluggable `rosterOrder` seam, recruitment-order default), R2 (the pre-turn draw-pile [bottom-right] + discard-pile [bottom-left] views — the R1 modal generalized into a shared `CardListModal`/`CardListButton`; piles ride `turn:starting`/`turn:handRedrawn` resolved to templates in recruitment order, contents-only/unordered via `Run.resolvePileForDisplay`), R3 (the procedural-map label unify — one `PROCEDURAL_MAP_NAME` = "Uncharted Ground" constant + cleanup chores: `.gitattributes`, inline-SVG favicon, three.js vendor-chunk split; catapult SFX deferred). **UI-only — no snapshot/fuzz change** (v25/v17 hold). User-confirmed in the native browser. Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase R; deferred bugs in [TODO.md](TODO.md) §Phase Qb.
- **Phase Qb COMPLETE (#2 + #3)** — the two deferred bug investigations, both user-confirmed in the native browser. **#3** (`b6da01f`) the corridor archer kite-pin: a too-close kiting ranged unit (inside `minRange`) with a blocked retreat fell through to the target-cell anti-freeze fallback and stepped ONTO the soft-excluded target; fix = omit that fallback when `dist < minRange` (abstain/queue), guarded by a corridor fixture + a "no two combatants share a cell at a tick boundary" invariant test. **#2** (`0d9413e`) the billboard depth-sort: the `depthWrite:false` glyph instances had no per-instance depth sort, so draw order (= spawn slot) decided occlusion; fix = `SpriteRenderer.sortByDepth(camera)` reorders the instance buffers back-to-front each frame, wired into `Game.onFrame`. No snapshot/fuzz change (v25/v17 hold); 1021 tests. Detail: git log + [TODO.md](TODO.md) §Phase Qb.
- **Phase S COMPLETE (S1 + S2)** — encounter-system groundwork: **S1** (`deceb6a`) the **Floor→Hop** rename (node-map progression concept + dev surface; **RunSnapshot v17→v18**; battlefield-floor tile / `Math.floor` left alone); **S2** (`f25fc8f` + glyph follow-up `e927308`) the **selectable root** via a pre-root sentinel (`PRE_ROOT_NODE_ID = -1`, [NodeMap.ts](src/run/NodeMap.ts); **v18→v19**; adds one battle/run). **GOTCHAS #110** — don't "fix" `currentNodeId = -1` back to `rootId` (`currentHop`/`hopOf` throw at pre-root by design). Both user-confirmed. Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase S.
- **Phase T COMPLETE (T1–T3 + follow-ups)** — the run-container round: **T1** (`e3a03eb`) the **Sector schema** ([sectors.ts](src/config/sectors.ts) + `config/sectors.json`, zod — hop-gated layout pool; procedural = reserved sentinel literal-matching `RunConfig.FORCE_PROCEDURAL`); **T2** (`6295d66` + `33b4fd5` weight-seam + `eb9b2ab` banner) **the run = a sequence of sectors** (`sector-map.json` DAG + [sectorWalk.ts](src/run/sectorWalk.ts); `Run` → a current-sector model, **RunSnapshot v19→v20**; weighted board roll; player pool + roster carry across; ships one sector **"The Start"**, source == sink; **GOTCHAS #111** — `pickOne` zero-draws a singleton); **T3** (`cf773ea` + `46207e6` + `84bf7ea`) the dev tooling (sector editor + `/tools/` index + the layout-editor "add to sector" toggle; its swallowed-`ReferenceError` button bug fixed). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase T + [TODO.md](TODO.md).
- **Phase U COMPLETE (U1–U3b)** — the keystone: `rollEnemyWave` RETIRED from `Run`, replaced by an authored **Encounter** model. **U1** (`89957c0`) the pure wave resolver [wave.ts](src/run/encounters/wave.ts) (`resolveWave` — budget/count/weight; `distributeWeightedLevels`; per-instance level weighting [USER-LOCKED]); **U2** (`010d35e`) the wave-list grammar + sequencer [sequencer.ts](src/run/encounters/sequencer.ts) (`waveForTurn` — wave | weighted-pick | loop{N|forever} | condition-gated **stages**; nests to any depth; resumable cursor; terminal = last-wave-repeats [USER-LOCKED]); **U3a** (`2b4f733`) the Encounter schema [encounters.ts](src/config/encounters.ts) (`kind` enum normal|elite|boss + `rewards?` seam); **U3b** (`252b7ea`) the Run wiring (`beginTurn` resolves via `waveForTurn`→`resolveWave`; **RunSnapshot v20→v21**; per-encounter `enemyHealthPoolMax`; encounter name → HUD). `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow still call them). User-confirmed. **V0/V1 superseded U3's empty catalog + code-built reproduction** (see the ACTIVE block). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase U.
- **Phase V COMPLETE (V0–V2c).** The `sector-owns-both` flip (`9b8d818`); the keyed `selectEncounter` resolver + `config/selection.json` (`498745d`+`c647ebf`, **RunSnapshot v21→v22**); the **encounter editor** + visual wave-grammar builder + byte-faithful `formatEncountersJson` (`f92a63a`+`18a59d5`); placement on both editors via `addEncounterToSectorPools` (`8ddbdc7`); the 6-encounter catalog in "The Start" — Brigands/Highwaymen/Deserters + the grammar demos Artillery Company/Ronin-and-Mages/Guarded Adventurer (`c932593`); editor tab-restore (`e061c8e`). No snapshot bump; user-confirmed. Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase V".
- **Phase W COMPLETE (W1 boss + W2 elites; scope EXPANDED; both user-confirmed).** **W1** (`5046960`) **"The Bandit King"** (`kind:'boss'`/pool 20) — the first to exercise the condition-gated **`stages`** grammar (3-phase escalation at pool ≤0.66/≤0.33). **W2** (`d08a696`) **elite map-nodes** (pulled forward): a new `'elite'` `NodeKind` + a `NodeMap.generate` scatter pass (structure+rest byte-identical; `eliteChance 0.25`/`eliteMinSpacing 2`; always a non-elite sibling = optional detour); `*` glyph NEON_PURPLE; two elites (pool 12/budget 1.35×); no Run dispatch change (reuses the battle path); **RunSnapshot v22→v23**; fuzz `PATH_KINDS += 'elite'`. Boss/elite numbers UNTUNED (X targets). Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase W".
- **Phase Wb COMPLETE (QoL interstitial; Wb1–Wb4; pre-X polish).** The pre-turn encounter NAME (`turn:starting` gains `encounter:{name,kind}`, elite/boss badged; `9ea338e`); two editor fixes — add-to-sector tab-restore (`427d57d`) + the `← Tools` link on every dev tool (`51fd160`); and the sector fight pool split BY KIND `encounters:{normal,elite,boss}` (exhaustive `Record<EncounterKind,…>` + a kind-consistency guard + a kinded `encounterPoolAtHop`; both editors reworked; `c99ab3f`). No snapshot bump. Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase Wb".
- **Phase X COMPLETE (X1 + X2 + X3) — the band RE-DERIVATION** (closes the S→X encounter round). `levelCap` (per-wave, opt-in) + **X1** per-run difficulty multipliers (`waveSizeMultiplier`/`levelBudgetMultiplier`; `difficulty.json` default + `RunConfig` seam) + **X2** the per-encounter pool-damage harness (`--per-encounter` / `--encounter=<id>` / `--seed-offset`; dev-only + a one-line `RunConfig.forcedEncounterId`) + **X3** the 5-step sweep: per-kind bands **normal≈3 / elite≈6 / boss≈10** pool-damage/instance, **win rate 0%→~37% optimal, +24pt gradient**, bands holding out-of-sample. Config + tests only — **no snapshot bump**. ⚠️ pending the user's playtest (elites lean slightly hard). Detail: [BALANCE.md](BALANCE.md) run log + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §X + git.
- **Cluster 1 (Combat Depth) roadmap WRITTEN** (planning session, docs-only) — [ROADMAP.md](ROADMAP.md) is now Cluster 1 (Phases **Y→31**); the encounter roadmap archived to [archive/post-r-roadmap.md](archive/post-r-roadmap.md), the design brief to [archive/cluster-one-feedback.md](archive/cluster-one-feedback.md). Full cursor + locked decisions in the top ACTIVE block.
- **Phase Y COMPLETE (Y1–Y5; the data-driven attack/effect model keystone) & user-confirmed.** Y1/Y2 (`f075930`/`5a74fdb`/`925d9c5`) the `src/sim/effects/` vocabulary (`EffectOp`/`TargetSelector`/`AbilityDef` + `'fill'` timeline) + interpreter (`targeting`/`reposition`/`interpreter`/`EffectAction`). Y3/Y4 (`affe68d`→`9ba9588`; `80eab1d`/`b9720b5`) strangler-migrated every combat verb to the generic `EffectAbility`/`EffectAction`, each proven byte-identical via the (now-retired) determinism oracle — melee/bow/heal/gambit/dash, then the FX-cue magic/catapult. Y5 (`3c9189c`/`d107f3a`/`d23df6b`/`6a7f632`/`c0be74a`) the cleanup: deleted the legacy ability+action classes + the oracle, bumped **WorldSnapshot v26** (reject pre-migration in-flight saves), and consolidated to one `config/abilities.json` `AbilityDef` catalog (browser-verified byte-identical across all 9 archetypes). As-built facts in the top ACTIVE "Phase-Y as-built" block; per-step detail in git.
- **Phase Yb COMPLETE (`5434d99`; bonus interstitial between Y and Z; user-confirmed).** Two QoL changes on the fresh abilities schema: **(1) per-phase `scalesWithSpeed`** ([timeline.ts](src/sim/effects/timeline.ts) + `speedScaledSeconds` in [stats.ts](src/sim/stats.ts)) — a FIXED timeline phase can shrink with caster `speed` via the cadence curve, instead of clamping a constant floor under the cadence. Default `false` (every other ability byte-identical); rejected on a `'fill'` phase. Applied to `magic_bolt` (windup 1.5s) + `catapult_shot` (windup 2s): a snappy speed-responsive windup with `recovery: fill`, so busy window == cadence at every speed (full 60% range restored vs a fixed windup's ~26%). ⚠️ catapult edge: above ~speed 8 the unscaled 0.6s `travel` gets clamped (recovery→0); a heavy unit never reaches it, cosmetic only. **(2) Required `AbilityDef.name`** ([schema.ts](src/sim/effects/schema.ts)) — display name is config now, decoupled from `id`; retired the dead `ABILITY_UI` map in [UnitCard.ts](src/ui/UnitCard.ts) (`effect` was vestigial — read off the op) + the editor's raw-id labels → both read `def.name`. **No snapshot bump.** Detail: git + the top ACTIVE block.
- **Phase Z COMPLETE & user-confirmed (Z1+Z2+Z3) — renderer comms / the FX seam.** **Z1** (`d44c2de`) the renderer-owned **FX registry** ([fxRegistry.ts](src/render/fxRegistry.ts), pure-data `FxKey→FxDescriptor` + boot assert) + the FX driver (`BattleRenderer.onActionPhase` resolves `def.fx[phase]`→registry, holds Renderer+AudioPlayer, one key = visual+SFX) + re-homed the mage bolt / catapult lob FX onto it + **retired the ad-hoc `magic:detonated`/`catapult:fired` events** (catapult dud now always-fires). **Z2** (`f2ebdc8`) **camera shake** as the registry's first non-sprite channel (Renderer-owned `shakeCamera`, transient apply/clear around render, per-key intensity — user-loved). **Z3** added the `shove`+`tracer` channels + the `melee_swing`/`ranged_shot` keys (authored on sword/club/katana/whip/gambit/bow) and re-homed the melee shove + bow tracer + their `melee`/`shoot` whoosh off `unit:attacked`/`unit:missed` onto `action:phase` (so they fire on hit AND miss for free); `triggerAttackVisual` split into `triggerShoveFx`/`triggerTracerFx` (no more archetype skip-guard), `onUnitAttacked`/`onUnitMissed` kept only the hitsplat+HP, and BattleScene's attack-audio handlers were deleted. Heal cue stayed on `unit:healed`. No snapshot bump.
- **Tests:** **1222 main** pass (0 `it.todo()`) via `npm test` (Z3 added 6 fxRegistry cases — the 2 Z3 strike-cue keys [`melee_swing`/`ranged_shot`] + 4 config-derived def-key pins [the 4 melee weapons share `melee_swing`@impact, gambit@windup, bow `ranged_shot`@impact, all resolve]); the full per-phase test-count history lives in git log + [archive/post-h-roadmap.md](archive/post-h-roadmap.md); **210 fuzz:smoke** via `npm run fuzz:smoke`; `npm run typecheck` clean. One known residual — `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, out of GP5 scope). Fuzz sweep `npm run fuzz` (20 seeds default; `--per-hop` for per-hop analysis; `--layout=procedural` to isolate the M6 maps); the `scored` strategy + `--search` (→ `output/best-strategy.json`) drive the balance tuning. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`. **READ [BALANCE.md](BALANCE.md) FIRST for any balance work.** The per-phase test-count history lives in git log + [archive/post-h-roadmap.md](archive/post-h-roadmap.md).
- **Dev server:** Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent). **The user usually has their own `npm run dev` on 5173 during a session**, and the preview MCP refuses to attach to a server it didn't start ("not a preview server"). For agent browser-verify, use the **`dev-preview` launch config** ([.claude/launch.json](.claude/launch.json)) — `npm run dev -- --port 5191 --strictPort` binds a dedicated port that never collides with 5173, so `preview_start name=dev-preview` always works. **Dev handle:** `window.__game` (DEV-only) exposes `bus`/`renderer`/`run`/`activeScene`/`keybindings`/`sprites`; drive a battle headlessly with `__game.dispatch({kind:'enterNode',nodeId})` then `{kind:'advanceTurn'}` (past the PreTurnScene gate), and freeze the auto-advancing run with `scene.clock.advance = () => {}` for a stable screenshot.
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**Phases A → N are COMPLETE (bar the VPS-gated N4).** The full as-built history lives in [archive/post-h-roadmap.md](archive/post-h-roadmap.md) (Phases I → N, the detailed roadmap this round superseded), [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md) (E → GP), and [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md) (A → E).

**The Phase-N playtest feedback was synthesized into [ROADMAP.md](ROADMAP.md) — "Post-N": the anti-blobbing agency/control round, Phases O → R — now ✅ COMPLETE (+ the Qb deferred-bug tail, closed 2026-06-18)** (the brief is archived at [archive/phase-n-feedback.md](archive/phase-n-feedback.md)). In brief — **O** the objective-system refactor (always-an-objective TYPED per-team model: atWill/engage/focus/hold; enemy support inert; focus's 3 switchable tile-resolutions; ranged `minRange`) + fuzz, headless-first, one WorldSnapshot bump; **P** the shared `UnitCard` component (compact + full variants, consolidating recruit/promotion; pre-turn adopts full cards); **Q** the HUD overhaul (4 panes: speed TR / objective BR / player-cards BC / enemy-cards top) + the co-located pacing model (a pre-battle countdown REPLACING the spawn anim, 0.5× + pause, per-speed enable for difficulty); **R** roster + draw/discard pile views + the Uncharted-Ground/Nowhere unify. The brief's other two anti-blob ideas (side-objectives, dynamic terrain) are DEFERRED to future rounds. Six design decisions are pre-locked (see the roadmap's resolved decision points).

**The ENCOUNTER-SYSTEM round (Phases S→X) is ✅ CONTENT-COMPLETE** — its as-built roadmap is archived at [archive/post-r-roadmap.md](archive/post-r-roadmap.md) (it replaced random `rollEnemyWave` with authored **Encounters**, introduced **Sectors** as the run container, added boss/elite tiers, and **RE-DERIVED** the difficulty band; per-phase detail in the Current-state list above + git). Pending the user's playtest of the X3 rebalance (a cheap loosen/tighten if off).

**The road to feature-complete is the LOCKED post-X meta-roadmap — [META-ROADMAP.md](META-ROADMAP.md), 6 ordered clusters.** **Cluster 1 — Combat Depth is now the ACTIVE [ROADMAP.md](ROADMAP.md)** (synthesized this session from the archived [archive/cluster-one-feedback.md](archive/cluster-one-feedback.md)): the **data-driven attack/effect model** keystone, then the depth on top (non-stat statuses + chain/status-on-hit/summon) + the attack/archetype editors. Phases **Y → Z → 27 → 28 → 29 → 30 → 31**; **NEXT = Phase Y** (the schema + interpreter + the strangler migration). The top ACTIVE block carries the full cursor + the locked decisions. Smaller follow-ups in [TODO.md](TODO.md); the later clusters (Spatial / Economy / Drafting / Map Content / Meta & Ship) live in [META-ROADMAP.md](META-ROADMAP.md).

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

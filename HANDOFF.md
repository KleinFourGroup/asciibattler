# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

> ## ⏯️ ACTIVE SESSION HANDOFF — N2 closeout: rogue re-measure + N3 + turn-cap unification DONE (2026-06-15)
>
> **Where we are:** **N2 band LOCKED `1.25 × 1.5`** (committed to `config/difficulty.json`, was `0.75 × 2.0`; heavy/full-floor/realistic/procedural → `output/n2-stage3/`). **THIS SESSION the two owed N2/N3 threads CLOSED — Phase N near-term work is COMPLETE:** (1) **Rogue `weakest` re-measure — STILL backfires even WITH the N1 dash → kept `nearest`, no config change** (reverted byte-identical to HEAD). Forced lvl-5 rogue lineup at the band, same lineup under each targeting: `dmg/dep` collapsed **19.6 (`nearest`) → 2.6 (`weakest`)**, deaths/run 13.5→20.8 over 125–171 deployments — the 2-cell/10s dash can't safely bridge to the backline; the lone rogue dies surrounded (H7c's mechanism, unchanged). The "backline assassin" identity needs more than a short dash → folds into the flagged **archetype-balance** future work. (2) **N3 leveling consistency check — GREEN, no nudge:** the locked `50/1.1` curve + the band are mutually consistent (player avg level +0.39/floor; player-over-enemy gap a STABLE `+0.5…+1.08` across all 10 floors — **no snowball, no fall-behind**; enemy scales in lockstep). Full detail + tables: the **N2 ROGUE RE-MEASURE** and **N3 LEVELING CONSISTENCY CHECK** entries at the BOTTOM of **[BALANCE.md](BALANCE.md)**; the **STAGE 3 COMPLETE** entry has the band table + OP read.
>
> **The band (LOCKED) — `1.25 × 1.5`:** 70% best / **60% held-out** / +30 gradient, on the ~67% target with the tightest train→test gap (`1.125 × 1.625` had a steeper +37 but a 73→50 overfit gap, so rejected). **OP read (the owed telemetry): a mercenary + ranged DUOPOLY at the band** — everything else benched. Stage-2's "diversity emerges at the band" did NOT survive full floors (diversity only appears *below* the band, where the optimizer is losing). Per-unit, ranged actually out-damages mercenary; mercenary leads on volume + tanking. Making the specialists band-worthy = **archetype-balance work**, not a difficulty knob.
>
> **The blocker (now diagnosed + mitigated): an environmental `dwm.exe` leak — NOT our code, NOT the GPU driver.** Heavy `--jobs` sweeps intermittently die on Windows `0xC0000142` (DLL-init-failed) when burst-spawning children, because `dwm.exe` leaks **committed memory** continuously (~3–10 MB/min, scaling with composition load) and over multi-day uptime reaches ~24 GB / ~200K handles, shrinking the session margin until a spawn burst tips it over. **A 5-month NVIDIA driver update (591→610) did NOT fix it, and the leak is cross-machine/cross-GPU-vendor (the prior AMD box had it too)** → a Windows/dwm-level issue (leading suspect: the accessibility programs that hook the compositor). **Mitigation: a reboot fully reclaims it, and a fresh session has hours of clean headroom — so reboot before any heavy/overnight `--jobs` run;** `--jobs=1` is immune (never spawns). Full post-mortem: **[archive/dwm-leak-diagnosis.md](archive/dwm-leak-diagnosis.md)**.
>
> **Done this session:** confirmed the dwm-leak post-mortem carries no PII (hardware/driver specs aren't identifying; the eye-tracking refs are something the user is publicly open about → repo safe to link as-is, no git surgery). Ran the **rogue `weakest` re-measure** (A/B forced-roster, quick tier; kept `nearest`) + the **N3 leveling consistency check** (`--per-floor`, greedy×20 + scored×50; GREEN) — logged both to BALANCE. Then did the **turn-cap UNIFICATION** (2 commits — `cf4913a` fuzz half + `9043cd6` live half): collapsed the config cap + the two hardcoded `secondsToTicks(150)` copies (harness + arena) onto the SINGLE `HEALTH.maxTurnSeconds`, and made `resolveAsDraw`-at-cap uniform across harness/arena/**live BattleScene** (which never enforced the cap before — a stalled live battle used to soft-lock). 'hang' now means genuine non-termination only; `AggregateStats.cappedDraws` carries the indecisive-turn signal. typecheck + 950 main + 191 fuzz:smoke green; browser-verified the live draw (PostTurnScene "SKIRMISH DRAWN", both pools chip, CONTINUE → recruit, no errors).
>
> **Then (remaining Phase N):** **only N4** is left — the **overnight verify, DEFERRED INDEFINITELY to a VPS** (`--seed-offset` holdout + `--search --preset=overnight`; the user won't risk an unattended overnight run dying on the local dwm leak). The optional ~67%-nudge-sweep was SKIPPED (user call — 70%/60% is close enough to 2/3). **So Phase N is COMPLETE bar the VPS-gated N4.** The natural next major thread is **archetype-balance** (the mercenary+ranged duopoly + the non-viable rogue/specialists from the stage-3 OP read). **Fuzz baseline note:** the cap change is byte-identical for non-capping runs (the common case); only formerly-hanging seeds shift (now a draw-and-continue), so the locked band is unaffected.
>
> **Open threads (non-blocking):** the **dwm leak has no permanent fix yet** — driver update tried + ruled out, cross-vendor; leading suspect = Windows/dwm or the accessibility hooks ([archive/dwm-leak-diagnosis.md](archive/dwm-leak-diagnosis.md)). Live with the reboot-before-heavy-runs mitigation. Optional hardening: a **persistent-worker-pool refactor** of the `--jobs` sharding (spawn `jobs` workers once, feed them grid points; cuts spawns `G×J`→`J`) would sidestep the flake entirely. NB: do **not** `Stop-Process dwm` on this machine — the accessibility (eye-tracker) programs don't survive a dwm crash. Separately, an **object-pooling pass** for the sim's hot per-tick allocators is logged in [TODO.md](TODO.md) (GC-pause reduction for the rendered game; the fuzz heap probe found heavy allocation churn but **no** leak in our code).
>
> **Tree state:** the turn-cap unification is COMMITTED (`cf4913a` fuzz half + `9043cd6` live half); the rogue/N3 measurement doc-log is committed (`10a5214`). The rogue `weakest` flip was reverted byte-identical (no config change shipped from the measurement). `1.25 × 1.5` remains in `config/difficulty.json`. Uncommitted after this: the ROADMAP/HANDOFF doc updates for the unification. `output/` is gitignored (reproducible reports). Preview server should be stopped before session end.

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phase A complete** — A1–A5 (action selector, command channel, fuzz harness, config externalization, scene system).
- **Phase B complete** — B1/B1.1 (palette + selective bloom), B3 (HP/progress bars), B5 (CSS scanlines), B6 (audio), B7 (root node `@`).
- **Phase C1 complete** — C1a/b/c/d (terrain + walls + LOS + faceted prisms + layout editor + Labyrinth pathfinding fix).
- **Phase D complete** — D1 (renderer cap), D2 (drag-paint), D3 (variable sizes), D4 (fit/scroll camera), D5.A–E (spawn regions + overflow queue), D6 (half-cover), D7.A–C (chasm + fire + healing), D8 (theming).
- **Phase E complete** — combat depth (E1–E7). E1 (stat vocabulary + `deriveStats`), E2 (generic ability layer, retiring `AttackBehavior`), E3 (per-archetype `growthRates` + leveling), E3.5 (`TICK_RATE` doubled 10 to 20), E3.6 (DOM unit overlays + hitsplats), E4 (XP loop + PromotionScene + half-cover mult), E5 (pathfinding refresh: target stickiness + boids sidestep), E6 (combat visuals: shove/projectile/hitsplats), E7 (the six combat archetypes: melee/ranged + rogue/healer/mage/catapult). Full per-step worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md); forward plan in [ROADMAP.md](ROADMAP.md).
- **Phase F complete** — Phase-E playtest response (F1–F6). F1 (draft-pool pull-forward: the E7 archetypes become draftable), F2 (first-class action phase timeline windup->...->recovery), F3 (projectile & impact timing), F4 (rogue gambit sequencing), F5 (heal feedback VFX), F6 (heal/utility XP). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase G complete** — run progression (G1–G5). G1 (`RunConfig` + short-run harness), G2 (longer maps + full-viewport map scene), G3 (rest & boss nodes + map icons), G4 (enemy level-*budget* model + recruit leveling), G5 (fuzz-strategy factory + GUI launcher). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase GP complete** — combat & stat-legibility playtest response (GP1–GP5). GP1 (`endurance`->`mobility` / `speed`->`agility` rename + universal move base), GP2 (`defense` stat + `World.applyDamage` consolidation), GP3 (recruit-card + HUD legibility), GP4 (ranged/catapult acting-position pathing), GP5 (healer chokepoint yield via `SwapAction` + centroid navigable-snap). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase H complete** — the multi-turn deckbuilder battle trial (H1–H7). H1 (`power` stat), H2 (spawn-tile range), H3 (deployment counter / fatigue hook), H4 (health-pool encounter→turn loop: H4a headless + H4b pre/post-turn screens & pool HUD), H5 (card-drawn hand + `playerTeamLevel` seam swap + H5b hand display), H6 (rest-heal + recruit-pass + inert spawn-time fatigue hook), H7 (search-driven balance tooling: scored DSL → `--search` → difficulty band SET `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance + telemetry + sweep GUI). Detail in [ROADMAP.md](ROADMAP.md) §Phase H + [BALANCE.md](BALANCE.md).
- **Phase I complete** — foregone conclusions & combat identity (I1–I6). I1 (`agility→speed` revert + `precision`/`evasion` dodge stats + canonical reorder; World v19 / Run v10), I2 (subtractive hit/miss roll at `applyDamage` + "Miss" hitsplat; live `hitChanceBase` 0.6, turn caps 100→150s), I3 (fast-forward 1×/2×/3× tick-batch, config-backed `F` hotkey), I4 (schema-driven archetype editor + dev-only `/__save-config`), I5 (melee subclass family `mercenary`/Adventurer/Ronin/Bandit + Bandit enemy + rogue dodge re-measure → N1 mobility CONFIRMED, scoped for reach; World v20 / Run v11), I6 (per-ability combat profile `might`/`accuracy`/`critBase` + evadable/critable gates, weapon split sword/club/katana/whip/bow; World v21→v22). Detail in [ROADMAP.md](ROADMAP.md) §Phase I + [BALANCE.md](BALANCE.md).
- **J1 ✅ (Phase J, `5ce703c`) — the in-battle objective MODEL + targeting preemption (sim, headless):** one shared player-team objective (`tile` persist-until-cleared / `enemy` auto-clears on death) via the `setObjective`/`clearObjective` WorldCommands; engage radius `min(attackRange, rangedLeashCells)` + retaliation; **WorldSnapshot v22→v23**; byte-identical with no objective set → fuzz unchanged; 742 tests.
- **J2 ✅ (Phase J, `55be903`) — the pathing SEAM:** shared movement-intent primitive in [movement.ts](src/sim/movement.ts) (`MovementIntent` + `advance` [dash hook via `maxCells>1`] over the pure `routeToward` findPath wrapper = the cache boundary); MovementBehavior thinned to a goal-selector; **cache DEFERRED seam-first (user call)** — the `pathfindingStats` counter + [pathing-perf.test.ts](tests/integration/pathing-perf.test.ts) guard the recompute budget (profiled ~1.8% of a 3× frame, 60-unit board); byte-identical; 755 tests.
- **J3 ✅ (Phase J, `7751c97`+`ab1c061`, playtest fixes `59cd1d0`+`496d26e`) — objective UI + rebindable hotkeys, Phase J's FIRST PLAYTESTABLE surface:** runtime-rebindable [Keybindings](src/ui/Keybindings.ts) registry ([config/keybindings.json](config/keybindings.json); fast-forward `F` folded in); right-click / Set-arm / Clear via the J1 commands + HUD buttons + the amber `X` marker (**the LAST FontAtlas cell — the 32/32 atlas is FULL, the next glyph needs a resize**); fixes: opt-in `findPath` `bestEffort` (the unpathable-rally freeze), marker size/camera-up anchor, terrain-mesh picking, billboard-aware enemy selection ([pick.ts](src/render/pick.ts)). No sim/snapshot/fuzz change; 780 tests; browser-verified. Still logged, not fixed: the amber marker is low-contrast on Volcanic.
- **J4 ✅ (Phase J, `b5d454e`+`8672764`+`b497c3b`) — fuzz objective strategy + arena harness → PHASE J COMPLETE (J1–J4 ✅):** dev-only tooling, ZERO `src/` change — a serializable `ObjectiveProclivity` (`none`|`random`|`stat:hi/lo`|`hp:hi/lo`|`archetype:<name>`) + the no-thrash `decideObjectiveCommand` (refill-on-null, leans on J1's auto-clear); the arena harness ([arena.ts](tests/fuzz/arena.ts) — one forced World, no Run → `output/best-objective.json`); the `--objective=<json|random|none>` flag on run/`--search`/`--balance-sweep` + ShardJob (default none = byte-identical); fuzz:smoke 89→117. Detail: [ROADMAP.md](ROADMAP.md) §Phase J + the git log.
- **K1 ✅ (Phase K, `3bfe3a3`+`0f9088d`) — the generic status-effect system + fatigue migration:** `StatusEffect`/`foldEffects` ([statusEffects.ts](src/sim/statusEffects.ts); empty-effects identity = the byte-identical fast path) + `effectiveStats` live-read swaps; the FULL trigger set ([triggers.ts](src/sim/triggers.ts), World + Run dispatchers — the L daemon seam, no production handlers); the Run-side `encounterEffects` store + `fatigueEffect` (the power-bake retired); **WorldSnapshot v23→v24 + RunSnapshot v11→v12**; byte-identical at default (stash-compared); 822 tests.
- **K2 ✅ (Phase K, `206c249`) — roster/hand decouple (roster 5→10, `handSize` 5→6) + the latent H5 wave-size BUG:** `rollEnemyWave` sized the enemy COUNT off the whole roster, not the fielded `min(roster, handSize)` — the budget hand-capped but the count didn't = the "massacre"; fixed + `enemyArcherRatio` hoisted to config (0.4→0.3) + band re-swept `budgetFactor 0.625→0.75 × swarmMax 1.75→2.0` (cliff-adjacent, provisional → N2 re-sweeps). No snapshot bump. Diagnosis arc: the K2 entry in [BALANCE.md](BALANCE.md).
- **K3 ✅ (Phase K, `3be762e`+`ddf575b` mechanic/UI + dev-only `7db5100`/`5043468`/`b32809e`) — the redraw mechanic:** `redrawCards` at the turn-intro gate, one-batch-per-turn + the `maxCardsPerTurn` dial (an L daemon can flip either mode), discard rule (no deployment count) free off the `beginTurn` seam, pure validator [redraw.ts](src/run/redraw.ts), **RunSnapshot v12→v13**; PreTurnScreen selectable cards + `Redraw (N)` button, **pre-turn auto-advance REMOVED** (user call; post-turn keeps its timer); fuzz CLI split into `tests/fuzz/commands/`, the scored objective proclivity, the `--redraw` policy bot (PINNED: `none` ≡ absent AND the gates-on control ≡ headless byte-identical); fuzz:smoke 117→157. MEASURED ([BALANCE.md](BALANCE.md) K3c3): naive redraw lift SMALL (best `level:2` +6 wins/100 ≈ 1.5σ; `random:2` −1.2 avg floors), and the map experiment found a HUGE comp×map interaction → map-aware scored terms measurement-justified, BANKED; flat melee>ranged arena dominance flagged for N2.
- **K3.5 ✅ (Phase K, `2efd3b1`) — ONE MAP PER ENCOUNTER + the pre-turn map label:** the user's playtest insight (redraw was a blind map-guess); `rollEncounterMap` in `beginEncounter` → `Run.encounterMap` (**RunSnapshot v13→v14**), `beginTurn` keeps only worldSeed + the wave re-roll; PreTurnScreen shows `⌖ <name> — W×H`; the fuzz read shifted EASIER at unchanged knobs (~20% weak-bot win / floor 5.9 — the K3.5 entry in [BALANCE.md](BALANCE.md), incl. the favorable-map-compounding hypothesis; left loose deliberately → N2 calibrates); 848 tests. **The user then completed a FULL run and loved it.**
- **K4 ✅ (Phase K, `a75fd2b`+`5efb01d`+`34d7ddc`) — the pre-turn EMPOWER → PHASE K COMPLETE (K1–K4 ✅):** `empowerUnit { handIndex }` at the turn-intro gate → the config buff (universal-offense +4 STR/RNG/MAG, STACKING merge `add`) onto the roster SLOT's K1 encounter store (live the same turn, survives redraw-away/benching; [config/empower.json](config/empower.json) + pure [empower.ts](src/run/empower.ts); **RunSnapshot v14→v15**); PreTurnScreen shares the K3 card selection (`Empower ▲` + per-stack badge); fuzz `--empower` policy bot (`none` ≡ absent pinned); 867 main / 173 fuzz:smoke. MEASURED ([BALANCE.md](BALANCE.md) K4c3): empower is **BAND-MOVING** — +21…+27 wins/200 (~4σ) on BOTH bots and TARGETING-INSENSITIVE (the free stats are the value, not the pick) → the N2 re-sweep is firmly required. Deferred (user playtest call): empowered-unit standout UI → the L/M status-VFX pass ([ROADMAP.md](ROADMAP.md) §K4). Detail: [ROADMAP.md](ROADMAP.md) §Phase K + the git log.
- **L1 ✅ (Phase L, 2026-06-12, 3 commits) — DAEMONS: the design round + the four-idol catalog + daemon-only gates.** The design round locked: **daemon-only gates** (the K3/K4 static `enabled`s ship FALSE — availability is whatever the run's daemon grants; an idol granting neither tool = a run without redraw/empower, the user's deliberate roguelite-variance call), **four idols** (Roman-statue-in-terminal-frame flavor — the synthwave blend; unix-daemon names TABLED for playtester accessibility, the 8 trigger-daemon sketches KEPT for the shop round — [ROADMAP.md](ROADMAP.md) §Phase L), **one uniform roll at run start** (placeholder for the user's planned starting profiles; `RunConfig.daemon` + `?daemon=<id|none>` is the seam), **`chance` as a first-class per-turn gate condition** (dedicated `daemonRng` stream).
  - **Commit 1 (`84f487b`) — the headless mechanic.** [config/daemons.json](config/daemons.json) (Mars +4 STR/RNG/MAG empower 1/turn = the K4 buff; Minerva +2 DEF empower 1/turn `warded` — shipped +4, nerfed same-day off the L1c3 measurement; Mercury 50%/turn coin → FULL redraw; Janus guaranteed redraw ≤2 cards/turn) + [src/config/daemons.ts](src/config/daemons.ts) (zod; `BuffSchema` shared from empower.ts); pure [src/run/daemon.ts](src/run/daemon.ts) (`rollDaemon` + `resolveTurnGates` → effective Redraw/EmpowerConfigs **the K3/K4 validators consume unchanged**; an RNG draw ONLY when 0<chance<1, redraw-before-empower order); Run: `daemonRng` forked after deckRng (the H5 append-fork convention — re-baselines fuzz, statistically neutral), `turnGates` re-resolved each `startNextTurn` on BOTH gated+headless paths, the redraw/empower getters+handlers read `turnGates` (the EMPOWER singleton retired from Run); **RunSnapshot v15→v16** (daemon stored WHOLE + `daemonRng` + `turnGates` — a save at the gate keeps its Mercury flip, v15 rejected). K3/K4 gate tests run under a `K_DEFAULT_DAEMON` fixture derived from the config dials. **892 main (+25)**.
  - **Commit 2 (`b6bce07`) — the PreTurnScreen daemon surface.** Idol banner under the K3.5 map line (FLOURESCENT_BLUE `.preturn-daemon`); the empower hint + badge title derive from the ACTIVE daemon's buff via the extended `turn:starting` payload (`daemon.redrawGate`/`empowerGate`/`empowerBuff` — no catalog lookup in the UI, bespoke daemons render correctly; fixes the c1 interim where a Minerva run hinted the old +4-offense); a chance gate that DENIED this turn renders "◈ the idol is silent — no redraw this turn" (computed ONCE from the fresh budget — **denied ≠ spent**, browser-proven). Browser-verified all five arms via eval-probes (minerva e2e `warded`, mercury denied+granted, janus cap-2 third-click refused, mars, none); pixel layout fell to the known 0×0-viewport capture limitation → the user's native playtest confirmed the look.
  - **Commit 3 — the fuzz daemon arm + per-daemon telemetry.** [daemonSelection.ts](tests/fuzz/daemonSelection.ts) (`random`/`none`/`fixed:<id>` → the `RunConfig.daemon` override; plain JSON so it rides the ShardJob); `HarnessOptions.daemon` (PINNED: `random` ≡ absent byte-identical — the Run's own roll IS the default); `RunResult.daemonId` + a `daemon` summary.csv column + `perDaemonStats`/`renderDaemonAnalysis` (a `--daemon=random` batch buckets per idol in one pass — the future per-PROFILE read); `--daemon=<id|random|none>` on run/`--search`/`--balance-sweep` + ShardJob (live-verified jobs=1 ≡ jobs=2 with a forced idol). **fuzz:smoke 186 (+13). Measurement: [BALANCE.md](BALANCE.md) §L1c3** (per-idol win/floor vs the daemon-less control, naive redraw `level:2` + empower `random` bots).
  - **Next: the rest of Phase L is DEFERRED to the daemon-economy round** (shop/loot acquisition first — the user's call): the `battleTrigger` config vocabulary + the 8 kept sketches, multi-daemon composition, DoT/stun primitives, roster removal, the daemon-list HUD (ROADMAP §Phase L). **Next phase: M** (per-turn leveling + presentation; the empowered-unit/status VFX pass lands there) → **N** (rogue mobility + the now-firmly-required band re-sweep — daemons join empower as band-movers).
- **M1 ✅ COMPLETE (Phase M, 2026-06-12, 2 commits: `89c3cae` headless + the scene-flow pass) — per-turn promotion cadence.** XP banks at the TURN boundary (`handleTurnEnded` → `bankXpAwards` per turn) instead of accruing to encounter end; promotions pause the loop AT the boundary — gated: PostTurn screen first, then `advanceTurn` interposes `phase='promotion'` before `continueAfterTurn` (the stash rides `pendingPromotions` across the `turn-outcome` save, both persisted); headless: the pause interposes directly (the fuzz harness's generic `promotion` case absorbs it — zero harness logic change). `handleDismissPromotion` discriminates on `encounterMap !== null` (mid-encounter → re-enter the loop via the pure `turnResult`; rest → map; legacy leg defensive). A LOSING turn never banks (defeat is terminal — no level-up screen ahead of the defeat screen). `pendingEncounterXp` RETIRED → **RunSnapshot v16→v17** (v16 rejected). The leveling RATE untouched (N3). **MEASURED band-neutral by stash-compare** (pure-random 22.0%→22.0%, greedy 26.0%→24.0% over 50, floors ±0.04 — the M1 entry in [BALANCE.md](BALANCE.md)): mid-encounter crossings are rare at the shipped rate, so cadence = surfacing, not power. 899 main / 186 fuzz:smoke / typecheck+lint clean. **Commit 2 = the scene-flow truth pass + browser e2e proof** (comment-only code change — Game.ts scene wiring was already event-driven, exactly as predicted): on `dev-preview` with a `?roster=…:1×5&daemon=none` level-1 run, BOTH legs verified live — **mid-encounter** (turn 1 → PostTurn → "2 PROMOTIONS" screen with `encounterMap` still set + pools alive → CONTINUE → TURN 2 pre-turn on the SAME river map, the hand fielding the freshly-LV-2 cards) and **won-final-turn** (turn 2 → outcome → "LEVEL UP!" with enemy pool 0 but `encounterMap` STILL set [the pause precedes `finishEncounter`] → CONTINUE → `encounterMap` null + RecruitScene offer); event trace pinned `turn:resolved → promotion:pending → recruit:offered`, zero console errors. Standing roadmap note: **M5 (layout-editor save path) must land before M6.**
- **M2 ✅ COMPLETE (Phase M, 2026-06-12, 2 commits) — the level-up screen staggered-reveal redesign.** The design round locked 4 calls; the user's playtest then **REVISED the sequencing call: cascading pipeline → two-phase card-by-card** ("we don't want eyes drawn to multiple units at once") and **slowed the cadence** (CARD_STAGGER 200→400 / LAND_TO_REVEAL 260→660 / REVEAL_STAGGER 200→400ms). Final shape: cards pop in staggered (scale-overshoot via `.is-landed`), each landing in its PRE-level state (old level, old stats, all rows dim amber); once ALL cards have landed, cards reveal **strictly one at a time** — level value ticks first (stays green), then each grown stat turns green and flips old→new with a `+N` chip (clean resting state, no arrow), healtick blip per beat; the active card carries `.is-revealing` (brightened FLOURESCENT_BLUE border = the explicit focal point) and a `CARD_HANDOFF_MS` (400) breath separates cards. **Click anywhere (except Continue) skips** all pending beats to the fully-revealed end state with audio muted; **Continue is always enabled**. Implementation: a setTimeout beat timeline in [PromotionScreen.ts](src/ui/PromotionScreen.ts) (`scheduleBeat`/`finishTimeline`/`cancelTimeline` — `hide()` cancels so no blip fires on a dismissed screen; phase 2 scheduling is one flat `at` cursor over the rendered cards) + the state CSS in [ui.css](src/ui/ui.css) (`promotion-pop` keyframe; `inline-block` so the scale transform applies to value spans). **Render-only — zero sim/run/snapshot/fuzz impact.** Browser-verified on `dev-preview` via forced `promotion:pending` emits, both rounds: timestamped DOM traces pinned the final shape — zero reveals during phase 1, `is-revealing` flags read `100→000→010→000→001→000` (never two at once), 4/3/3 gain rows fill per-card, skip jumps 0→fully-revealed synchronously incl. highlight cleanup, zero console errors. **Same session: the user live-tuned the leveling curve** ([config/leveling.json](config/leveling.json) `baseXp 100→20, exponent 1.1` — levels MUCH faster; separate `balance` commit) which broke a `Run.test.ts` promotion pin that hardcoded `xpGained: 100` (== old `xpToNext(1)`) — fixed by switching the four promotion-phase cases to the file's own `xpToNext(1)` convention (balance-proof rule). **The tune was a TESTING knob (`baseXp 20`) at M2; the user then raised it to `baseXp 50` and LOCKED it as a playtest-validated feel target (N2-opening, commit `8e37203`) — N3 is now a CONSISTENCY CHECK, not a from-scratch re-derivation; N2 sweeps the band AROUND 50/1.1. See the BALANCE.md N2-opening entry.** M3 followed same-session (below).
- **M3 ✅ COMPLETE (Phase M, 2026-06-12, 1 commit) — scene polish: kill auto-progression + turn fade-in/out.** (a) The post-turn auto-timer (`POSTTURN_AUTO_MS` 3000) is REMOVED — [PostTurnScreen.ts](src/ui/PostTurnScreen.ts) advances only on Continue; with K3's pre-turn removal, turn pacing is now fully player-driven (the [Command.ts](src/run/Command.ts)/PreTurnScreen comments truthed up). (b) **Turn-intro materialize:** initial combatant placements fade in via the D5.C channel (`onUnitSpawned` now fades BOTH branches — `instant` picks the window; walls/neutrals still pop, they're scenery) over the new **`turnIntroSeconds`** knob ([config/spawn.json](config/spawn.json), 0.8s, render-only — no ticks derivation), while [BattleScene.ts](src/scenes/BattleScene.ts) parks the sim clock for the same window (`introRemaining`, counted in REAL dt so fast-forward doesn't shorten it; renderer + terrain still update so the fade plays) — units materialize, one breath, then combat. The hold only delays when ticking STARTS; the tick sequence is byte-identical. (c) **After-turn outro:** [Game.ts](src/Game.ts) defers the `turn:resolved` → PostTurnScene swap via `swapAfter(TURN_OUTRO_MS = 900)` — the resolved board lingers while death fades/hitsplats drain (`World.tick()` no-ops once `_ended`, so the still-running clock is harmless); `swap()` cancels any pending deferred swap defensively. **Presentation-only — zero sim/run/snapshot/fuzz impact; 899 main green, typecheck+lint clean.** Browser-verified on `dev-preview`: a driven-tick probe (tab-throttle-immune — `scene.tick(0.1)` by hand) pinned tick 0 through the 0.8s hold with all 13 combatant sprite fades + overlay fade-ins running at mount, then ticking resumed; the outro kept `BattleScene` active through the window before PostTurnScene mounted (measured ~1.9s under the background-tab timer clamp; the mechanism is the 900ms const); the outcome screen was still up 4s later (`turn-outcome` phase held = no auto-advance) and Continue advanced normally; zero console errors. ARCHITECTURE drive-by: the H4b PreTurn/PostTurn scene+screen files were missing from the catalog — added. The user's M3 feel verdict: "a lot better — particularly the linger at the end." M4 followed next session (below).
- **M4 ✅ IMPLEMENTED (Phase M, 2026-06-12, 2 commits `24297ab`+`b86da92`) — the battle backdrop: apron + fog + mist floor.** The design round locked the user's own proposal (= the roadmap's ground-plane-extension branch): a **2-tile non-playable apron ring** continuing the board outward, fog-faded into the void — **dynamically generated, render-only** (the sim never sees the tiles; non-traversable by construction), with **clamp-to-edge tile sampling** (the user's call, overriding the floor-only suggestion: the river layout's water flows out into the mist instead of cutting to lawn; walls don't extend — they're entities, and a wall *ending* is natural). [ApronRenderer.ts](src/render/ApronRenderer.ts) is a SEPARATE mesh (board buffer capacity, pickCell-unclickable by construction, canonical terrain shader untouched) reading heights through the live `TerrainRenderer.heightAt` (the fixed-seed simplex continues outside the board) + the now-exported `topColorFor`/lighting consts — canonical-by-construction. `APRON_TILES = 2` is THE width knob (buffer + fade derive from it). **Playtest revision same session (`b86da92`), 3 user calls:** (1) **smooth fade DEFAULT** (the Bayer stipple read out of place — nothing else dithers yet; `__game.apron.setDither(true)` keeps the A/B); (2) **edge band** — crisp near-black outline on the apron's innermost ~0.12 tiles = the strong playable-boundary read; (3) **the mist floor** — [BackdropRenderer.ts](src/render/BackdropRenderer.ts), a 600-unit noise plane at BOTTOM_Y (slow two-octave value noise around TERMINAL_BLACK, calms to flat background with distance → no seam at plane edge or ultrawide horizon), and the apron's fog target = `fogColorAt` sampled **where the view ray meets the mist plane** → a fully fogged tile is pixel-identical to the mist behind it (the board dissolves INTO the mist). The shared noise fn lives ONCE in [fogcolor.glsl](src/render/shaders/fogcolor.glsl), TS-concat prepended to both frags. The summed-sine **creep** animates the fog edge (uFadeEnd shortened by the creep amplitude so the rim never ghosts). Verified by pixel probes (screenshot times out under tab-throttle): edge-band dip exactly at the camera-projected playable edge, mist spatial+temporal drift, rim≡mist seamless, programs diagnostics-null (console shader errors were STALE HMR-window artifacts — check live `material.fragmentShader`/`properties.get(material).currentProgram.diagnostics`, not the session-aggregated console buffer). Zero sim/run/snapshot/fuzz impact; 899 main green. **First-eyeball verdict: "This looks great!"; the THOROUGH playtest then PASSED (2026-06-13) — the user's one question, an intermittent "double edge" at the mist's outer reach, was diagnosed as the creep folding at the grazing 45° pitch (frozen-sim advance-only-`uTime` rim scans: the only moving edge; no static brightness step at the apron→plane boundary, so the plane is aligned by construction — the two `uTime`s advance in lockstep) and kept as-is → M4 SIGNED OFF.** Knobs if ever wanted: `MIST_AMPLITUDE`/calm radii/drift speeds (fogcolor.glsl), `EDGE_BAND_TILES`+strength (apron.frag.glsl), `APRON_TILES`.
- **M5 ✅ COMPLETE (Phase M, 2026-06-13, 3 commits `928ad60`+`4496042`+`968f69b`) — the layout-editor auto-edit (write straight to config).** The "auto-edit layouts instead of copy-pasting JSON" dev ask, landed BEFORE M6 so the map redesign isn't a copy-paste grind. **Dev-tooling only — no sim/run/snapshot/fuzz impact** (the one `src/` change is an `export` of `LayoutsSchema`). **Commit 1 (`928ad60`) — formatter + normalize + test (headless-provable):** lifted the inline single-entry JSON formatter out of [editor.ts](tools/layout-editor/editor.ts) into a node-safe [format.ts](tools/layout-editor/format.ts) (sibling of I4's [archetype-editor/format.ts](tools/archetype-editor/format.ts)) — an indent-parameterized per-entry generator + `formatLayoutJson` (the standalone Copy/Download snippet) + a new `formatLayoutsJson` (the whole-array formatter Save POSTs). `config/layouts.json` was internally INCONSISTENT (labyrinth/river/endlessCorridors packed spawn tiles 4-per-line, the other three 1-per-line) so no single deterministic formatter could reproduce it → **normalized to canonical 1-per-line** (the editor's long-standing emit, most diff-friendly; whitespace-only, order preserved, proven data-identical to HEAD by canonical-JSON deep-compare — the 48/12-line diff). [tests/tools/layout-editor.test.ts](tests/tools/layout-editor.test.ts) pins the two guarantees (mirroring `archetype-editor.test.ts`): `formatLayoutsJson(LAYOUTS)` reproduces the committed file verbatim + round-trips through the exported `LayoutsSchema` to a deep-equal config. **Commit 2 (`4496042`) — Save to config (browser-verified):** a **Save to config** button writes the current layout into `config/layouts.json` via the dev-only `/__save-config` endpoint (I4 built it; it already allowlisted `layouts.json` — zero server work). A `working` LayoutDef[] (boot `LAYOUTS` + session saves) is the merge base + the source for Load / the dropdown / the id-collision check / Save. **New id → appends; existing id → overwrites IN PLACE behind a `window.confirm`** (the user's overwrite-with-confirm call; array order preserved — `LAYOUT_IDS` order seeds rng.pick determinism). The whole merged file is POSTed through `formatLayoutsJson` (`LayoutsSchema.safeParse` gates the write); Save enabled only when validation has no errors + id/name/description present (`isSavable`, synced in `refreshValidation`); the id-collision note reframed "append-only" → "Save will overwrite in place". **The Vite reload (saving rewrites an imported file) is MASKED via sessionStorage:** a successful save stashes `{savedId,status}`, the next boot (`restoreAfterSave`) re-loads that now-on-disk layout into the canvas + re-shows the confirmation — so a save feels seamless. (Dependency-accept HMR suppression was tried first and didn't reliably catch the json→ts→editor chain; the stash is deterministic.) Copy/Download stay as offline fallbacks. Browser-verified e2e on `dev-preview`: editor loads clean; Save disabled→enabled; an overwrite save (confirm→write) is BYTE-IDENTICAL (clean git diff = the formatter guarantee proven live through the real endpoint); an append lands a new entry at the END; the reload restores canvas+status; the throwaway append-test entry was reverted. **Commit 3 (`968f69b`) — square cells for non-square grids (user request):** `buildGrid` previously stretched cells to flat rectangles when a tall/wide grid's height budget was tight (independent cellW/cellH); now the cell side is a single `min(width-fit, height-fit)` so cells stay SQUARE and the whole grid fits without scrolling whatever the aspect ratio (a tall layout shrinks to a narrower square-celled column; 6px floor kept for clickability). Browser-verified (1600×1000): 12×18→~43px square, 8×32→~24px square, 32×8→~25px square — square within sub-pixel across every aspect ratio, fits no scroll, zero console errors. **901 main / typecheck + lint clean. User verdict: "working like a dream."**
- **M6 ✅ (Phase M, 2026-06-13) — the water mechanic ✅ + a from-scratch PROCEDURAL-MAP rework ✅ DEPLOYED + half-cover windows ✅.** **Floor-gating DEFERRED** (the one remaining M6 item — user call: a proper encounter-system spec is close and will reshape the per-floor difficulty targets, so tuning `rollLayoutId`'s depth curve now would just be redone). **WATER MECHANIC ✅ (2 commits, DESIGN ROUND → "slow + miss more", both playtested + signed off):** (a) `a24f62f` **bog-down precision penalty** — a unit striking *while standing on a `shallow_water` tile* fights at `precision − waterPrecisionPenalty` (new knob, [config/stats.json](config/stats.json) = 10) before the I2 to-hit roll, in `World.applyDamage` (occupant-ATTACKER only — shooting INTO water from dry land is unaffected; only *evadable* strikes; mage AoE / catapult unmissable so they ignore it); read live off `tileGrid` like the fire/heal pass → **NO snapshot bump**, byte-identical on water-free boards; +4 `World.test.ts` cases. (b) `a70533d` **the move-duration slow** — the playtest caught that cost-2 `shallow_water` only ever weighted A\* route SELECTION, never the move DURATION (a unit forced through water forded it at full speed); new `stepDurationTicks(world, dest, base)` in [movement.ts](src/sim/movement.ts) scales a step's lockout + render-lerp by the destination's TileGrid cost (floor 1→base, water 2→2×), applied to the normal one-cell step (direct + sidestep) AND the 3 `SupportMovementBehavior` healer moves; the N1 dash (`maxCells>1`) + the healer SWAP stay at base (a leap's terrain interaction is N1's call); `MoveAction` already serializes `durationTicks` → no bump; +3 `movement.test.ts` cases; **browser-verified live on the river layout: 94 floor steps all == base, 18 water steps all == 2×base.** 908 main / fuzz:smoke 186 / typecheck + lint clean. (Both shift the water-board fuzz baseline → N2.) **PROCEDURAL-MAP REWORK ✅ DEPLOYED (4 commits `f2add1c`→`3808657`; the M6 *placement* question grew into a full from-scratch rework — user call).** Procedural walls were a uniform 6%-scatter with NO chokepoints, so "puddles on the path" couldn't bite; the design round LOCKED a **crossbar + divider + noise blend** built around our top/bottom-clash topology, prototyped in [tools/mapgen-prototype/](tools/mapgen-prototype/) to nail the look, then ported into `src/`.
  - **(1) `f2add1c` — the port + the CONFIG ENVELOPE.** The generator lives in [proceduralMap.ts](src/sim/proceduralMap.ts) (`generateProceduralMap` consumes the per-encounter RNG fork DIRECTLY — no re-seed — and emits a real `GeneratedTerrain`: `TileGrid` + walls + halfCovers + `SpawnRegion[]`; chasms/fires/healings empty), wired into [terrainGen.ts](src/sim/terrainGen.ts)'s procedural path (the uniform scatter + its helpers REMOVED; the legacy `wallDensity`/`shallowWaterDensity`/`ensureConnectivity` knobs kept in config but UNUSED, slated for removal). **Knobs are RANGES, not fixed values (the user's design call):** `config/terrain.json#procedural` declares per-knob ranges — uniform, or biased toward a `center` by an `intensity` (0=uniform→1=peaked) via a uniform↔triangular blend (NOT a power-law — the user wanted a middle-heavy option) — plus weighted discrete choices for crossbars/gapsPerBar/dividers/symmetry; `sampleProceduralParams` draws a concrete `ResolvedMapParams` per encounter (ONE RNG draw per knob, fixed order) so maps vary seed-to-seed within a designer-set envelope. The samplers ([sampling.ts](src/core/sampling.ts) — `weightedPick`/`sampleRange`/`sampleIntRange`, one draw each) are generic + tested. Generator shape: **CROSSBARS** = wavy horizontal walls with fordable GAPS (a watered gap = an M6 ford; waver samples the noise field, vertical-bridged so no diagonal leak); **DIVIDERS** = vertical partial walls; **NOISE** = one value-noise field → SOLID cover clumps + low-ground water pools; **SYMMETRY** = `point`(default)/`mirror`/`none`, with the obstacle-cap + connectivity guards kept symmetry-aware via a `partner()` map (the guard carves a watered breach if the gaps ever seal).
  - **(2) `1fe0861` — force procedural: `?layout=procedural`** (browser) **+ `--layout=procedural`** (fuzz) via a `FORCE_PROCEDURAL` sentinel threaded through `forcedLayoutId` ([RunConfig.ts](src/run/RunConfig.ts)/[Run.ts](src/run/Run.ts); `null` still = "not forced", a named id = that layout, the sentinel = force a fresh procedural roll). The fuzz isolate mirrors `--layout=<id>` — the clean full-sample read the N2 sweep wants for the new maps.
  - **(3) `9c6ba98` — the prototype rewired to the src generator** (its standalone `generator.ts` copy DELETED; [mapgen.ts](tools/mapgen-prototype/mapgen.ts) imports `src/sim/proceduralMap.ts`, so the tool shows EXACTLY what the game makes) **+ a "Roll knobs from config" toggle** (samples the live envelope per seed — sample-then-generate as the game does — so the Variety strip previews the real seed-to-seed spread; sampled knobs reflect into the disabled sliders).
  - **(4) `3808657` — half-cover as WINDOWS** (user playtest note: scattered open-ground half-cover read as nonsensical). Half-cover moved OFF the noise pass (now SOLID-only — rocks/rubble) INTO the structural walls: each crossbar/divider cell has a `windowChance` of being a half-cover WINDOW — **shoot-through but movement-blocking**, so the barrier still funnels to the fords while ranged units get a firing lane across it. Knob renamed `halfCoverFraction`→`windowChance` (per-wall-cell prob). Browser-verified: windows render embedded in the wall lines, not floating.
  - **All 4 commits playtested + signed off.** **Open:** the `coverDensity` mapping is non-linear (value-noise clusters mid-range — recalibrate if cover should bite harder); **floor-gating still DEFERRED** behind the encounter spec; **the terrain change SHIFTS the fuzz baseline → the N2 re-sweep is required** against the final model (isolate procedural via `--layout=procedural`).
- **N1 ✅ COMPLETE (Phase N, 2026-06-14, 5 commits `c9520e8`→`6cc7484` + SFX `dc12c9f`→`adebfd2`) — the rogue DASH.** The ability config became a **`kind` discriminated union** (`attack`|`heal`|`movement`) — the runtime ability model stays FLAT (propose+score; `AbilityBehavior` never sees `kind`; the discriminant is data-shape only + the home for future special attacks), heal split to its own kind (dead accuracy/crit dropped), typed `attackConfig`/`healConfig`/`movementConfig` accessors. **`DashAbility`** = aggressive-close (dash when the target is beyond `derived.attackRange` & the dash is ready, even if it can't fully close), lands ADJACENT (target stays a soft-blocker), motion duration DECOUPLED from the cooldown (builds its own proposal; `cooldownKey:'dash'` so it never touches the move cadence); [config/abilities.json](config/abilities.json) `#dash` = movement / range 2 / 0.25s motion / 10s cd (flat, not speed-scaled); rides J2's new `leapLanding`+`walkAlongPath` ([movement.ts](src/sim/movement.ts)); `rangeForArchetype` EXCLUDES movement so the dash range can't inflate strike reach; **NO snapshot bump** (rides the serialized `actionCooldowns`). **dash SFX** promoted to a first-class **`unit:dashed`** event ([DashAction.ts](src/sim/actions/DashAction.ts), mirrors `unit:swapped`) after a >1-cell-`unit:moved` heuristic was caught dropping SILENT 1-cell dashes; BattleScene plays `dash` off it (`dash.wav`). Browser-verified live (rogues spawn `[gambit_strike,dash]`, attackRange stays 1, 2-cell leaps over 5 ticks, 6 dashes→6 `unit:dashed`→6 `play('dash')` incl. three 1-cell dashes, zero console errors); 950 main / 186 fuzz:smoke (byte-identical — the dash only changes rogue-bearing sims). **The rogue fuzz baseline SHIFTED → N2 (the band re-sweep + the rogue `weakest`-targeting re-measure).** Playtest signed off.
- **Tests:** 950 passed (N1 — +11 over M6's 939: +3 `abilities.test.ts` [the `kind` discriminant — heal is its own kind / every attack declares kind / evadable+critable gates re-pinned per-kind] + 6 `dash.test.ts` [DashAbility propose: aggressive-close gate / decoupled duration-vs-cooldown / lands-adjacent / score-between-walk-and-strike] + 2 `DashAction.test.ts` [dual-emit unit:moved+unit:dashed / toData round-trip]; the rogue-battle integration test gained a `unit:dashed` end-to-end assertion in place; M6 — +38 over M5's 901: water +7 [+4 `World.test.ts` bog-down precision-penalty [`a24f62f`] + +3 `movement.test.ts` move-duration slow [`a70533d`]]; procedural rework +31 [+12 `sampling.test.ts` weightedPick/sampleRange-triangular; +17 `proceduralMap.test.ts` = param-sampling envelope + generator invariants (determinism / stats / wall-cap / connectivity across symmetry / point+mirror symmetry / windows-only-on-structural-walls); `terrainGen.test.ts` net −2 rewritten from density-counts to structural invariants; +2 `RunConfig.test.ts` + +2 `Run.test.ts` for the `?layout=procedural` sentinel]; M5 — +2 over M1's 899: 2 `tests/tools/layout-editor.test.ts` formatter byte-for-byte + schema round-trip; M1 commit 1 — +7: the `Run.test.ts` per-turn-cadence block [mid-encounter pause + dismiss-continues / multi-pause per encounter / leveled-template re-field / gated outcome→promotion→intro interpose / outcome-save keeps the stash / v17 promotion-pause round-trip / same-seed determinism], with the 3 old encounter-end banking pins reworked in place + the H4 `encounter-loop` integration driver gaining the harness-style dismiss-and-continue branch; 892 at L1 — +25: 14 `daemon.test.ts` [pure roll/gate resolution incl. the chance-draw contract + the shipped catalog's design-shape pins] + 11 `Run.test.ts` L1 block [roll determinism + catalog coverage / force + daemon-less / per-idol gate behavior / mercury's per-turn coin / payload identity / v16 round-trips incl. the saved flip + a bespoke daemon]; 867 at K4 — +19: 10 `empower.test.ts` pure rules [both budget modes] + 9 `Run.test.ts` K4 block [store apply/fielded fold/budget/rejects/phase guard/reset+stacking/slot-survives-redraw/events/determinism/v15 round-trip]; 848 at K3.5 (K3.5 — +6 `Run.test.ts` encounter-map block; 842 at K3 commit 1 — +20 over K2's 822: 10 `redraw.test.ts` pure rules [both config modes] + 10 `Run.test.ts` redraw block; 822 at K1 COMPLETE — +42 over J4's 780: commit 1 +32 [14 `statusEffects.test.ts` + 8 `Unit.test.ts` effect + 9 `triggers.test.ts` + 1 snapshot-roundtrip v24], commit 2 +10 [9 `Run.test.ts` encounter-store/run-trigger/v12 + 1 net `fatigue.test.ts` `fatigueEffect`]; 780 at J3 billboard-aware unit pick — +5 over the J3 playtest fixes: 5 `pick.test.ts` [`pickInstanceAtNdc`: cursor-over / off-billboard / empty list / frontmost-of-overlap / behind-camera]; 775 at the J3 playtest fixes — +5 over the J3 UI commits: 4 `Pathfinding.test.ts` [`findPath` best-effort: closest-reachable on a blocked goal / approach a walled-off goal / identical-to-strict when reachable / `[start]`-hold when walled in] + 1 `MovementBehavior.test.ts` [a wall on the rally cell doesn't freeze]; 770 at the J3 objective UI + rebindable hotkeys — +15 over J2: 11 `Keybindings.test.ts` [dispatch/repeat-skip/unbound+no-subscriber preventDefault gating/multi-handler/unsubscribe/rebind-reroute/config-default coverage + `keyLabel`] + 4 `objective.test.ts` [`objectiveAtCell` enemy-vs-tile/full-coord-match/empty-list]; 755 at J2 pathing seam — +13 over J1: `movement.test.ts` [context/route/`advance` incl the `maxCells>1` dash hook] + 2 `pathing-perf.test.ts` bounded-recompute guard cases; 742 at J1 objective model + targeting preemption — +20 over I6: Targeting/MovementBehavior/World objective blocks + snapshot v23 round-trip/reject + integration determinism; 722 at I6 per-ability combat profile COMPLETE — +3 net over I5: the `abilities.test.ts` gate pins + reworked `critChanceFor`/`hitChanceFor` balance-proofs + the I6 snapshot round-trip, MINUS the rogue/healer battle smokes consolidating 6 seed-cases → 2 robust ones; 719 at I5 commit-1 melee subclass family — `melee→mercenary` + Adventurer/Ronin/Bandit, +6 over I4: 3 from config-derived `ALL_ARCHETYPES` loop growth + 3 new `archetypes.test.ts` cases; 713 at I4 archetype-editor formatter fidelity/round-trip; 711 at I3 fast-forward `PlaybackSpeed` cycle; 706 at I2 dodge hit/miss roll; 694 at I1 dodge-stat plumbing, 689 at H7c targeting strategies, 675 at H6, 665 at H5b, 664 at H5, 654 at H4b, 628 pre-H4), 0 `it.todo()`. Run with `npm test`. Fuzz smoke (separate suite, opt-in): 186 pass (L1c3 added +13 — 7 `daemonSelection.test.ts` [flag parse incl. unknown-id throw / config resolution / labels] + 6 `harnessDaemon.test.ts` [random≡absent byte-identical / none / fixed per idol / mercury-coin determinism / live-vs-control divergence under a redraw bot / `perDaemonStats` bucketing]; K4c3 added +16 — 12 `empowerPolicy.test.ts` [flag/JSON/selector] + 4 `harnessEmpower.test.ts` [none≡absent / determinism / live / composes-with-redraw]; K3c3 added +40 over J4's 117 — +14 scored-objective [`objectiveStrategy`/`arena` blocks] + +26 redraw-policy [`redrawPolicy.test.ts` rules/JSON + `harnessRedraw.test.ts` incl. the gates-on control]; J4 had added +28 over the H7d-era 89) — `npm run fuzz:smoke`. **Fuzz sweep (`npm run fuzz`, 20 seeds default): K3.5's one-map-per-encounter RESTRUCTURED the run RNG stream, so the K2-band read shifted at unchanged knobs (`budgetFactor 0.75 × swarmMax 2.0 × enemyArcherRatio 0.3`): pure-random now ~20% win / avg floor ~5.9 over 50 seeds (greedy 15% over 20) + 1 procedural hang/50 — EASIER than the K2-era ~5–10% / ~4.9 / 0-hang read and above the weak-bots-rarely-win target; left loose DELIBERATELY → re-tuned in N2 against the final model — and K4c3 then MEASURED the empower shift: a naive empower bot lifts BOTH baseline strategies to ~28–34% win / floor ~6.7–7.1 (+13pp, ~4σ — see the K4c3 entry in [BALANCE.md](BALANCE.md)), so the N2 re-sweep is now firmly required — while M1's per-turn promotion cadence stash-compared band-NEUTRAL on top of it all (pure-random 22%→22%, greedy 26%→24% over 50; the M1 entry in [BALANCE.md](BALANCE.md)). **M6's procedural-map rework + windows change the terrain structure substantially → the fuzz baseline shifts AGAIN (terrain strongly moves win rates — the K3 comp×map interaction); the new procedural maps are isolable for the N2 sweep via `--layout=procedural` / `?layout=procedural`.** See the K3.5 entry in [BALANCE.md](BALANCE.md) (incl. the favorable-map-compounding hypothesis). K2 had MOVED this band (it was `0.625 × 1.75`): the hand-6 budget bump + the wave-size-count fix + archer 0.3 + the 2-D re-sweep — see the K2 entry + [BALANCE.md](BALANCE.md). COARSE + provisional → re-swept in N2. (Supersedes a stale pre-H7c "100% win" note that used to live here.)** H7a added the `scored` strategy (`npm run fuzz -- --strategy=scored`, or `--strategy=<file>.json` for any weight vector); H7b's `npm run fuzz -- --search` random-searches its weights for the best-achievable win rate (quick/overnight presets; emits `output/best-strategy.json`). At the current (unbalanced) config the best discoverable play is still **100%** — that's H7c's signal to tune against. Full strategy menu: `npm run fuzz -- --strategy=all` — **one pre-existing residual: `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, NOT a positioning deadlock — out of GP5 scope; see the GP5.1 entry).** Per-floor balance analysis: `npm run fuzz -- --per-floor`. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`.
- **Dev server:** Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent). **The user usually has their own `npm run dev` on 5173 during a session**, and the preview MCP refuses to attach to a server it didn't start ("not a preview server"). For agent browser-verify, use the **`dev-preview` launch config** ([.claude/launch.json](.claude/launch.json)) — `npm run dev -- --port 5191 --strictPort` binds a dedicated port that never collides with 5173, so `preview_start name=dev-preview` always works. **Dev handle:** `window.__game` (DEV-only) exposes `bus`/`renderer`/`run`/`activeScene`/`keybindings`/`sprites`; drive a battle headlessly with `__game.dispatch({kind:'enterNode',nodeId})` then `{kind:'advanceTurn'}` (past the PreTurnScene gate), and freeze the auto-advancing run with `scene.clock.advance = () => {}` for a stable screenshot.
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**Phase H is COMPLETE (H1–H7 all ✅, 2026-06-07)** — the multi-turn "deckbuilder" trial: `power` stat, spawn-tile range, deployment counter, the health-pool encounter loop (H4a headless + H4b screens/HUD), the card-drawn hand + `playerTeamLevel` seam swap (H5/H5b), recruit-pass + rest-heal + an inert fatigue hook (H6), and the H7 search-driven balance tooling + difficulty/archetype sweep (H7a scored DSL → H7b `--search` → H7c the sweep + band-set `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance + per-layout/floor telemetry → H7d the sweep GUI + `--jobs`-for-`--search`).

**The Phase-H playtest feedback is now synthesized into a fresh [ROADMAP.md](ROADMAP.md) — "Post-H" (Phases I → N); the brief is archived at [archive/phase-h-feedback.md](archive/phase-h-feedback.md) and the prior roadmap at [archive/post-f-roadmap.md](archive/post-f-roadmap.md).** **Phase I IN PROGRESS (foregone conclusions & combat identity) — I1 ✅** (the `agility→speed` revert + `precision`/`evasion` mid-block adds + canonical reorder `CON·STR·RNG·MAG·LCK·DEF·PRC·EVA·SPD·MOB·POW`; WorldSnapshot v19 + RunSnapshot v10; fuzz re-baselined — see the I1 entry above). **I2 ✅** (the Fire-Emblem subtractive hit/miss roll at `world.applyDamage`, single-target strikes only [mage AoE / catapult / env unmissable]; the `unit:missed` event + "Miss" hitsplat; NO further snapshot bump — see the I2 entry above. **`hitChanceBase` shipped at 0.75 but the user tuned it to `0.6` live [commit `36b3648`, 40% whiff] — the current base is 0.6.** Two pre-existing render bugs were also fixed in the playtest follow-up [glyph-atlas serif race; off-axis hitsplat drift] + a louder Miss splat + [HITSPLATS.md](src/ui/HITSPLATS.md) styling guide. The fuzz hang FINDING [misses lengthen battles] was **RESOLVED by bumping both turn caps 100→150s**; at 0.6 the hangs drop pure-random 5→0 / greedy 7→3, and cap-unification is logged for N2). **I3 ✅** (fast-forward 1×/2×/3× — a tick-batching `dt`-multiplier on the fixed-timestep loop [`BattleScene.tick` scales `dt` for the `Clock` + renderer + terrain], NOT a `TICK_RATE` change so the sim is byte-identical [no snapshot/fuzz impact]; HUD button + `F` hotkey [config-backed in `config/playback.json` so J3's rebindable layer can absorb it]; the chosen speed **persists across battles**; profiled **≤0.236 ms/tick on big boards → 3× is safe, kept `[1,2,3]`** [re-check after J's objective-thrash path recompute] — see the I3 entry above). **I4 ✅** (commit `feat(I4)` `2d27f73`): the schema-driven archetype editor (`tools/archetype-editor/`) — live `ArchetypesSchema` validation + live derived-stat preview + a save-to-disk path via a new dev-only Vite `/__save-config` endpoint that **M5 reuses for the layout editor**; dev-tooling only, no sim/snapshot/fuzz change — see the I4 entry above. + a separate `balance` commit (`9441f5c`) for the user's editor-authored rogue dodge-tank tuning (re-measured in I5). **I5 IN PROGRESS (DESIGN ROUND done, staged 3 commits):** all 3 commits ✅ — (1) the melee subclass family (`melee→mercenary` rename + Adventurer/Ronin/Bandit, all recruitable; WorldSnapshot v20 + RunSnapshot v11); (2) enemy melee `mercenary→bandit` swap (Bandit fodder scales at ~half growth, EASING deep floors — Phase-N re-sweep recalibrates); (3) the rogue dodge-tank re-measure (**VERDICT: dodge alone insufficient — Phase N N1 mobility STAYS, scoped for *reach* not survival; dodge fixed the rogue's durability but it's now damage-starved**). See the I5 entry above + [BALANCE.md](BALANCE.md). **I1–I5 ✅; a playtest insight then added I6 — per-ability combat profile (`might`/`accuracy`/`critBase` on each ability/weapon).** The diagnosis: every attack computes damage/hit/crit from the SAME formulas, so only stats differentiate them — I6 moves those onto each ability (finishing E5's per-ability move for cadence/range), which is what makes I5's subclasses actually feel distinct (e.g. Bandit Club +2/40%/0% vs Mercenary Sword +5/60%/5% vs Ronin Katana +4/60%/20%). Splits `melee_strike` into per-subclass weapons (Sword/Club/Katana/**Whip** — the Indiana-Jones adventurer) + renames `ranged_shot→bow`; also adds explicit **`evadable`/`critable` config flags** — a designation system that migrates I2's hard-coded per-call-site `evadable` into the ability config (mage/catapult `critable:false`, user call). Behavior-neutral plumbing first (defaults reproduce today byte-for-byte), then author the weapon values; an ability display-name/description system is logged as a TODO (pairs with the I5 archetype-label TODO). Full plan: [ROADMAP.md](ROADMAP.md) §I6. **I6 ✅ COMPLETE — commit 1 (byte-identical plumbing + WorldSnapshot v20→v21 for the per-ability crit move) + commit 2 (the weapon rename/split `melee_strike`→sword/club/katana/whip + `ranged_shot`→bow, authored per-weapon values, mage/catapult `critable:false`, v21→v22; see the I6 entry above). PHASE I IS NOW COMPLETE (I1–I6 all ✅).** **Phase J IN PROGRESS — J1 ✅** (the objective MODEL + targeting preemption, sim/headless: one shared player-team objective [tile persist-until-cleared / enemy auto-clears-on-death], engage radius `min(attackRange, rangedLeashCells)` + retaliation, set via `setObjective`/`clearObjective` WorldCommands, WorldSnapshot v22→v23, byte-identical with no objective so fuzz is unchanged — see the J1 entry above). Phase J = the in-battle objective system (the largest single agency item); it forces the **pathing overhaul** (J2), which is the seam N1's rogue mobility then builds on. **J2 ✅** (the pathing seam — shared movement-intent primitive in [movement.ts](src/sim/movement.ts) + cache DEFERRED behind the pure `routeToward` boundary + a `pathfindingStats` bounded-recompute guard; byte-identical, profiled ~1.8% of a 3× frame on a 60-unit board — see the J2 entry above). **J3 ✅** (objective UI + rebindable hotkeys — the FIRST PLAYTESTABLE Phase-J surface: a runtime-rebindable `Keybindings` registry seeded from `config/keybindings.json` [fast-forward `F` folded in], right-click / Set-button-arm / Clear objective input via the J1 commands, the amber `X` marker; no sim/snapshot/fuzz change, browser-verified — see the J3 entry above). **J4 ✅** (fuzz objective strategy + arena harness mode + the `--objective=<json|random|none>` flag on the full run fuzz / search / sweep — dev-only tooling, zero src change; see the J4 entry above). **PHASE J COMPLETE (J1–J4 ✅).** **PHASE K COMPLETE (K1–K4 ✅, 2026-06-11)** — the generic status-effect system + fatigue migration (K1), the roster/hand decouple + latent wave-size bug fix (K2), redraw (K3) + one-map-per-encounter (K3.5), and empower (K4 — MEASURED band-moving, +13pp on both baseline bots; N2 re-sweeps). **PHASE L — L1 ✅ (2026-06-12, design round LOCKED + 3 commits: the four-idol catalog [Mars/Minerva/Mercury/Janus] + DAEMON-ONLY gates + the PreTurnScreen idol surface + fuzz `--daemon` w/ per-idol telemetry — see the L1 entry above; the `battleTrigger` daemons + multi-daemon economy are DEFERRED to the shop/loot round, [ROADMAP.md](ROADMAP.md) §Phase L). MEASURED ([BALANCE.md](BALANCE.md) §L1c3): the idol roll is the game's biggest single lever — Minerva at +4 DEF hit +55pp over the daemon-less control (the flat-DEF stacking outlier) → **NERFED +4→+2 same-day (user call) and re-measured: 34.0%/200 ≈ Mars (31.5%) — the empower idols are peers, catalog spread now 14%→34%**; Mars ≈ the old always-on empower (clean cross-validation), the redraw idols ≈ texture (~1σ); N2 re-sweeps per-idol.** **PHASE M IN PROGRESS — M1 ✅ COMPLETE** (2026-06-12: per-turn promotion cadence — XP banks at the turn boundary, promotions pop mid-encounter, RunSnapshot v16→v17, stash-compared band-NEUTRAL; see the M1 entry above + [BALANCE.md](BALANCE.md) §M1). **M2 ✅ COMPLETE** (level-up screen staggered-reveal redesign — two-phase card-by-card reveal [playtest-revised from the pipeline] + clean green reveals + click-to-skip, render-only, browser-verified; the user also live-tuned the leveling curve `baseXp 20 / exp 1.1`; see the M2 entry above). **M3 ✅ COMPLETE** (scene polish — post-turn auto-timer removed [turn pacing fully player-driven], turn-intro materialize w/ sim hold [`turnIntroSeconds` 0.8s], after-turn outro [`TURN_OUTRO_MS` 900]; presentation-only, browser-verified; see the M3 entry above). **M4 ✅ IMPLEMENTED** (battle backdrop: 2-tile clamp-to-edge apron + smooth fog fade w/ creep + edge band + the BackdropRenderer mist floor; render-only, 2 commits, pixel-probe verified, first eyeball "looks great" — see the M4 entry above). M4 thorough playtest PASSED + signed off (2026-06-13; the "double edge" was the creep, benign). **M5 ✅ COMPLETE** (2026-06-13, 3 commits — the layout editor now writes straight to `config/layouts.json` via the I4 `/__save-config` endpoint [new id appends / existing id overwrites-in-place behind a confirm, the Vite reload masked via sessionStorage]; a node-safe `format.ts` whole-array formatter + byte-for-byte/round-trip test [`layouts.json` normalized to 1-per-line]; square editor cells for non-square grids; user verdict "working like a dream" — see the M5 entry above). **M6 ✅ — water mechanic ✅ + the from-scratch PROCEDURAL-MAP REWORK ✅ DEPLOYED (config-envelope range/weight sampling + the ported crossbar/divider/noise generator in [proceduralMap.ts](src/sim/proceduralMap.ts) + `?layout=procedural` + half-cover WINDOWS; 4 commits `f2add1c`→`3808657`) + the prototype rewired to src; floor-gating DEFERRED behind the encounter spec. See the M6 bullet above. **PHASE N IN PROGRESS — N1 ✅ DONE** (the rogue dash — `kind`-discriminated ability config + aggressive-close DashAbility + the first-class `unit:dashed` event; see the N1 bullet above + [ROADMAP.md](ROADMAP.md) §N1). **NEXT = N2 (fresh session): the band re-sweep** against the FINAL model (daemons / empower / per-turn leveling / terrain are ALL band-movers now stacked; isolate procedural via `--layout=procedural`) + N3 leveling consistency check (the curve is now LOCKED at `baseXp 50 / exp 1.1`, playtest-validated — N3 confirms the N2 band didn't distort the cadence, not a from-scratch re-derivation). The stale original-plan text that follows is superseded:** ~~NEXT = M6 — Maps: floor-gating + water mechanic~~ (the layout auto-editor that makes the map redesign painless is now in place): depth-weight `rollLayoutId` so hard/open layouts ramp in by floor (config-driven, [nodemap.json](config/nodemap.json) / [NodeMap.ts](src/run/NodeMap.ts)), and give **water a real mechanical effect** (movement-slow / evasion-precision modifier tying to I1's stats / partial cover — **DESIGN ROUND on the effect**), placing it where it's exercised. The map *authoring* redesign is the USER'S (one-tile corridors too spawn-dependent); we supply the gating + the water mechanic + the now-shipped M5 auto-editor. ROADMAP §M6. Then **N** rogue mobility (confirmed ON) + balance closure last. The deferred K4 empowered-unit-standout polish stays adjacent M-material. Earlier forward detail: **J** in-battle objective system (the pathing overhaul), **K** status-effect system + redraw/empower, **L** daemons, **M** per-turn leveling + scene/map polish (+ the layout auto-editor reusing I4's `/__save-config` endpoint), **N** rogue mobility [contingent] + balance closure. **Vocab note:** the brief's *skirmish* = our **turn**, its *battle* = our **encounter** (mapped in the roadmap). **The rogue salvage (mobility) is resequenced to Phase N and made contingent** — dodge is the interim fix (Phase I re-measure), and the mobility rework now builds on Phase J's pathing seam; the stage-5 overnight verify + the leveling-rate pass stay last (Phase N), now behind more band-movers. **READ [BALANCE.md](BALANCE.md) FIRST** for any balance work (the authoritative H7c run log + funnel); forward steps in [ROADMAP.md](ROADMAP.md), smaller follow-ups in [TODO.md](TODO.md), gameplay backlog (shop, synergies, boss variety) under [DESIGN.md](DESIGN.md) "Out of scope".

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phase A complete** — A1–A5 (action selector, command channel, fuzz harness, config externalization, scene system).
- **Phase B complete** — B1/B1.1 (palette + selective bloom), B3 (HP/progress bars), B5 (CSS scanlines), B6 (audio), B7 (root node `@`).
- **Phase C1 complete** — C1a/b/c/d (terrain + walls + LOS + faceted prisms + layout editor + Labyrinth pathfinding fix).
- **Phase D complete** — D1 (renderer cap), D2 (drag-paint), D3 (variable sizes), D4 (fit/scroll camera), D5.A–E (spawn regions + overflow queue), D6 (half-cover), D7.A–C (chasm + fire + healing), D8 (theming).
- **Phase E complete** — combat depth (E1–E7). E1 (stat vocabulary + `deriveStats`), E2 (generic ability layer, retiring `AttackBehavior`), E3 (per-archetype `growthRates` + leveling), E3.5 (`TICK_RATE` doubled 10 to 20), E3.6 (DOM unit overlays + hitsplats), E4 (XP loop + PromotionScene + half-cover mult), E5 (pathfinding refresh: target stickiness + boids sidestep), E6 (combat visuals: shove/projectile/hitsplats), E7 (the six combat archetypes: melee/ranged + rogue/healer/mage/catapult). Full per-step worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md); forward plan in [ROADMAP.md](ROADMAP.md).
- **Phase F complete** — Phase-E playtest response (F1–F6). F1 (draft-pool pull-forward: the E7 archetypes become draftable), F2 (first-class action phase timeline windup->...->recovery), F3 (projectile & impact timing), F4 (rogue gambit sequencing), F5 (heal feedback VFX), F6 (heal/utility XP). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase G complete** — run progression (G1–G5). G1 (`RunConfig` + short-run harness), G2 (longer maps + full-viewport map scene), G3 (rest & boss nodes + map icons), G4 (enemy level-*budget* model + recruit leveling), G5 (fuzz-strategy factory + GUI launcher). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase GP complete** — combat & stat-legibility playtest response (GP1–GP5). GP1 (`endurance`->`mobility` / `speed`->`agility` rename + universal move base), GP2 (`defense` stat + `World.applyDamage` consolidation), GP3 (recruit-card + HUD legibility), GP4 (ranged/catapult acting-position pathing), GP5 (healer chokepoint yield via `SwapAction` + centroid navigable-snap). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase H complete** — the multi-turn deckbuilder battle trial (H1–H7). H1 (`power` stat), H2 (spawn-tile range), H3 (deployment counter / fatigue hook), H4 (health-pool encounter→turn loop: H4a headless + H4b pre/post-turn screens & pool HUD), H5 (card-drawn hand + `playerTeamLevel` seam swap + H5b hand display), H6 (rest-heal + recruit-pass + inert spawn-time fatigue hook), H7 (search-driven balance tooling: scored DSL → `--search` → difficulty band SET `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance + telemetry + sweep GUI). Detail in [ROADMAP.md](ROADMAP.md) §Phase H + [BALANCE.md](BALANCE.md).
- **Phase I complete** — foregone conclusions & combat identity (I1–I6). I1 (`agility→speed` revert + `precision`/`evasion` dodge stats + canonical reorder; World v19 / Run v10), I2 (subtractive hit/miss roll at `applyDamage` + "Miss" hitsplat; live `hitChanceBase` 0.6, turn caps 100→150s), I3 (fast-forward 1×/2×/3× tick-batch, config-backed `F` hotkey), I4 (schema-driven archetype editor + dev-only `/__save-config`), I5 (melee subclass family `mercenary`/Adventurer/Ronin/Bandit + Bandit enemy + rogue dodge re-measure → N1 mobility CONFIRMED, scoped for reach; World v20 / Run v11), I6 (per-ability combat profile `might`/`accuracy`/`critBase` + evadable/critable gates, weapon split sword/club/katana/whip/bow; World v21→v22). Detail in [ROADMAP.md](ROADMAP.md) §Phase I + [BALANCE.md](BALANCE.md).
- **J1 ✅ (Phase J) — the in-battle objective MODEL + targeting preemption (sim, headless). FIRST step of Phase J (in-battle agency: the low-intensity steering layer — set ONE shared objective; units honor it only when not already engaged).** One commit (`feat(J1)`). **WorldSnapshot v22→v23** (the shared objective is mid-battle state — reject stale, no migration; RunSnapshot UNAFFECTED — objective is World-side + transient). **Byte-identical with no objective set → fuzz:smoke 89 unchanged, NO fuzz re-baseline** (every objective code path is gated on `world.objective !== null && unit.team === 'player'`).
  - **The model** ([objective.ts](src/sim/objective.ts)): `BattleObjective = { kind:'enemy'; unitId } | { kind:'tile'; cell }`, one shared player-team objective on `World` (`world.objective` getter + private `_objective`). Player-only (enemy AI never reads it). Set/cleared via the now-real `setObjective`/`clearObjective` [WorldCommand](src/sim/Command.ts)s (the C5-era `noop` placeholder finally filled in — `applyCommand` switch), so the mutation point is the deterministic top-of-tick drain + it rides `pendingCommands` for mid-battle saves. New `objective:set`/`objective:cleared` [events](src/core/events.ts) for J3's UI.
  - **User-locked semantics** (the AskUserQuestion at kickoff): **tile = persist-until-cleared** (an attractor — units path "as close as they can", never auto-clears); **enemy = auto-clears on its death** ([World.clearObjectiveIfResolved](src/sim/World.ts), run at top-of-tick so the same tick reverts to default targeting); **ranged preemption = leash + retaliation**.
  - **Preemption** ([updateObjectiveTarget](src/sim/Targeting.ts), the once-per-tick `updateTarget` authority, player-units-only branch): (1) ENGAGED (valid committed target within engage radius) → not preempted (still allows a markedly-better engageable switch via `shouldRetarget`); (2) EN ROUTE → an engageable enemy preempts; (3) PURSUE — an `enemy` objective becomes the target (path + attack), a `tile` objective leaves `targetId` null. **Engage radius = `min(attackRange, OBJECTIVE.rangedLeashCells)`** (new [config/objective.json](config/objective.json), `rangedLeashCells:3`) so melee uses its range (1) and a long-range archer is CAPPED at the leash (won't plink the map); **retaliation** lets a unit engage an attacker past the leash but within its own range. `currentTarget` now suppresses its nearest-enemy fallback under an active player objective (a deliberate null `targetId` = pursuing the tile / holding, NOT "find nearest").
  - **Movement** ([MovementBehavior](src/sim/behaviors/MovementBehavior.ts)): when a player unit has no enemy target AND a `tile` objective is active, it paths toward the rally cell (`stepTowardObjectiveTile` — same soft-block + E5.B sidestep model; clusters near an occupied cell, holds once on it). An `enemy` objective never hits this branch (updateTarget commits the unit to the objective enemy → normal path-to-target drives it). **Healers honor the objective transitively** (they trail the advancing team — `SupportMovementBehavior` untouched, a deliberate J1 scope call).
  - **Verification:** **742 tests** (was 722; +20 — Targeting objective block [engaged/en-route/tile-null/enemy-commit/leash-cap/retaliation/enemy-AI-unaffected], MovementBehavior tile-pursuit [attractor/reach+hold/adjacent-interrupt], World objective block [command apply + objective:set/cleared events + tile-persist + enemy auto-clear-on-death], snapshot-roundtrip v23 round-trip+reject, integration determinism [same-seed byte-identical + mid-objective snapshot resume]). Typecheck clean; lint 0 errors (lone pre-existing `Renderer.ts` warning); **fuzz:smoke 89 unchanged** (zero sim impact with no objective). **Headless-only — NO render surface (the objective UI is J3); no browser-verify applicable** (per the verify-when-observable rule). The v22→v23 pins bumped in [spawn-overflow.test.ts](tests/integration/spawn-overflow.test.ts) + the snapshot-roundtrip description.
  - **Next (Phase J):** J2 pathing overhaul + perf (path caching / shared-objective flow + a clean **movement-intent hook** — the seam Phase N's rogue dash reuses; don't reintroduce the retired `pickGoalCellInRange` freeze) → J3 objective UI + rebindable hotkeys (first playtestable surface) → J4 fuzz strategy + arena harness mode.
- **J2 ✅ (Phase J) — pathing seam: the shared movement-intent primitive + a deferred-cache perf guard. SECOND step of Phase J.** One commit (`feat(J2)`). **Byte-identical → NO snapshot bump, NO fuzz re-baseline** (fuzz:smoke 89 unchanged; the determinism + exact-position movement tests stay green). Factors the tangled "decide goal → find route → step" logic out of [MovementBehavior](src/sim/behaviors/MovementBehavior.ts) (and de-dups [SupportMovementBehavior](src/sim/behaviors/SupportMovementBehavior.ts)) into a three-layer seam, so Phase N's rogue dash reuses the SAME pathing as a new ability + a `maxCells>1` intent — confirmed NOT a pathing-algorithm rewrite (the user's upfront question).
  - **The seam** ([movement.ts](src/sim/movement.ts)): `MovementIntent { goals[]; approachToward; excludeUnitId?; maxCells }` (the WANT) → `routeToward` (the pure `findPath` wrapper — **the cache attaches HERE, behind a pure boundary, when a profile demands it**) → `advance` (intent → `MoveAction`: a 1-cell step + E5.B sidestep at `maxCells:1`, byte-identical to pre-J2; a multi-cell leap at `maxCells>1` = the dash hook). `buildMovementContext` / `sidestep` / `costAt` / `moveProposal` / `key` / `chebyshev` now shared.
  - **MovementBehavior** is a thin goal-selector now (firing-cell → target-cell preference, or rally-tile) that builds an intent + calls `advance`; the goal-preference list IS the load-bearing fallback-to-charge anti-freeze guarantee (don't collapse it → the retired `pickGoalCellInRange` freeze). **SupportMovementBehavior** shares the leaf helpers (safe de-dup); its bespoke healer logic stays.
  - **Cache DEFERRED (user decision = cache aggressiveness; chose seam-first):** `findPath` stays pure, no cache built. Profiled the §J2 worst case (32×32, 60 combatants, objective re-set every tick): **~0.10 ms/tick → ~0.31 ms/frame at 3× ≈ 1.8% of budget**, max 10 A* calls in any single tick — so a cache now is premature; it's a localized later add when the guard trips.
  - **The perf guard** ([Pathfinding.ts](src/sim/Pathfinding.ts) `pathfindingStats` counter, output-neutral): [pathing-perf.test.ts](tests/integration/pathing-perf.test.ts) asserts per-tick A* calls ≤ `2 × live mobile units` under objective thrash (the §J2 "bounded recompute" guard — thrash is free in the no-cache model) + thrash-costs-no-more-than-static. The deferred cache will show up as a DROP in this number.
  - **Verification:** **755 tests** (was 742; +13 — `movement.test.ts` [context/route/advance incl the `maxCells>1` dash hook] + 2 perf-guard cases). Typecheck clean; lint 0 errors (lone pre-existing `Renderer.ts` warning); **fuzz:smoke 89 unchanged** (byte-identical). **Headless-only — NO render surface (objective UI is J3); no browser-verify applicable.** Also trimmed completed Phases H + I in `## Current state` to one-liners (the docs-hygiene cap, which the J1 entry had pushed over).
  - **Next (Phase J):** **J3** objective UI + rebindable hotkeys (right-click set / Set+Clear buttons, a config-backed minimal keybinding layer — the **first PLAYTESTABLE** J surface) → **J4** fuzz objective strategy + arena harness mode.
- **J3 ✅ (Phase J) — objective UI + rebindable hotkeys: the FIRST PLAYTESTABLE Phase-J surface. THIRD step of Phase J.** Two commits (`feat(J3)`). **No sim/snapshot/fuzz change** — presentation + input on J1's model; fuzz:smoke 89 unchanged, main suite 755→770 (+15). Browser-verified on the `dev-preview` server (a dedicated-port vite — see Dev server below).
  - **Commit 1 — rebindable-hotkey plumbing + fold in fast-forward `F`** (`7751c97`). The **user's middle-ground call:** build the runtime-rebindable REGISTRY now (so a future in-game rebind screen is a one-line `rebind` caller) but seed it from config defaults this round — no rebind SCREEN yet. New [config/keybindings.json](config/keybindings.json) + [src/config/keybindings.ts](src/config/keybindings.ts) (zod, mirrors `playback.ts`; `KEYBIND_ACTIONS` = `fastForward`/`setObjective`/`clearObjective`, defaults `KeyF`/`KeyO`/`KeyC`). [Keybindings](src/ui/Keybindings.ts): `codeFor`/`actionFor`/`rebind(action,code)`/`on(action,handler)` + a **DOM-free `handleKeyDown`** dispatch (node-testable — env is `node`, no DOM). Owned by `Game` (the single page-lifetime `window` keydown listener), threaded via `SceneContext.keybindings`; lifetimes split — registry + listener page-lifetime, the `on(...)` handlers battle-scoped (HUD subscribes on mount, tears down on dispose) so a hotkey is inert outside a battle. **Migrated I3's `F`:** the `hotkey` field left `playback.json`/`.ts`/`PlaybackSpeed`; the HUD subscribes `keybindings.on('fastForward', …)` and the button tooltip reads the live binding via `labelFor`.
  - **Commit 2 — the objective UI** (`feat(J3)` commit 2). Capital-`X` marker appended to [glyphs.ts](src/render/glyphs.ts) — **the LAST atlas cell (32/32, the grid is now FULL; the next glyph needs a FontAtlas resize)**; no FontAtlas code change. [Renderer.pickCell](src/render/Renderer.ts) raycasts a click → grid cell against a `y=0.5` plane + inverts `gridToWorld` (verified: canvas-center → board-center cell exactly). A pure [objectiveAtCell](src/sim/objective.ts) (enemy under cursor → enemy objective, else rally tile) — node-tested, takes plain `EnemyAtCell[]` so no render↔sim cycle. [ObjectiveController](src/ui/ObjectiveController.ts) (battle-scoped, owned by `BattleScene`): right-click sets directly; the **Set** button/hotkey arms a "pick a target" mode (next left-click sets; a left-click is otherwise inert); **Clear** clears — all via `world.enqueueCommand` (the J1 top-of-tick drain). HUD gains the two buttons (labels carry the live hotkey; Set pulses green `.is-armed` while waiting), `setObjectiveArmed` reflects the controller's `onArmedChange`. [BattleRenderer](src/render/BattleRenderer.ts) subscribes `objective:set`/`cleared` → the `X` marker (amber `TERMINAL_AMBER`, larger `size 1.6` on a tile, `size 1.0` atop the enemy billboard via `HITSPLAT_Y_OFFSET`+lift, tracked per-frame by `unitId` in `update()` after overlays). Marker driven PURELY off the events, so it's correct however the objective was set (mouse / hotkey / future AI/fuzz).
  - **Verification (browser, the `dev-preview` server):** buttons render with correct hotkey labels; `pickCell` center→cell exact; an enemy objective set via the command path → `world.objective` + `br.objective` set, marker spawned, **all 5 player units retargeted** (J1 preemption firing from the UI); enemy auto-clear-on-death removed the marker; a tile objective's marker world-XZ === `gridToWorld(cell)` (exact) at `size 1.6`; Clear removed marker + state; the armed toggle propagated controller→HUD button. Typecheck clean; lint 0 errors (lone pre-existing `Renderer.ts` warning). **Observation (logged, not fixed):** the amber marker is low-contrast on the **Volcanic** theme (fine on default) — a possible polish tweak (brighter/outlined/non-amber) if playtest bothers.
  - **Playtest fixes (2026-06-09, commit `fix(J3)`) — four tweaks from the first J3 playtest:** (1) **the freeze** — right-clicking an UNPATHABLE rally tile (a wall) froze the whole player roster: a single unreachable goal → `findPath []` → no step, and a tile objective suppresses the nearest-enemy fallback, so the team idled (the retired `pickGoalCellInRange` freeze, now for the attractor). Fixed with an opt-in **`bestEffort`** mode on [findPath](src/sim/Pathfinding.ts) — route to the CLOSEST reachable cell when the goal is unreachable; only the tile-pursuit ([MovementBehavior](src/sim/behaviors/MovementBehavior.ts)) opts in (via `MovementIntent.bestEffort`), so every other caller is byte-identical (fuzz:smoke 89 unchanged). (2) the enemy mark was ~2× too big → `OBJECTIVE_MARKER_ENEMY_SIZE` 1.0→0.5. (3) the enemy mark **skewed sideways** for off-axis units — a world-Y lift projects off-axis under the pitched perspective (the I2 hitsplat-drift bug again); the lift moved onto the **camera-up** axis ([BattleRenderer.updateObjectiveMarker](src/render/BattleRenderer.ts), needs the camera now) → verified **0 NDC-x delta** with the unit at the board edge. (4) [Renderer.pickCell](src/render/Renderer.ts) raycast a flat `y=0.5` plane (off by a tile on raised/lowered terrain) → now raycasts the **terrain mesh** (`ObjectiveController` passes `terrain.mesh`) → a project-unit-to-pixel-then-pick round-trip lands the exact cell. **775 tests** (+5: 4 `findPath` best-effort + 1 MovementBehavior wall-rally); browser-verified on `dev-preview`. **Still logged, not fixed:** the amber marker is low-contrast on the **Volcanic** theme.
  - **Unit-pick fix (2026-06-09, commit `fix(J3)`) — billboard-aware enemy selection.** A follow-up to the playtest fixes: enemy selection was **cell-based** (`pickCell` terrain raycast → grid cell → `objectiveAtCell` enemy-on-cell), which does NOT account for billboarding — a unit glyph is a camera-facing sprite floating ~0.5 above its tile, so a ray through the glyph lands on the tile BEHIND the unit (and for a back-row enemy, **misses the board entirely → no objective at all**). Added a screen-space **billboard hit-test** ([pick.ts](src/render/pick.ts) `pickInstanceAtNdc` → [Renderer.pickInstance](src/render/Renderer.ts)) that replicates `billboard.vert.glsl` exactly (center→view space, quad half-extent offset in VIEW space, project) and returns the FRONTMOST candidate under the cursor. [BattleRenderer.enemyBillboards](src/render/BattleRenderer.ts) supplies candidates from the LIVE rendered sprite positions (so a mid-move glyph still hits); BattleScene wires it as a callback. [ObjectiveController.setFromClient](src/ui/ObjectiveController.ts) now tries the billboard hit FIRST, then falls back to the terrain cell (clicking the feet/ground → enemy-on-cell or rally tile). **Pure hit-test is node-tested** (`pick.test.ts`: cursor-over / off / empty / frontmost-of-overlap / behind-camera); browser-verified end-to-end (right-clicking a back-row enemy's glyph → enemy objective, where the old terrain pick returned null; empty ground → exact rally tile). 780 tests (+5). Render/UI only — no sim/snapshot/fuzz change.
  - **Next (Phase J):** **J4** fuzz objective strategy (objectives target units only, only-after-kill, per-archetype proclivities) + an **arena harness mode** (one `World`, no run wrapper). Then **K** status effects + redraw/empower → **L** daemons → **M** progression/presentation → **N** rogue mobility + balance closure.
- **J4 ✅ (Phase J) — fuzz objective strategy + arena harness: exercise J1's objective system in the bots without paralyzing them. FOURTH + FINAL Phase-J step → PHASE J COMPLETE.** Two commits, **dev-only fuzz tooling, ZERO `src/` change** (main suite 780 unchanged; fuzz:smoke 89→114, +25). The user's envisioned workflow: tune an objective strategy in ISOLATION (arena), save it, then feed it into the full run fuzz / search / sweep to tune the OTHER (difficulty/archetype) knobs.
  - **Commit 1 (`feat(J4)` `b5d454e`) — the objective-strategy primitive + arena harness.** [objectiveStrategy.ts](tests/fuzz/objectiveStrategy.ts): a serializable `ObjectiveProclivity` (`none` | `random` | `stat:hi/lo` per `STAT_KEY` | `hp:hi/lo` | `archetype:<name>` — added in the follow-up) + zod load/serialize (mirrors `scoredWeights.ts`) + the config-derived menu (per-stat entries track `STAT_KEYS`). `selectObjectiveTarget` picks a **living ENEMY only** (ascending-id tie-break, no RNG except `random`); `decideObjectiveCommand` is the **no-thrash gate** — refill ONLY when `world.objective === null`, which leans on J1's auto-clear-on-death so the brief's "only after the previous target is killed" falls out for free. `none` enqueues nothing → byte-identical. [arena.ts](tests/fuzz/arena.ts): `runArena` (one forced `World`, NO `Run` wrapper — `scaledUnit` roster + `buildEnemyTeam` wave + `spawnEncounter`, objective bot on a dedicated forked RNG stream) + `runArenaSearch` (enumerate the menu, rank by player win rate → the top entry IS the "optimal objective strategy"). `--arena` CLI (+ `--objective`/`--seeds`/`--roster`/`--layout`): enumerate → write `output/best-objective.json` (gitignored), or `--objective=<one>` to inspect one. **Verified e2e:** default lvl-5 carry comp, `none` 50% → `hp:lowest` 75% (a real skill gradient), **0 hangs across all 26 proclivities**.
  - **Commit 2 (`feat(J4)`) — the `--objective` flag on the FULL run fuzz + `--search` + `--balance-sweep`.** `HarnessOptions.objective` drives the shared objective in the battle loop (per-battle objRng forked off `worldSeed`); **default `none`/absent = BYTE-IDENTICAL** (no objRng forked, nothing enqueued — a unit test pins `none ≡ absent`, and the existing fuzz baselines are untouched). Threaded through the child-process `ShardJob` too (the user's call to cover the sweep), so `--jobs` stays byte-identical with an objective active (verified: single-process vs `--jobs=2` search → identical 66.7%/100% train/test). Flag grammar: `none` | `random` | `<file>.json` (the arena's winner) | inline `stat:<stat>:<dir>` / `hp:<dir>`. **The workflow:** `--arena` to tune → save JSON → `--objective=<json>` on `--search` / `--balance-sweep` to hold the objective fixed while tuning the difficulty/archetype knobs (N2-ready).
  - **Verification:** typecheck clean; lint 0 errors (lone pre-existing `Renderer.ts` warning); main suite **780** (unchanged — zero `src/`); **fuzz:smoke 89→114** (+25: 18 `objectiveStrategy.test.ts` selector/gate/JSON/menu + 4 `arena.test.ts` termination/determinism/search-ranking + 3 `harnessObjective.test.ts` byte-identical-none/determinism/live-injection). All three consumption paths + the sharded `--jobs` path live-verified. **Headless-only — no render surface.**
  - **Follow-up commit (`feat(J4)`) — enemy-archetype proclivity.** A `{ kind: 'archetype'; archetype }` proclivity — the user's actual reading of the brief's "per-archetype" (**"focus the mage" / "kill the archers first"**, NOT a per-player-unit strategy). Targets a living enemy of a given archetype (lowest-id pick; **null → default targeting when none of that archetype is alive**). Menu auto-extends off `ALL_ARCHETYPES`. **Live TODAY for `bandit`/`ranged`** (the only enemy types `rollEnemyWave` spawns); the other 7 behave like `none` until enemy-roster diversity lands (a proper encounter system, Phase L+). `objectiveStrategy.ts` only — no plumbing change (it rides the existing `ObjectiveProclivity`). Verified e2e: `archetype:mage` ≡ `none` byte-for-byte (42% / 289 ticks), `archetype:ranged` actively re-steers (284 ticks). +3 tests (**fuzz:smoke 114→117**).
  - **Next: PHASE J COMPLETE (J1–J4 ✅).** → **Phase K** status effects + redraw/empower (DESIGN ROUND on K1's generic status-effect system + the fatigue migration) → **L** daemons → **M** progression/presentation → **N** rogue mobility (CONFIRMED ON) + balance closure.
- **Tests:** 780 passed (J3 billboard-aware unit pick — +5 over the J3 playtest fixes: 5 `pick.test.ts` [`pickInstanceAtNdc`: cursor-over / off-billboard / empty list / frontmost-of-overlap / behind-camera]; 775 at the J3 playtest fixes — +5 over the J3 UI commits: 4 `Pathfinding.test.ts` [`findPath` best-effort: closest-reachable on a blocked goal / approach a walled-off goal / identical-to-strict when reachable / `[start]`-hold when walled in] + 1 `MovementBehavior.test.ts` [a wall on the rally cell doesn't freeze]; 770 at the J3 objective UI + rebindable hotkeys — +15 over J2: 11 `Keybindings.test.ts` [dispatch/repeat-skip/unbound+no-subscriber preventDefault gating/multi-handler/unsubscribe/rebind-reroute/config-default coverage + `keyLabel`] + 4 `objective.test.ts` [`objectiveAtCell` enemy-vs-tile/full-coord-match/empty-list]; 755 at J2 pathing seam — +13 over J1: `movement.test.ts` [context/route/`advance` incl the `maxCells>1` dash hook] + 2 `pathing-perf.test.ts` bounded-recompute guard cases; 742 at J1 objective model + targeting preemption — +20 over I6: Targeting/MovementBehavior/World objective blocks + snapshot v23 round-trip/reject + integration determinism; 722 at I6 per-ability combat profile COMPLETE — +3 net over I5: the `abilities.test.ts` gate pins + reworked `critChanceFor`/`hitChanceFor` balance-proofs + the I6 snapshot round-trip, MINUS the rogue/healer battle smokes consolidating 6 seed-cases → 2 robust ones; 719 at I5 commit-1 melee subclass family — `melee→mercenary` + Adventurer/Ronin/Bandit, +6 over I4: 3 from config-derived `ALL_ARCHETYPES` loop growth + 3 new `archetypes.test.ts` cases; 713 at I4 archetype-editor formatter fidelity/round-trip; 711 at I3 fast-forward `PlaybackSpeed` cycle; 706 at I2 dodge hit/miss roll; 694 at I1 dodge-stat plumbing, 689 at H7c targeting strategies, 675 at H6, 665 at H5b, 664 at H5, 654 at H4b, 628 pre-H4), 0 `it.todo()`. Run with `npm test`. Fuzz smoke (separate suite, opt-in): 117 pass (J4 added +28 — arena + objective-strategy [incl. the enemy-archetype proclivity] + harness-objective — over the H7d-era 89) — `npm run fuzz:smoke`. **Fuzz sweep (`npm run fuzz`): post-H5 pure-random + greedy = 100% win / avg floor 10 / 0 hangs (unchanged from post-H4a — the no-attrition pools, not the budget, drive the foregone conclusion; the seam swap shrinking the budget for grown rosters + the smaller drawn hand roughly cancel for win rate). This stays the H7 balance-sweep signal (H6c shipped the *inert* spawn-time fatigue hook; H6 left the fuzz baselines unchanged — turning fatigue on is H7's data-driven call).** H7a added the `scored` strategy (`npm run fuzz -- --strategy=scored`, or `--strategy=<file>.json` for any weight vector); H7b's `npm run fuzz -- --search` random-searches its weights for the best-achievable win rate (quick/overnight presets; emits `output/best-strategy.json`). At the current (unbalanced) config the best discoverable play is still **100%** — that's H7c's signal to tune against. Full strategy menu: `npm run fuzz -- --strategy=all` — **one pre-existing residual: `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, NOT a positioning deadlock — out of GP5 scope; see the GP5.1 entry).** Per-floor balance analysis: `npm run fuzz -- --per-floor`. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`.
- **Dev server:** Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent). **The user usually has their own `npm run dev` on 5173 during a session**, and the preview MCP refuses to attach to a server it didn't start ("not a preview server"). For agent browser-verify, use the **`dev-preview` launch config** ([.claude/launch.json](.claude/launch.json)) — `npm run dev -- --port 5191 --strictPort` binds a dedicated port that never collides with 5173, so `preview_start name=dev-preview` always works. **Dev handle:** `window.__game` (DEV-only) exposes `bus`/`renderer`/`run`/`activeScene`/`keybindings`/`sprites`; drive a battle headlessly with `__game.dispatch({kind:'enterNode',nodeId})` then `{kind:'advanceTurn'}` (past the PreTurnScene gate), and freeze the auto-advancing run with `scene.clock.advance = () => {}` for a stable screenshot.
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**Phase H is COMPLETE (H1–H7 all ✅, 2026-06-07)** — the multi-turn "deckbuilder" trial: `power` stat, spawn-tile range, deployment counter, the health-pool encounter loop (H4a headless + H4b screens/HUD), the card-drawn hand + `playerTeamLevel` seam swap (H5/H5b), recruit-pass + rest-heal + an inert fatigue hook (H6), and the H7 search-driven balance tooling + difficulty/archetype sweep (H7a scored DSL → H7b `--search` → H7c the sweep + band-set `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance + per-layout/floor telemetry → H7d the sweep GUI + `--jobs`-for-`--search`).

**The Phase-H playtest feedback is now synthesized into a fresh [ROADMAP.md](ROADMAP.md) — "Post-H" (Phases I → N); the brief is archived at [archive/phase-h-feedback.md](archive/phase-h-feedback.md) and the prior roadmap at [archive/post-f-roadmap.md](archive/post-f-roadmap.md).** **Phase I IN PROGRESS (foregone conclusions & combat identity) — I1 ✅** (the `agility→speed` revert + `precision`/`evasion` mid-block adds + canonical reorder `CON·STR·RNG·MAG·LCK·DEF·PRC·EVA·SPD·MOB·POW`; WorldSnapshot v19 + RunSnapshot v10; fuzz re-baselined — see the I1 entry above). **I2 ✅** (the Fire-Emblem subtractive hit/miss roll at `world.applyDamage`, single-target strikes only [mage AoE / catapult / env unmissable]; the `unit:missed` event + "Miss" hitsplat; NO further snapshot bump — see the I2 entry above. **`hitChanceBase` shipped at 0.75 but the user tuned it to `0.6` live [commit `36b3648`, 40% whiff] — the current base is 0.6.** Two pre-existing render bugs were also fixed in the playtest follow-up [glyph-atlas serif race; off-axis hitsplat drift] + a louder Miss splat + [HITSPLATS.md](src/ui/HITSPLATS.md) styling guide. The fuzz hang FINDING [misses lengthen battles] was **RESOLVED by bumping both turn caps 100→150s**; at 0.6 the hangs drop pure-random 5→0 / greedy 7→3, and cap-unification is logged for N2). **I3 ✅** (fast-forward 1×/2×/3× — a tick-batching `dt`-multiplier on the fixed-timestep loop [`BattleScene.tick` scales `dt` for the `Clock` + renderer + terrain], NOT a `TICK_RATE` change so the sim is byte-identical [no snapshot/fuzz impact]; HUD button + `F` hotkey [config-backed in `config/playback.json` so J3's rebindable layer can absorb it]; the chosen speed **persists across battles**; profiled **≤0.236 ms/tick on big boards → 3× is safe, kept `[1,2,3]`** [re-check after J's objective-thrash path recompute] — see the I3 entry above). **I4 ✅** (commit `feat(I4)` `2d27f73`): the schema-driven archetype editor (`tools/archetype-editor/`) — live `ArchetypesSchema` validation + live derived-stat preview + a save-to-disk path via a new dev-only Vite `/__save-config` endpoint that **M5 reuses for the layout editor**; dev-tooling only, no sim/snapshot/fuzz change — see the I4 entry above. + a separate `balance` commit (`9441f5c`) for the user's editor-authored rogue dodge-tank tuning (re-measured in I5). **I5 IN PROGRESS (DESIGN ROUND done, staged 3 commits):** all 3 commits ✅ — (1) the melee subclass family (`melee→mercenary` rename + Adventurer/Ronin/Bandit, all recruitable; WorldSnapshot v20 + RunSnapshot v11); (2) enemy melee `mercenary→bandit` swap (Bandit fodder scales at ~half growth, EASING deep floors — Phase-N re-sweep recalibrates); (3) the rogue dodge-tank re-measure (**VERDICT: dodge alone insufficient — Phase N N1 mobility STAYS, scoped for *reach* not survival; dodge fixed the rogue's durability but it's now damage-starved**). See the I5 entry above + [BALANCE.md](BALANCE.md). **I1–I5 ✅; a playtest insight then added I6 — per-ability combat profile (`might`/`accuracy`/`critBase` on each ability/weapon).** The diagnosis: every attack computes damage/hit/crit from the SAME formulas, so only stats differentiate them — I6 moves those onto each ability (finishing E5's per-ability move for cadence/range), which is what makes I5's subclasses actually feel distinct (e.g. Bandit Club +2/40%/0% vs Mercenary Sword +5/60%/5% vs Ronin Katana +4/60%/20%). Splits `melee_strike` into per-subclass weapons (Sword/Club/Katana/**Whip** — the Indiana-Jones adventurer) + renames `ranged_shot→bow`; also adds explicit **`evadable`/`critable` config flags** — a designation system that migrates I2's hard-coded per-call-site `evadable` into the ability config (mage/catapult `critable:false`, user call). Behavior-neutral plumbing first (defaults reproduce today byte-for-byte), then author the weapon values; an ability display-name/description system is logged as a TODO (pairs with the I5 archetype-label TODO). Full plan: [ROADMAP.md](ROADMAP.md) §I6. **I6 ✅ COMPLETE — commit 1 (byte-identical plumbing + WorldSnapshot v20→v21 for the per-ability crit move) + commit 2 (the weapon rename/split `melee_strike`→sword/club/katana/whip + `ranged_shot`→bow, authored per-weapon values, mage/catapult `critable:false`, v21→v22; see the I6 entry above). PHASE I IS NOW COMPLETE (I1–I6 all ✅).** **Phase J IN PROGRESS — J1 ✅** (the objective MODEL + targeting preemption, sim/headless: one shared player-team objective [tile persist-until-cleared / enemy auto-clears-on-death], engage radius `min(attackRange, rangedLeashCells)` + retaliation, set via `setObjective`/`clearObjective` WorldCommands, WorldSnapshot v22→v23, byte-identical with no objective so fuzz is unchanged — see the J1 entry above). Phase J = the in-battle objective system (the largest single agency item); it forces the **pathing overhaul** (J2), which is the seam N1's rogue mobility then builds on. **J2 ✅** (the pathing seam — shared movement-intent primitive in [movement.ts](src/sim/movement.ts) + cache DEFERRED behind the pure `routeToward` boundary + a `pathfindingStats` bounded-recompute guard; byte-identical, profiled ~1.8% of a 3× frame on a 60-unit board — see the J2 entry above). **J3 ✅** (objective UI + rebindable hotkeys — the FIRST PLAYTESTABLE Phase-J surface: a runtime-rebindable `Keybindings` registry seeded from `config/keybindings.json` [fast-forward `F` folded in], right-click / Set-button-arm / Clear objective input via the J1 commands, the amber `X` marker; no sim/snapshot/fuzz change, browser-verified — see the J3 entry above). **J4 ✅** (fuzz objective strategy + arena harness mode + the `--objective=<json|random|none>` flag on the full run fuzz / search / sweep — dev-only tooling, zero src change; see the J4 entry above). **PHASE J COMPLETE (J1–J4 ✅).** Next phase: **K** status-effects + redraw/empower, **L** daemons, **M** progression/presentation, **N** rogue mobility (now confirmed ON) + balance closure. Earlier forward detail: **J** in-battle objective system (the pathing overhaul), **K** status-effect system + redraw/empower, **L** daemons, **M** per-turn leveling + scene/map polish (+ the layout auto-editor reusing I4's `/__save-config` endpoint), **N** rogue mobility [contingent] + balance closure. **Vocab note:** the brief's *skirmish* = our **turn**, its *battle* = our **encounter** (mapped in the roadmap). **The rogue salvage (mobility) is resequenced to Phase N and made contingent** — dodge is the interim fix (Phase I re-measure), and the mobility rework now builds on Phase J's pathing seam; the stage-5 overnight verify + the leveling-rate pass stay last (Phase N), now behind more band-movers. **READ [BALANCE.md](BALANCE.md) FIRST** for any balance work (the authoritative H7c run log + funnel); forward steps in [ROADMAP.md](ROADMAP.md), smaller follow-ups in [TODO.md](TODO.md), gameplay backlog (shop, synergies, boss variety) under [DESIGN.md](DESIGN.md) "Out of scope".

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

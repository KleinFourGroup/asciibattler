# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

> ## 🧭 ACTIVE — **Cluster 2: Spatial & Movement** ([ROADMAP.md](ROADMAP.md)). **READ ROADMAP.md FIRST.** Cluster 1 (Combat Depth, Phases Y→34) is **COMPLETE & user-confirmed**, archived at [archive/post-x-roadmap.md](archive/post-x-roadmap.md). This round (Phases **35→41**, synthesized 2026-06-28 from the now-archived [archive/cluster-two-spec.md](archive/cluster-two-spec.md) + the design discussion that locked flight + the unit overhaul) is the **second engine round**: harden the occupancy core, give it terrain depth, **unify every unit into one data-driven `UnitDef` model**, then land multi-tile footprints + destructible terrain on top. Get the occupancy core + the unit model right once — Clusters 3–6 author against both.
>
> **The phase sequence (full Shape/Cost/Tests/Decisions per phase in ROADMAP.md):**
> - **35 — Harden the occupancy core:** one `src/sim/occupancy.ts` abstraction (the chokepoint every spatial query routes through) + the **footprint** (single-cell) + **plane** (ground-only) SEAMS + proactive destination checks + an abort primitive + **shove** (the co-location backstop; also the future-knockback primitive) + a **fuzz occupancy invariant**. No snapshot bump; fuzz baseline may shift. **Subdivided 35a–35d** (see ROADMAP §35 sub-steps).
> - **36 — Non-instant moves (the claim system):** a unit claims its destination on move-start (occupied-OR-claimed blocks pathing; release on arrival), and the logical tile flips partway through the move. The round's **WorldSnapshot bump** (claims + the deferred-position state); fuzz re-baseline. **Subdivided 36a–36d** (ROADMAP §36).
> - **37 — Terrain, palettes & new tiles:** tundra/desert/swamp palettes + rename `rock→barren` / `default→grassland`; deep-water/hills/ice/sand/mud as cost+passability+evasion/accuracy mods; tile→status hooks (mud→poison via a flag, water/deep-water→remove burn). A `TileDef` table generalizes `TILE_COSTS`. No WorldSnapshot bump (TileKinds are forward-compatible strings); possible RunSnapshot touch from the theme rename (audit). Layout editor paints it. **Subdivided 37a–37f** (ROADMAP §37).
> - **38 — The unit-data KEYSTONE:** unify playable/enemy/neutral/static into one data-driven `UnitDef` catalog (`config/units.json`) — the third leg of the AbilityDef/StatusDef stool, **NOT an ECS**. Relax the closed `Archetype` union → a catalog id; fold `spawnEnvironment` neutrals in; add optional `footprint`/`layer`/`ignoresTerrain`/`statusSusceptibility`/flat-HP fields. **Audit archetype-LITERAL branches FIRST** (gates the phase shape). A byte-identical migration (the Y determinism-oracle pattern) → **no snapshot bump expected**. The archetype editor's closed-union wire-up panel DISAPPEARS. **Subdivided 38a–38e** (ROADMAP §38; 38a audit gates it).
> - **39 — Multi-tile footprints (the fill):** `cellsOccupiedBy` returns the N×N block (N∈1..4; `position` stays the canonical corner); footprint-passability pathfinding + scaled glyph render + the `corner` (in-bounds-biased) anchoring policy (`random-intersect` deferred to camps). No bump expected; inert until §40's rubble. **Subdivided 39a–39e** (ROADMAP §39).
> - **40 — Destructible terrain:** HP-bearing neutral `UnitDef`s (rubble 1×1–3×3 + optional wall/cover destructibility) + the targeting-neutrals hook (rubble auto-targeted BELOW hostiles; walls/cover manual/AoE-only) + susceptibility (burnable, not poisonable). No WorldSnapshot bump expected (HP already serialized). **Subdivided 40a–40d** (ROADMAP §40).
> - **41 — The closing balance pass** (BALANCE.md loop, scoped to what terrain/multi-tile/destructibles/§36-timing actually moved). No bump. **Subdivided 41a–41c** (ROADMAP §41).
>
> **Locked decisions (the design discussion, 2026-06-28):**
> - **Unit overhaul = IN + FULL** (active archetypes become data too, not just neutrals) — a `UnitDef` catalog, not an ECS (optional HP/size/susceptibility are schema fields). Audit-first.
> - **Flight = DEFERRED build, design LOCKED:** always-fly · shares the single `ground` occupancy plane (a `plane` seam keeps air-overlap a CHEAP future fill — the costly part, scattered single-layer assumptions, is exactly what the §35 abstraction eliminates) · passes over all ground obstacles (`blocksFlight` predicate seam, always-false) · mobility-only (everyone hits everyone incl. adjacent melee; `targetsLayer` ability seam for future anti-air). Flyer `layer`/`ignoresTerrain` fields planted in §38, inert.
> - **Spawn anchoring = a POLICY** — ship in-bounds-biased `corner` (handles edge tiles; reuses the single-tile spawn path — NOT a new spawn class); `random-intersect` deferred to camps (Cluster 5).
> - **`barren`, not "mountain"** (the palette rename would collide with the Hills tile that renders mini-mountains). **mud→poison behind a flag** (trial; no near-duplicate "mire" tile yet).
>
> **Live seams to extend (NOT rebuild):** `World.isOccupied` + `buildMovementContext` ([movement.ts](src/sim/movement.ts)) → **§35a DONE: now route through [occupancy.ts](src/sim/occupancy.ts)**; the Cluster-1 `unitsInCells` ([effects/targeting.ts](src/sim/effects/targeting.ts), "the Cluster-2 footprint seam") = the footprint seam's other half; `MoveAction` ([actions/MoveAction.ts](src/sim/actions/MoveAction.ts)) snaps position at `start` (§35b DONE: a proactive destination check + abort via `Action.destinationCell` + `unit:moveAborted`; §36 defers the flip); `spawnEnvironment` + the `'environment'` archetype ([environment.ts](src/sim/environment.ts)) = the unit-unification seam (walls already carry `maxHp` + orthogonal `blocksLineOfSight`); `TILE_COSTS` ([TileGrid.ts](src/sim/TileGrid.ts)) → the §37 `TileDef` table; `applyDamage`'s to-hit roll ([World.ts](src/sim/World.ts)) folds §37 tile mods. The reserved Cluster-1 `move` knockback/pull modes are now BUILDABLE on §35c's `World.shove` (a knockback = a directional shove) — fill only if a consumer appears (no spec content asks for it).
>
> **Conventions:** headless-first (occupancy/claims/tile-defs/unit-resolve/footprint geometry are pure — vitest before the browser); **the determinism oracle is the §38 equivalence proof**; balance-proof tests derive from config; keep DESIGN/ARCHITECTURE honest in-commit; commit per logical change + **pause between commits** for the user's playtest. ⚠️ `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow).
>
> **Tree state:** on `main`. WorldSnapshot **v31** / RunSnapshot **v23**; **1453 main** + **212 fuzz:smoke** green; typecheck + lint clean. **✅ Phase 35 COMPLETE (35a–35d)** — the occupancy core. **35a** `src/sim/occupancy.ts` is the chokepoint every spatial query routes through (`cellsOccupiedBy` footprint seam / `isFree`·`unitAt` / `occupiedCells` / `footprintFits` / `distanceBetween` / `findOverlappingCells` + the `OccupancyPlane` ground-only plane seam); `World.isOccupied`/`buildMovementContext`/`nearestFreeCells`/`retreatCell` route through it. **35b** proactive destination check + abort: a relocation onto an occupied/untraversable cell is a clean no-op (cooldown intact, retry next tick) + the new `unit:moveAborted` event (the §36 settle-back hook); `Action.destinationCell?()` seam. **35c** `World.shove(unit)` — the de-overlap backstop (`nearestFreeCells`, `SIM.shoveSearchRadiusCells`=3) + `unit:shoved`; the future-knockback primitive (a knockback = a directional shove). **35d** the opt-in `HarnessOptions.assertOccupancy` fuzz invariant (no-two-units-share-a-cell per plane after every tick; holds across the greedy+random corpus — generalizes Qb#3). **All byte-identical / inert on the instant model** (no snapshot bump; fuzz baseline unchanged), load-bearing in §36/§40. **✅ Phase 36a COMPLETE** — the claim-registry leg: the in-flight `Claim` registry on `World` (`claimCell`/`releaseClaim`/`releaseClaimsBy`) + the `occupancy.ts` claim queries (`claimantOf`/`isClaimed`/`claimedCells`); **"blocked-for-pathing = occupied OR claimed"** wired into `destinationBlocked` (§35b) + `buildMovementContext` (claimed cells join the soft-block + sidestep sets); serialized → **WorldSnapshot v30→v31** (reject-stale). **INERT / byte-identical on the instant move model** (nothing calls `claimCell` yet, so the registry is empty in real play; fuzz baseline unchanged), load-bearing once 36b defers the flip. **✅ Phase 36b COMPLETE** — the non-instant logical position flip: `MoveAction.start` now CLAIMS `to` + emits `unit:moved` (no position write); `MoveAction.applyEffect` flips `unit.position = to` + releases the claim at the **50%** mark (`SIM.moveFlipFraction`, a config dial), carried by the move proposal's `travel`→0-length `impact`→`recovery` phase timeline (`floor(durationTicks*0.5)`, so a 1-tick move stays instant + byte-identical). Claim lifecycle: claim on start, release on flip + on reap (`removeUnit`→`releaseClaimsBy`). **Targeting / adjacency / pathing read the *logical* `unit.position`** (LOCKED). The claim went load-bearing — the §35d occupancy invariant caught that the PLACEMENT paths also had to exclude claimed cells, else a unit materialises where a deferred mover is arriving: `nearestFreeCells` (shove/summon), `runOverflowScan` (reinforcement) + the gambit `retreatCell` now treat a claimed cell as occupied. **No new serialized state / no second bump** (v31 holds; the claim is the only persistent piece, the flip tick derives from the activeAction + the phase timeline). New `moveDeferred.test.ts` (flip-timing / claim-release / re-route / reap) + a snapshot mid-move round-trip. **36b playtest follow-up:** a melee/ranged pursuer now HOLDS when its target's in-flight claim lands in its firing band — pre-fix it read itself out of range (target still logically on its old tile) and sidestepped around the reservation (the corridor kite-pin); the abstain in `MovementBehavior` now checks the target's logical position OR its claim via the new `claimedDestinationOf` (the inverse claim query), the locomotion dual of the claim's pathing-block. **1453 main + 212 fuzz:smoke green** (the fuzz config's default `testTimeout` raised to 30s — real-battle cases run longer under parallel load now). **▶ NEXT = Phase 36c** (the smooth mid-flight abort via §35b's `unit:moveAborted` + the renderer settle-back — a render design call) **then 36d** (the fuzz re-baseline + the win-rate-shift record, carried to §41). Full Shape/Tests/Decisions in ROADMAP §36. Per-phase detail in ROADMAP.md + git.
>
> ---
>
> **🗄️ Cluster 1 — Combat Depth (Phases Y→34): COMPLETE & user-confirmed.** Full as-built archived → [archive/post-x-roadmap.md](archive/post-x-roadmap.md); per-phase detail in git + [BALANCE.md](BALANCE.md) + the project memory. In one breath: the data-driven `EffectAbility`/`EffectAction` attack/effect keystone (legacy attack classes + the determinism oracle DELETED) · non-stat statuses (burn/bleed/poison/blind/confusion/panic/frozen, periodic + behavior axes) · new ops (status-on-hit / chain / summon + the 9-archetype demo roster) · the attack & archetype-create editors · effect-scaling (`ScaledValue`) · FX·SFX·status-viz · the §33 balance re-derive (a caster-summoner equilibrium; merc+ranged duopoly dissolved) · §34 polish (double-KO soft-lock + §29 ability-detail rows). **WorldSnapshot v30 / RunSnapshot v23.** Load-bearing Cluster-1 facts still in play: every combat verb is one `AbilityDef` (`config/abilities.json`, [src/sim/effects/](src/sim/effects/)); statuses are DEF-RESOLVED by key ([statusEffects.ts](src/sim/statusEffects.ts) — a runtime `StatusEffect` carries only `nextTickAt`/`sourceUnitId`/`magnitude`); the renderer-owned FX registry ([fxRegistry.ts](src/render/fxRegistry.ts), one `FxKey` = visual+SFX, boot-asserted); `applyDamage`/`dealDamage` ([World.ts](src/sim/World.ts)) is the single damage chokepoint (`bypassDefense`/`evadable`); ⚠️ the action registry holds NON-verb ids only (a verb's `EffectAction.id` == its `AbilityDef` id — registering a colliding id would shadow the snapshot-decode fallback); ⚠️ `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow).

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phases A–D complete** — the pre-combat foundation (selectors/commands/fuzz/config/scenes · palette+bloom/bars/scanlines/audio/root `@` · terrain/walls/LOS/prisms/layout-editor · renderer-cap/drag-paint/variable-sizes/fit-scroll-camera/spawn-overflow/half-cover/chasm+fire+healing/theming). Detail: [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).
- **Phases E · F · G · GP complete** — combat depth + run progression + the early playtest responses (the stat vocabulary/`deriveStats`/ability layer/leveling/`TICK_RATE` 20/XP-promotion/the six archetypes; the action-phase timeline + gambit/heal VFX; `RunConfig`/rest+boss nodes/level-*budget*/fuzz GUI; the `mobility`/`speed` renames + `defense` + `World.applyDamage` + healer chokepoint yield). Full per-step detail: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phases H · I · J · K complete** — the deckbuilder battle trial + combat-identity + objectives + the status-effect foundation: the `power` stat / health-pool encounter→turn loop + pool HUD / card-drawn hand / search-balance tooling (H); the `precision`/`evasion` dodge stats + subtractive hit/miss + per-ability weapon profiles (sword/club/katana/whip/bow) + the schema-driven archetype editor (I); the in-battle objective MODEL + the [movement.ts](src/sim/movement.ts) pathing seam + rebindable [Keybindings](src/ui/Keybindings.ts) (J); the generic `StatusEffect`/`foldEffects`/trigger system + roster/hand decouple + redraw + pre-turn empower (K). Carried World v21→v24 / Run v11→v15. Full per-step detail + the snapshot-version trail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §§I–K + [BALANCE.md](BALANCE.md).
- **Phase L complete** — L1 daemons (relics): the four-idol catalog (Mars/Minerva/Mercury/Janus) + DAEMON-ONLY gates + a run-start roll + `chance` as a first-class gate + the PreTurnScreen surface + fuzz `--daemon`; **Run v15→v16** (Minerva +4→+2 nerf; `battleTrigger` daemons + multi-daemon economy deferred to the shop round). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase L + [BALANCE.md](BALANCE.md) §L1c3.
- **Phase M complete** — progression & presentation (M1–M6): the per-turn promotion cadence (**Run v16→v17**, band-neutral), the staggered level-up screen + LOCKED curve `50/1.1`, the battle backdrop, layout-editor save-to-config, + the water mechanic + procedural-map rework (`?layout=procedural`). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase M.
- **Phase N complete (bar N4)** — rogue mobility & balance closure: N1 the rogue DASH (the ability `kind` union + an aggressive-close `DashAbility` on J2's movement-intent seam + the `unit:dashed` event), N2 the band re-sweep → **LOCKED `1.25 × 1.5`** (a mercenary+ranged duopoly), N3 the leveling check (GREEN at `50/1.1`), + the turn-cap unification (`resolveAsDraw`-at-cap across harness/arena/live). **N4 (overnight out-of-sample verify) DEFERRED INDEFINITELY to a VPS** (the dwm leak). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase N + [BALANCE.md](BALANCE.md).
- **Phase O complete** — the Post-N agency round's typed objective refactor (O1–O5). O1 (always-an-objective TYPED per-team model `atWill`/`engage`/`hold`, enemy inert-but-symmetric; **WorldSnapshot v24→v25**), O2 (`hold`), O3 (`focus` + 3 switchable tile-resolutions), O4 (ranged `minRange` kiting — bow 2 / mage 2 / catapult 4), O5 (dev-only `--objective=coverage` churn bot, kept separate from the measurement proclivity). Band **re-confirmed `1.25 × 1.5`** (no retune). Detail: git log + [BALANCE.md](BALANCE.md) §O5.
- **Phase P complete** — the shared `UnitCard` component ([src/ui/UnitCard.ts](src/ui/UnitCard.ts)). P1 (extract + recruit/promotion parity + the rarity-accent seam), P2 (full-variant XP-to-next bar, config-derived `xpProgress`), P3 (PreTurnScreen adopts the full card; the screen scrolls for a tall hand), + the POW-display rework (its own accented meta row). **UI-only — no snapshot/fuzz change** (WorldSnapshot v25 / RunSnapshot v17 hold). Detail: git log + the top ACTIVE block.
- **Phase Q COMPLETE (Q1–Q6)** — the in-battle HUD overhaul + pacing: the `PlaybackSpeed` set-by-value model (0.5×–3× + pause), the pre-battle countdown (replacing the spawn-anim), the four HUD panes (speed TR / objective BR / player-cards BC / enemy-cards top, on P's `compact`/`full` UnitCard + a `team`-tinted enemy mirror), and dismantling the old monolithic HUD (net −325 lines). **Presentation/UI/input — no snapshot/fuzz change** (v25/v17 hold). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase Q.
- **Phase R COMPLETE (R1–R3)** — auxiliary card surfaces + cleanup; closes the Post-N O→R round. R1 (the shared roster-view modal — a top-right "Roster" button on Map/Recruit/pre-turn opens a dimmed overlay of the roster as `full` UnitCards [a new `roster` skin]; order via the pluggable `rosterOrder` seam, recruitment-order default), R2 (the pre-turn draw-pile [bottom-right] + discard-pile [bottom-left] views — the R1 modal generalized into a shared `CardListModal`/`CardListButton`; piles ride `turn:starting`/`turn:handRedrawn` resolved to templates in recruitment order, contents-only/unordered via `Run.resolvePileForDisplay`), R3 (the procedural-map label unify — one `PROCEDURAL_MAP_NAME` = "Uncharted Ground" constant + cleanup chores: `.gitattributes`, inline-SVG favicon, three.js vendor-chunk split; catapult SFX deferred). **UI-only — no snapshot/fuzz change** (v25/v17 hold). User-confirmed in the native browser. Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase R; deferred bugs in [TODO.md](TODO.md) §Phase Qb.
- **Phase Qb COMPLETE (#2 + #3)** — the two deferred bug investigations, both user-confirmed in the native browser. **#3** (`b6da01f`) the corridor archer kite-pin: a too-close kiting ranged unit (inside `minRange`) with a blocked retreat fell through to the target-cell anti-freeze fallback and stepped ONTO the soft-excluded target; fix = omit that fallback when `dist < minRange` (abstain/queue), guarded by a corridor fixture + a "no two combatants share a cell at a tick boundary" invariant test (the §35d fuzz invariant generalizes it). **#2** (`0d9413e`) the billboard depth-sort: the `depthWrite:false` glyph instances had no per-instance depth sort, so draw order (= spawn slot) decided occlusion; fix = `SpriteRenderer.sortByDepth(camera)` reorders the instance buffers back-to-front each frame, wired into `Game.onFrame`. No snapshot/fuzz change (v25/v17 hold); 1021 tests. Detail: git log + [TODO.md](TODO.md) §Phase Qb.
- **Phase S COMPLETE (S1 + S2)** — encounter-system groundwork: **S1** (`deceb6a`) the **Floor→Hop** rename (node-map progression concept + dev surface; **RunSnapshot v17→v18**; battlefield-floor tile / `Math.floor` left alone); **S2** (`f25fc8f` + glyph follow-up `e927308`) the **selectable root** via a pre-root sentinel (`PRE_ROOT_NODE_ID = -1`, [NodeMap.ts](src/run/NodeMap.ts); **v18→v19**; adds one battle/run). **GOTCHAS #110** — don't "fix" `currentNodeId = -1` back to `rootId` (`currentHop`/`hopOf` throw at pre-root by design). Both user-confirmed. Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase S.
- **Phase T COMPLETE (T1–T3 + follow-ups)** — the run-container round: **T1** (`e3a03eb`) the **Sector schema** ([sectors.ts](src/config/sectors.ts) + `config/sectors.json`, zod — hop-gated layout pool; procedural = reserved sentinel literal-matching `RunConfig.FORCE_PROCEDURAL`); **T2** (`6295d66` + `33b4fd5` weight-seam + `eb9b2ab` banner) **the run = a sequence of sectors** (`sector-map.json` DAG + [sectorWalk.ts](src/run/sectorWalk.ts); `Run` → a current-sector model, **RunSnapshot v19→v20**; weighted board roll; player pool + roster carry across; ships one sector **"The Start"**, source == sink; **GOTCHAS #111** — `pickOne` zero-draws a singleton); **T3** (`cf773ea` + `46207e6` + `84bf7ea`) the dev tooling (sector editor + `/tools/` index + the layout-editor "add to sector" toggle; its swallowed-`ReferenceError` button bug fixed). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase T + [TODO.md](TODO.md).
- **Phase U COMPLETE (U1–U3b)** — the keystone: `rollEnemyWave` RETIRED from `Run`, replaced by an authored **Encounter** model. **U1** (`89957c0`) the pure wave resolver [wave.ts](src/run/encounters/wave.ts) (`resolveWave` — budget/count/weight; `distributeWeightedLevels`; per-instance level weighting [USER-LOCKED]); **U2** (`010d35e`) the wave-list grammar + sequencer [sequencer.ts](src/run/encounters/sequencer.ts) (`waveForTurn` — wave | weighted-pick | loop{N|forever} | condition-gated **stages**; nests to any depth; resumable cursor; terminal = last-wave-repeats [USER-LOCKED]); **U3a** (`2b4f733`) the Encounter schema [encounters.ts](src/config/encounters.ts) (`kind` enum normal|elite|boss + `rewards?` seam); **U3b** (`252b7ea`) the Run wiring (`beginTurn` resolves via `waveForTurn`→`resolveWave`; **RunSnapshot v20→v21**; per-encounter `enemyHealthPoolMax`; encounter name → HUD). `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow still call them). User-confirmed. **V0/V1 superseded U3's empty catalog + code-built reproduction** (see the ACTIVE block). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase U.
- **Phase V COMPLETE (V0–V2c).** The `sector-owns-both` flip (`9b8d818`); the keyed `selectEncounter` resolver + `config/selection.json` (`498745d`+`c647ebf`, **RunSnapshot v21→v22**); the **encounter editor** + visual wave-grammar builder + byte-faithful `formatEncountersJson` (`f92a63a`+`18a59d5`); placement on both editors via `addEncounterToSectorPools` (`8ddbdc7`); the 6-encounter catalog in "The Start" — Brigands/Highwaymen/Deserters + the grammar demos Artillery Company/Ronin-and-Mages/Guarded Adventurer (`c932593`); editor tab-restore (`e061c8e`). No snapshot bump; user-confirmed. Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase V".
- **Phase W COMPLETE (W1 boss + W2 elites; scope EXPANDED; both user-confirmed).** **W1** (`5046960`) **"The Bandit King"** (`kind:'boss'`/pool 20) — the first to exercise the condition-gated **`stages`** grammar (3-phase escalation at pool ≤0.66/≤0.33). **W2** (`d08a696`) **elite map-nodes** (pulled forward): a new `'elite'` `NodeKind` + a `NodeMap.generate` scatter pass (structure+rest byte-identical; `eliteChance 0.25`/`eliteMinSpacing 2`; always a non-elite sibling = optional detour); `*` glyph NEON_PURPLE; two elites (pool 12/budget 1.35×); no Run dispatch change (reuses the battle path); **RunSnapshot v22→v23**; fuzz `PATH_KINDS += 'elite'`. Boss/elite numbers UNTUNED (X targets). Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase W".
- **Phase Wb COMPLETE (QoL interstitial; Wb1–Wb4; pre-X polish).** The pre-turn encounter NAME (`turn:starting` gains `encounter:{name,kind}`, elite/boss badged; `9ea338e`); two editor fixes — add-to-sector tab-restore (`427d57d`) + the `← Tools` link on every dev tool (`51fd160`); and the sector fight pool split BY KIND `encounters:{normal,elite,boss}` (exhaustive `Record<EncounterKind,…>` + a kind-consistency guard + a kinded `encounterPoolAtHop`; both editors reworked; `c99ab3f`). No snapshot bump. Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase Wb".
- **Phase X COMPLETE (X1 + X2 + X3) — the band RE-DERIVATION** (closes the S→X encounter round). `levelCap` (per-wave, opt-in) + **X1** per-run difficulty multipliers (`waveSizeMultiplier`/`levelBudgetMultiplier`; `difficulty.json` default + `RunConfig` seam) + **X2** the per-encounter pool-damage harness (`--per-encounter` / `--encounter=<id>` / `--seed-offset`; dev-only + a one-line `RunConfig.forcedEncounterId`) + **X3** the 5-step sweep: per-kind bands **normal≈3 / elite≈6 / boss≈10** pool-damage/instance, **win rate 0%→~37% optimal, +24pt gradient**, bands holding out-of-sample. Config + tests only — **no snapshot bump**. Detail: [BALANCE.md](BALANCE.md) run log + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §X + git.
- **Cluster 1 (Combat Depth) — Phases Y→34 COMPLETE & user-confirmed.** Full as-built archived → [archive/post-x-roadmap.md](archive/post-x-roadmap.md); the load-bearing facts are condensed in the top ACTIVE block's "🗄️ Cluster 1" pointer. Per-phase detail in git + [BALANCE.md](BALANCE.md) + the project memory.
- **Tests:** **1426 main** pass (0 `it.todo()`) via `npm test` (§35a added the 12 `occupancy.test.ts` cases; the docs-hygiene HANDOFF char-budget test was restored to green by this trim); **210 fuzz:smoke** via `npm run fuzz:smoke`; `npm run typecheck` clean. One known residual — `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, out of GP5 scope). Fuzz sweep `npm run fuzz` (20 seeds default; `--per-hop` for per-hop analysis; `--layout=procedural` to isolate the M6 maps); the `scored` strategy + `--search` (→ `output/best-strategy.json`) drive the balance tuning. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`. **READ [BALANCE.md](BALANCE.md) FIRST for any balance work.** The per-phase test-count history lives in git log + [archive/post-h-roadmap.md](archive/post-h-roadmap.md).
- **Dev server:** Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent). **The user usually has their own `npm run dev` on 5173 during a session**, and the preview MCP refuses to attach to a server it didn't start ("not a preview server"). For agent browser-verify, use the **`dev-preview` launch config** ([.claude/launch.json](.claude/launch.json)) — `npm run dev -- --port 5191 --strictPort` binds a dedicated port that never collides with 5173, so `preview_start name=dev-preview` always works. **Dev handle:** `window.__game` (DEV-only) exposes `bus`/`renderer`/`run`/`activeScene`/`keybindings`/`sprites`; drive a battle headlessly with `__game.dispatch({kind:'enterNode',nodeId})` then `{kind:'advanceTurn'}` (past the PreTurnScene gate), and freeze the auto-advancing run with `scene.clock.advance = () => {}` for a stable screenshot.
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**Phases A → N are COMPLETE (bar the VPS-gated N4).** The full as-built history lives in [archive/post-h-roadmap.md](archive/post-h-roadmap.md) (Phases I → N, the detailed roadmap this round superseded), [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md) (E → GP), and [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md) (A → E).

**The Phase-N playtest feedback was synthesized into the Post-N agency/control round, Phases O → R — now ✅ COMPLETE** (the brief is archived at [archive/phase-n-feedback.md](archive/phase-n-feedback.md)): **O** the objective-system refactor + ranged `minRange`; **P** the shared `UnitCard`; **Q** the HUD overhaul + pacing; **R** roster + draw/discard pile views. Detail in the Current-state list + git.

**The ENCOUNTER-SYSTEM round (Phases S→X) is ✅ COMPLETE** — archived at [archive/post-r-roadmap.md](archive/post-r-roadmap.md) (authored **Encounters** replacing random `rollEnemyWave`, **Sectors** as the run container, boss/elite tiers, the X3 band re-derivation).

**The road to feature-complete is the LOCKED post-X meta-roadmap — [META-ROADMAP.md](META-ROADMAP.md), 6 ordered clusters.** **Cluster 1 — Combat Depth is COMPLETE & user-confirmed** (Phases Y→34, archived → [archive/post-x-roadmap.md](archive/post-x-roadmap.md)). **Cluster 2 — Spatial & Movement is the ACTIVE [ROADMAP.md](ROADMAP.md)** (Phases **35→41**): harden the occupancy core → non-instant moves → terrain/tiles → the data-driven `UnitDef` keystone → multi-tile footprints → destructible terrain → balance; **✅ Phase 35 COMPLETE (35a–35d); ▶ NEXT = Phase 36** (non-instant moves / the claim system). The top ACTIVE block carries the full cursor + the locked decisions. Smaller follow-ups in [TODO.md](TODO.md); the later clusters (Economy / Drafting / Map Content / Meta & Ship) live in [META-ROADMAP.md](META-ROADMAP.md).

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phase A complete** — A1–A5 (action selector, command channel, fuzz harness, config externalization, scene system).
- **Phase B complete** — B1/B1.1 (palette + selective bloom), B3 (HP/progress bars), B5 (CSS scanlines), B6 (audio), B7 (root node `@`).
- **Phase C1 complete** — C1a/b/c/d (terrain + walls + LOS + faceted prisms + layout editor + Labyrinth pathfinding fix).
- **Phase D complete** — D1 (renderer cap), D2 (drag-paint), D3 (variable sizes), D4 (fit/scroll camera), D5.A–E (spawn regions + overflow queue), D6 (half-cover), D7.A–C (chasm + fire + healing), D8 (theming).
- **Phase E complete** — combat depth (E1–E7). E1 (stat vocabulary + `deriveStats`), E2 (generic ability layer, retiring `AttackBehavior`), E3 (per-archetype `growthRates` + leveling), E3.5 (`TICK_RATE` doubled 10 to 20), E3.6 (DOM unit overlays + hitsplats), E4 (XP loop + PromotionScene + half-cover mult), E5 (pathfinding refresh: target stickiness + boids sidestep), E6 (combat visuals: shove/projectile/hitsplats), E7 (the six combat archetypes: melee/ranged + rogue/healer/mage/catapult). Full per-step worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md); forward plan in [ROADMAP.md](ROADMAP.md).
- **Phase F complete** — Phase-E playtest response (F1–F6). F1 (draft-pool pull-forward: the E7 archetypes become draftable), F2 (first-class action phase timeline windup->...->recovery), F3 (projectile & impact timing), F4 (rogue gambit sequencing), F5 (heal feedback VFX), F6 (heal/utility XP). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase G complete** — run progression (G1–G5). G1 (`RunConfig` + short-run harness), G2 (longer maps + full-viewport map scene), G3 (rest & boss nodes + map icons), G4 (enemy level-*budget* model + recruit leveling), G5 (fuzz-strategy factory + GUI launcher). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase GP complete** — combat & stat-legibility playtest response (GP1–GP5). GP1 (`endurance`->`mobility` / `speed`->`agility` rename + universal move base), GP2 (`defense` stat + `World.applyDamage` consolidation), GP3 (recruit-card + HUD legibility), GP4 (ranged/catapult acting-position pathing), GP5 (healer chokepoint yield via `SwapAction` + centroid navigable-snap). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **H1 complete (Phase H) — the `power` stat; first of Phase H's six (H1 → H2 → H3 → H4 → H5 → H6).** One commit (`feat(H1)`). Adds `power` to the unit stat block as the Phase-H meta-currency — a turn's surviving units will chip the *opposing* health pool by their Σ`power` (consumed in H4; **H1 is behavior-neutral** — no combat/`deriveStats`/`applyDamage` path reads it). Mirrors GP2's "add a stat" plumbing exactly. **Shipped values (roadmap-locked):** base **1** / growth **0.20**, **uniform across all 6 archetypes** (pure knob-tuning, re-balanced in H6).
  - **Plumbing** (the GP1/GP2 pattern): `power` added to `UnitStats` ([Unit.ts](src/sim/Unit.ts)) / `ZERO_STATS` ([stats.ts](src/sim/stats.ts)) / the base+growth zod schemas ([archetypes.ts](src/config/archetypes.ts)) / every archetype in [archetypes.json](config/archetypes.json) / `STAT_KEYS` (**appended LAST**, the 9th draw — each existing stat keeps its within-level draw position) + both [leveling.ts](src/sim/leveling.ts) return objects / the shared `STAT_LABELS` (`POW`, [statLabels.ts](src/ui/statLabels.ts) — auto-surfaces on the recruit + promotion cards, which iterate it) / the in-battle HUD line ([HUD.ts](src/ui/HUD.ts) `formatStats` → `DEF·MOB·AGI·POW`, user call to show it now).
  - **Roadmap correction:** the H1 card said base stats live in `config/stats.json` — stale; they're in **`config/archetypes.json`** (`stats.json` is balance scalars only).
  - **Snapshot bumps — BOTH (user call, beyond the roadmap's "WorldSnapshot bump"):** `WorldSnapshot` **v17→v18** + `RunSnapshot` **v5→v6**, because the roster `team: UnitTemplate[]` in the Run save also carries the stat block — a pre-H1 save would otherwise load a power-less roster → `NaN` power on the next level-up. Both reject stale versions outright (no migration; same stat-shape-contract rationale as GP1/GP2).
  - **Fuzz `stat:power` auto-appeared (intended):** the G5 strategy menu is config-derived off `STAT_KEYS`, so `power` auto-added a `stat:power` recruit strategy — and [strategies.test.ts](tests/fuzz/strategies/strategies.test.ts) is *also* config-derived (count + per-stat asserts off `STAT_KEYS`), so it stayed green. Updated the now-stale [registry.ts](tests/fuzz/strategies/registry.ts) comment: the genuinely-deferred-to-H6 piece is the **pass/no-recruit** strategy variant (needs new code) + the H6 balance-sweep *usage*.
  - **Verification:** **610 tests** (was 605; +balance-proof `power`-levels-per-config-growth cases in [leveling.test.ts](src/sim/leveling.test.ts) [derived from `ARCHETYPE_CONFIG`, never hardcoded] + H1 round-trip / stale-version-reject cases for World v18 AND Run v6 in [snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts); 16 `UnitStats` fixtures patched, driven off `tsc --noEmit`; the v17 pin in [spawn-overflow.test.ts](tests/integration/spawn-overflow.test.ts) bumped to v18). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); build green; fuzz:smoke 22/22. **Browser-confirmed (DOM-verified, 0 console errors):** drove a live run — HUD reads `DEF·MOB·AGI·POW` per unit, recruit cards show all 9 stats incl `POW` in canonical order, and a forced-level-up PromotionScene renders `POW 2 → 3 (+1)`. **User-playtested: "looks good."**
  - **Fuzz baseline shifts** (the extra per-level RNG draw advances the shared `levelupRng` stream → recruited rosters differ from pre-H1); expected for any stat addition, re-baselined in H6. Regenerate the gitignored `tests/fuzz/output/*`.
  - **TODOs logged** ([TODO.md](TODO.md)): the 4-stat HUD line is getting crowded (revisit presentation); `power` wants distinct visual treatment on the recruit/promotion cards (categorically different mechanical impact). Both cosmetic, out of Phase H scope.
- **H2 complete (Phase H) — spawn-region tile count relaxed to a 1–10 range; random-subset placement locked in tests. Second of Phase H's six (H1 ✅ → H2 ✅ → H3 → H4 → H5 → H6).** One commit (`feat(H2)`). **Finding that reshaped the step:** H2's specced mechanic — *"in `spawnTeam`, when units < region tiles, pick the used tiles at random instead of filling first-N (both teams)"* — was **already live since D5.B** (`9410227`). [`spawnTeam`](src/sim/battleSetup.ts) shuffles `region.tiles` (Fisher–Yates) then places `min(team, tiles)` units on the first N, which is a uniformly random subset, in random order, for BOTH teams, every battle. Regions are 8 tiles and the roster is 5, so the subset case engages in normal play. So H2 became (1) **relax the hard 8-tile-per-region constraint** and (2) **lock the property in tests**.
  - **The relaxation:** new `SPAWN_REGION_MIN_TILES=1` / `SPAWN_REGION_MAX_TILES=10` in [config/layouts.ts](src/config/layouts.ts); the zod `tiles` rule went `.length(8)` → `.min(1).max(10)` (the "8" was an old 8-card-hand relic). `SPAWN_REGION_TILE_COUNT=8` is **repurposed as the procedural/editor default** (procedural top/bottom edge bands + the layout editor's starting region template stay 8-wide); only hand-authored regions get the range. Bigger regions ⇒ more positional variance (the H2 foregone-conclusion antidote); a region *smaller* than the team trickle-spawns the remainder through the **D5.C overflow queue** (a "gate"/reinforcement lever).
  - **Sim untouched — it was already tile-count-agnostic:** every consumer reads `region.tiles.length` (`spawnTeam`'s `min(team,tiles)` + overflow, snapshot round-trip, camera anchor, centroid helpers); nothing indexes a fixed slot. No sim/snapshot change. `SpawnRegion` round-trips its tiles as-is, so **no snapshot bump.**
  - **Layout editor** ([tools/layout-editor/editor.ts](tools/layout-editor/editor.ts)): paint FIFO-cap moved to `SPAWN_REGION_MAX_TILES`, export validation flags out-of-range (only the empty/`<min` case is reachable since the cap holds the top), pill label reads `n/10`. The default-region builder still lays an 8-tile band.
  - **No fuzz baseline shift** (notable — the roadmap predicted one): the shuffle's RNG was already consumed and no shipped layout changed (all still 8, comfortably in range). This *confirms* the mechanic predated H2.
  - **Roadmap/GP5 correction:** the GP5 note "H2 randomizes spawn tiles, which defeats spawn-order bias" assumed spawning was *currently* order-based — it wasn't (D5.B already shuffled), so spawn-order bias was never an available lever. The healer-yield fix (GP5.1) was the right durable call regardless.
  - **Verification:** **620 tests** (was 610; +6 in new [spawn-placement.test.ts](tests/integration/spawn-placement.test.ts) — subset ⊆ region, every tile covered over 200 seeds, deterministic per seed, varies across seeds, both teams, exact-fill boundary; +4 schema-range accept/reject in [layouts.test.ts](src/sim/layouts.test.ts) — min/max/default accepted, out-of-range + duplicate rejected, default ∈ range; the per-layout exact-8 assertion relaxed to the range). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz:smoke 22/22. **Headless-only — the editor UI change (paint cap / validation / label) is the user's eyeball pass** in the standalone editor tool; no game-render surface was touched.
- **H3 complete (Phase H) — per-unit deployment counter (the fatigue hook); sim/run-layer. Third of Phase H's six (H1 ✅ → H2 ✅ → H3 ✅ → H4 → H5 → H6).** One commit (`feat(H3)`). A per-roster-slot counter that bumps each time a unit is deployed into a turn and **resets per encounter** — pure bookkeeping now, shipped with a clean seam for the *future* fatigue debuff (deferred to H6 if the long-run sweep shows turns are foregone conclusions). **User decisions (both as recommended):** (1) **storage** = a **parallel `Run.deploymentCounts: number[]`** keyed by rosterIndex (NOT a `UnitTemplate` field) — keeps the template a clean unit-definition, makes the per-encounter reset a plain `fill(0)`, and avoids leaking player-only state onto enemy/live-spawn templates; (2) **wired live at the deploy boundary now** (not dormant-until-H4).
  - **Two public methods are the stable seam the H4 turn loop will drive:** `resetDeploymentCounts()` (zero all — call at encounter start) + `recordDeployment(rosterIndices)` (bump each deployed slot — call per turn with the deployed hand; out-of-range indices ignored). [Run.ts](src/run/Run.ts).
  - **Live wiring (pre-H4: an encounter == one battle):** `handleEnterNode` resets at encounter start (right after `phase='battle'`) then records one deployment of the **full roster** (pre-H5 the whole roster is the "hand") → every count reads **1** mid-battle. H4 moves the reset to the encounter-loop entry + the record to each turn's hand spawn so multi-turn encounters accumulate; H5 narrows the recorded set to the drawn hand. **Stays synced with the roster:** a recruit (`handleChooseRecruit`) appends a fresh `0`.
  - **Snapshot:** **RunSnapshot v6→v7** (`deploymentCounts: number[]` added; a v6 save is rejected outright — no counts ⇒ out-of-sync-with-roster risk). **No WorldSnapshot bump** (counts are Run-side; the future debuff reads them at deploy time, the World never needs them).
  - **No RNG / fuzz / determinism impact** — incrementing a counter draws no RNG and feeds no behavior. Fuzz:smoke 22/22 (no baseline shift).
  - **Verification:** **628 tests** (was 620; +7 in [Run.test.ts](src/run/Run.test.ts) — init zeros, deploy records 1/slot, per-encounter reset proven (a 2nd battle reads 1 not 2), recruit appends 0, accumulate-then-reset (the H4 seam), out-of-range ignored, toJSON→fromJSON round-trip; +1 in [snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts) — v7 carries counts parallel to roster + v6 rejected). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Headless-only — no render surface touched** (the counter has no UI yet; any fatigue readout is H6+).
- **H4a complete (Phase H) — health pools + the multi-turn encounter loop, HEADLESS core. Fourth of Phase H's six (H1 ✅ → H2 ✅ → H3 ✅ → H4 → H5 → H6); H4 is split into H4a (this, headless + fully vitest-tested) → H4b (render + the two screens, NOT started).** The riskiest orchestration change in the plan: a "battle node" is now an **encounter** = a series of **turns**, each turn the full roster vs a **freshly-rolled** enemy wave, with two **health pools** deciding it. The tactical `World` stays a single-turn engine; the loop lives on `Run`. **Five commits** (each green; no browser pass — H4a is headless by the user's split, verified by `npm test`):
  1. **`enemyBudget` split** (`refactor`, byte-identical) — `enemyBudgetFor(team)→number` (budget, no RNG) + `rollEnemyWave(rng, team, budget)→UnitTemplate[]` (composition) out of `buildEnemyTeam` (now their composition). H4 fixes the budget once per encounter and re-rolls the wave per turn.
  2. **`battle:ended` reporting** (`feat`) — payload gains `winner: …|'draw'` + `survivorPower: {player,enemy}` (Σ `stats.power` over each team's living **on-grid** units; spawn queue excluded). `World.checkBattleEnd` routes through a shared `emitBattleEnded()` and **drops the `winner==='player'` gate on `xpAwards`** (`computeXpAwards` was already winner-agnostic) so every turn's damage banks. New **`World.resolveAsDraw()`** lets the *driver* force-end an unresolved turn as a draw; it also terminates a genuine mutual wipe (which `checkBattleEnd` still leaves un-ended to preserve the walls-only/pre-spawn invariant — gotcha: a blanket draw-on-`!playerAlive&&!enemyAlive` would trip the setup tick). Flipped the `World.test`/`Run.test` enemy-win cases. `survivorPower` is **optional** on the event so test fakes don't need it; every real emit sets it.
  3. **`config/health.json` + loader** (`feat`) — `playerHealthMax 20`, `enemyHealthMax 8`, `maxTurns 20`, `maxTurnSeconds 100` (→`secondsToTicks` at the consumer), `chipMultiplier 1`. Mirrors `difficulty.ts`.
  4. **The encounter loop + `RunSnapshot` v7→v8** (`feat`) — `Run` gains `playerHealth` (run-wide, persists), `enemyHealth`/`turnIndex`/`encounterBudget` (per-encounter), `pendingEncounterXp` (accrued, banked once at end). Structured `beginEncounter→beginTurn→resolveTurn→endTurnAndContinue→finishEncounter` **so H4b can insert the pre/post-turn screen gates without restructuring the loop** (mirrors the `promotion`-phase pause). `handleEnterNode`'s battle branch → `beginEncounter`; budget fixed via `enemyBudgetFor`, wave re-rolled via `rollEnemyWave`. Chip is symmetric off `survivorPower` (winner ignored). **Precedence: `playerHealth≤0`→defeat FIRST (run-loss terminal even on a both-zero turn), then `enemyHealth≤0`→win, then `turnIndex≥maxTurns`→resolve by remaining pool fraction (player loses ties).** XP banks once at encounter end (one PromotionScene), discarded on defeat. `handleBattleEnded`→`handleTurnEnded` (passes `survivorPower`, not `winner`). `Run.test` reworked onto `winEncounter`/`loseEncounter`/`chipTurn` helpers + 11 new loop cases.
  5. **Integration test + v8 round-trip** (`test`) — `tests/integration/encounter-loop.test.ts` drives the loop over **real Worlds** (a World per turn from `run.currentEncounter`, **polling `currentEncounter`** so there's no `battle:started` re-entrancy; the per-turn cap calls `resolveAsDraw`): terminates + ends the node (4 seeds), **resume determinism** (snapshot→restore→identical turn-for-turn encounters), 1-tick cap draws chipping **both** pools, distinct per-turn `worldSeed`s (fresh roll). `snapshot-roundtrip` gains the v8 mid-encounter pool round-trip + v7-reject.
  - **Determinism:** the per-turn `battleRng` forks at `beginTurn` (never looked ahead / stashed), so the snapshotted `this.rng` ALONE reconstructs every future turn's wave; **turn 1 is byte-identical to the pre-H4 single battle** (`enemyBudgetFor` draws no RNG). `encounterBudget` is **persisted, not recomputed on resume**.
  - **ROADMAP correction:** the H4 card predicted a **WorldSnapshot bump** — **none was needed** (driver owns the per-turn cap, not the World; there is NO World between turns — `BattleScene` rebuilds it per `battle:started` — so a mid-*encounter* save is pure Run state). Only `RunSnapshot` bumped (v7→v8).
  - **Verification:** **649 tests** (+8 new loop/round-trip; `World.test`/`Run.test`/`determinism.test` enemy-win + helper cases updated, not deleted). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **fuzz:smoke 22/22.** **Fuzz default+greedy: 0 hangs — BUT win rate jumped ~45-60% → 100%, avg floor 10.** That is the roadmap's **foregone-conclusion** risk made real: with **no within-turn attrition** (units recycle to full HP each turn) + a big player pool (20) vs small enemy pool (8), the player chips the enemy out faster than they take damage and almost never loses. **This is expected + is the H6 signal** — the **fatigue debuff** (built on H3's `deploymentCounts`) is the documented escape hatch, wired in H6 only if the balance sweep confirms it. H4a's job is the *mechanism* (correct, deterministic, hang-free); the *balance* (pool sizes, `power` growth, fatigue) is H6.
  - **H4b ✅ (render + the two screens, the user's design — see the H4b entry below):** a **pre-turn screen** + a **post-turn outcome screen**, both auto-advancing, built extensible; + the in-battle pool HUD. The async screen gates break the live `battle:ended→battle:started` re-entrancy H4a's headless tests sidestep.
- **H4b complete (Phase H) — the render half of H4: the pre/post-turn screens + the in-battle pool HUD + the live turn loop. PHASE H4 IS NOW COMPLETE (H4a headless ✅ + H4b render ✅).** Two commits.
  1. **Turn-gate wiring** (`feat(H4b)`) — `Run` gains **`pauseAtTurnGates`** (default OFF = the H4a synchronous loop, so every headless test + the fuzz harness stay byte-identical; **Game flips it ON**). When on, the loop pauses on two new phases — **`turn-intro`** before each turn (emits **`turn:starting`**) and **`turn-outcome`** after a turn resolves (emits **`turn:resolved`** with the per-side chips + the encounter `result`) — each resumed by the new **`advanceTurn`** command. These pauses also break the live `battle:ended→battle:started` re-entrancy. Refactor: `resolveTurn` now only chips/accrues; the decision (`turnResult`) + continuation (`continueAfterTurn`) are split so the post-turn screen can show the result before the loop acts. **Presentation-only — the phases + flag are NOT snapshotted; no schema bump.** +5 gated-path tests; the flag-OFF path proven byte-unchanged (existing Run/encounter suites + fuzz:smoke green).
  2. **Screens + pool HUD + Game wiring** (`feat(H4b)`) — `PreTurnScene`/`PreTurnScreen` (Turn N + both pool gauges + a deck-draw placeholder + a "Fight" skip) and `PostTurnScene`/`PostTurnScreen` (the tactical winner + the Σ`power` chips + post-chip pools + the `Encounter cleared` / `Your run ends here` / `Next: Turn N+1` status + a "Continue" skip), both **auto-advancing on a timer** (`PRETURN_AUTO_MS` 2.0s / `POSTTURN_AUTO_MS` 3.0s — user-tuned by feel) via `advanceTurn`, built **extensible** for the H5/H6 deck-draw + input. Shared `renderPoolGauge` ([poolGauge.ts](src/ui/poolGauge.ts)) drives both screens; the HUD gains a compact `Turn N · You X/20 · Foe Y/8` line. Game flips `pauseAtTurnGates` on in `createRun`, maps `turn:starting`→`PreTurnScene` + `turn:resolved`→`PostTurnScene`, forwards `advanceTurn`. The `turn:resolved` swap fires from INSIDE the ending `world.tick()` — **safe** because `BattleScene.tick` + the `Clock` callback null-guard everything `dispose()` tears down (`checkBattleEnd` is `tick`'s last step).
  - **Verification (browser, DOM + captured event payloads, 0 console errors):** drove map → pre-turn (Turn 1, 20/20 vs 8/8) → battle (HUD pools render) → post-turn across all three outcomes — **won** (green, enemy pool → 0, → recruit), **ongoing draw** (chips 2/3, pools **carry** 17/6 into Turn 2 — confirmed via the `turn:starting` payload; **NB the synchronous-eval DOM read is polluted by the ~180ms screen fade-out**, so the *events* are authoritative, not a mid-fade `querySelector`), **lost** (red, "Your run ends here." → GameOver). 654 tests; tsc + lint clean. **Subjective feel — screen timing / layout / copy — is the user's playtest;** all knobs are isolated consts + CSS. **Carries the H4a foregone-conclusion note: with the placeholder balance the fuzz wins ~100% (the H6 fatigue-debuff signal).**
- **H5 (headless mechanics) complete (Phase H) — the card-drawn hand + the `playerTeamLevel` seam swap. The deckbuilder is now mechanically live.** One commit (the **pre-turn hand DISPLAY is the H5b follow-up** — user split it "headless mechanics first," our cadence). **Deck resets per encounter** (user call — no deck state carries between encounters, matching the no-carried-wounds model).
  1. **The seam swap** ([enemyBudget.ts](src/run/enemyBudget.ts)) — `playerTeamLevel` flipped from "Σ of roster levels" → **`avgLevel × min(rosterSize, handSize)`** (the *expected hand* level, since only a `handSize` hand fights each turn). Broke the old `avgTeamLevel → playerTeamLevel` call chain via a private `rosterLevelSum` (recruits still read the plain mean; the public seam is now a non-average product). **For a roster ≤ `handSize` the new value EQUALS the old sum** (`avg × size == sum`), so the starting roster's battles + every existing balance-proof test are unchanged; the swap only diverges once the roster outgrows the hand — which is the **treadmill fix** (recruiting past `handSize` adds deck cards, NOT enemy budget; only your *average* level moves it).
  2. **The deck** ([Run.ts](src/run/Run.ts)) — `Run` gains `drawPile` / `discardPile` / `hand` (all hold `rosterIndex` values) + a dedicated **`deckRng`** stream (isolated like `levelupRng`, forked **last** in the ctor). `beginEncounter` rebuilds + Fisher–Yates-shuffles the draw pile from the **current** roster (so a fresh recruit is in the deck) and clears hand/discard — **encounter-scoped**. `beginTurn` discards the previous hand, then `drawHand()` draws up to `DECK.handSize` (popping the draw pile; reshuffling the discard back in when it empties; stopping early only when both piles are exhausted, i.e. roster < handSize fields everyone). **Only the drawn hand fights** (the per-turn `currentEncounter.playerTeam` is the hand, not the roster); `recordDeployment` now narrows to the hand, so **H3's deployment counter finally varies per turn** (the fatigue hook gets real signal). New `config/deck.json` + [src/config/deck.ts](src/config/deck.ts) (`handSize: 5`, A4 two-layer pattern).
  - **RunSnapshot v8→v9** (adds the three piles + `deckRng`; v8 rejected). **No WorldSnapshot bump** — same as H4: the World just receives the hand's templates, the deck is pure `Run` state. Round-trip + resume determinism pinned (a restored mid-encounter Run draws the identical *next* hand).
  - **The cliff (why handSize 5):** the **starting roster (5) == handSize (5)**, so `min(roster, handSize)` is pinned at 5 — the deck "turns on" only after the **first recruit** (roster 6 > 5). Before then every hand is the full (shuffled) roster; variance comes from the fresh-wave + random-spawn, not the draw.
  - **Determinism / baseline note:** the extra `deckRng` ctor fork **shifts every subsequent `this.rng.fork()`** (per-turn waves, recruit offers), so **H5 re-baselines the fuzz output** — *expected* here (unlike H2–H4's byte-identical changes), since the seam swap + the drawn-hand subset already change battle outcomes wholesale. Regenerate the gitignored `tests/fuzz/output/*`.
  - **Verification:** **664 tests** (+10 net: −2 reframed whole-roster-hand assertions, +12 H5 — seam `>handSize` cases, deck cap/overdraw/partition/coverage/per-encounter-rebuild/determinism, snapshot v9 round-trip + resume-determinism). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **fuzz:smoke 22/22.** Live smoke (`__game`, dev build = gated path): map → `turn-intro` → `battle` spun up a World from a **shuffled 5-card hand** `[1,4,0,2,3]`, deck partition `[0..4]` intact, **0 console errors** (screenshot times out — the documented backgrounded-tab rAF throttle, not a failure). **The post-recruit dilution (a 5-of-6 hand) + the live deck feel are the user's playtest** (the headless suite pins the subset/cap/dilution logic).
  - **H5b ✅ (the render follow-up — see the entry directly below):** the drawn hand is now SHOWN on the pre-turn screen.
- **H5b complete (Phase H) — the pre-turn hand display. PHASE H5 IS NOW COMPLETE (H5 mechanics ✅ + H5b render ✅).** One commit.
  - **The draw moved before the gate** ([Run.ts](src/run/Run.ts)) — the hand was drawn in `beginTurn` (which runs on `advanceTurn`, *after* the pre-turn screen shows), so the screen had nothing to display. Split a new `drawTurnHand()` (discard previous hand + draw) out of `beginTurn` and call it at the top of `startNextTurn` (BOTH paths), so the hand exists before `turn:starting` fires. `beginTurn` now just fields the already-drawn `this.hand`. **Determinism preserved** — the lone `deckRng` draw still fires once/turn in the same order, and `this.rng` (the per-turn `battleRng` fork in `beginTurn`) is an independent stream, so moving the deck draw a few lines earlier shifts nothing: the headless path stays byte-identical (fuzz:smoke 22/22 unchanged).
  - **The payload + screen** — `turn:starting` gains **`hand: UnitTemplate[]`** (this turn's drawn cards, in draw order; [events.ts](src/core/events.ts)). `PreTurnScreen` ([PreTurnScreen.ts](src/ui/PreTurnScreen.ts)) replaces the H4b "Deploying your squad…" placeholder with a **row of compact cards** — archetype glyph (green, player units) over a `Lv N` tag, label "Your hand — N drawn". New `.preturn-hand*` / `.preturn-card*` CSS (mirrors the recruit-card palette). **No snapshot/config change** (a render + payload addition; the hand is already in `Run` state).
  - **Verification:** **665 tests** (+1: `turn:starting` carries the drawn hand, capped, matching `Run.hand`). Typecheck + lint clean; fuzz:smoke 22/22 (reorder byte-identical on the headless path). **Browser-verified** (`?roster=melee,melee,melee,ranged,ranged,mage,rogue` = 7 units): the pre-turn screen rendered a **5-of-7 hand** — DOM cards `M r a M m` exactly matched the drawn archetypes (melee/rogue/ranged/melee/mage), label "Your hand — 5 drawn", capped at handSize; `advanceTurn` → battle fielded that exact same 5-card team (displayed hand == fighting hand); **0 console errors** (only the expected dev roster-override warning). Screenshot times out (the documented backgrounded-tab rAF throttle — the three.js loop runs behind the DOM overlay), so DOM inspection is the authoritative channel; **the subjective card look/layout is the user's playtest.**
- **H6 complete (Phase H) — the Phase-H gameplay closers. PHASE H6 IS NOW COMPLETE (H6a ✅ + H6b ✅ + H6c ✅).** Three commits, playtest pause between each; **no snapshot bumps** (all touched persisted state already existed). Leaves the trial fully playable end-to-end — rough balance is H7's job.
  - **H6a — rest-node pool heal** (`1b45c65`). [Run.ts](src/run/Run.ts) `resolveRest` now heals the run-wide `playerHealth` by the new `restHealAmount` knob (default **5**, capped at `playerHealthMax`; [config/health.json](config/health.json) + [health.ts](src/config/health.ts)), unconditionally + ahead of the XP/promotion branch so a rest that ALSO levels a unit still heals. Placeholder beside the rest-XP award until the real event system. 2 balance-proof tests (heal-when-wounded + never-overfill, derived from `HEALTH.*`).
  - **H6b — pass / no-recruit option** (`e221271`). New `passRecruit` [RunCommand](src/run/Command.ts) + `handlePassRecruit` (the `handleChooseRecruit` sibling MINUS the roster/`deploymentCounts` push — drops the offer, → `phase='map'`); [Game.ts](src/Game.ts) swaps MapScene on the silent fall-through (mirrors `chooseRecruit`); a **"Pass" button** on [RecruitScreen](src/ui/RecruitScreen.ts) + `.recruit-pass` CSS (always available + free, the trial default). **Fuzz interface widened** `pickRecruit → number | null` ([Strategy.ts](tests/fuzz/Strategy.ts)/[policies.ts](tests/fuzz/strategies/policies.ts)/[harness.ts](tests/fuzz/harness.ts), `null` → `passRecruit`); existing policies still return a `number` so their draw sequences + the default-sweep baselines are byte-identical. New opt-in `pass:weak` strategy (`declineBelowPower(2)`, OUT of the default set) proves the path — 12/12 complete, 0 hangs. **Browser-verified** the button renders below the cards + click → map with roster unchanged, console clean.
  - **H6c — inert spawn-time fatigue hook** (`f4ce83d`). **⚠ RE-SCOPED with the user — the ROADMAP's §H6c describes a CHIP-time recompute; we shipped a SPAWN-time unit debuff instead.** Cleaner: the debuff rides the unit into battle and flows to the chip via the already-fatigued power, with **zero** World/event/chip change. [Run.beginTurn](src/run/Run.ts) bakes `fatigueFactor(stacks)` (new [src/run/fatigue.ts](src/run/fatigue.ts) — `1 − fatiguePerStack·stacks` clamped at 0; default rate **0** → factor 1.0 → **INERT**) into a **fresh copy** of each fielded unit's `power` at the spawn-prep seam. `stacks` = `deploymentCounts[idx]` read BEFORE the `recordDeployment` bump (so a debut unit = 0 stacks; a unit that fought 3 prior turns this encounter = 3); the fresh `stats` object keeps `this.team` canonical. **`power` is the placeholder target** — the eventual vision (user's) is a stackable "Fatigued" status owned by a future status-effect system; target stat + curve + magnitude are H7's, a localized re-wire. **No snapshot bump** — the fatigued stats bake into the already-persisted `currentEncounter.playerTeam`. Tests: `fatigueFactor` inert-default canary + linear/clamp mechanic ([fatigue.test.ts](src/run/fatigue.test.ts)); Run integration (inert at default; a redeployed unit fields weaker once the knob is flipped; roster never mutated). **Fuzz baselines unchanged — confirms inert.**
  - **Verification:** **675 tests** (main suite, +10 over H5b's 665: 2 H6a + 2 H6b-Run + 3 fatigue-unit + 3 H6c-integration); **fuzz:smoke 24** (+2 `declineBelowPower`); fuzz sweep baselines **unchanged** (H6c inert, H6a/b don't touch the default strategies); typecheck + lint clean (lone pre-existing `Renderer.ts` warning).
- **H7a ✅ — the linear scored fuzz strategy + JSON config.** First half of **H7 (search-driven balance tooling)**; H7b (the random-search driver) is next. Adds an expressive `scored` strategy — a weighted sum of normalized features → argmax expressing **path + unit + pass** preference *simultaneously* (vs the one-axis-at-a-time menu). **Zero `src/` changes** (everything in `tests/fuzz/` + `config/`), so the `pure-random`/`greedy` byte-for-byte baselines + the whole `src` suite are untouched — no fuzz re-baseline. New `config/fuzz-strategies.json` is one weight vector — BOTH the `--strategy=<file>.json` input format AND the format H7b's search will emit as its winner (ships **all-zeros = neutral**: take offer card 0, lowest-id path, never pass). Loaded by [scoredWeights.ts](tests/fuzz/strategies/scoredWeights.ts) (A4 zod pattern, but colocated with the fuzz tooling — dev-only, kept out of the production bundle; the path/archetype/stat sub-schemas are built **dynamically from `PATH_KINDS`/`ALL_ARCHETYPES`/`STAT_KEYS`** so the **~21-weight** vector auto-tracks the vocabulary, and an unknown/missing key throws). [scored.ts](tests/fuzz/strategies/scored.ts): node policy = **full-path backward DP** over `run.nodeMap` (`bestScore(n)=kindWeight(n)+max(child)`, memoized; layered DAG → max-total == max-average → no long-path bias); recruit policy = **min–max-normalized-over-{offer∪roster} argmax** (the spec's explicit anti-trap — NOT min-max-within-offer) + a **virtual pass candidate** scored on the continuous terms only (PASS iff `cont(bestCard) − cont(rosterAvg) + passBias < 0`). **Spec-staleness fix:** the ROADMAP §H7a says "8 stats (7 + power)" — GP1/GP2 since made it **9** (`speed→agility`, `endurance→mobility`, + `defense`, + `power`); derived from `STAT_KEYS` so it's self-correcting. **Selection seam (user call — built INERT):** both decisions route through `selectByScore(scores, rng, {temperature=0, tiebreak='lowest'})` which at the defaults is exactly today's argmax + lowest-index and **draws nothing** from `rng`; softmax (τ>0) / random tiebreak are reserved (throw loudly if requested) so turning them on later is a one-flag flip (the harness already threads `strategyRng` into both decisions). Optional inert `temperature` field reserved in the schema (default 0, **NOT** a search dimension this cycle). `scored` registered in the menu (joins `all`, kept OUT of `DEFAULT_STRATEGY_NAMES` like `pass:weak`); `--strategy=<file>.json` loads an arbitrary vector ([cli.ts](tests/fuzz/cli.ts)). `PATH_KINDS` hoisted to [policies.ts](tests/fuzz/strategies/policies.ts) as the single source (was a registry-local const). **Verification:** `npm test` **675/675** (src untouched); **fuzz:smoke 34** (+10 scored: seam-inert + draws-nothing, path-DP looks past the local max, pass-boundary sweep, `maximizeStat` generalization across every stat, config round-trip + unknown/missing-key reject); `npm run lint` 0 errors (lone pre-existing `Renderer.ts` warning); strict `tsc` on the new files clean (the only flagged lines are the env `node:fs` class the existing `cli.ts` already shows — the repo has no `@types/node` and never tsc's `tests/fuzz`). `--strategy=scored` + `--strategy=config/fuzz-strategies.json` both run end-to-end → **100% win / floor 10** = the documented foregone-conclusion baseline the all-zero neutral vector can't escape; **moving that band is H7b/H7c's job, not this vector's.**
- **H7b ✅ — the random-search driver.** Second half of H7. Random-searches the H7a scored-strategy weight space for the **best-achievable win rate** (the balance signal). Structure = **propose → evaluate → keep-best** ([search.ts](tests/fuzz/search.ts)): `propose = sampleWeights(box, seededRNG)` (uniform draw of all ~21 weights from a `[-1,1]` box in **fixed draw order** → reproducible); `evaluate = aggregate(runMany(seeds, scoredStrategy(w))).winRate`; `keep-best` ranks by train fitness, lowest-index tiebreak. **Train/test split from the start** (`splitSeeds`: train `1..T`, test `1_000_000..` → disjoint by construction): search/select on TRAIN, score only the winner on the held-out TEST set — without it the "best" memorizes seed luck and the signal inflates. **Seeded sampler** (`samplerSeed`) → the whole experiment reproduces. Both `propose` AND `evaluate` are injectable (the tests stub `evaluate`); a later hill-climb is `propose = perturb(best)` reusing the same evaluate/fitness/split/reporting. **Box + presets live in code** (experiment knobs, not balance — so H7's only config surface stays H7a's vector). **Two presets:** `quick` (50 vectors × 8 train + 4 test × short `floorCount:4` runs → **~17s**, the "did my change move balance?" check) + `overnight` (500 × 200 + 50 × full runs → hours; runs independent → parallelizable). **CLI** ([cli.ts](tests/fuzz/cli.ts)): `--search [--preset=quick|overnight] [--vectors=N] [--seeds=N (total, ~80/20 split)] [--sampler-seed=N]`; writes `output/best-strategy.json` (the single-vector format → re-runnable via `--strategy=<file>.json`) + `output/search-results.csv` (per-vector train win rate). **Wipe-logic fix:** the normal sweep now wipes only `output/failures/` (was the whole dir), so a search's artifacts survive a later sweep — the round-trip `--strategy=output/best-strategy.json` no longer deletes the file it just loaded. **Verification:** `npm test` **675/675** (src untouched — H7b is all `tests/fuzz/`); **fuzz:smoke 40** (+6 search: sampler-in-box + reproducible sample sequence, keep-best=argmax via injected evaluate, splitSeeds-disjoint + winner-scored-on-held-out via injected evaluate, end-to-end reproducible at a fixed sampler seed with the REAL harness); lint 0 errors; strict tsc on the new files clean. `--search --preset=quick` ran end-to-end (~17s): best **100% train / 100% test** — the documented **foregone-conclusion ceiling** (no-attrition pools → ~every reasonable vector wins, so the search can't yet discriminate). Re-running the emitted winner on **full-length** runs gave **80% / 60%** — a live illustration of why the quick preset's short runs overstate and why **H7c needs full-run overnight scale**. **H7c (run the overnight sweep + tune `difficulty`/`leveling`/`health` JSONs until best-achievable win rate drops below 100%, then decide if the inert H6c fatigue debuff needs teeth) + H7d (launcher GUI / VPS wrapper) remain — the tooling is now in place for both.**
- **H7c — PLANNED, decisions locked (not started). Protocol + decisions + run log live in [BALANCE.md](BALANCE.md) — read it before starting.** H7c is an *experiment*, not a clean code step: tune the balance JSONs (starting with `difficulty.json` budget×swarm) until the **best-achievable win rate** (the H7b search) lands in a target band, using a broad-cheap→narrow-expensive funnel. **Decisions locked with the user:** (1) target = a **skill gradient** with best-achievable ≈ **2/3 (~67%)** + `pure-random`/`greedy` meaningfully lower (humans assumed to beat the linear ceiling by an unknown margin → ~67% keeps it winnable-but-losable); (2) a **config-level seed holdout** for the final overnight verify (the config can overfit seeds just like the weights — H7b's split guards weights, fresh verify seeds guard the config); (3) **telemetry** to add to the harness — per-archetype damage/healing/deaths/picks/final-composition + per-turn pool chips + per-battle XP (the tester gathers outcomes today, not these mechanism breakdowns); (4) **stage-1 grid** `budgetFactor` 0.25→1.5 × `swarmMaxMultiplier` 1.0→3.0 (~6×5), light tier, **time-estimate-first**. **Parallelism** deferred: built-in `node:child_process` grid-sharding (zero deps; each shard owns its config; doubles as H7d) only when the heavy/overnight tiers warrant it. **User's priors to test:** melee OP (GP2 `defense` → high damage + low deaths) + healer OP (too-high healing + self-leveling via `xpPerHealing=3`). **Key caveat:** the no-attrition pools (20 vs 8) may dominate over budget/swarm — stage 1 doubles as a leverage diagnosis (if budget/swarm plateaus above-band, fold `health.json` in). **H7c step 0 = BUILD the `--balance-sweep` harness + `medium`/`heavy` tiers + the telemetry pass + tests (one commit)**, THEN stage 1. The `--search` + tiers from H7a/b are the substrate.
- **H7c IN PROGRESS (2026-06-05/06) — full per-stage run log + the LOCKED next-session plan are in [BALANCE.md](BALANCE.md); READ IT FIRST.** Done so far (all `tests/fuzz/`, zero src change; fuzz smoke 64, main 675 unchanged): **step 0** (`--balance-sweep` harness + medium/heavy tiers + per-archetype/pool-chip/XP telemetry) → **stages 1–3** (broad→narrow→heavy full-length: **band = `difficulty.json` budgetFactor 0.625 × swarmMax 1.75**, both now SET; at full length baselines are 0% so the foregone conclusion is broken) → **step 0.5** (damage-taken telemetry + `--floors` cheap full-length reads + `--report` readable breakdown) → **per-deployment telemetry + `--roster` forced-roster override** (+ `--search` honors `--roster`/`--floors`). **Archetype rebalance WIP** (commits `8eefd76`, `1f7c208`): con-spread narrowed + melee con 30→22 nerf + glass-cannon defense pass + offense-growth trim; **rogue deliberately untouched.** **KEY FINDING — the strategy schema can't reach caster comps:** forced-roster eval (the real lvl-5 start) shows catapult & healer each take a carry comp 75%→**100%**, mage/rogue are net-negative; but the free search NEVER recruits casters because [scored.ts](tests/fuzz/strategies/scored.ts:216)'s `diversity × rosterCount` recruit term is **rich-get-richer** off a **fixed** carry start (PROVEN: a catapult-seeded winning vector replayed on the default roster recruits ~0 catapults / scores 60%). So "the search picks only melee/ranged" is a BOT ARTIFACT, not balance truth. **LOCKED PLAN (user):** (1) **add composition-target support to the strategies** (replace the scalar `diversity` with per-archetype composition targets so the search can naturally seed+stack a comp and MEASURE "swap an archer for a healer → +X%" — NOT lean on forced-roster, which is a diagnostic only) → (2) **re-confirm the broad difficulty sweep** (the band will shift once the search can reach caster comps) → (3) **then tune rogue** (LAST, against a stable baseline). New tooling commands: `npm run fuzz -- --balance-sweep --knob=… --range=min:max:steps [--knob2 --range2] --tier=quick|medium|heavy [--floors=N] [--roster=arch[:lvl],…] [--dry-run]`; `--report[=csv]`; `--search … [--floors] [--roster]` emits `output/best-strategy.json`. output/*.csv + *.report.txt gitignored, reproducible at samplerSeed=1.
- **H7c step 3 (rogue) ✅ (2026-06-07) — per-archetype TARGETING STRATEGIES shipped** (general `targeting` config field + [targetingStrategies.ts](src/sim/targetingStrategies.ts) registry `nearest`/`weakest`, resolved at spawn onto `Unit.targeting`; no snapshot bump). The rogue=`weakest` "backline assassin" idea was **measured + DISPROVED** (forced-roster eval: `weakest` HALVES the range-1 rogue's dmg, 6.1→3.1/dep, and the free search won't recruit it) — root cause is no gap-closer, so all archetypes stay `nearest`, `weakest` ships **dormant+tested**, and the real fix (a dash/leap movement ability) is logged in [TODO.md](TODO.md) + [BALANCE.md](BALANCE.md) step 3. Fuzz baselines unchanged (rogue still effectively `nearest`).
- **H7c — layout-difficulty telemetry ✅ (2026-06-07; tests/fuzz only, zero src):** `npm run fuzz -- --per-layout` (per-layout + layout×floor wave-win/deaths/team-size tables + CSVs) and `--layout=<id>` (force one layout for a clean full sample). **Finding:** chokepoint layouts (corridors/funnel/labyrinth) are easy (~85% player wave-win); open/ambush layouts (`junctionAmbush`, `river`, procedural-open) ~45% — Junction Ambush IS a real difficulty spike but ~procedural-equivalent per-floor (the swarm surrounds you where you can't chokepoint). **User call: leave as-is — the post-Phase-H rework will add map/difficulty gating** (logged in [TODO.md](TODO.md)). Detail in [BALANCE.md](BALANCE.md).
- **H7d ✅ (2026-06-07) — sweep GUI + `--jobs` for `--search` (the H7 tooling closer; tests/fuzz + tools + docs, zero src).** A dev-only command-builder GUI ([tools/sweep-gui/](tools/sweep-gui/)) over the fuzz CLI — pick a balance-sweep grid / search and it emits the `npm run fuzz -- …` line to paste (knob menu enumerated LIVE from `DIFFICULTY`/`HEALTH`/`LEVELING` via [sweepCommand.ts](tests/fuzz/sweepCommand.ts), so it can't drift / offer a rejected knob; +12 fuzz:smoke). **`--jobs` now parallelizes `--search` too** (was balance-sweep-only — the gap that left the overnight verify single-process; vector-level sharding, proven byte-identical jobs=1 vs jobs=4), so the overnight verify is a plain `--search --preset=overnight --jobs=<cores>` (no VPS wrapper needed). Also: `scratch/**` added to eslint ignores. **The overnight verify itself is DEFERRED** (the lone remaining H7 step); a `--seed-offset` for a rigorous config-overfit holdout is logged in [TODO.md](TODO.md). Detail in [BALANCE.md](BALANCE.md).
- **I1 ✅ (Phase I) — the `agility → speed` revert + `precision`/`evasion` dodge stats + canonical stat reorder. FIRST step of Phase I (foregone conclusions & combat identity).** One commit (`feat(I1)`), playtest-confirmed. The stat-layer foundation the rest of Phase I leans on (I2 consumes the dodge stats; I5 tunes them per-subclass). Three changes in one stat-vocab cluster → **one WorldSnapshot bump (v18→v19) + one RunSnapshot bump (v9→v10)** (the GP1/GP2/H1 pattern): (1) reverted GP1's `speed → agility` (everyone read `agility` as "dodge chance"); (2) added `precision`/`evasion` (the H1 "add a stat" plumbing, done twice) — **behavior-NEUTRAL until I2 wires hit/miss in `world.applyDamage`**; (3) reordered to the canonical `CON·STR·RNG·MAG·LCK·DEF·PRC·EVA·SPD·MOB·POW` (combat → dodge → cadence → meta), nudging `DEF` up next to `LCK`.
  - **Full-mirror revert (user call):** kept `mobility` (clean — move cadence, never dodge-confused); renamed the stat `agility → speed` AND the config knobs `agilityCdPerStat/MinCdScale → speedCdPerStat/MinCdScale` ([config/stats.json](config/stats.json) + [stats.ts](src/config/stats.ts) — config-only, no snapshot impact).
  - **Uniform placeholder values (user call):** `precision`/`evasion` ship base **5** / growth **0.20**, identical across all 6 archetypes (mirrors H1's uniform `power`); per-archetype dodge identities deferred to I4/I5 (tuned by feel against the subclass split). So I2 starts from a flat global hit rate.
  - **Reorder broke H1's "append last" discipline — deliberately + safely:** that rule only minimized the level-up draw-shift; I1 re-baselines the fuzz output regardless (two extra per-level `levelupRng` draws), so the mid-block insert + `DEF` move ride the same regen at zero cost. `STAT_KEYS` (draw order) + `STAT_LABELS` (card order) reordered IN SYNC. Snapshots are name-keyed (order-independent) + bumped anyway; the fuzz `scored` strategy + the config-derived tests auto-track `STAT_KEYS`.
  - **Plumbing** (the GP1 mirror + H1 pattern): `UnitStats` ([Unit.ts](src/sim/Unit.ts)) / `ZERO_STATS` + `attackCooldownTicksFor` param+knobs ([stats.ts](src/sim/stats.ts)) / `STAT_KEYS` + both leveling return objects ([leveling.ts](src/sim/leveling.ts)) / the base+growth zod schemas ([archetypes.ts](src/config/archetypes.ts)) / all 6 archetypes ([archetypes.json](config/archetypes.json)) / `STAT_LABELS` (`PRC`/`EVA`, `AGI→SPD`; [statLabels.ts](src/ui/statLabels.ts)) / the 5 `unit.stats.agility` read sites (4 ability strikes + RecruitScreen) / the HUD line (`DEF·MOB·SPD·POW`, [HUD.ts](src/ui/HUD.ts)) / `config/fuzz-strategies.json`. Docs kept honest same-commit: [DESIGN.md](DESIGN.md) stat list, [ARCHITECTURE.md](ARCHITECTURE.md) (v19 + knob/HUD labels), [TODO.md](TODO.md) (AGI→SPD).
  - **Verification:** **694 tests** (was 689; +3 balance-proof `precision`/`evasion`-levels-per-config-growth cases in [leveling.test.ts](src/sim/leveling.test.ts) [derived from `ARCHETYPE_CONFIG`, never hardcoded] + 2 I1 round-trip/stale-reject cases for World v19 AND Run v10 in [snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts); ~16 `UnitStats` fixtures patched, driven off `tsc --noEmit`; the v18 pins in [spawn-overflow.test.ts](tests/integration/spawn-overflow.test.ts) bumped to v19; the GP1 snapshot test's now-dead `agility` assertion repointed to `mobility`). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); build green; fuzz:smoke 89. **Browser-confirmed (DOM-authoritative, 0 console errors):** HUD reads `DEF·MOB·SPD·POW`; recruit card shows all 11 stats incl `PRC`/`EVA` in canonical order; a forced PromotionScene renders `PRC 5→6 (+1)` / `EVA 6→7 (+1)` / `SPD 7→8 (+1)` gain rows. **User-playtested: "looks good."** (No new unit glyph → no `glyphs.ts` entry; screenshot times out — the documented backgrounded-tab rAF throttle, DOM is authoritative.)
  - **Fuzz re-baseline:** the two extra level-up draws shift the default/greedy sweep + recruited rosters (expected — as any stat addition). Regenerate the gitignored `tests/fuzz/output/*`; removed the now-stale `best-strategy.json` (it carried the old `agility` key + would fail `STAT_KEYS` validation). The foregone-conclusion signal itself is unchanged (I1 is behavior-neutral).
- **I2 ✅ (Phase I) — dodge in combat: the hit/miss roll at the `world.applyDamage` chokepoint + the "Miss" hitsplat. SECOND step of Phase I.** One commit (`feat(I2)`). **NO snapshot bump** (shares I1's WorldSnapshot v19 / RunSnapshot v10 — the roll is transient, `combatRng` state already round-trips; only draw COUNTS change). **User decisions (the ROADMAP §I2 *Decision points*):** (1) **subtractive** formula (Fire-Emblem, the user's lean — not the ratio form); (2) **single-target strikes only** are evadable (melee/ranged basic + the rogue gambit) — the **mage AoE blast, the catapult shot, and environmental fire/chasm are UNMISSABLE** (area-denial / artillery / hazards dodged positionally or not at all; the catapult's own docs already framed its counterplay as "kill it mid-charge, not dodge the shot", and per-cell AoE rolls would gut the mage ring — the roadmap's explicit watch-item); (3) **base 0.75** ("decent swing for now") → a flat **25% whiff** at equal stats.
  - **The formula** ([hitChanceFor](src/sim/stats.ts), pure, no RNG): `clamp(hitChanceBase + precision·hitChancePerPrecision − evasion·dodgeChancePerEvasion, hitChanceFloor, hitChanceCap)`. Five new [config/stats.json](config/stats.json) knobs: base **0.75**, perPrecision/perEvasion **0.02** each, floor **0.10**, cap **1.0**. **With I1's uniform `precision == evasion == 5` the prc/eva terms cancel → every unit sits at `hitChanceBase` (0.75) today**; the real dodge differentiation only switches on once I5 spreads subclass precision/evasion. The floor is the whiff-analogue of GP2's `minDamage` (a low-precision attacker is never fully shut out by a high-evasion target).
  - **The chokepoint wiring** ([World.applyDamage](src/sim/World.ts)): a new `opts.evadable` opts a call into the roll. The caller has ALREADY drawn the crit roll (in its `start`/`applyEffect`), so the `combatRng` draw order is fixed at **crit → miss** (a missed strike just discards the pre-rolled crit). On a miss: deal **0**, emit the new **`unit:missed {attackerId, targetId}`** event, skip the HP / `recordDamage` / `unit:attacked` path, return before mitigation. `evadable` defaults **false**, so all four AoE/direct-`applyDamage` call sites + the GP2 tests are untouched unless they opt in: [AttackAction](src/sim/actions/AttackAction.ts) + [GambitStrikeAction](src/sim/actions/GambitStrikeAction.ts) pass `evadable:true`; [MagicBoltAction](src/sim/actions/MagicBoltAction.ts) + [CatapultShotAction](src/sim/actions/CatapultShotAction.ts) pass `evadable:false` explicitly (self-documenting). Env fire/chasm never routes through `applyDamage` at all (the GP2 carve-out), so it's unmissable for free.
  - **Render** (the E3.6 hitsplat channel): [BattleRenderer](src/render/BattleRenderer.ts) `onUnitMissed` plays the SAME `triggerAttackVisual` lunge/tracer as a hit (the attacker still swung/shot) then floats a desaturated **"Miss"** instead of a number; new `'miss'` hitsplat kind threaded through [UnitOverlayLayer](src/render/UnitOverlayLayer.ts) + a `.hitsplat--miss` rule ([ui.css](src/ui/ui.css) — steel-grey, italic, a hair smaller, so a whiff reads as a non-event). [BattleScene](src/scenes/BattleScene.ts) plays the swing sound on a miss too (the swing happened). No `glyphs.ts` entry (DOM text).
  - **Verification:** **706 tests** (was 694; +6 `hitChanceFor` balance-proofs in [stats.test.ts](src/sim/stats.test.ts) [cancel-at-equal / formula-vs-config / floor+cap clamp / monotonic, all off `STATS.*`] + 6 World miss-path cases in [World.test.ts](src/sim/World.test.ts) [miss→0+no-HP+no-ledger+`unit:missed`; cap→always-hit; exactly-one-draw; non-evadable unmissable+zero-draw; per-seed determinism; **the crit→miss order pinned** via a seed where the two draws straddle the crit threshold so a reversed order would flip the observed crit flag]). The **2 pre-existing `AbilityBehavior.test.ts` cadence/cooldown tests** that broke (a strike whiffed → exact attack-count off by one) fixed by **capping the attacker's precision** in `buildStats` so the hit chance parks at the cap (isolates cadence from the dodge roll, exactly as that helper already zeroes `defense` to isolate from GP2 mitigation); same one-line cap added to [AttackAction.test.ts](src/sim/actions/AttackAction.test.ts)'s damage-resolution `STATS_BLOCK`. Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **fuzz:smoke 89.** **Browser-verified (DOM-authoritative, 0 console errors):** drove a live battle via `__game` (manual `world.tick()` past the backgrounded-tab rAF throttle) — first contact tick 52, then **6 hits + 2 misses ≈ 25% whiff** (matches base 0.75 at uniform stats), `unit:missed` carries `{attackerId,targetId}`, 2 live `.hitsplat--miss` DOM nodes read **"Miss"**. **The Miss hitsplat look/timing + the 75% rate itself are the user's playtest call** (all knobs are config + a CSS rule).
  - **I2 playtest follow-up (render, 2 commits — both PRE-EXISTING bugs surfaced by staring at the new whiffs, not I2 regressions):** (1) **glyph atlas baked serif on a cold load** — [FontAtlas.create](src/render/FontAtlas.ts) awaited only `document.fonts.ready`, which settles fonts *already* loading but doesn't *trigger* the `@fontsource` JetBrains Mono fetch; if nothing used the font first, the atlas rasterized the serif fallback (visible in a clean browser without the font as a system install). Fix: `await document.fonts.load(\`${FONT_PX}px '${FONT_FAMILY}'\`)` BEFORE `ready` (forces the fetch; resolves empty + never throws if undeclared). Browser-verified: JB-Mono loads + rasters distinct from serif (690 vs 814 covered px). (2) **hitsplats drifted sideways for off-axis units** — [UnitOverlayLayer.spawnHitsplat](src/render/UnitOverlayLayer.ts) anchored at `project(center + worldUp·0.5)`, but under the `PerspectiveCamera` a WORLD-up offset projects diagonally toward the vanishing point for side sprites (the sprites are screen-aligned billboards, so their on-screen top is straight up from center). Fix: take **X from the projected center, only the vertical pixel delta from the lifted point** (kept perspective-correct). Browser-verified: `deltaX == 0` for units ~194px off-axis (was non-zero). (3) **the "Miss" splat now reads louder** (user ask) — [ui.css](src/ui/ui.css) `.hitsplat--miss` → bright cool-white #eaf2ff / 20px / weight 800 / italic / blue glow (distinct from white-damage/red-crit/cyan-heal/amber-burn), replacing the deliberately-muted grey. All three: 706 tests still green, 0 console errors; the Miss look stays the user's eyeball (CSS, hot-reloads). **The user then tuned `hitChanceBase` 0.75 → 0.6 (40% whiff, more swing) and committed it (`36b3648`) — so the LIVE base is 0.6, not the 0.75 this entry's I2 commit shipped.** A beginner-friendly CSS guide for restyling the hitsplats (base + per-kind rules, colour primer, recipes) lives at [src/ui/HITSPLATS.md](src/ui/HITSPLATS.md), linked from a pointer comment above the `.hitsplat` block in [ui.css](src/ui/ui.css).
  - **Fuzz re-baseline + a hang FINDING (flagged for the user):** the extra `combatRng` draw per evadable strike + the misses re-baseline the gitignored `tests/fuzz/output/*` (regenerate). **The 25% whiff lengthens battles ~33%, so the weak-bot hang count rose `1→5` (pure-random) / `1→7` per 50 seeds** — battles hitting the fuzz harness's **100s decisive-wipe cap** (`DEFAULT_MAX_TICKS = secondsToTicks(100)`), concentrated on the known-slow corridor/fire layouts (strafingFunnel/endlessCorridors/spiralFireLife). **These are NOT freezes:** the miss path can't deadlock (a miss deals 0 and the unit re-proposes next cadence), and IN-GAME the same un-wiped turn resolves as a **turn-cap draw** via `Run.resolveAsDraw` (the harness drives the World directly without that cap, so it labels them hangs). Expected at a flat 25% whiff; the real re-tune is **Phase N's N2 band re-sweep** (the roadmap already sequences the sweep after all combat-structural changes), and **I3 fast-forward** is the dead-time antidote for the added length. Confirmed an A/B: stash→pre-I2 sweep gave `1`/`1` hangs, post-I2 `5`/`7`. **RESOLVED (user call) — bumped BOTH turn caps `100 → 150s`:** the in-game `maxTurnSeconds` ([config/health.json](config/health.json) → `Run.resolveAsDraw`) AND the fuzz harness `DEFAULT_MAX_TICKS` ([tests/fuzz/harness.ts](tests/fuzz/harness.ts)), in lockstep, so dodge-era battles get room to resolve before the draw/hang fallback. (The user hadn't realized an in-game cap existed separate from the harness one — there are TWO independent constants today.) At the user's **0.6** base (40% whiff): pure-random hangs `5→0`, greedy `7→3` (the residual 3 are genuine deep-stalls on the slow corridor/fire layouts; greedy now even wins ~6% — the swing makes upsets possible). **Unifying the two caps onto one source (+ optionally having the harness `resolveAsDraw` at the cap instead of hanging) is logged as a Phase-N cleanup** ([ROADMAP.md](ROADMAP.md) §N2).
- **I3 ✅ (Phase I) — fast-forward (1× / 2× / 3×): a tick-batching multiplier on the fixed-timestep loop + a HUD button + the `F` hotkey. THIRD step of Phase I.** One commit (`feat(I3)`). The direct antidote to the low-unit dead-time (and the slightly-longer battles I2's whiffs introduce). **Presentation-only — NO snapshot bump, NO fuzz impact** (the sim is byte-identical; only how many ticks advance per rAF frame changes — the fuzz harness drives `World` directly and never sees `BattleScene`).
  - **The mechanism** ([BattleScene.tick](src/scenes/BattleScene.ts)): scale the real frame `dt` by the active speed and feed the scaled value to EVERYTHING in the battle — the sim `Clock`, the `BattleRenderer` animations, AND the terrain shader's `uTime` — so the whole scene stays visually coherent at 2×/3× (units, projectiles, hitsplats, fades, and fire-flicker all speed up together). **Determinism preserved:** the `Clock` still fires whole fixed-timestep ticks; scaling `dt` only changes how many `world.tick()`s land per frame, not the tick sequence or its RNG draws (a tick-batch, NOT a `TICK_RATE` change). Read live each tick so a mid-battle cycle takes effect on the next frame.
  - **Config** ([config/playback.json](config/playback.json) + [src/config/playback.ts](src/config/playback.ts), A4 two-layer zod): `speeds: [1,2,3]` (the cycle list; zod `.refine` pins the first step to **1** = normal play is the cycle's home + wrap target) + `hotkey: "KeyF"`. **Dropping the trailing `3` is exactly how you cap the ceiling at 2×** if a future board can't keep up (the §I3 perf watch-item).
  - **The controller** ([PlaybackSpeed.ts](src/ui/PlaybackSpeed.ts) + [PlaybackSpeed.test.ts](src/ui/PlaybackSpeed.test.ts)): a tiny page-lifetime holder (`current`/`cycle()`/`label`/`hotkey`) owned by `Game`, threaded through `SceneContext`. **Persists across turns/battles (user call — "keep the chosen speed"):** set 2×/3× once and every later battle keeps it; the *control surface* (button + hotkey) is rebuilt per battle by the HUD, but the *value* lives on the controller. Steps injectable so the mechanic test pins explicit literals (never the shipped JSON).
  - **The HUD control** ([HUD.ts](src/ui/HUD.ts) + [ui.css](src/ui/ui.css)): a `.hud-speed` button under the floor label — a chevron run mirroring the multiplier (`▶`/`▶▶`/`▶▶▶`) + the explicit `N×`, title "Fast-forward (F)", `aria-pressed` flagging any >1× speed → an "engaged" GREEN-glow CSS state (amber at 1×). A window `keydown` listener (the `F` hotkey) torn down in `dispose` so it doesn't accumulate across battles; the value survives on the page-lifetime controller. The listener guards `e.repeat` (held key = one step) and only the configured `code` fires.
  - **User decisions (asked upfront):** (1) **hotkey = `F`** — config-backed (`KeyboardEvent.code`) so **Phase J's rebindable-hotkey layer (§J3) can absorb it by relocating one config field**, no abstraction built speculatively now (the user flagged J's rebindable hotkeys; this is the non-speculative forward-compat); (2) **persist the chosen speed across battles** (set-and-forget).
  - **Perf profile (the ROADMAP §I3 watch-item — "profile pathing at 3× on a big board before committing to 3×"):** browser-measured single-`world.tick()` cost mid-fight via `__game` — **17×17 / 25 units = 0.092 ms/tick; 8×32 / 112 units (mostly walls) / 12 live combatants = 0.236 ms/tick** (the worst observed). At 3× the loop drives **~1 tick per 60 fps frame** (~60 ticks/s), so that's **≈0.24 ms of sim per 16.7 ms frame ≈ 1.4% of budget**, render cost unchanged from 1×. **3× is comfortably safe — kept `[1,2,3]`.** Pathing is far too cheap for the "2× ceiling" concern to bite today; **re-check after Phase J adds objective-thrash path recompute** (the roadmap already sequences this).
  - **Verification:** **711 tests** (was 706; +5 `PlaybackSpeed` cycle/wrap/label/custom-ceiling in [PlaybackSpeed.test.ts](src/ui/PlaybackSpeed.test.ts), explicit literals). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); **fuzz:smoke 89 unchanged** (confirms zero sim impact). **Browser-verified (DOM + CSSOM + computed-style authoritative, 0 console errors across a full autopiloted run):** the button cycles 1→2→3→wrap on click AND on `F`, the `e.repeat` guard holds (held key = one step), a non-configured key is a no-op; the engaged-green styling confirmed via the CSSOM rule + a transition-snapped `getComputedStyle` (color/border/glow `rgb(51,255,0)` at >1×, amber at 1× — the 120 ms `transition` defeats a naive synchronous read); **`dt`-scaling proven exact — 80/160/240 `Clock`-fired ticks for 1×/2×/3× over the same 4.0 s frame, under the 250-tick catchup cap; per-tick cost as above. Screenshot captured the green `▶▶ 2×` button in a live Floor-1 battle. **User-playtested 2026-06-08: "looks good."** The subjective feel — speed steps, button placement/copy, the faster fire-flicker at 3× — stayed the user's call (all knobs are config + a CSS rule; the terrain-time scaling is one trivially-reversible line).
- **I4 ✅ (Phase I) — schema-driven archetype editor + a dev-only config-save endpoint. FOURTH step of Phase I.** One commit (`feat(I4)` `2d27f73`) + a separate `balance` commit (`9441f5c` — the user's rogue tuning, authored *through* the new editor; see below). **Dev-tooling only — NO sim/snapshot/fuzz behavior change** (the lone `src` touch is an `export` of `ArchetypeSchema`/`ArchetypesSchema` in [archetypes.ts](src/config/archetypes.ts) so the editor validates against the real schema; nothing reads those exports at runtime). Pulled into Phase I to make I5's by-feel subclass tuning painless (co-located with the work it serves — the project ethos).
  - **The editor** ([tools/archetype-editor/](tools/archetype-editor/), Vite-served at `/tools/archetype-editor/`, never in `dist/` — same posture as the layout editor): edits the six archetypes' glyph / abilities / targeting / `baseStats` / `growthRates`. **Schema-driven** — stat fields enumerate from `STAT_LABELS`, abilities/targeting from the live registries (`knownAbilityIds`/`knownTargetingIds`), so a future stat/ability surfaces with no editor edit. **Live validation** re-runs the real `ArchetypesSchema` on every edit (Save disabled while invalid, zod paths shown). **Live derived preview** computed by the REAL game functions (`deriveStats`/`hitChanceFor`/`attackCooldownTicksFor`/`scaleStats`/`damageStatFor`) — never reimplemented — with a **level dial** (deterministic `scaleStats` growth preview) + **vs-PRC/vs-EVA reference inputs** for tuning the dodge identities by feel. **Scope = the six EXISTING archetypes**; a brand-new archetype still needs code (the closed `Archetype` union + glyph + ability factories), after which the new key auto-appears here.
  - **The save endpoint** ([vite.config.ts](vite.config.ts) `configSavePlugin`): a dev-only Vite middleware `POST /__save-config {file, content}` that writes `config/<file>` — **filename ALLOWLIST** (`archetypes.json`, `layouts.json`) + a path-escape guard, `apply:'serve'` so it never runs during `vite build`. **Built generically so M5's layout-editor auto-edit is now just client wiring + a Save button, no new server.** The editor's Copy/Download remain offline fallbacks.
  - **Byte-faithful formatter** ([tools/archetype-editor/format.ts](tools/archetype-editor/format.ts), pure + node-safe): emits `config/archetypes.json` matching the hand-authored layout (2-space, `abilities` inline, stats in `STAT_LABELS` order). Pinned by [tests/tools/archetype-editor.test.ts](tests/tools/archetype-editor.test.ts) — re-emitting the committed config reproduces the file (line-ending-normalized) + round-trips through `ArchetypesSchema`. **Proven live:** an edited-stat Save wrote a clean ONE-LINE git diff.
  - **Toolchain:** `vite.config.ts`'s `writeFile` needs Node types, but the project scoped tsconfig `types` to `["vite/client"]` (no `@types/node`, and a `node:` module can't be ambiently shimmed — TS2664). Added **`@types/node`** (devDep) + `"node"` to tsconfig `types` — the idiomatic Vite-config setup, consistent with [eslint.config.js](eslint.config.js) which already includes `globals.node`.
  - **Verification:** **713 tests** (was 711; +2 formatter fidelity/round-trip); typecheck + lint clean (lone pre-existing `Renderer.ts` warning); **fuzz:smoke 89 unchanged** (zero sim impact). **Browser DOM-verified, 0 console errors:** 6 archetype tabs; melee/catapult derived numbers match config exactly (maxHp, crit, to-hit at the live 0.6 base, catapult mobility −7 slow move, level-10 growth via `scaleStats`); invalid edits surface zod errors + disable Save; the real Save button wrote `config/archetypes.json` to disk (clean one-line diff), then restored. (Screenshot times out on this page — the documented preview quirk; DOM is authoritative + the look is the user's eyeball, confirmed "looks amazing".) **CSS follow-up** widened the stat inputs (`5ch → 4.5rem`) so the value isn't hidden behind the native number spinner (user-flagged, fixed, DOM-measured).
  - **Rogue tuning — separate `balance` commit `9441f5c` (NOT part of the I4 tooling):** the user dodge-tank-tuned the rogue THROUGH the new editor — luck 10→12, evasion 5→7; growth luck 0.6→0.7, precision 0.2→0.45, evasion 0.2→0.5 — the first pass at I5's dodge-tank direction, committed on its own. **To be formally re-measured with the H7 tools in I5.** Shifts the gitignored fuzz output baselines (regenerate `tests/fuzz/output/*`). Config-derived tests stayed green (713) — no hardcoded rogue values.
- **I5 commit 1/3 ✅ (Phase I) — the melee subclass FAMILY: `melee → mercenary` rename + Adventurer/Ronin/Bandit added. FIFTH step of Phase I (a DESIGN ROUND — stat blocks locked with the user before authoring).** One commit (`feat(I5)`). The single `melee` archetype split into four melee subclasses now that I1/I2's dodge stats can differentiate them (the brief's "one melee class forced player/enemy symmetry"). **Player-side additions only — enemy difficulty UNCHANGED this commit** (the enemy melee slot is the *renamed* `mercenary`, byte-identical stats; the Bandit-as-enemy swap is commit 2/3, the rogue re-measure is commit 3/3).
  - **Design round (decisions locked with the user):** (1) **rename `melee → mercenary`** everywhere (key==concept) over a display-label layer — costs a snapshot bump but cleaner for a 4-member family, + a label layer is now a [TODO.md](TODO.md) item (the general "decouple shown name from key" idea); (2) **Bandit recruitable** (auto-joins the `ALL_ARCHETYPES` recruit pool — no pool split); (3) **bold stat extremes** (subclasses feel distinct + give the dodge/crit systems signal); (4) **3 staged commits**, playtest pause between each.
  - **The four identities (axis split; stat VALUES are tuned by feel in the I4 editor, deliberately NOT pinned in tests):** **Mercenary** (`M`, = the old melee, UNCHANGED) = armor anchor (CON 22 / DEF 4); **Adventurer** (`A`) = durable dodge (DEF 2 / EVA 11 + EVA growth) — beats armor vs hard hitters, worse vs floor-1 chip, fully exposed to AoE (mage/catapult unmissable); **Ronin** (`R`) = crit duelist (LCK 14 base / 0.8 growth → ~14%+ crit climbing, +1 STR/PRC) — high-variance, NOT evasive (stays out of Adventurer's lane); **Bandit** (`B`) = fodder (Mercenary base, ~half growth) → floor-1 enemies byte-identical, weaker-scaling at depth. Rogue stays the fragile evasion+luck+speed `gambit_strike` skirmisher (distinct ability + CON 16).
  - **Plumbing:** `Archetype` union ([Unit.ts](src/sim/Unit.ts)) + `ArchetypesSchema` z.object ([archetypes.ts](src/config/archetypes.ts) — key order == JSON order so the editor formatter round-trips) + [archetypes.json](config/archetypes.json) (9 entries, melee family first) + `damageStatFor` switch ([stats.ts](src/sim/stats.ts) — the family all strike on `strength`) + [glyphs.ts](src/render/glyphs.ts) (`A`/`R`/`B` appended — **atlas now 31/32, one cell left; the NEXT glyph needs a grid resize**, gotcha #33) + [config/fuzz-strategies.json](config/fuzz-strategies.json) (archetype + composition blocks) + the editor `OUTPUT_LABEL` ([editor.ts](tools/archetype-editor/editor.ts)). **Two FALSE renames the bulk `'melee'→'mercenary'` script caught + I reverted:** the `melee` *sound effect* (`audio/melee.wav` — `SoundKey` + the two [BattleScene](src/scenes/BattleScene.ts) swing-sound calls; the swing SFX, NOT the class) and two version-history *comments* (old saves carry the old `'melee'` key).
  - **Snapshot bump — WorldSnapshot v19→v20 + RunSnapshot v10→v11** (the SECOND Phase-I bump, justified differently: an archetype-KEY rename, not a stat-shape change — a v19/v10 save carries `'melee'`-tagged units that no longer resolve to a config → crash on rehydrate, so reject outright). The version-relative stale tests ([snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts) use `schemaVersion − 1`) auto-adapt; the hardcoded v19 pin in [spawn-overflow.test.ts](tests/integration/spawn-overflow.test.ts) bumped to v20.
  - **Auto-coverage:** 8 config-derived test files iterate `ALL_ARCHETYPES` (leveling balance-proof, recruitment pool, glyph coverage, fuzz menu) → the new subclasses are exercised automatically (source of the +3 main-test count). Added a focused I5 block ([archetypes.test.ts](src/sim/archetypes.test.ts)) pinning the STRUCTURAL melee-family contract (each carries `melee_strike` @ range 1, strikes on `strength`, distinct `M`/`A`/`R`/`B` glyphs) + an all-archetype baseStats round-trip — NOT stat values (by-feel, per BALANCE.md).
  - **Verification:** **719 tests** (was 713, +6: 3 from config-derived `ALL_ARCHETYPES` loops auto-growing + 3 from the new I5 `archetypes.test.ts` block); typecheck clean; lint 0 errors (lone pre-existing `Renderer.ts` warning); **fuzz:smoke 89** (the `fuzz-strategies.json` round-trip validates the 9-archetype vector). **Browser-verified** (DOM-authoritative, 0 console errors): `?roster=adventurer,ronin,bandit,mercenary,ranged` → drove map → turn-intro → battle; all five fielded with the correct glyphs (`adventurer→A`, `ronin→R`, `bandit→B`, `mercenary→M`, `ranged→a`), enemy = `mercenary` (commit-1 correct), 40 manual ticks clean, only the expected dev roster-override warning. Screenshot times out (documented backgrounded-tab rAF throttle — DOM authoritative; the subjective glyph/card look is the user's playtest).
  - **Fuzz re-baseline:** 3 new recruitable archetypes shift the gitignored `tests/fuzz/output/*` (regenerate). The committed `config/fuzz-strategies.json` is the all-zero neutral vector (re-validated). The difficulty band itself doesn't move yet (enemy unchanged — that's commit 2's Bandit swap + Phase N's re-sweep).
  - **Commits 2/3 pending (playtest pause first):** (2) swap the enemy melee `mercenary → bandit` in [rollEnemyWave](src/run/enemyBudget.ts) (deep-floor easing, isolated for the difficulty-feel playtest); (3) lock the rogue dodge-tank block + run the H7 forced-roster eval + free `--search` → answer the rogue decision (dodge alone viable? → Phase N mobility optional) and flag it to the user before Phase N.
 - **Next up** — **Phase G COMPLETE (G1 ✅ G2 ✅ G3 ✅ G4 ✅ G5 ✅).** A post-G5 playtest surfaced a 7-item feedback cluster (movement/attack-speed legibility, healer + ranged AI, the missing defense stat), synthesized into a **new Phase GP** ("Phase-G playtest response") in [ROADMAP.md](ROADMAP.md) §Phase GP — sequenced **before Phase H**. **Do GP first, then H.** **Phase GP** (5 commits, playtest pause between each): **GP1 `endurance→mobility` + `speed→agility` rename + universal move base + split `cdPerStat` into per-axis `mobilityCdPerStat`/`agilityCdPerStat` (WorldSnapshot bump) → GP2 defense stat + `world.applyDamage` damage-site consolidation (subtractive `max(1, raw−def)`, env damage unmitigated; WorldSnapshot bump) → GP3 card legibility overhaul (ability list + driving stats shown directly + DEF row; render-only) → GP4 ranged firing-position pathing (path to nearest in-range+LOS cell, fallback to target-cell; no snapshot bump) → GP5 healer navigable-snap + yield rule (no snapshot bump).** **User decisions LOCKED this round:** subtractive defense (NOT diminishing-returns); `speed→agility`; healer #5 fix = **yield rule** (rear-bias rejected — healers spawn at the *front* in labyrinth/corridor layouts, also partly a maps problem); per-axis cooldown-rate split (recommend splitting `minCdScale` too). **GP1 ✅ GP2 ✅ GP3 ✅ GP4 ✅ shipped** (GP1: the `endurance→mobility`/`speed→agility` rename + per-axis cooldown-knob split + universal move base, WorldSnapshot v16, user-playtested feel-preserving; GP2: `defense` stat + `world.applyDamage` damage-site consolidation, subtractive `max(1, raw−def)`, env damage UNMITIGATED, WorldSnapshot v17, melee-tanky values — **fuzz win 25%→90%, proper balance sweep DEFERRED by the user**; GP3: card legibility overhaul — recruit card → raw 8-stat block + per-ability list, HUD → `DEF·MOB·AGI` line, render-only, no snapshot bump, playtester pivoted the card's top block to raw stats; **GP4: acting-position pathing — ranged + catapult path to the nearest cell they can fire from via `nearestActingCell`, fallback to charging when no firing cell is reachable OR the step to one is blocked; healer deferred to GP5; 596 tests; 1/40 strafingFunnel hang remains, a pre-existing healer deadlock for GP5**). **Phase GP COMPLETE (GP1 ✅ GP2 ✅ GP3 ✅ GP4 ✅ GP5 ✅).** GP5 shipped as **GP5.1** (the #5 yield rule, but as a **`SwapAction` pass mechanic** rather than the planned "step aside" — the 1-wide layouts have no cell to step to; cleared the seed-30 hang) + 2 playtest follow-ups (live-position swap lerp; `unit:swapped` event) + **GP5.2** (the #4 centroid navigable-snap) — see the GP5.1/GP5.2 entries above. **Phase H IN PROGRESS — H1 ✅ + H2 ✅ + H3 ✅ + H4 ✅ shipped (the `power` stat; spawn-tile range; deployment counter; the encounter loop [H4a headless] + the pre/post-turn screens & pool HUD [H4b render] — see the H1–H4b entries above); **H5 ✅ shipped** (H5 mechanics = card-drawn hand + the `playerTeamLevel` seam swap, headless; H5b = the pre-turn hand display — see the H5/H5b entries); **H6 ✅ COMPLETE 2026-06-05 (rest-heal + pass option + inert spawn-time fatigue hook — see the H6 entry above); **H7a ✅ + H7b ✅ shipped** (the linear scored strategy + `config/fuzz-strategies.json`; the random-search driver `--search`), **H7c ✅ + H7d ✅** (difficulty band SET budgetFactor 0.625 × swarmMax 1.75 + the archetype rebalance/rogue-targeting/telemetry passes; the sweep-GUI command-builder + `--jobs`-for-`--search`) — **PHASE H IS NOW EFFECTIVELY COMPLETE (user call, 2026-06-07): H1–H7 all ✅.** The stage-5 overnight verify + the leveling pass are NOT dropped but deliberately RESEQUENCED behind the next focus — **the rogue salvage** (a mobility / gap-closer ability; [TODO.md](TODO.md) "Movement abilities (dash / gap-closer)"). The rogue rework is a structural combat change that will move the difficulty band + XP flow, so verifying/leveling against a baseline we're about to disturb is wasted — measure both against the stable post-rogue config (the H7c "tune against a stable baseline" rule that sequenced rogue LAST). **Post-rogue order:** rework rogue → re-sweep the band (the sweep GUI / `--jobs` search are ready) → leveling pass → overnight verify (ideally after the `--seed-offset` add for a strict config-overfit holdout). Phase H (the deckbuilder battle trial — biggest + most experimental, design-locked to sub-step level): **H1 power stat ✅ → H2 randomized spawn tiles ✅ → H3 deployment counter (fatigue hook) ✅ → H4 health pools + turn/encounter loop (H4a ✅ headless + H4b ✅ render) → H5 card-drawn hand + `playerTeamLevel` seam swap (mechanics ✅ + H5b pre-turn hand display ✅) → H6 recruit pass + rest-pool-heal + balance sweep** (H6 also folds in the deferred `power`-stat / pass-option fuzz strategies + the health-pool launcher knobs + the full long-run balance sweep). G4 shipped the `playerTeamLevel()` **seam** H5 swaps (live in [enemyBudget.ts](src/run/enemyBudget.ts)); **GP2's "add a stat" plumbing + GP3's card overhaul are what H1/H6 build on** (GP de-risks H). **Hard prereqs already met:** G4 (seam) + G3 (rest nodes) + G1 (short-run harness). Full Phase H detail in [ROADMAP.md](ROADMAP.md) §Phase H. **Difficulty re-tune expected after GP+H** — the user kept presets A/C in [difficulty.ts](src/config/difficulty.ts) precisely because the dumb fuzz bots understate human winnability; revisit once defense + smarter strategies + the turn loop land.
- **Tests:** 719 passed (I5 commit-1 melee subclass family — `melee→mercenary` + Adventurer/Ronin/Bandit, +6 over I4: 3 from config-derived `ALL_ARCHETYPES` loop growth + 3 new `archetypes.test.ts` cases; 713 at I4 archetype-editor formatter fidelity/round-trip; 711 at I3 fast-forward `PlaybackSpeed` cycle; 706 at I2 dodge hit/miss roll; 694 at I1 dodge-stat plumbing, 689 at H7c targeting strategies, 675 at H6, 665 at H5b, 664 at H5, 654 at H4b, 628 pre-H4), 0 `it.todo()`. Run with `npm test`. Fuzz smoke (separate suite, opt-in): 89 pass (grew through the H7c tooling + H7d's +12 `sweepCommand.test.ts`) — `npm run fuzz:smoke`. **Fuzz sweep (`npm run fuzz`): post-H5 pure-random + greedy = 100% win / avg floor 10 / 0 hangs (unchanged from post-H4a — the no-attrition pools, not the budget, drive the foregone conclusion; the seam swap shrinking the budget for grown rosters + the smaller drawn hand roughly cancel for win rate). This stays the H7 balance-sweep signal (H6c shipped the *inert* spawn-time fatigue hook; H6 left the fuzz baselines unchanged — turning fatigue on is H7's data-driven call).** H7a added the `scored` strategy (`npm run fuzz -- --strategy=scored`, or `--strategy=<file>.json` for any weight vector); H7b's `npm run fuzz -- --search` random-searches its weights for the best-achievable win rate (quick/overnight presets; emits `output/best-strategy.json`). At the current (unbalanced) config the best discoverable play is still **100%** — that's H7c's signal to tune against. Full strategy menu: `npm run fuzz -- --strategy=all` — **one pre-existing residual: `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, NOT a positioning deadlock — out of GP5 scope; see the GP5.1 entry).** Per-floor balance analysis: `npm run fuzz -- --per-floor`. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`.
- **Dev server:** not running. Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent).
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**Phase H is COMPLETE (H1–H7 all ✅, 2026-06-07)** — the multi-turn "deckbuilder" trial: `power` stat, spawn-tile range, deployment counter, the health-pool encounter loop (H4a headless + H4b screens/HUD), the card-drawn hand + `playerTeamLevel` seam swap (H5/H5b), recruit-pass + rest-heal + an inert fatigue hook (H6), and the H7 search-driven balance tooling + difficulty/archetype sweep (H7a scored DSL → H7b `--search` → H7c the sweep + band-set `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance + per-layout/floor telemetry → H7d the sweep GUI + `--jobs`-for-`--search`).

**The Phase-H playtest feedback is now synthesized into a fresh [ROADMAP.md](ROADMAP.md) — "Post-H" (Phases I → N); the brief is archived at [archive/phase-h-feedback.md](archive/phase-h-feedback.md) and the prior roadmap at [archive/post-f-roadmap.md](archive/post-f-roadmap.md).** **Phase I IN PROGRESS (foregone conclusions & combat identity) — I1 ✅** (the `agility→speed` revert + `precision`/`evasion` mid-block adds + canonical reorder `CON·STR·RNG·MAG·LCK·DEF·PRC·EVA·SPD·MOB·POW`; WorldSnapshot v19 + RunSnapshot v10; fuzz re-baselined — see the I1 entry above). **I2 ✅** (the Fire-Emblem subtractive hit/miss roll at `world.applyDamage`, single-target strikes only [mage AoE / catapult / env unmissable]; the `unit:missed` event + "Miss" hitsplat; NO further snapshot bump — see the I2 entry above. **`hitChanceBase` shipped at 0.75 but the user tuned it to `0.6` live [commit `36b3648`, 40% whiff] — the current base is 0.6.** Two pre-existing render bugs were also fixed in the playtest follow-up [glyph-atlas serif race; off-axis hitsplat drift] + a louder Miss splat + [HITSPLATS.md](src/ui/HITSPLATS.md) styling guide. The fuzz hang FINDING [misses lengthen battles] was **RESOLVED by bumping both turn caps 100→150s**; at 0.6 the hangs drop pure-random 5→0 / greedy 7→3, and cap-unification is logged for N2). **I3 ✅** (fast-forward 1×/2×/3× — a tick-batching `dt`-multiplier on the fixed-timestep loop [`BattleScene.tick` scales `dt` for the `Clock` + renderer + terrain], NOT a `TICK_RATE` change so the sim is byte-identical [no snapshot/fuzz impact]; HUD button + `F` hotkey [config-backed in `config/playback.json` so J3's rebindable layer can absorb it]; the chosen speed **persists across battles**; profiled **≤0.236 ms/tick on big boards → 3× is safe, kept `[1,2,3]`** [re-check after J's objective-thrash path recompute] — see the I3 entry above). **I4 ✅** (commit `feat(I4)` `2d27f73`): the schema-driven archetype editor (`tools/archetype-editor/`) — live `ArchetypesSchema` validation + live derived-stat preview + a save-to-disk path via a new dev-only Vite `/__save-config` endpoint that **M5 reuses for the layout editor**; dev-tooling only, no sim/snapshot/fuzz change — see the I4 entry above. + a separate `balance` commit (`9441f5c`) for the user's editor-authored rogue dodge-tank tuning (re-measured in I5). **I5 IN PROGRESS (DESIGN ROUND done, staged 3 commits):** commit **1/3 ✅** — the melee subclass family (`melee→mercenary` rename + Adventurer/Ronin/Bandit, all recruitable; WorldSnapshot v20 + RunSnapshot v11; enemy difficulty unchanged this commit — see the I5 commit-1 entry above). **Next = commit 2/3** (swap the enemy melee `mercenary→bandit` in `rollEnemyWave` — the deep-floor easing, isolated for the difficulty-feel playtest) **then 3/3** (lock the rogue dodge-tank block + the H7 forced-roster eval + free `--search` → the rogue decision, flagged to the user before Phase N). Then **J** in-battle objective system (the pathing overhaul), **K** status-effect system + redraw/empower, **L** daemons, **M** per-turn leveling + scene/map polish (+ the layout auto-editor reusing I4's `/__save-config` endpoint), **N** rogue mobility [contingent] + balance closure. **Vocab note:** the brief's *skirmish* = our **turn**, its *battle* = our **encounter** (mapped in the roadmap). **The rogue salvage (mobility) is resequenced to Phase N and made contingent** — dodge is the interim fix (Phase I re-measure), and the mobility rework now builds on Phase J's pathing seam; the stage-5 overnight verify + the leveling-rate pass stay last (Phase N), now behind more band-movers. **READ [BALANCE.md](BALANCE.md) FIRST** for any balance work (the authoritative H7c run log + funnel); forward steps in [ROADMAP.md](ROADMAP.md), smaller follow-ups in [TODO.md](TODO.md), gameplay backlog (shop, synergies, boss variety) under [DESIGN.md](DESIGN.md) "Out of scope".

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

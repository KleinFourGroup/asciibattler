# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

> ## ✅ Phases U + V — COMPLETE (prior sessions; condensed — full one-liners in "Current state" §Phase U/§Phase V + git log + ROADMAP). **U** retired `rollEnemyWave` from `Run` for the authored **Encounter** model (U1 `89957c0` `resolveWave` budget/count/weight, per-instance level weighting [USER-LOCKED]; U2 `010d35e` the `waveForTurn` grammar `wave|pick|loop{N|forever}|condition-gated stages`, last-wave-repeats [USER-LOCKED]; U3a `2b4f733` the zod Encounter schema `kind` enum + `rewards?` seam; U3b `252b7ea` Run wiring, **RunSnapshot v20→v21**, per-encounter `enemyHealthPoolMax`, `encounter.name`→HUD). **V** opened it to a catalog: V0 `9b8d818` the **sector-owns-both** flip (sector owns a hop-gated/weighted `encounters` pool); V1 `498745d`+`c647ebf` the keyed `selectEncounter` resolver (`encounterFirst`|`layoutFirst` via `config/selection.json`) + **Brigands**/**Highwaymen**/**Deserters**, **RunSnapshot v21→v22**; V2a `f92a63a`+`18a59d5` the **encounter editor** (visual wave-grammar builder + resolution preview + byte-faithful `formatEncountersJson`); V2b `8ddbdc7` placement (sector-editor pool section + encounter-editor add-to-sector toggle via `addEncounterToSectorPools`); V2c `c932593` the roadside catalog (**Artillery Company**/**Ronin-and-Mages**/**Guarded Adventurer**) + `e061c8e` editor tab-restore. `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow). USER-FLAGGED for X: frozen Brigands constants end its live-config tracking; `--search` must learn to sweep encounter params.
>
> ## ✅ Phase W COMPLETE (boss + elites; both ✅ user-confirmed; condensed — full one-liner in "Current state" §Phase W + git). **W1** (`5046960`) **"The Bandit King"** (`kind:'boss'`/pool 20) — the first to exercise the condition-gated **`stages`** grammar (3-phase escalation flipping at pool ≤0.66/≤0.33). **W2** (`d08a696`) **elite map-nodes** (pulled forward): a new `'elite'` `NodeKind` + a `NodeMap.generate` scatter pass (structure+rest byte-identical; `eliteChance 0.25`/`eliteMinSpacing 2`; always a non-elite sibling = optional detour), `*` glyph NEON_PURPLE, two elites (pool 12, budget 1.35×) in "The Start", payoff more-XP-only; **RunSnapshot v22→v23**; fuzz `PATH_KINDS += 'elite'`. ⚠️ X TUNING TARGETS (UNTUNED): boss pool 20; elite pool 12/budget 1.35×/`eliteChance 0.25`; `path.elite` weight 0.
>
> ## ⏯️ ACTIVE — **Phase Wb COMPLETE (QoL interstitial); cursor → X (band RE-DERIVATION).** (2026-06-20.) Four user-requested items; **Wb1 ✅ user-confirmed in playtest**, Wb2–Wb4 browser+headless-verified (dev-tool + config), pending the user's manual run. No `RunSnapshot` bump.
> - **Wb1 — encounter NAME on the pre-turn screen (`9ea338e`).** A new `turn:starting` `encounter:{name,kind}` field (from `Run.selectedEncounter`) → a styled amber headline line between "Turn N" and "Hop N" ([PreTurnScreen.ts](src/ui/PreTurnScreen.ts)); elite/boss kind-badged. Presentation event only — no snapshot bump, fuzz byte-identical (the field lives in the gated branch).
> - **Wb2 — editor add-to-sector reload restores the active tab (`427d57d`).** The add-to-sector stash now carries the active encounter id (mirroring the Save path), so the Vite reload re-selects the edited tab instead of snapping to #0 ([encounter-editor/editor.ts](tools/encounter-editor/editor.ts)). Dev-tool only.
> - **Wb3 — `← Tools` home link on every dev tool (`51fd160`).** The other six tools (sector/layout/archetype editors, run-config, sweep-gui, mapgen) gained the encounter editor's home link, palette-matched per tool (row bars right-aligned, column bars left-under-subtitle).
> - **Wb4 — split the sector fight pool BY KIND (`c99ab3f`).** `sector.encounters` → a per-kind `{ normal, elite, boss }` object (exhaustive `Record<EncounterKind, …>`; **USER-LOCKED** keyed-object) so per-kind weighting reads at a glance. New **kind-consistency guard** (a `boss`-bucket entry must be `kind:'boss'`); `encounterPoolAtHop(sector, hop, kind)` takes the kind ([sectors.ts](src/config/sectors.ts)); the formatter emits the keyed object byte-verbatim; `addEncounterToSectorPools` routes by kind; the sector editor shows 3 per-kind groups (kind-filtered dropdowns) + per-kind preview. Config not run state → **no snapshot bump**. 1182 main + 205 fuzz:smoke + lint + typecheck green.
>
> **NEXT = Phase X — balance RE-DERIVATION + per-encounter telemetry. READ [BALANCE.md](BALANCE.md) FIRST.** The authored model replaced the random generator → the band is **re-derived from scratch, NOT re-confirmed**. X1: extend `tests/fuzz/` reporters per-encounter + a `--encounter=<id>` force-select; teach `--search` to sweep encounter params. X2: re-run the difficulty funnel (Brigands = the faithful anchor), tune the encounter knobs + `difficulty.json`, fold in the archetype-balance thread + hop-difficulty curve. Heavy `--jobs` sweep — mind the `dwm.exe` leak. [ROADMAP.md](ROADMAP.md) §"Phase X" + the untuned X targets above.
>
> **Where we are:** **Phases S + T COMPLETE (prior sessions); Phase U COMPLETE + user-confirmed THIS session** (the verbose U record is the ACTIVE block above; the T1/T2/T3 detail below is the prior-phase record). **Phase S** (S1 Floor→Hop v17→v18 `deceb6a`; S2 selectable root v18→v19 `f25fc8f`; `e927308` glyph follow-up). **Phase T — T1 + T2 + T3 + 2 follow-ups + a tools index:**
> - **T1 ✅ (`e3a03eb`)** — the **Sector schema** (`config/sectors.json` + `src/config/sectors.ts`, zod, mirroring `layouts.ts`): a sector = id/title/description/`length`/`theme`/`layouts` pool. Each pool entry `{ layoutId, minHop?, weight? }`; **procedural = a reserved `layoutId` sentinel** in the pool (T1 decision: **sentinel + uniform**; `weight?` is the deferred seam), sharing the literal `'procedural'` with `RunConfig.FORCE_PROCEDURAL` (drift cross-checked). Two guards: every `layoutId` real-or-sentinel; the hop-gated pool non-empty at every reachable hop. Ships **one** sector **"The Start"** (default theme, all layouts + procedural, ungated, length 11). **No behavior change** (not yet wired) → no snapshot/fuzz change.
> - **T2 ✅ (`6295d66`)** — **the run is now a sequence of sectors** (still fed old `rollEnemyWave` until U): the sector-selection meta-DAG (`config/sector-map.json` + `sectorMap.ts`; acyclic + non-sink-has-outgoing guards) + the pure walk `sectorWalk.ts` (**`pickOne` zero-draws a singleton** — GOTCHAS #111). `Run` swapped nodeMap-at-construction → a current-sector model (`currentSectorId` + `currentSectorNodeId`; **RunSnapshot v19→v20**); `rollEncounterMap` rolls the board from the current sector's hop-gated pool + procedural inherits the sector theme. Clearing a terminal → advance to a successor (player pool + roster carry across) unless the DAG node is a sink (→ victory). **Multi-sector BUILT + headless-tested but unshipped** — only "The Start" ships (source == sink).
>
> **Two T1 decisions came from the user this session (both = the recommendations):** layout pool = **sentinel + uniform**; multi-sector **carries the player pool + roster across** sectors. The remaining T2/T3 recommendations were taken as defaults (recruit/rest cadence inherits the node-map kinds; the T3 layout-editor toggle = add-to-existing only).
>
> **T2 board roll** is **weighted** (`pickWeighted`, `weight ?? 1`); "The Start" weights procedural to `weight: 2` → procedural ~25% (each layout ~12.5%), procedural boards inherit the sector theme. The node-map stays byte-identical for "The Start" (length 11 == HOP_COUNT). The fuzz baseline RESET wholesale at U3 anyway; the band is re-derived at X (READ BALANCE.md first).
>
> - **T3 ✅ (`cf773ea` + `46207e6` + `84bf7ea`)** — the dev tooling: the **sector editor** (`tools/sector-editor/`, live zod-validate + a weighted-pool preview + Save via `/__save-config`), a **tools index** at `/tools/`, and the **layout-editor "add to sector" toggle** (writes the sector file via fetch + the shared `formatSectorsJson`; pure headless-tested `poolEdit.ts`). The toggle's button bug (missing `LAYOUT_IDS` import → swallowed `ReferenceError`, `tools/` was outside the tsconfig `include`) + the typecheck-coverage extension (`tools/`+`tests/` → 49 pre-existing errors fixed type-only, GOTCHAS #112) are recorded in the Current-state list + [TODO.md](TODO.md).
>
> **NEXT = Phase V — encounter selection + the encounter editor** ([ROADMAP.md](ROADMAP.md) §"Phase V"): open the U model to a catalog. **V1** `selectEncounter(catalog, { sectorId, hop, nodeKind }, rng)` built as **ONE swappable keyed resolver** (config-selected, à la O3's `focusTileResolution` — NOT two hard-coded forks): the default `encounterFirst` (USER-LOCKED — filter by `sector ∈ e.sectors ∧ hop ≥ e.minHop ∧ e.kind === encounterKindFor(nodeKind)`, pick one, then roll the layout from `sector.layouts(hop-gated) ∩ (e.layouts ?? all)`) + a switchable `layoutFirst` for the playtest A/B; a per-encounter **weighting seam** designed-in but defaulting uniform (do NOT populate — the user wants hop-ramped difficulty *later*); a boot/editor guard that every (sector, reachable hop, kind) has ≥1 eligible encounter with a non-empty layout ∩. Replaces U3's hold-the-single-reproduction in `beginEncounter` with a real pick. **V2** `tools/encounter-editor/` (modeled on the archetype editor) — author name/pool/sectors/layouts/hop-gate/**wave list** with live zod-validate + a **resolution preview** (given a sample roster + hand, show the resolved teams for the first N turns — the wave grammar's "feel" surface; it MUST share the pure `resolveWave`/`waveForTurn` modules, never re-implement them); Save via `/__save-config` (`encounters.json` → the allowlist); ship a **small authored catalog** beyond Brigands (a few encounters exercising sequences / picks / loops / a stage block). **No snapshot bump** (the selected id already persists from U3). Re-baseline fuzz (more than one encounter now appears) — measured at X. Headless-first: selection is pure logic (vitest before the editor). **Then W** (boss encounters — a `kind: 'boss'` encounter per sector, the boss node selects from the boss pool, `minHop` ignored) **then X** (per-encounter `tests/fuzz/` telemetry + `--encounter=<id>` → the band RE-DERIVATION; READ [BALANCE.md](BALANCE.md) first).
>
> **Two naming decisions LOCKED this session** (synthwave/terminal double-coding, matching the project's `daemon` = Unix-daemon ⊗ classical-daimon flavor): **"Floor" → `Hop`** (the node-row progression index; a journey leg ⊗ a network/`traceroute` hop) and **the brief's "Zone" → `Sector`** (the run container; a disk *sector* ⊗ a deep-space *sector*). So: a run **hops through a sector**, then on to the next. The ROADMAP + this handoff use these names throughout; **the archived brief still says "Floor"/"Zone"** (historical).
>
> **The round in one breath:** replace the random `rollEnemyWave` with an authored **Encounter** model (name / health pool / a *wave list* with a small grammar), introduce a **Sector** seam as the run container (selected via its own DAG; eventually 3, initially ONE = "The Start"), and **RE-DERIVE** the difficulty band against the new content (it is *invalidated*, not perturbed). The phase breakdown (full detail + per-step Shape/Cost/Tests/Decisions in ROADMAP.md):
> - **S — Groundwork ✅ COMPLETE (S1 `deceb6a` + S2 `f25fc8f`):** rename **"Floor" → `Hop`** **[S1 ✅ v17→v18]** + the root node became a normal selectable node via a pre-root sentinel (`PRE_ROOT_NODE_ID`) **[S2 ✅ v18→v19]**. Pervasive-but-mechanical; the schemas are now born with the final vocabulary. (S1+S2 took TWO snapshot bumps, not the roadmap's anticipated one — they shipped as separate commits for the playtest pause.)
> - **T — Sectors:** the sector schema (title/desc/length/theme/hop-gated layout pool) + the sector-selection DAG + "The Start"; each sector IS a node-map; replaces the global node-map + `rollLayoutId` — **still feeding old `rollEnemyWave`** (isolated swap). + sector editor + the layout-editor "add to sector" toggle. RunSnapshot bump #2.
> - **U — Encounter model ✅ COMPLETE (U1 `89957c0` + U2 `010d35e` + U3a `2b4f733` + U3b `252b7ea`):** the wave resolver (budget/count/weight) + the wave-list grammar sequencer (wave | weighted-pick | loop{N|forever} | **stage block** = condition-gated boss phases, `until: {enemyPoolAtOrBelow}`, nests to any depth) + the Encounter schema (`kind: normal|elite|boss` **enum**, `rewards?` seam) + the code-built **reproduction encounter** "Brigands" (re-creates today's swarm; NOT byte-identical → fuzz baseline RESET); wired into `Run.beginEncounter`/`beginTurn` (NOT `rollEnemyWave`). **RunSnapshot v20→v21** (`selectedEncounterId` + `waveCursor`; `encounterBudget` retired); encounter.name → HUD ([HUD.ts:558](src/ui/HUD.ts)). USER-LOCKED: per-instance level weighting; last-wave-repeats terminal policy.
> - **V — Selection + editor:** select an encounter per battle node — **encounter-first-then-layout (user-LOCKED as the default), built as a SWAPPABLE keyed resolver** (à la O3's `focusTileResolution`) so encounter-first vs layout-first A/B's with a config flip; filters on node→encounter `kind`; + a designed-in-but-unpopulated per-encounter **weighting seam** (user: wanted later — incl. hop-ramped difficulty — out of scope now). + the encounter editor (with a resolution preview — the "feel" surface) + a small authored catalog.
> - **W — Boss + elite encounters ✅ COMPLETE (`5046960` boss + `d08a696` elites; scope EXPANDED to include elites):** the terminal boss node selects a `kind:'boss'` encounter ("The Bandit King", exercising the `stages` grammar); and — pulled forward from the deferral list — **elite map-nodes** scatter the middle hops (a new `'elite'` NodeKind + scatter pass), an optional harder detour selecting from the sector's `elite` pool. RunSnapshot v22→v23. See the ACTIVE block.
> - **X — Balance re-derivation + per-encounter telemetry:** extend `tests/fuzz/` reporters per-encounter + `--encounter=<id>`; re-derive the band; fold in the archetype-balance thread + the hop-difficulty curve.
>
> **Data-model recommendation (the brief's open judgment call):** layouts stay relationship-free (geometry only); **Sector owns its layout pool** (the sector↔layout edge — the editor "add to sector" toggle writes the SECTOR file), **Encounter owns its sector-list + optional layout-list + hop gate + waves**. The two layout references are deliberate, not redundant (Sector.layouts = "which boards exist here"; Encounter.layouts = "which boards this fight fits"; selection uses their intersection). Full table in ROADMAP.md §"The data model".
>
> **A future-proofing pass (user-reviewed) shaped the schema:** BUILT this round — the encounter `kind` enum (reserves elite), the optional `rewards?` seam, and the condition-gated **stage** grammar (boss phases). SEAMED-but-deferred (ROADMAP §"What we're NOT doing" → "Seamed this round but deliberately NOT built"): elite *map-nodes*, the reward/loot *economy* that consumes `rewards?`, **player-chosen sector branching** (T2 auto-picks a random successor — the swap point for a future region-choice UI), encounter `tags`/once-per-run/prereq filters, and ascension-style difficulty tiers.
>
> **DECISION POINTS — ALL LOCKED:** ✅ phase ordering = **sectors-before-encounters**; selection = **encounter-first-then-layout as the default, built swappable** (keyed resolver) with a deferred weighting seam (V); the **`Hop`** term; the **`Sector`** container name; the U1 **level-distribution = per-instance weighting** (USER-LOCKED at U1 build — `distributeWeightedLevels` generalizes `distributeBudget`, uniform → even split); the U2 **terminal policy = last-wave-repeats** (USER-LOCKED); the U3 reproduction-count = **faithful (`hand × swarmMax`, drops `rollEnemyWave`'s `count ≤ budget` bound)** — user chose "ship faithful, retune at X" (minor at the 1.25×1.5 band since budget ≈ count). The V-era open call (deferred, not blocking): the `encounterFirst`/`layoutFirst` config key + where the switch lives (`config/encounters.json` vs a new `config/selection.json`).
>
> **⚠️ This UPENDS BALANCE** — the band (`1.25 × 1.5`) is **RE-DERIVED, not re-confirmed** (X). **READ [BALANCE.md](BALANCE.md) FIRST** for any balance work. Headless-first: the wave resolver, the grammar sequencer, the sector-DAG walk, and encounter selection are ALL pure logic (vitest before the browser); the editors are where wave *authoring/feel* gets exercised (the user's "not testable without the dev tool" point).
>
> **Prior art to mine when building:** `rollEnemyWave` + `distributeBudget`/`chooseSwarmCount` ([enemyBudget.ts](src/run/enemyBudget.ts) — the wave-resolver primitives, reuse don't rewrite); the H4 health-pool encounter loop in `Run.beginEncounter`/`beginTurn`/`rollEncounterMap` ([Run.ts](src/run/Run.ts)); `BattleEncounter`/`EncounterMap` (unchanged in shape — only WHO builds the team changes); `NodeMap.generate` (its `floorCount` becomes the sector's `length`); the `/__save-config` allowlist ([vite.config.ts](vite.config.ts)) + the [layout-editor](tools/layout-editor/)/[archetype-editor](tools/archetype-editor/) pattern (the new editors' home); the `tests/fuzz/` `--per-layout`/`--per-floor` reporters (extend per-encounter; `--per-floor` → `--per-hop` at S1).
>
> **Open threads (non-blocking):** the environmental **`dwm.exe` committed-memory leak** (reboot before heavy `--jobs` fuzz; `--jobs=1` immune; [archive/dwm-leak-diagnosis.md](archive/dwm-leak-diagnosis.md)); the **M6 hop-gated layout roll** + **layout-difficulty telemetry** (now resolved structurally by T1/V1, tuned at X2); an **object-pooling pass** ([TODO.md](TODO.md)); the deferred **catapult SFX**; card-cosmetic TODOs (RNG-vs-rng label, archetype/ability display-names).
>
> **Tree state:** on `main`. This session shipped **all of Phase U** (the encounter-model keystone): **`89957c0`** (U1 — pure wave resolver) → **`010d35e`** (U2 — wave-list grammar + sequencer) → **`2b4f733`** (U3a — Encounter schema + recursive `waves` zod + the code-built reproduction encounter + the `mean`/`median` budget refinement) → **`252b7ea`** (U3b — Run wiring, **RunSnapshot v20→v21**, `encounterBudget` retired, `encounter.name` → HUD). **HEAD = `252b7ea`** (+ this handoff docs commit). Current tree: WorldSnapshot **v25** (unchanged) / RunSnapshot **v21**; fuzz **205**; **1127 main tests** green; typecheck + lint clean. `output/` gitignored. **The dev-preview :5191 was started for browser-verify, then stopped.** New code lives in **`src/run/encounters/`** (`wave.ts` U1, `sequencer.ts` U2, `reproduction.ts` U3) + **`src/config/encounters.ts`** + **`config/encounters.json`** (empty catalog). **`rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported** — the fuzz arena ([tests/fuzz/arena.ts](tests/fuzz/arena.ts)) + [spawn-overflow.test.ts](tests/integration/spawn-overflow.test.ts) still call them directly; only `Run` stopped using them (don't delete them). **The reproduction encounter is CODE-BUILT** (`reproductionEncounter()` reads live `DIFFICULTY`/`HEALTH`/`DECK`) — NOT in `encounters.json` — so it tracks the band through X's in-process sweeps; the snapshot persists `selectedEncounterId='reproduction'` and `fromJSON` re-synthesizes it.

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phase A complete** — A1–A5 (action selector, command channel, fuzz harness, config externalization, scene system).
- **Phase B complete** — B1/B1.1 (palette + selective bloom), B3 (HP/progress bars), B5 (CSS scanlines), B6 (audio), B7 (root node `@`).
- **Phase C1 complete** — C1a/b/c/d (terrain + walls + LOS + faceted prisms + layout editor + Labyrinth pathfinding fix).
- **Phase D complete** — D1 (renderer cap), D2 (drag-paint), D3 (variable sizes), D4 (fit/scroll camera), D5.A–E (spawn regions + overflow queue), D6 (half-cover), D7.A–C (chasm + fire + healing), D8 (theming).
- **Phase E complete** — combat depth (E1–E7). E1 (stat vocabulary + `deriveStats`), E2 (generic ability layer, retiring `AttackBehavior`), E3 (per-archetype `growthRates` + leveling), E3.5 (`TICK_RATE` doubled 10 to 20), E3.6 (DOM unit overlays + hitsplats), E4 (XP loop + PromotionScene + half-cover mult), E5 (pathfinding refresh: target stickiness + boids sidestep), E6 (combat visuals: shove/projectile/hitsplats), E7 (the six combat archetypes: melee/ranged + rogue/healer/mage/catapult). Full per-step worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md); forward plan in [ROADMAP.md](ROADMAP.md).
- **Phase F complete** — Phase-E playtest response (F1–F6). F1 (draft-pool pull-forward: the E7 archetypes become draftable), F2 (first-class action phase timeline windup->...->recovery), F3 (projectile & impact timing), F4 (rogue gambit sequencing), F5 (heal feedback VFX), F6 (heal/utility XP). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase G complete** — run progression (G1–G5). G1 (`RunConfig` + short-run harness), G2 (longer maps + full-viewport map scene), G3 (rest & boss nodes + map icons), G4 (enemy level-*budget* model + recruit leveling), G5 (fuzz-strategy factory + GUI launcher). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase GP complete** — combat & stat-legibility playtest response (GP1–GP5). GP1 (`endurance`->`mobility` / `speed`->`agility` rename + universal move base), GP2 (`defense` stat + `World.applyDamage` consolidation), GP3 (recruit-card + HUD legibility), GP4 (ranged/catapult acting-position pathing), GP5 (healer chokepoint yield via `SwapAction` + centroid navigable-snap). Worklog: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phase H complete** — the multi-turn deckbuilder battle trial (H1–H7). H1 (`power` stat), H2 (spawn-tile range), H3 (deployment counter / fatigue hook), H4 (health-pool encounter→turn loop: H4a headless + H4b pre/post-turn screens & pool HUD), H5 (card-drawn hand + `playerTeamLevel` seam swap + H5b hand display), H6 (rest-heal + recruit-pass + inert spawn-time fatigue hook), H7 (search-driven balance tooling: scored DSL → `--search` → difficulty band SET `budgetFactor 0.625 × swarmMax 1.75` + archetype rebalance + telemetry + sweep GUI). Detail in [ROADMAP.md](ROADMAP.md) §Phase H + [BALANCE.md](BALANCE.md).
- **Phase I complete** — foregone conclusions & combat identity (I1–I6). I1 (`agility→speed` revert + `precision`/`evasion` dodge stats + canonical reorder; World v19 / Run v10), I2 (subtractive hit/miss roll at `applyDamage` + "Miss" hitsplat; live `hitChanceBase` 0.6, turn caps 100→150s), I3 (fast-forward 1×/2×/3× tick-batch, config-backed `F` hotkey), I4 (schema-driven archetype editor + dev-only `/__save-config`), I5 (melee subclass family `mercenary`/Adventurer/Ronin/Bandit + Bandit enemy + rogue dodge re-measure → N1 mobility CONFIRMED, scoped for reach; World v20 / Run v11), I6 (per-ability combat profile `might`/`accuracy`/`critBase` + evadable/critable gates, weapon split sword/club/katana/whip/bow; World v21→v22). Detail in [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase I + [BALANCE.md](BALANCE.md).
- **Phase J complete** — the in-battle objective system (J1–J4). J1 (the objective MODEL + targeting preemption, sim/headless: one shared player-team objective, `tile` persist / `enemy` auto-clears, engage radius `min(attackRange, rangedLeashCells)` + retaliation; **WorldSnapshot v22→v23**, byte-identical with no objective). J2 (the pathing SEAM: a movement-intent primitive in [movement.ts](src/sim/movement.ts) over the pure `routeToward` wrapper = the cache boundary; **cache DEFERRED seam-first** + a `pathfindingStats` bounded-recompute guard). J3 (objective UI + the runtime-rebindable [Keybindings](src/ui/Keybindings.ts) registry — Phase J's first playtestable surface). J4 (fuzz objective strategy + arena harness + the `--objective` flag — dev-only, zero `src/` change). As-built detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase J + the git log.
- **Phase K complete** — status effects & pre-battle agency (K1–K4). K1 (the generic status-effect system [`StatusEffect`/`foldEffects`/`effectiveStats` + the full trigger set] + fatigue migration; **World v23→v24 / Run v11→v12**, byte-identical at default). K2 (roster/hand decouple 10/6 + the latent H5 wave-size bug fix — `rollEnemyWave` sized off the whole roster not the fielded hand; `enemyArcherRatio`→0.3, band re-swept). K3 (the redraw mechanic, turn-intro gate + `maxCardsPerTurn` dial; **Run v12→v13**) + K3.5 (ONE MAP PER ENCOUNTER; **Run v13→v14**). K4 (the pre-turn empower; **Run v14→v15** — MEASURED **band-moving**, +21…+27 wins/200). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase K + [BALANCE.md](BALANCE.md) (K3c3 / K4c3).
- **Phase L complete** — L1 daemons (relics): the four-idol catalog (Mars/Minerva/Mercury/Janus) + **DAEMON-ONLY gates** (the K3/K4 static enables ship false) + one uniform roll at run start + `chance` as a first-class gate + the PreTurnScreen idol surface + fuzz `--daemon` with per-idol telemetry; **Run v15→v16**. Minerva shipped +4 DEF, **nerfed +4→+2 same-day** (the L1c3 outlier). The `battleTrigger` daemons + multi-daemon economy DEFERRED to the shop/loot round. Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase L + [BALANCE.md](BALANCE.md) §L1c3.
- **Phase M complete** — progression & presentation (M1–M6). M1 (per-turn promotion cadence: XP banks at the turn boundary, promotions pop mid-encounter; **Run v16→v17**, stash-compared band-neutral). M2 (the level-up screen staggered-reveal redesign — two-phase card-by-card; the user also live-tuned the leveling curve, now LOCKED `50/1.1`). M3 (scene polish: no auto-progression + turn-intro materialize + after-turn outro). M4 (the battle backdrop: apron + fog + the BackdropRenderer mist floor). M5 (the layout-editor save-to-config via the I4 `/__save-config` endpoint). M6 (water mechanic "slow + miss more" + a from-scratch procedural-map rework [crossbar/divider/noise + half-cover windows] + `?layout=procedural`; floor-gating DEFERRED). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase M.
- **Phase N complete (bar N4)** — rogue mobility & balance closure. N1 (the rogue DASH: ability config became a `kind` discriminated union `attack|heal|movement`, an aggressive-close `DashAbility` riding J2's movement-intent seam, the first-class `unit:dashed` event; NO snapshot bump). N2 (the band re-sweep against the full post-I–M model → **LOCKED `1.25 × 1.5`**; OP read = a mercenary+ranged duopoly). N3 (the leveling consistency check, GREEN at `50/1.1`). + the turn-cap unification (one `HEALTH.maxTurnSeconds`, `resolveAsDraw`-at-cap uniform across harness/arena/**live BattleScene**). **N4 (overnight out-of-sample verify) DEFERRED INDEFINITELY to a VPS** (the dwm leak). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase N + [BALANCE.md](BALANCE.md).
- **Phase O complete** — the Post-N agency round's typed objective refactor (O1–O5). O1 (always-an-objective TYPED per-team model `atWill`/`engage`/`hold`, enemy inert-but-symmetric; **WorldSnapshot v24→v25**), O2 (`hold`), O3 (`focus` + 3 switchable tile-resolutions), O4 (ranged `minRange` kiting — bow 2 / mage 2 / catapult 4), O5 (dev-only `--objective=coverage` churn bot, kept separate from the measurement proclivity). Band **re-confirmed `1.25 × 1.5`** (no retune). Detail: git log + [BALANCE.md](BALANCE.md) §O5.
- **Phase P complete** — the shared `UnitCard` component ([src/ui/UnitCard.ts](src/ui/UnitCard.ts)). P1 (extract + recruit/promotion parity + the rarity-accent seam), P2 (full-variant XP-to-next bar, config-derived `xpProgress`), P3 (PreTurnScreen adopts the full card; the screen scrolls for a tall hand), + the POW-display rework (its own accented meta row). **UI-only — no snapshot/fuzz change** (WorldSnapshot v25 / RunSnapshot v17 hold). Detail: git log + the top ACTIVE block.
- **Phase Q COMPLETE (Q1–Q6)** — the in-battle HUD overhaul + pacing: the `PlaybackSpeed` set-by-value model (0.5×–3× + pause), the pre-battle countdown (replacing the spawn-anim), the four HUD panes (speed TR / objective BR / player-cards BC / enemy-cards top, on P's `compact`/`full` UnitCard + a `team`-tinted enemy mirror), and dismantling the old monolithic HUD (net −325 lines). **Presentation/UI/input — no snapshot/fuzz change** (v25/v17 hold). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase Q.
- **Phase R COMPLETE (R1–R3)** — auxiliary card surfaces + cleanup; closes the Post-N O→R round. R1 (the shared roster-view modal — a top-right "Roster" button on Map/Recruit/pre-turn opens a dimmed overlay of the roster as `full` UnitCards [a new `roster` skin]; order via the pluggable `rosterOrder` seam, recruitment-order default), R2 (the pre-turn draw-pile [bottom-right] + discard-pile [bottom-left] views — the R1 modal generalized into a shared `CardListModal`/`CardListButton`; piles ride `turn:starting`/`turn:handRedrawn` resolved to templates in recruitment order, contents-only/unordered via `Run.resolvePileForDisplay`), R3 (the procedural-map label unify — one `PROCEDURAL_MAP_NAME` = "Uncharted Ground" constant + cleanup chores: `.gitattributes`, inline-SVG favicon, three.js vendor-chunk split; catapult SFX deferred). **UI-only — no snapshot/fuzz change** (v25/v17 hold). User-confirmed in the native browser. Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase R; deferred bugs in [TODO.md](TODO.md) §Phase Qb.
- **Phase Qb COMPLETE (#2 + #3)** — the two deferred bug investigations, both user-confirmed in the native browser. **#3** (`b6da01f`) the corridor archer kite-pin: a too-close kiting ranged unit (inside `minRange`) with a blocked retreat fell through to the target-cell anti-freeze fallback and stepped ONTO the soft-excluded target; fix = omit that fallback when `dist < minRange` (abstain/queue), guarded by a corridor fixture + a "no two combatants share a cell at a tick boundary" invariant test. **#2** (`0d9413e`) the billboard depth-sort: the `depthWrite:false` glyph instances had no per-instance depth sort, so draw order (= spawn slot) decided occlusion; fix = `SpriteRenderer.sortByDepth(camera)` reorders the instance buffers back-to-front each frame, wired into `Game.onFrame`. No snapshot/fuzz change (v25/v17 hold); 1021 tests. Detail: git log + [TODO.md](TODO.md) §Phase Qb.
- **Phase S COMPLETE (S1 + S2)** — encounter-system groundwork: **S1** (`deceb6a`) the **Floor→Hop** rename (node-map progression concept + dev surface; **RunSnapshot v17→v18**; battlefield-floor tile / `Math.floor` left alone); **S2** (`f25fc8f` + glyph follow-up `e927308`) the **selectable root** via a pre-root sentinel (`PRE_ROOT_NODE_ID = -1`, [NodeMap.ts](src/run/NodeMap.ts); **v18→v19**; adds one battle/run). **GOTCHAS #110** — don't "fix" `currentNodeId = -1` back to `rootId` (`currentHop`/`hopOf` throw at pre-root by design). Both user-confirmed. Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase S.
- **Phase T COMPLETE (T1–T3 + follow-ups)** — the run-container round: **T1** (`e3a03eb`) the **Sector schema** ([sectors.ts](src/config/sectors.ts) + `config/sectors.json`, zod — hop-gated layout pool; procedural = reserved sentinel literal-matching `RunConfig.FORCE_PROCEDURAL`); **T2** (`6295d66` + `33b4fd5` weight-seam + `eb9b2ab` banner) **the run = a sequence of sectors** (`sector-map.json` DAG + [sectorWalk.ts](src/run/sectorWalk.ts); `Run` → a current-sector model, **RunSnapshot v19→v20**; weighted board roll; player pool + roster carry across; ships one sector **"The Start"**, source == sink; **GOTCHAS #111** — `pickOne` zero-draws a singleton); **T3** (`cf773ea` + `46207e6` + `84bf7ea`) the dev tooling (sector editor + `/tools/` index + the layout-editor "add to sector" toggle; its swallowed-`ReferenceError` button bug fixed). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase T + [TODO.md](TODO.md).
- **Phase U COMPLETE (U1–U3b)** — the keystone: `rollEnemyWave` RETIRED from `Run`, replaced by an authored **Encounter** model. **U1** (`89957c0`) the pure wave resolver [wave.ts](src/run/encounters/wave.ts) (`resolveWave` — budget/count/weight; `distributeWeightedLevels`; per-instance level weighting [USER-LOCKED]); **U2** (`010d35e`) the wave-list grammar + sequencer [sequencer.ts](src/run/encounters/sequencer.ts) (`waveForTurn` — wave | weighted-pick | loop{N|forever} | condition-gated **stages**; nests to any depth; resumable cursor; terminal = last-wave-repeats [USER-LOCKED]); **U3a** (`2b4f733`) the Encounter schema [encounters.ts](src/config/encounters.ts) (`kind` enum normal|elite|boss + `rewards?` seam); **U3b** (`252b7ea`) the Run wiring (`beginTurn` resolves via `waveForTurn`→`resolveWave`; **RunSnapshot v20→v21**; per-encounter `enemyHealthPoolMax`; encounter name → HUD). `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow still call them). User-confirmed. **V0/V1 superseded U3's empty catalog + code-built reproduction** (see the ACTIVE block). Detail: git log + [ROADMAP.md](ROADMAP.md) §Phase U.
- **Phase V COMPLETE (V0–V2c).** V0 (`9b8d818`) sector-owns-both data-model flip; V1a (`498745d`) the pure `selectEncounter` keyed resolver + `config/selection.json`; V1b (`c647ebf`) wired it into `beginEncounter`, retired the code reproduction, hoisted **Brigands** + authored **Highwaymen**/**Deserters**, pooled in "The Start", **RunSnapshot v21→v22**. **V2a** (`f92a63a` + `18a59d5`) the encounter editor + visual wave-grammar builder + byte-faithful `formatEncountersJson`. **V2b** (`8ddbdc7`) placement, BOTH UIs — a sector-editor **Encounter pool** section + the encounter-editor **add-to-sector** toggle (pure headless-tested `addEncounterToSectorPools`). **V2c** (`c932593`) the roadside catalog — 3 grammar-demo encounters (**Artillery Company** looped sequence / **The Ronin and the Mages** per-turn pick / **Guarded Adventurer** finite-loop + flat sequence) pooled into "The Start"; pinning tests updated to 6, `Run.test.ts` name-check derives from the live pool. + `e061c8e` the editor save-reload tab-restore. No snapshot bump; user-confirmed in playtest. Detail: git log + [ROADMAP.md](ROADMAP.md) §"Phase V".
- **Phase W COMPLETE (W1 boss + W2 elites; scope EXPANDED; both user-confirmed).** **W1** (`5046960`) **"The Bandit King"** (`kind:'boss'`/pool 20) — the first to exercise the condition-gated **`stages`** grammar (3-phase escalation at pool ≤0.66/≤0.33). **W2** (`d08a696`) **elite map-nodes** (pulled forward): a new `'elite'` `NodeKind` + a `NodeMap.generate` scatter pass (structure+rest byte-identical; `eliteChance 0.25`/`eliteMinSpacing 2`; always a non-elite sibling = optional detour); `*` glyph NEON_PURPLE; two elites (pool 12/budget 1.35×); no Run dispatch change (reuses the battle path); **RunSnapshot v22→v23**; fuzz `PATH_KINDS += 'elite'`. Boss/elite numbers UNTUNED (X targets). Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase W".
- **Phase Wb COMPLETE (QoL interstitial; Wb1–Wb4; pre-X polish).** **Wb1** (`9ea338e`) the encounter NAME on the pre-turn screen (`turn:starting` gains `encounter:{name,kind}` → a styled headline, elite/boss badged) — ✅ user-confirmed. **Wb2** (`427d57d`) the encounter-editor add-to-sector reload restores the active tab (stash the active id, mirror the Save path). **Wb3** (`51fd160`) the `← Tools` home link on every dev tool (palette-matched). **Wb4** (`c99ab3f`) the sector fight pool split BY KIND — `encounters:{normal,elite,boss}` (exhaustive `Record<EncounterKind,…>`, USER-LOCKED keyed-object), a new kind-consistency guard, `encounterPoolAtHop(sector,hop,kind)`, a byte-verbatim keyed formatter, `addEncounterToSectorPools` routing by kind, both editors reworked. No snapshot bump (Wb1 a presentation field, Wb4 config shape); 1182 main + 205 fuzz:smoke green. Detail: git + [ROADMAP.md](ROADMAP.md) §"Phase Wb".
- **Tests:** **1127 main** pass (0 `it.todo()`) via `npm test` (U1 `src/run/encounters/wave.test.ts` = 21 [incl. the brief's worked example + `distributeWeightedLevels`]; U2 `sequencer.test.ts` = 18 [flat/loop/pick/stages, nesting, resumable cursor]; U3a `src/config/encounters.test.ts` = 6 [recursive grammar parse + rejects] + `src/run/encounters/reproduction.test.ts` = 6 [identity + the balance-proof vs `enemyBudgetFor`]; U3b re-pointed the Run snapshot/encounter tests at the encounter model, v21 reject rides the generic `schemaVersion − 1` test. The prior T-phase additions [sectors/sectorMap/sectorWalk/editor tests] carry over); **205 fuzz:smoke** via `npm run fuzz:smoke`; `npm run typecheck` clean. One known residual — `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, out of GP5 scope). Fuzz sweep `npm run fuzz` (20 seeds default; `--per-hop` for per-hop analysis; `--layout=procedural` to isolate the M6 maps); the `scored` strategy + `--search` (→ `output/best-strategy.json`) drive the balance tuning. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`. **READ [BALANCE.md](BALANCE.md) FIRST for any balance work.** The per-phase test-count history lives in git log + [archive/post-h-roadmap.md](archive/post-h-roadmap.md).
- **Dev server:** Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent). **The user usually has their own `npm run dev` on 5173 during a session**, and the preview MCP refuses to attach to a server it didn't start ("not a preview server"). For agent browser-verify, use the **`dev-preview` launch config** ([.claude/launch.json](.claude/launch.json)) — `npm run dev -- --port 5191 --strictPort` binds a dedicated port that never collides with 5173, so `preview_start name=dev-preview` always works. **Dev handle:** `window.__game` (DEV-only) exposes `bus`/`renderer`/`run`/`activeScene`/`keybindings`/`sprites`; drive a battle headlessly with `__game.dispatch({kind:'enterNode',nodeId})` then `{kind:'advanceTurn'}` (past the PreTurnScene gate), and freeze the auto-advancing run with `scene.clock.advance = () => {}` for a stable screenshot.
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**Phases A → N are COMPLETE (bar the VPS-gated N4).** The full as-built history lives in [archive/post-h-roadmap.md](archive/post-h-roadmap.md) (Phases I → N, the detailed roadmap this round superseded), [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md) (E → GP), and [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md) (A → E).

**The Phase-N playtest feedback was synthesized into [ROADMAP.md](ROADMAP.md) — "Post-N": the anti-blobbing agency/control round, Phases O → R — now ✅ COMPLETE (+ the Qb deferred-bug tail, closed 2026-06-18)** (the brief is archived at [archive/phase-n-feedback.md](archive/phase-n-feedback.md)). In brief — **O** the objective-system refactor (always-an-objective TYPED per-team model: atWill/engage/focus/hold; enemy support inert; focus's 3 switchable tile-resolutions; ranged `minRange`) + fuzz, headless-first, one WorldSnapshot bump; **P** the shared `UnitCard` component (compact + full variants, consolidating recruit/promotion; pre-turn adopts full cards); **Q** the HUD overhaul (4 panes: speed TR / objective BR / player-cards BC / enemy-cards top) + the co-located pacing model (a pre-battle countdown REPLACING the spawn anim, 0.5× + pause, per-speed enable for difficulty); **R** roster + draw/discard pile views + the Uncharted-Ground/Nowhere unify. The brief's other two anti-blob ideas (side-objectives, dynamic terrain) are DEFERRED to future rounds. Six design decisions are pre-locked (see the roadmap's resolved decision points).

**The ENCOUNTER-SYSTEM round is BUILDING — [ROADMAP.md](ROADMAP.md) "Post-R" (Phases S→X)**, synthesized from the now-archived [archive/phase-R-feedback.md](archive/phase-R-feedback.md): replace random `rollEnemyWave` with authored **Encounters** (a wave list + a small grammar), introduce **Sectors** as the run container, and **RE-DERIVE** the band (it is invalidated, not perturbed — READ [BALANCE.md](BALANCE.md) FIRST). **S + T + U + V + W + Wb are ✅ COMPLETE** (S Hop rename + selectable root; T Sectors; **U the Encounter model retiring `rollEnemyWave`**; **V** encounter selection + editor + catalog; **W** boss [the `stages` grammar] + elite map-nodes [scope-expanded] — RunSnapshot **v23**; **Wb** the pre-X QoL interstitial — pre-turn encounter name, two editor fixes, and the **per-kind sector pool** `encounters:{normal,elite,boss}`). **NEXT = the ONLY remaining phase, X** — per-encounter fuzz telemetry + a `--encounter=<id>` flag (heads-up: Wb4 made the sector fight pool per-kind, so force-select must respect the node's kind / draw from the right bucket) + teaching `--search` to sweep encounter params, then the band **RE-DERIVATION** against the authored content (boss/elite difficulty numbers are untuned X targets). Smaller follow-ups in [TODO.md](TODO.md); gameplay backlog (shop, synergies, boss variety) under [DESIGN.md](DESIGN.md) "Out of scope". The top ACTIVE block carries the full cursor.

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

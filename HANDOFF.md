# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phase A complete** — A1–A5 (action selector, command channel, fuzz harness, config externalization, scene system).
- **Phase B complete** — B1/B1.1 (palette + selective bloom), B3 (HP/progress bars), B5 (CSS scanlines), B6 (audio), B7 (root node `@`).
- **Phase C1 complete** — C1a/b/c/d (terrain + walls + LOS + faceted prisms + layout editor + Labyrinth pathfinding fix).
- **Phase D complete** — D1 (renderer cap), D2 (drag-paint), D3 (variable sizes), D4 (fit/scroll camera), D5.A–E (spawn regions + overflow queue), D6 (half-cover), D7.A–C (chasm + fire + healing), D8 (theming).
- **E1 complete** — stats overhaul. Replaced MVP `{maxHp/attackDamage/...}` block with the new vocabulary (constitution/strength/ranged/magic/luck/speed/endurance) + `UnitDerived` layer (maxHp, critChance, cooldown ticks, attackRange) computed via `deriveStats`. Knobs in [config/stats.json](config/stats.json). AttackAction rolls crit at start against `world.combatRng`. WorldSnapshot v5→v6. Difficulty curve scales constitution (not post-derive maxHp) so `deriveStats` stays canonical.
- **E2 complete** — ability primitives. `AttackBehavior` retired in favor of generic `AbilityBehavior` walking per-unit `Ability[]`. Concrete `MeleeStrike` + `RangedShot` in [src/sim/abilities/strikes.ts](src/sim/abilities/strikes.ts); registry at [src/sim/abilities/registry.ts](src/sim/abilities/registry.ts). Archetype config gains `abilities: string[]` validated against the registry at boot (A4 pattern). `ActionProposal.cooldownKey` lets a multi-ability unit get independent cooldowns even when several wrap the same Action. Per-ability scoring is context-dependent (each `Ability.propose` decides). Half-cover damage modifier explicitly deferred to E4 (tune alongside the rest of the stat curve). WorldSnapshot v6→v7.
- **E3 complete** — archetype config + leveling. `growthRates` hoisted alongside `baseStats` in [config/archetypes.json](config/archetypes.json) (per-stat `[0,1]` probabilities). New [src/sim/leveling.ts](src/sim/leveling.ts) exports `simulateLevelUps` (player recruits, per-stat RNG vs growth) + `scaleStats` (enemies, deterministic `stat += round(growth × n)`). `UnitTemplate` + `Unit` gain `level: number`; the template's `stats` field is now the *post-level-up* snapshot (level is display metadata, not a runtime modifier). Difficulty curve moved from `enemyHpPerFloor` (constitution multiplier) to `enemyLevelPerFloor` × `scaleStats` — same axis as player progression. Player recruits come in at `currentFloor` simulated level-ups (a floor-4 recruit gets 4 rolls), starting roster stays level 1. `deriveStats` widened to accept optional `baseMoveCooldownSeconds` per archetype (knob defaults to global from `config/stats.json`); attack-CD overrides deliberately stay off this axis — they'll land on the Ability layer where multi-ability units can carry different timings. WorldSnapshot v7→v8, Run snapshot v2→v3. 347 tests pass (was 336; +11 in [src/sim/leveling.test.ts](src/sim/leveling.test.ts)). Browser-verified floor-1 enemies at level 1 baseStats, floor-2 enemies at level 2 with deterministic `+1 con / +1 str / +1 ranged` matching the round(growth × 1) math, recruit cards display "LEVEL N <archetype>".
- **E3.5 complete** — tick rate + cooldown sensitivity. `TICK_RATE 10 → 20` in [src/config.ts](src/config.ts); `cdPerStat 0.01 → 0.05` in [config/stats.json](config/stats.json). At the previous values, every starting unit had the same attack/move CD (need ~9-14 stat points to shift a single tick); after E3.5 each stat point shifts attack CD by ~1 tick and every other point shifts move CD. Per gotcha #6 the seconds-authored balance re-derives automatically (tile cadences, spawn lockout, cooldowns all bumped through `secondsToTicks`). Test churn: `src/config.test.ts` rewritten to derive boundaries from TICK_RATE instead of hardcoding `10` (gotcha #99). One latent bug surfaced: `RNG.toJSON()` returned the raw signed state while `fromJSON` normalized via `>>> 0`; at the new tick rate a 50-tick round-trip happened to land on a state with the sign bit set, breaking `.toEqual()` even though the RNG sequence was functionally identical. `toJSON()` now also normalizes — fix in [src/core/RNG.ts](src/core/RNG.ts), see gotcha #100. **Behavior shift to know:** baseline cooldowns shift at non-zero speed/endurance — a melee unit at speed=5 now fires 25% faster than at speed=0 (was 5%). This is the design intent; surface it during browser-verify of E3.6 / E4 if combat feels noticeably faster than the E3 baseline. 347 tests pass; fuzz smoke 7/7. No snapshot version bump (schema shape unchanged; only tick-count values inside doubled).
- **E3.6 complete** — DOM migration for unit overlays. Retired `BarRenderer` (+ `bar.vert.glsl` / `bar.frag.glsl`) in favor of a new [src/render/UnitOverlayLayer.ts](src/render/UnitOverlayLayer.ts) that owns a `#unit-overlays` DOM container inserted between `#game-canvas` and `#ui` (so the HUD paints above; `#scanlines` at z=1000 still rakes across). Each non-neutral unit gets a single `<div class="unit-overlay">` with three children — `.level-badge` (top-right `Lv N` pill, amber on near-black), `.hp-bar` with `.hp-bar-fill` (universal HP-state gradient green→amber→red, refreshed on `unit:attacked` + tile chip events), `.action-progress` (hidden by default, fills smoothly between sim ticks for in-flight `activeAction`s that aren't MoveAction/SpawnAction). Per-frame `UnitOverlayLayer.updatePosition(handle, worldPos)` projects the sprite's *current* world position via `camera.project(...)` → CSS px, then writes `transform: translate3d(...)` — one projection per unit per frame, GPU-composited so there's no layout thrash. Reads sprite position from `SpriteRenderer.getPosition(handle, out)` so overlays pick up SpriteAnimator lerps for free. Death fade keeps the existing JS elapsed-timer machinery (now writing to `element.style.opacity` instead of an instance attribute) so it stays in lockstep with the sprite-side `SpriteAnimator.startFade`; same lane handles the D5.C overflow-spawn opacity-0→1 fade-in. `BattleRenderer.detach()` calls `overlays.clear()` which removes every overlay `<div>` in one sweep, covering both live overlays and mid-fade ones. The pre-E3.6 `BarHandle` / `barPairs` / `barFades` / `barFadeIns` names are renamed to overlay-* throughout the file; lifecycle is otherwise unchanged. Game's `SceneContext.bars: BarRenderer` becomes `overlays: UnitOverlayLayer`. **Tradeoff:** overlay element sizes are fixed CSS px (56px wide bar, 9px badge font) rather than world-anchored — keeps text rendering crisp + simplifies layout, but at extreme camera zooms (small board scroll-in, large board fit-out) the bars look slightly disproportionate to the sprite. Acceptable per the ROADMAP discussion; revisit if it bothers playtest. **Progress bar still doesn't fire visibly** in current archetypes since `MeleeStrike` / `RangedShot` are 1-tick activeActions — first real consumer will be E7's mage charge-up, as planned in B3. No tests added; the project's convention is "render code is not tested here, visual verification handles that" (see [vite.config.ts](vite.config.ts) test config comment). Browser-verified across fit + scroll camera modes on procedural + Endless Corridors + Junction Ambush + Spiral Fire/Life layouts: HP fill width + color update on damage (saw green→amber transition in real time), Lv 1 and Lv 2 badges render, neutral walls + half-cover units correctly skip overlay creation, detach drains DOM cleanly (`#unit-overlays` returns to 0 children on battle end). Typecheck clean.
- **E4 complete** — XP loop + PromotionScene + half-cover damage modifier. Shipped in 6 commits (E4.1–E4.6) plus a pre-existing test fix (multiTick fixture stale at the post-6244561 hpPerConstitution=1.0). E4.1: `config/leveling.json` + `xpToNext` (quadratic per-level: `baseXp * L^exponent`, cap 20) + half-cover mult knob all isolated in [src/sim/xp.ts](src/sim/xp.ts) so swapping the curve is a one-file edit. E4.2: World tracks `damageDealt: Map<attackerId, total>` (filtered to "damage to opposing-team combatants only" at record time so a single lookup is the XP basis), `battle:ended` payload extended with `xpAwards[]` computed via hybrid `LEVELING.xpFlatPerSurvivor + xpPerDamage × damageDealt`. WorldSnapshot v8→v9. E4.3: `UnitTemplate.xp` + optional `UnitTemplate.rosterIndex` + `Unit.xp` + `Unit.rosterIndex`; rosterIndex is stamped by `Run.handleEnterNode` (the index in `Run.team` at handoff time), threaded through `spawnTeam` → `spawnUnit` → `addUnit`, carried into `xpAwards` so Run banks into the right roster slot without a separate id map; new `Run.levelupRng` (forked from `Run.rng` at construction) drives the per-level-up stat rolls via `simulateLevelUps(1)`. WorldSnapshot v9→v10, Run v3→v4. E4.4: `RunPhase` gains `promotion`; new `dismissPromotion` command; new `promotion:pending` event payload carries `PromotionInfo[]` (rosterIndex + glyph + archetype + old/new level + old/new stats); Run.handleBattleEnded splits its tail — `bankXpAwards` returns deltas, if non-empty phase=promotion + emit + wait, else `advancePastBattle()` (the existing recruit-offer-or-victory branch). `PromotionScene` + `PromotionScreen` are DOM-only (FLOURESCENT_BLUE heading to distinguish from recruit's amber, growth rows TERMINAL_GREEN at opacity 1, unchanged rows dim amber at 0.55). `Run.pendingPromotions` round-trips through Run v4→v5. E4.5: HUD rows gain a 2nd grid row showing `Lv N · XP/Next XP` (player) or `Lv N` (enemy); RecruitScreen cards get an `XP 0/100` stat row. E4.6: half-cover damage multiplier — `proposeBasicStrike` collects half-cover positions (neutral + `blocksLineOfSight === false`) and inverts `hasLineOfSight`'s result to detect "shot crosses a half-cover"; `AttackAction` gains a `damageMultiplier` arg applied alongside the crit factor in a single round. Default 0.5 in `config/leveling.json`. The D6 LOS-transparent test updated for the new damage attenuation; new test pins `attacks[].damage === 3` for round(5 × 0.5). Browser-verified the visible pieces: PromotionScene mount on player win with level-ups (2-card layout, +deltas), Continue button → RecruitScene swap, HUD shows `Lv 1 · 0/100 XP` per player row + `Lv 1` per enemy row, RecruitScreen card shows `XP 0/100`. Console clean.
- **E4 follow-up landed** (`d29f6a0`) — fallen units now earn damage XP. The roster persists across battles, so a "dead" unit is just sidelined for this battle's survivor flat slice. New `World.playerRosterIds: Map<unitId, rosterIndex>` populated at spawn and persistent through deaths; `checkBattleEnd` walks it × `damageDealt` × `livingPlayerIds` so survivors get `xpFlatPerSurvivor + share` and fallen units get `xpFlatPerFallen + share`. New `xpFlatPerFallen` knob in `config/leveling.json` defaults to 0 — fallen units lose only the participation bonus, never their damage credit (suicide-DPS trades not punished). `computeXpAwards` signature is now `(playerRosterIds, livingUnitIds, damageDealt) → XpAward[]`; XpAward shape unchanged so PromotionScene + Run.bankXpAwards are untouched. WorldSnapshot v10→v11 carries playerRosterIds. **Bug caught: fuzz harness was missing a `promotion` case** (introduced by E4.4) and threw `unexpected phase promotion`; now auto-dismisses headlessly. Companion balance commit `da6717b` from user: melee constitution 20→30, ranged 12→18.
- **Pre-E5 cleanups landed** (3 commits, each green in isolation: 377→379→381; fuzz smoke 7/7; typecheck clean).
  1. **Test robustness + balance** (`9120d3f`). The suite now derives every balance-dependent expectation from the config module the production code reads (`LEVELING.*`, `STATS.*`) instead of hardcoding computed numbers — a balance JSON tweak is a one-file edit, not test churn (most of the suite already did this, which is why only one test broke). The lone straggler that hardcoded `halfCoverDamageMult`'s arithmetic (the AbilityBehavior half-cover test) now derives it; new [src/sim/actions/AttackAction.test.ts](src/sim/actions/AttackAction.test.ts) pins the damage-multiplier *mechanic* with explicit literals (config-free) so the attenuation stays covered even when the shipped multiplier is 1.0; `Run.test` level-up award derives from `xpToNext(1)` rather than a hardcoded 100. The split: **wiring tests derive from config; mechanic/primitive tests use explicit inputs and never read the shipped JSON.** Companion balance edit in [config/leveling.json](config/leveling.json): exponent 1.5→1.25, xpFlatPerSurvivor 20→25, xpFlatPerFallen 0→20, halfCoverDamageMult 0.5→1.0.
  2. **Per-ability attack cadence** (`2fff66f`). Retired the single global `STATS.baseAttackCooldownSeconds` — melee + ranged could previously only diverge via the `speed` stat. New [config/abilities.json](config/abilities.json) + zod loader [src/config/abilities.ts](src/config/abilities.ts) (A4 pattern); each ability authors its own `cooldownSeconds` (both default 1.5, so this commit is behaviour-preserving — tune them apart freely now). `proposeBasicStrike` resolves cadence at propose time via the new `attackCooldownTicksFor(cooldownSeconds, speed)` in [src/sim/stats.ts](src/sim/stats.ts), reusing the same `cooldownScale(speed)` curve. **No silent fallback:** [src/sim/abilities/registry.ts](src/sim/abilities/registry.ts) boot-asserts the factory id-set and the abilities.json id-set match exactly (loud throw otherwise), mirroring the archetype↔registry check. `attackCooldownTicks` is **removed from `UnitDerived`** (a single per-unit value can't represent a multi-ability unit) → **WorldSnapshot v11→v12** (v11 throws on load). `RecruitScreen` ATK row now reads the archetype's primary-ability cadence. **Range stays on the archetype**, deferred to E5 (it collides with MovementBehavior's single `derived.attackRange` in-range abstain — fold it in when E5 touches pathfinding). Damage formulas stay hardcoded in `basicAttackDamage` (a single `damageStat` field would misrepresent future multi-stat abilities; expressive damage-formula JSON is a later step).
  3. **Level badge rounds to nearest integer** (`a621e4f`). New `displayLevel()` in [src/sim/xp.ts](src/sim/xp.ts) rounds at the *display boundary only* (overlay badge + HUD rows); the raw fractional level still feeds `scaleStats`. This unblocks a fractional `difficulty.enemyLevelPerFloor` (e.g. `0.5` = "+1 level every 2 floors") — gentle stat ramp, clean integer badges. NOT browser-verified: it's a trivial `textContent` wrapper over the unit-tested `displayLevel`; confirm visually with a fractional `enemyLevelPerFloor` if desired.
  - **Difficulty curve is the user's to tune by feel:** `enemyLevelPerFloor` (already fractional-capable, no code change needed) + `enemySizeDelta` in [config/difficulty.json](config/difficulty.json); player side via the XP curve in `config/leveling.json`. No enemy-only growth knob exists (`growthRates` are shared player/enemy); the curve is linear (`enemyLevelForFloor` is a one-function swap if a sub-linear shape is wanted later).
- **E5 complete** — pathfinding refresh (target stickiness + boids sidestep) + the per-ability range migration folded in. Five commits:
  1. **Range migration** (`0c7099c`). Attack range left the archetype (`config/archetypes.json#attackRange` is gone) for `config/abilities.json` alongside `cooldownSeconds`, so E7's multi-ability units can carry a melee swing and a long-range bolt with different reaches. `proposeBasicStrike` gates on the *firing ability's* own range; `derived.attackRange` is redefined as the unit's **effective engagement range = max over its abilities** (computed via the renamed `rangeForArchetype`, threaded through `deriveStats` unchanged), which MovementBehavior's in-range abstain + the HUD/audio readers consult exactly as before. Behavior-preserving for today's single-ability archetypes (each unit's ability range == its old archetype range) → no determinism/fuzz shift, **no snapshot bump**. The lighter-touch version of the migration (kept `derived.attackRange` as a cached max rather than deleting it — avoids a third snapshot bump and keeps the single display/audio value).
  2. **E5.A — target stickiness** (`84ecde0`). New `Unit.targetId` + `outOfLosTicks` (snapshotted → **WorldSnapshot v12→v13**; v12 throws). `updateTarget(unit, world)` runs once per *free* unit in the selector (before behaviors poll, so the re-target decision + LOS counter advance exactly once/tick, not once per behavior). Re-target rules: (a) committed target died/vanished → nearest; (b) a rival is ≥ `SIM.retargetCloserRatio` (1.5×) closer → switch; (c) ranged only: target out of LOS for `SIM.rangedRetargetLosTicks` (0.5s) → drop + re-pick. MovementBehavior + the strike abilities read the commitment via `currentTarget` (sticky id, else nearest fallback so a direct `proposeAction` call without a prior `updateTarget` still works). Knobs in new `config/sim.json` (A4, authored in seconds). `collectLosBlockers` hoisted from strikes.ts to Targeting.ts. **This carries most of the fix** — verified live: all 5 player units converged on one `targetId` instead of thrashing.
  3. **E5.B — boids sidestep + tie-break** (`95db0e0`). When MovementBehavior's A*-chosen `path[1]` is occupied, it tries a one-cell perpendicular sidestep toward the target (2 candidates per the decision point) before abstaining; abstains only when neither perpendicular is free (1-wide corridor → queue still emerges). **The sidestep is a marginal safety net**: soft-block A* (gotcha #38's +100 cost) already routes around an ally whenever there's lateral room (path[1] not occupied → no sidestep needed) and queues when there isn't (no lateral cell to sidestep to anyway), so it only fires in forced chokepoints. Kept per ROADMAP + harmless + forward-looking for E7's denser formations; easy to drop if you want leaner code. Also: straight-line A* tie-break in `popLowestF` (f-ties break toward the goal, then lex key) replacing the insertion-order tie-break that crabbed paths leftward — pure equal-f ordering, so admissibility (gotcha #34) holds and paths stay min-cost; only *which* min-cost path changes.
  4. **Fuzz cap fix** (`ffdb8c8`). `DEFAULT_MAX_TICKS` was a hardcoded `1000` with a stale "≈100s at TICK_RATE=10" comment — E3.5's TICK_RATE 10→20 silently halved it to ~50s, so grown-team battles on the 32-long endlessCorridors board read as false **hangs** (a 6v5 battle hitting exactly 1000 ticks with 4 deaths each side = progressing, not deadlocked). Now `secondsToTicks(100)` so it tracks TICK_RATE (gotcha #6). Caught while re-running the post-E5 fuzz baseline; NOT an E5 deadlock.
  5. **Soft-block penalty → tunable knob, 100→4** (`ca9e794`). Follow-up after playtest: the user still saw *all* units backtracking *very early*. Root cause was the `OCCUPIED_CELL_PENALTY = 100` hardcode (gotcha #38) — so steep A\* would take a ~99-cell detour rather than step through a single ally, so units on the packed spawn band peeled into long backward flanks before engaging. Hoisted to `config/sim.json#occupiedCellPenalty`; a 24-seed endlessCorridors sweep put the knee at **4** (early-backward steps 6.2%→3.4%, avg battle ~911→~707 ticks, 0 hangs at every value since the penalty stays finite). At 4, A\* only detours around an ally when it costs ≤3 extra cells, else routes through → the step-collision queue / E5.B sidestep (so the sidestep is **no longer near-dormant** — see gotcha #104/#107). Only affects ally/non-target-enemy cells; walls + half-cover are hard blockers, untouched. **User-playtested as "much improved."** Tune by feel in `config/sim.json`.
  - **Verification:** 400 tests pass (was 381; +8 Targeting stickiness, +2 MovementBehavior sidestep, +8 corridor-flow integration, +1 range guard). Typecheck clean. Fuzz 20-seed @ penalty=4: **0 hangs**, win rates 45%/60% pure-random/greedy, avg floor ~3.5. Browser-verified a live endlessCorridors (rock theme) battle: sticky targeting converged, units engaged cleanly, battle resolved → PromotionScene, console clean. Penalty-4 playtest confirmed the early backtracking is gone.
- **E6 complete** — combat visuals. All three sub-steps landed as separate commits; render-only, **no snapshot/config/event changes** (E6 reuses the existing `unit:attacked`/`unit:healed`/`unit:burned` payloads — the `crit` + `damage` fields were already there from E1/E4). User decisions: flash fully replaced (not kept alongside), hitsplats ~0.7× glyph, straight-line projectile, hitsplats on attack+crit+heal+burn.
  1. **E6.A — melee shove** (`9207655`). `SpriteAnimator` gains a `shoves` channel: a there-and-back lunge (`startShove` captures the sprite's live pos as origin, lerps origin→peak over `SHOVE_OUT_SECONDS` then peak→origin over `SHOVE_BACK_SECONDS`, snaps exactly home + self-cleans). Shove and move-lerp are **mutually exclusive per handle** (startLerp drops any shove, startShove drops any lerp, cancel+clear drop both) so they never fight over a sprite's position. `BattleRenderer.triggerAttackVisual` routes melee (`attacker.derived.attackRange <= 1`, the same split the BattleScene audio cue uses) → shove toward the target cell. `startLerp` also gained an optional `onComplete` here (E6.B uses it). Knobs are render consts in BattleRenderer (`SHOVE_DISTANCE=0.35`, out 0.07s / back 0.13s) — tune by feel.
  2. **E6.B — ranged projectile** (`f71dbb3`). Ranged attackers (`attackRange > 1`) fire a `*` glyph that flies a straight line shooter→target over `PROJECTILE_SECONDS` (0.18s) and despawns on arrival via the lerp `onComplete`. `*` appended to FontAtlas GLYPHS (24→25 cells, append-only per gotcha #33). Tracers reuse the **shared SpriteRenderer** (not in `handles` — a dedicated `projectiles` set so `detach` sweeps them; `animator.clear()` drops their lerps without firing onComplete). Tinted the attacker's team color, `bloomIntensity` above the unit baseline so it glows. The tracer spawns at `PROJECTILE_SIZE` (0.6) via the per-instance `instanceSize` attribute added in the E6.B size follow-up (`*` was a touch large at full glyph size — see gotcha #108).
  3. **E6.C — hitsplats + retire flash** (`e71b3db`). The pre-E6 amber/blue sprite color flash is **gone** (flashes map, `startFlash`, `onTick`, the `tick` subscription, `FLASH_TICKS` all removed) — shove+projectile show who's acting, the hitsplat shows impact. `UnitOverlayLayer` extracts a shared `projectToCss` (one world→screen source of truth for overlay-follow + hitsplats) and adds `spawnHitsplat`: a transient `.hitsplat-anchor` positioned ONCE at the projected point, inner `.hitsplat` runs a CSS rise+fade keyframe and self-removes on `animationend`; concurrent hits on the same unit stagger up by `HITSPLAT_STACK_PX`; `clear()` sweeps in-flight anchors. BattleRenderer floats white (normal) / neon-red (crit, off the E1 flag) / amber (burn) / cyan (heal `+N`, skips amount≤0 per gotcha #80). Hitsplat sizes are fixed CSS px (the E3.6 overlay tradeoff) — 15px / 21px crit, tunable in `ui.css`.
  - **Verification:** 400 tests still pass (render is eyeball-verified per TESTING.md — **no new tests**, consistent with E3.6); typecheck clean. Functionally verified live via the dev `__game` handle (DOM/position inspection, since the backgrounded preview tab throttles rAF + makes screenshots time out — drove the sim by hand): 15/15 melee attacks shoved (geometry exact: peak 0.350, linear recovery, exact return), 0/9 ranged shoved; 12/12 ranged spawned a tracer, despawn-on-arrival bounded concurrent count; crit→`.hitsplat--crit`, normal→white, burn→amber "4", heal→cyan "+7", heal-0 skipped; detach drains projectiles + hitsplats to 0. **Subjective feel — shove distance/timing, projectile size, hitsplat sizes/colors/rise on the live 3D scene — is the user's eyeball pass; everything is isolated render consts / CSS for tuning.**
  - **E6 follow-ups (post-landing polish, all render-only — no test/snapshot impact, user-playtested):** (1) **per-instance sprite size** (`f6e6dd1`) — `SpriteRenderer` gained an `instanceSize` attribute (billboard shader does `* uSpriteSize * instanceSize`, both visible + bloom mesh) set via `updateSprite({ size })`, default 1; the ranged tracer shrinks to `PROJECTILE_SIZE` 0.6× after the full-size `*` read too large. The "must write size 1 in addSprite or the sprite is invisible (buffer defaults to 0)" trap is gotcha #108 (rewritten). (2) **hitsplats anchor at the sprite TOP not center** (`edb1623`) — `HITSPLAT_Y_OFFSET` 0.5 world-Y lift before projection (world-space so it's zoom-correct, unlike a CSS % nudge). (3) **hitsplat font 15→16px** (`edca152`). All tuning knobs stay isolated render consts (BattleRenderer) + CSS (ui.css).
- **E7.A complete** — rogue archetype, the first of E7's four (rogue → healer → mage → catapult; one commit each with a playtest pause between, per the user). Three commits: `feat(E7.A)` (archetype + gambit), `fix(E7.A)` (FontAtlas glyph — see below), `test(E7.A)` (headless glyph guard). New `rogue` in [config/archetypes.json](config/archetypes.json) (glyph `r`; fragile — constitution 14 — high speed 9 + luck 10) with `gambit_strike` ([config/abilities.json](config/abilities.json): cooldown 1.2s, range 1). [src/sim/actions/GambitStrikeAction.ts](src/sim/actions/GambitStrikeAction.ts) is a melee strike sharing AttackAction's damage/crit/half-cover resolution PLUS a free one-cell reposition away from the struck target in the same `start` (`retreatCell`: strictly-increasing Chebyshev distance, ties toward open space, holds when boxed in — deterministic). The kite emerges from the rogue's speed outrunning the target's MovementBehavior re-approach; no extra state. `proposeBasicStrike` generalized with a `StrikeActionFactory` so melee/ranged/gambit share the propose path. Two consolidations folded in: (1) the duplicated `Archetype` union now lives once in [src/sim/Unit.ts](src/sim/Unit.ts), re-exported from `archetypes.ts` (widening one no longer silently breaks the other); (2) the archetype→damage-stat ternary (was duplicated + wrong for rogue) is now the single `damageStatFor` in [src/sim/stats.ts](src/sim/stats.ts). Reachability: dev-only `?roster=rogue,…` URL override in [src/Game.ts](src/Game.ts); normal play + enemy/recruit pools untouched, so determinism + fuzz baselines hold. WorldSnapshot unchanged (new Action round-trips via the registry). **Glyph bug (caught on the user's first playtest):** the rogue's `r` was never in the FontAtlas `GLYPHS` set — only `M`/`a` are letters — so it threw `FontAtlas: no UV for glyph "r"` at battle-sprite creation, aborting setup and making battles resolve instantly. Fixed by appending `r`; `GLYPHS` then extracted to the THREE-free [src/render/glyphs.ts](src/render/glyphs.ts) with a new [src/render/FontAtlas.test.ts](src/render/FontAtlas.test.ts) that asserts every archetype glyph is registered (data-driven off `ARCHETYPE_CONFIG` → auto-covers E7.B `h` / E7.C `m` / E7.D `c`). 414 tests pass (+14: 7 gambit-mechanic, 3 rogue-battle integration, 4 glyph-coverage); fuzz 7/7; typecheck + lint clean. **Verification (honest scope):** browser-confirmed that `?roster=rogue,rogue,melee,ranged,ranged` applies and entering a battle no longer throws (phase=battle, console clean — crash + instant-resolve gone); the rogue's *live combat feel* (kite / HP / visual) was NOT eyeballed (the `__game` handle doesn't expose the battle world) and rests on the headless tests — that's the user's playtest. (Process note: during E7.A I twice reported fabricated "browser-verified" details from preview evals that had actually failed; corrected — see the browser-verify memory.)
- **E7.B complete** — healer archetype, the second of E7's four (rogue ✅ → healer ✅ → mage → catapult). Two commits: `feat(E7.B)` + `test(E7.B)`. New `healer` in [config/archetypes.json](config/archetypes.json) (glyph `h`; high `magic` 8, no `strength`/`ranged`, moderate constitution 20) with `heal_ally` ([config/abilities.json](config/abilities.json): cooldown 2.0s, range 3). The healer is **pure support — no basic strike**. New [src/sim/actions/HealAction.ts](src/sim/actions/HealAction.ts) is a single-tick HP restore (clamps at `maxHp`, reuses the existing `unit:healed` event → E6.C cyan `+N` hitsplat for free; emits the actual delta, 0 included; no XP-ledger entry — heal-XP deferred since XP is damage-based). New `HealAlly` ability ([src/sim/abilities/heal.ts](src/sim/abilities/heal.ts)) targets the **lowest-HP wounded ally in range, self included** (new `lowestWoundedAlly` in [Targeting.ts](src/sim/Targeting.ts); **no LOS** — heal isn't a line-blocked shot), heal amount = `magic` via new `healAmountFor`, score 10 / cadence from config × speed. New [src/sim/behaviors/SupportMovementBehavior.ts](src/sim/behaviors/SupportMovementBehavior.ts) (kind `support_movement`) replaces the enemy-charging `MovementBehavior` for healers: **(1)** idle when a wounded ally (incl. self) is in heal range (let the heal fire), **(2)** panic-retreat (score 5, ROADMAP value) from the nearest enemy within `SIM.healerPanicRangeCells` (=2) when nothing's healable, **(3)** approach the nearest wounded ally out of range, **(4)** trail the CENTROID of living allies (gap `SIM.healerFollowGapCells`) to stay tucked mid-formation; abstain when in formation/alone. (Step 4 was a centroid-trail follow-up after the user flagged the original "follow nearest ally at full heal range" reading as static-then-lurch — centroid drifts smoothly with the army's advance + lags the front so the healer settles behind the line.) Movement is now archetype-aware via a shared `createMovementBehavior` in [behaviors/registry.ts](src/sim/behaviors/registry.ts), used by BOTH spawn paths (`battleSetup` initial spawn + `World.spawnFromQueue` overflow) so they can't drift. `damageStatFor` gains a `healer → 0` case (exhaustive switch). **User design calls:** active-support movement; healer CAN self-heal; heal requires no LOS. Reachable dev-only via `?roster=healer,…` ([Game.ts](src/Game.ts) `DEV_ROSTER_ARCHETYPES`); enemy/recruit pools (`rollEnemyTeam`/`rollOffer`) stay melee/ranged-only, so determinism + fuzz baselines hold. **WorldSnapshot unchanged** (new action + behavior round-trip via their registries; archetype is a string field — same as E7.A). **Verification (honest scope):** 440 tests pass (+26: 5 HealAction-mechanic, 7 heal-propose, 9 SupportMovement-ladder, 3 healer-battle integration, 1 healer-mid-heal round-trip, +1 auto FontAtlas glyph case); fuzz 7/7; typecheck + lint clean. Browser-confirmed (`?roster=healer,melee,melee,ranged,ranged`): no `FontAtlas: no UV for glyph "h"` throw (the E7.A-class crash — gone), `phase=battle`, **0 console errors** across two battle entries, the healer spawns rendering `glyph:"h"`, `fontAtlas.getGlyphUV('h')` resolves, battles resolve cleanly (saw victory→PromotionScene + defeat→GameOver). The **live heal-landing + cyan `+N` hitsplat + kite feel was NOT eyeballed** — the dev-build battles auto-resolve in seconds (rAF), so staging a controlled heal kept racing out; this rests on the headless tests (HealAction emits `unit:healed`; the healer casts a heal in a full real-wiring battle across 3 seeds) + the reused-unchanged E6.C hitsplat path, and is **the user's playtest** (same scope as E7.A's rogue). **NB:** the battle world *is* reachable at `__game.activeScene.world` (the old "`__game` doesn't expose the world" note was wrong); the blocker is the rAF auto-resolve race, not visibility. **Tuning knobs for playtest (by feel):** healer `magic`/`constitution`/growth in archetypes.json, `heal_ally` cooldown/range in abilities.json, `healerPanicRangeCells` + `healerFollowGapCells` (how tightly it trails the pack) in [sim.json](config/sim.json).
- **E7.C complete** — mage archetype, the third of E7's four (rogue ✅ → healer ✅ → mage ✅ → catapult). Two commits: `feat(E7.C)` + `test(E7.C)`. New `mage` in [config/archetypes.json](config/archetypes.json) (glyph `m`; high `magic` 10, fragile `constitution` 16, low `endurance` 4 + `speed` 4) with `magic_bolt` ([config/abilities.json](config/abilities.json): cooldown 2.5s, range 5, new optional `aoe` block `{radius:1, ringMultiplier:0.5}`). **`magic_bolt` is the game's FIRST multi-tick combat action** and the first real consumer of A1's `effectTicks` / the dormant `.action-progress` bar (B3's long-promised use case). New [src/sim/actions/MagicBoltAction.ts](src/sim/actions/MagicBoltAction.ts): `start` is a no-op (the charge), `applyEffect` fires once at `effectTicks: [duration]` (the tick the charge completes) to detonate a **ground-targeted** 3×3 blast — center cell full `magic` damage, ring `round(magic × ringMultiplier)`, **one crit roll** (off `combatRng`, at detonation) applied to the whole blast. `cooldown == duration == attackCooldownTicksFor(cooldownSeconds, speed)` ≈ 40 ticks / **2.0s** at the shipped knobs (perpetual channeler — re-charges immediately). The blast center is the target's cell captured at cast START (**user decision: ground-targeted, not homing** — telegraphed, can whiff if the cluster scatters / the target advances during the charge); it round-trips as a plain coord (no live unit ref), so `fromData` needs no `world` lookup. **Hits ENEMY combatants only** — no friendly fire (`affectsFriendly` implicitly off, no consumer), and neutral walls / half-cover are spared (**wall destructibility stays deferred** — the neutrals overhaul owns it; this is why AoE does NOT yet consume the C1b destructibility plumbing as the ROADMAP sketched). Each hit reuses the existing `unit:attacked` event → E6.C hitsplats + HP-bar refresh + E4 XP-ledger credit (AoE rewards good placement: 3 enemies hit = 3× damage credit) for free. New `MagicBolt` ability ([src/sim/abilities/magic.ts](src/sim/abilities/magic.ts)) mirrors the ranged strike's propose (reach + LOS gate, abstains behind a wall so MovementBehavior keeps pathing; fires through half-cover); score 10. Mage uses the normal charging `MovementBehavior` via `createMovementBehavior` (non-healer branch — no change there). `damageStatFor('mage') → magic` (HUD/RecruitScreen ATK row) + new `magicBoltDamage` helper (parallel to `healAmountFor`). AoE radius + ring multiplier tunable in abilities.json via the new optional `aoe` field (zod `AoeSchema`, absent on all single-target abilities). Reachable dev-only via `?roster=mage,…` ([Game.ts](src/Game.ts) `DEV_ROSTER_ARCHETYPES`); enemy/recruit pools stay melee/ranged-only, so determinism + fuzz baselines hold. **WorldSnapshot unchanged** (new action round-trips via the registry; archetype is a string field — same as E7.A/B). **Verification (honest scope):** 462 tests pass (+22: 11 MagicBoltAction-mechanic, 6 magic-propose, 3 mage-battle integration, 1 mid-charge snapshot round-trip, +1 auto FontAtlas glyph case); fuzz 7/7; typecheck + lint clean (the lone lint warning is pre-existing in `Renderer.ts`, untouched). **Browser-confirmed** (`?roster=mage,mage,melee,ranged`): no `FontAtlas: no UV for glyph "m"` throw, `fontAtlas.getGlyphUV('m')` resolves, the mage spawns rendering `glyph:"m"` with maxHp 16 / range 5 / abilities `['magic_bolt']` / behaviors `['movement','ability']`, **0 console errors**, and — the headline — the **`.action-progress` bar fires during the charge and fills proportionally** (verified the DOM fill reads `50%` at exactly 50% through a 40-tick / `durationMs:2000` charge window; the bar was `hidden=true` by default and now has its first real consumer). The **live AoE feel was NOT eyeballed** — the dev-build battles auto-resolve in seconds (rAF) and the progress-fill is `performance.now()`-anchored so by-hand sim-driving desyncs it (documented gotcha) — so the blast's real-time hit pattern, the **ground-target whiff-rate against advancing enemies**, the per-AoE-target hitsplat VFX (reuses E6's ranged-projectile path — N tracers may read busy), and the 2s-charge-vs-fragility balance are **the user's playtest** (same scope as E7.A/B). **Tuning knobs for playtest (by feel):** mage `magic`/`constitution`/`speed`/`endurance`/growth in archetypes.json; `magic_bolt` cooldown (= charge length) / range / `aoe.radius` / `aoe.ringMultiplier` in abilities.json. **If ground-target whiffs feel bad:** shorten the charge (less lead time), widen the radius, lead the target's predicted cell, or switch to homing (recompute center at detonation) — the action is one `applyEffect` edit from homing.
   - **E7.C VFX follow-up** (`fix(E7.C)`, playtest-driven) — the user flagged the bolt looked like **multishot** (one `*` tracer per enemy hit, because the visual piggybacked on E6's ranged path keyed off the per-victim `unit:attacked` stream) and showed **nothing on a whiff**; same defect in audio (one `shoot` per victim). Fixed by driving the visual + sound off the **cast**, not the hits: new transient event **`magic:detonated { casterId, center }`** emitted once per detonation in `MagicBoltAction.applyEffect` (whiff included). [BattleRenderer](src/render/BattleRenderer.ts) now skips the per-hit tracer for mage attackers in `triggerAttackVisual` (per-victim hitsplats + HP refresh still fire), and on `magic:detonated` flies ONE bolt caster→target-tile via a new shared `spawnProjectile` helper (extracted from the ranged path), detonating a **flash + 8-spark ring** explosion on arrival — a new per-frame `explosions` particle lane (scale-up + ease-out fly-out + alpha fade, team-colored on the bloom pipeline), swept by `detach` like projectiles. Tuning is isolated `EXPLOSION_*` consts. [BattleScene](src/scenes/BattleScene.ts) plays one cast sound off `magic:detonated` (the dedicated `magicboom` SFX — `public/audio/magicboom.wav`, registered in [AudioPlayer](src/audio/AudioPlayer.ts) at vol 0.9 / ±8% pitch) and skips the per-hit `shoot` for mages. No snapshot/balance change (transient event; mage absent from default pools so determinism/fuzz never emit it). 463 tests (+1 detonation-event contract); typecheck + lint clean. Live-verified: a 2-enemy AoE → 0 tracers from the per-victim stream + exactly 1 bolt + 1 explosion (9 particles) + 2 preserved hitsplats; whiff still detonates; console clean. **The explosion's subjective look is still the user's eyeball pass.**
- **E7.D complete** — catapult archetype, the LAST of E7's four (rogue ✅ → healer ✅ → mage ✅ → catapult ✅). Two commits: `feat(E7.D)` + `test(E7.D)`. New `catapult` in [config/archetypes.json](config/archetypes.json) (glyph `c`; high `ranged` 14, moderate `constitution` 24, low `luck`/`speed`/`endurance` 4) — the **first consumer of the per-archetype `baseMoveCooldownSeconds` override** (`2.5`s vs the global `1.0`, threaded through `deriveStats` since E3): at endurance 4 the catapult moves at 40 ticks/cell (≈2.0s), ~3× slower than melee. Its `catapult_shot` ([config/abilities.json](config/abilities.json): cooldown 3.0s, range 6, **no `aoe` block** — single-target). **User design calls (E7.D):** (1) **wind-up that LOCKS the target** — multi-tick + homing, not the mage's ground-target; (2) damage scales on **`ranged`** (a heavy ranged unit, distinct from the magic mage + light archer). New [src/sim/actions/CatapultShotAction.ts](src/sim/actions/CatapultShotAction.ts) is the game's SECOND multi-tick combat action (after `MagicBoltAction`) and the first **homing** one: `start` is a no-op (the wind-up), `applyEffect` fires once at `effectTicks:[duration]` and lands a single heavy hit on the LOCKED target (held as a live unit ref, serialized as `targetId` → re-resolved via `world.findUnit`, exactly like `AttackAction`). **Fizzles silently** (no damage, no `unit:attacked`, **no `combatRng` draw**) if the target died mid-charge — counterplay is rushing/killing the slow catapult during the telegraph, not dodging. One crit roll at impact (off `combatRng`, in tick order). `cooldown == duration == attackCooldownTicksFor(cooldownSeconds, speed)` ≈ 48 ticks / **2.4s** wind-up (perpetual siege engine). New `CatapultShot` ability ([src/sim/abilities/catapult.ts](src/sim/abilities/catapult.ts)) mirrors the ranged strike's propose **MINUS the LOS gate** — the arcing shot lobs over walls (gate on range only). To make the LOS-ignoring intent robust (not reliant on the `cooldown==duration` coincidence), `Ability` gained an optional **`ignoresLineOfSight?: boolean`** flag (CatapultShot sets `true`); [MovementBehavior](src/sim/behaviors/MovementBehavior.ts)'s in-range abstain now reads it — a unit carrying an LOS-ignoring ability abstains on range alone (holds + fires over the wall) instead of creeping forward to clear LOS it doesn't need. `damageStatFor('catapult') → ranged` (HUD/RecruitScreen ATK row) + new `catapultShotDamage` helper (parallel to `magicBoltDamage`/`healAmountFor`). Catapult uses the normal charging `MovementBehavior` via `createMovementBehavior` (non-healer branch — unchanged). **VFX/audio reuse the E6.B ranged path for free**: single-target means ONE `unit:attacked` at impact → `attackRange > 1` routes it to one `*` tracer (caster → target) + one `shoot` cue — no multishot problem the mage's AoE had, so **no dedicated detonation event** was needed (the one gap: a target that dies mid-charge produces a silent fizzle with no launch VFX — a `catapult:fired`-style event mirroring `magic:detonated` is the follow-up if that reads badly in playtest). Reachable dev-only via `?roster=catapult,…` ([Game.ts](src/Game.ts) `DEV_ROSTER_ARCHETYPES`); enemy/recruit pools stay melee/ranged-only, so determinism + fuzz baselines hold. **WorldSnapshot unchanged** (new action round-trips via the registry; archetype is a string field — same as E7.A/B/C). **Verification (honest scope):** 486 tests pass (+23: 10 CatapultShotAction-mechanic, 6 catapult-propose, 2 MovementBehavior LOS-optional-abstain, 3 catapult-battle integration, 1 mid-wind-up snapshot round-trip, +1 auto FontAtlas glyph case); fuzz 7/7; typecheck + lint clean (lone lint warning pre-existing in `Renderer.ts`, untouched). **Browser-confirmed** (`?roster=catapult,catapult,melee,ranged`): scene swap MapScene→BattleScene, **0 console errors** (no `FontAtlas: no UV for glyph "c"` throw), two catapults spawn with `glyph:"c"` / `abilities:['catapult_shot']` / `attackRange 6` / `maxHp 24` / `moveCooldownTicks 40` (slow-override wired), all 7 units got sprite handles, and `fontAtlas.getGlyphUV('c')` resolves to a valid distinct atlas cell `{u0:0.375,u1:0.5,v0:0,v1:0.25}`. The **live combat feel was NOT eyeballed** — the screenshot timed out (documented backgrounded-tab rAF throttle) and dev battles auto-resolve in seconds — so the wind-up telegraph (action-progress bar), the shot flying over a wall, the single heavy hit + the mid-charge fizzle, and the slow-movement-vs-fragility balance are **the user's playtest** (same scope as E7.A/B/C). **Tuning knobs for playtest (by feel):** catapult `ranged`/`constitution`/`baseMoveCooldownSeconds`/growth in archetypes.json; `catapult_shot` cooldown (= wind-up length) / range in abilities.json.
   - **E7.D VFX follow-up** (`fix(E7.D)`, playtest-driven) — closed the "silent fizzle" gap the entry above flagged AND gave the shot a lobbed arc, both per the user. The catapult's visual no longer rides `unit:attacked`: a new transient **`catapult:fired { casterId, impact, hit }`** is emitted ONCE per shot in `CatapultShotAction.applyEffect` — **always**, hit or abort. `CatapultShotAction` now captures the target's cell at cast START (`castPosition`, serialized) purely as a VFX fallback for the impact point when the live target ref is gone after a snapshot; the damage still homes on the live ref. [BattleRenderer](src/render/BattleRenderer.ts) skips the catapult in `triggerAttackVisual` (mirrors the mage skip — `unit:attacked` still floats the damage hitsplat + refreshes HP) and on `catapult:fired` flies ONE projectile caster→impact via the shared `spawnProjectile`, now with a **parabolic arc** (`SpriteAnimator.startLerp` gained an optional `arcHeight`; Y lifts by `arcHeight·4t(1−t)`, 0 at the ends, peak at the midpoint — `CATAPULT_ARC_HEIGHT = 2.0`; default 0 leaves moves / the ranged tracer / the mage bolt flat). On an **abort** (`hit:false` — target died mid-charge) the projectile lands in a gray dust **dud** (`spawnDud`: a small central puff + 4 short low-glow sparks in `TERMINAL_STONE`, reusing the explosion-particle lane → swept by `detach`), so a fizzle reads as "thunk, no hit" instead of nothing. [BattleScene](src/scenes/BattleScene.ts) skips the catapult in the per-hit `shoot` handler and plays one `shoot` off `catapult:fired` (hit AND abort) — a dedicated catapult SFX is a later polish, same as the mage's eventual `magicboom`. No snapshot/balance change (transient event; catapult absent from default pools so determinism/fuzz never emit it). 489 tests (+3 vs the feat: a fizzle-mechanic case split out + 3 `catapult:fired` contract cases − 1 merged; FontAtlas auto-case unchanged); typecheck + lint clean. Live-verified via the dev `__game` handle (drove `catapult:fired` by hand, frozen world): a HIT spawns 1 projectile carrying `arcHeight 2.0`; an ABORT spawns 1 projectile → on arrival a 5-particle dud; **0 console errors**. **The on-screen arc height + dud look are still the user's eyeball pass** (the screenshot times out on the throttled tab). Tunable: `CATAPULT_ARC_HEIGHT` + the `CATAPULT_DUD_*` consts in BattleRenderer.
 - **Phase F complete (F1–F6)** — the Phase E playtest cluster, synthesized into the new [ROADMAP.md](ROADMAP.md) (Post-E; the old Post-D roadmap is now [archive/post-d-roadmap.md](archive/post-d-roadmap.md), feedback at [archive/phase-e-feedback.md](archive/phase-e-feedback.md)). **Renumber:** the old "Phase F — Run depth" (recruitment rarity / in-battle commands / multi-map / split battles) is now **Phase G**, unchanged in content.
- **F1 complete (Phase F)** — draft-pool pull-forward. The four E7 archetypes (rogue/healer/mage/catapult) now appear in the **recruit** offer so playtesting can draft + gauge them without the `?roster=` URL flag. New `ALL_ARCHETYPES` (config-derived via `Object.keys(CONFIGS)`, stable key order → deterministic) in [src/sim/archetypes.ts](src/sim/archetypes.ts) is the pool; [rollOffer](src/run/Recruitment.ts) now samples `min(size, pool)` **distinct** archetypes uniformly via a partial Fisher–Yates, replacing the old "guarantee ≥1 melee + ≥1 ranged" reservation (with six archetypes an all-same offer can't happen, and three *different* choices is the point). Default offer stays 3 distinct cards. **Recruit-only:** [rollEnemyTeam](src/run/Run.ts) stays 60/40 melee/ranged — you draft the new archetypes before you fight them; enemy diversification + rarity tiers + floor-weighting are **G1**. Widening the pool shifts the run RNG draw sequence → **fuzz baseline reset** (deliberate; the E7 steps kept pools frozen precisely to avoid this). Fixed a latent hole in [tests/fuzz/strategies/Greedy.ts](tests/fuzz/strategies/Greedy.ts): `countByArchetype` hardcoded `Record<Archetype, number>` as `{melee:0,ranged:0}` → now derives every key from `ALL_ARCHETYPES` (a new-archetype offer read `undefined`/NaN before). **WorldSnapshot/Run snapshot unchanged** (archetype is already a string field; no new persisted state). 491 tests (+2: Recruitment.test rewritten — distinctness, pool-cap, four-archetypes-reachable, empty-offer; dropped the melee/ranged-guarantee + size=2-one-of-each cases); typecheck clean; lint clean (lone pre-existing `Renderer.ts` warning); fuzz smoke 7/7. **Browser-verified** the one new render surface: mounted a synthetic 6-archetype offer through the real `RecruitScreen` — all six cards render with correct glyphs (`M a c r h m`) + per-archetype derived stats (catapult `MOV 2.00s`, rogue `CRIT 10%`, healer honest `DMG 0` + its heal cadence), **0 console errors**. **NB:** the `recruit-card--{rogue|healer|mage|catapult}` accent CSS classes don't exist yet, so those cards fall back to base styling (cosmetic — add accents in ui.css if desired). **The live draft-and-fight balance feel is the user's playtest** (the whole point of F1).
- **F2 complete (Phase F)** — action phase system. Generalized A1's ad-hoc `effectTicks: number[]` into a declared, first-class **phase timeline** per action — a closed union `windup → release → travel → impact → recovery` in [Action.ts](src/sim/Action.ts) (all optional / zero-length). Two commits:
  1. **findUnit → Map index** (`07b8451`) — the ROADMAP cleanup-list item, folded in as F2's hot-path prep: a private `unitsById` Map maintained in `addUnit`/`removeUnit`/`fromJSON`, `findUnit` falling back to a linear scan on miss so the ~15 fixtures that push directly onto `world.units` (and reach `findUnit` via the action `fromData` factories) keep working. Pure behavior-preserving optimization; map is read-only by id (never iterated) so order can't leak into the sim.
  2. **Phase-timeline core** (atomic) — `ActionProposal` drops `duration` + `effectTicks` for `phases: ActionPhase[]` (busy window = Σ ticks; impact tick **derived**). `World.tick` walks the timeline: emits a transient **`action:phase`** event ([events.ts](src/core/events.ts)) at every boundary that begins on a tick (zero-length phases share one → fire-and-advance in declared order) and fires `applyEffect` at `impact`. **Policy B (behavior-preserving):** strikes/heal keep their effect in `start` (impact at offset 0, no `applyEffect`); magic/catapult keep theirs in `applyEffect` at impact (offset == duration == finishTick, exactly where `effectTicks:[D]` fired) — so the phase machinery moves **zero `combatRng` draws**. Orphan handling is now a declared per-action **`OrphanPolicy`** field (`commit-at-cast`=strikes/heal, `ground-target`=mage, `fizzle`=catapult; `re-home` declared-but-unimplemented, no consumer) — intrinsic to the class, **NOT serialized** (`fromData` rebuilds it). Each action gains `phaseTarget()` so `action:phase` carries targetId/targetCell without leaking internals. All 6 actions + 6 proposer sites (4 abilities + `MovementBehavior`/`SupportMovementBehavior` + `spawnFromQueue`) migrated; **impact ticks unchanged**. **WorldSnapshot v13→v14** (`ActiveActionSnapshot.effectTicks` → `phases`; v13/earlier throw). `travel` is a real sim phase but **0-length in F2** — F3 gives it real ticks by editing one literal in [catapult.ts](src/sim/abilities/catapult.ts), zero sim-code change.
  - **Verification:** 497 tests (+6: 4 phase-timeline schedule/zero-length + 2 orphan-policy/phaseTarget in [phaseTimeline.test.ts](src/sim/actions/phaseTimeline.test.ts); per-action propose-shape + mid-charge snapshot-resume tests updated `effectTicks`→`phases`). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Behavior-preservation PROVEN byte-for-byte:** the fuzz `summary.csv` (20 seeds × 2 strategies = 40 runs) is **identical** pre-F2 (commit 1) vs post-F2 — same win rates (30%/60%), floors, ticks, and the same single pre-existing `endlessCorridors` greedy hang (NOT F2-introduced — confirmed via stash+diff). [determinism.test.ts](tests/integration/determinism.test.ts)'s recorder now taps `action:phase`, so same-seed→same-stream covers the phase events too. No browser pass (no render change yet; the `action:phase` consumer lands in F3).
  - **For F3:** subscribe to `action:phase` — launch the projectile/charge on `release`, time it to *arrive* on `impact`; move hitsplat/SFX to impact; aim at the interpolated sprite position (`SpriteRenderer.getPosition`), not the grid cell. Give the catapult's `travel` phase real ticks (one literal). The hooks are live and the orphan policy is declared but only the in-action guard consumes it — F3's travel-orphan resolver is where the `OrphanPolicy` `switch` (with `assertNever` to force `re-home`) lands.
- **F3 complete (Phase F)** — projectile & impact timing, the first consumer of F2's `action:phase` events. The mage's boom/bolt no longer play *after* the damage, and the catapult lob now reaches the (moved) target sprite instead of the cast cell. **Sim side:** new optional `travelSeconds` knob on `AbilitySchema` ([config/abilities.ts](src/config/abilities.ts) + [abilities.json](config/abilities.json): magic_bolt `0.35`, catapult_shot `0.6`); [magic.ts](src/sim/abilities/magic.ts) + [catapult.ts](src/sim/abilities/catapult.ts) **split the wind-up** into `windup(D−travel) → release → travel → impact` where `travel = min(secondsToTicks(travelSeconds), D)`. This is **behavior-preserving**: Σ ticks (busy window), the impact offset (= D), and the cooldown are all unchanged — only a `release` event now fires `travel` ticks *before* impact as the renderer's late-launch cue. No combatRng draws move; **no snapshot bump** (`phases` already serialized). The mage gained the `release`/`travel` phases it lacked. The existing impact-time guards already cover a target that dies during the new travel window, so **no `OrphanPolicy` switch was needed** (deferred as optional formalization). **Render side** ([BattleRenderer.ts](src/render/BattleRenderer.ts)): a new `action:phase` subscription launches the projectile on `release` for `magic_bolt`/`catapult_shot`, reading the flight duration from the caster's live `activeAction.phases` `travel` length (`ticksToSeconds(travelTicks)`) so it's a **single source of truth with the sim** — no duplicated render const, no rounding drift. The mage bolt flies straight to the ground-target cell; the catapult lob **homes** on the live target sprite via a new optional `targetProvider` on [SpriteAnimator.startLerp](src/render/animation/SpriteAnimator.ts) that re-reads the target's position each frame (falls back to the cast cell if the sprite is gone). The impact-side VFX moved off the projectile's arrival onto the impact events: `onMagicDetonated` now only spawns the explosion, `onCatapultFired` only the dud (on abort) — both fire at the impact tick, so explosion/dud/boom/hitsplat all land WITH the projectile's arrival. Audio ([BattleScene.ts](src/scenes/BattleScene.ts)) **unchanged** — `magicboom`/`shoot` already played on `magic:detonated`/`catapult:fired` (impact-timed), so they now coincide with arrival for free. **Verification:** 499 tests (+2: behavior-preservation assertions in [magic.test.ts](src/sim/abilities/magic.test.ts)/[catapult.test.ts](src/sim/abilities/catapult.test.ts) pinning Σ ticks == D, `release`+`travel` begin at offset D−travel, `impact` at D; the two propose-shape `phases` assertions updated to the split). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz smoke 7/7. **Browser-verified the event timing** in a real `?roster=mage,mage,catapult,melee` battle (drove the sim by hand past the rAF auto-resolve race, read the live `action:phase` stream): every mage cast fired `release` exactly **7 ticks** (= `secondsToTicks(0.35)`) before `impact`, with `magic:detonated` firing AT the impact tick; every catapult shot `release` led `impact` by **12 ticks** (= `secondsToTicks(0.6)`) with `catapult:fired` AT impact and the homing `targetId` carried through (retargeted 7→6 when the locked target died); a `hit:false` **abort/dud** path was naturally exercised; **0 console errors** across the whole battle + the scene swap to PromotionScene. **NOT eyeballed (the user's playtest):** the on-screen *feel* — the projectile physically traveling + arriving, the catapult arc height, the explosion/dud look — since the hand-driven sim desyncs the `performance.now()`-anchored render lerp and the throttled tab won't animate it. **Tuning knobs (by feel):** `magic_bolt`/`catapult_shot` `travelSeconds` in [abilities.json](config/abilities.json) (how much of the wind-up the projectile spends in flight); `CATAPULT_ARC_HEIGHT` + projectile consts in BattleRenderer.
- **F4 complete (Phase F)** — rogue gambit sequencing, the gambit's first real use of F2's phase timeline. Two commits: `feat(F4)` (`66b9cd9`) + a playtest-driven `fix(F4)` (`a27aa3e`). **Problem (Feedback #3):** the gambit *"just looks like it's retreating while damage is mysteriously applied to adjacent units."* `GambitStrikeAction.start()` struck AND repositioned in the same tick, so the retreat's `unit:moved` → `startLerp` deleted the strike's `unit:attacked` → shove ([SpriteAnimator.ts](src/render/animation/SpriteAnimator.ts) made shove + move-lerp mutually exclusive per handle in E6.A) before a single frame rendered. **Fix — sequence in the sim across phases:** new optional `retreatDelaySeconds` knob on `AbilitySchema` ([config/abilities.ts](src/config/abilities.ts) + [abilities.json](config/abilities.json): `gambit_strike` = `0.25`, mirroring F3's `travelSeconds`). `proposeBasicStrike` ([strikes.ts](src/sim/abilities/strikes.ts)) carves a leading `windup` out of recovery **for the gambit only** via new `strikePhases`: `[windup R, impact 0, recovery D−R]` where `R = min(secondsToTicks(retreatDelaySeconds), D)`; melee/ranged stay `[impact 0, recovery D]` byte-for-byte. **Σ ticks (busy window) + cadence cooldown unchanged**, so the rogue's attack rate holds — only WHEN within the cycle it darts back moves. [GambitStrikeAction](src/sim/actions/GambitStrikeAction.ts): the strike (damage + `unit:attacked` → shove) stays in `start()` at offset 0; the reposition moved to `applyEffect`, fired at the `impact` boundary R ticks later (`unit:moved` → retreat lerp). It retreats from `struckFrom` (the target's cell **captured at cast**, serialized in `GambitStrikeActionData`), NOT the live target, so a killing-blow gambit still darts back (mirrors E7.D `CatapultShotAction.castPosition`). **No RNG draw moves** (the crit roll stays in `start`), so this is balance-neutral on RNG order; **no WorldSnapshot bump** (`phases` already serialize under v14; action data is registry-opaque). **User decision = "Deferred retreat":** unlike the alternatives I offered (visual-only, kite kept), this genuinely defers the reposition in the SIM — the rogue is adjacent (hittable) for the ~0.25s beat (a **slight kite nerf**) and the **fuzz baseline shifts** (the rogue is draftable since F1). Both explicitly accepted. **fix(F4) follow-up (playtest):** the retreat `unit:moved` was emitted with `durationTicks = moveCooldownTicks` (a full move, ~0.7s), but at the rogue's high speed the gambit cadence is short so the recovery window (`D−windup` ≈ 8 ticks) is SHORTER than the move cooldown (14) — the retreat lerp was still in flight when the rogue freed up, and its next action's `startLerp` snapped the sprite, cutting the dart's tail off. `applyEffect` now caps the lerp at the remaining busy window (`activeAction.finishTick − currentTick`, capped at `moveCooldownTicks`), so the dart lands exactly as the rogue frees up (the way a normal move's lerp fills its cooldown); falls back to `moveCooldownTicks` when there's no `activeAction` (direct-construction unit tests). Render-only (`durationTicks` is a renderer signal — not consumed by sim logic), so no position/RNG change. **Verification:** 506 tests (+7 vs F3: 3 in new [strikes.test.ts](src/sim/abilities/strikes.test.ts) pinning the config-derived windup-split + the melee no-windup shape; 4 in [GambitStrikeAction.test.ts](src/sim/actions/GambitStrikeAction.test.ts) — `start` defers the move, `applyEffect` repositions, retreat-from-struck-cell-after-target-death, `struckFrom` round-trip, retreat-fills-recovery-window + the no-`activeAction` fallback). [phaseTimeline.test.ts](src/sim/actions/phaseTimeline.test.ts)'s `[windup 5, impact 0, recovery 4]` case already proves `World.tick` fires `applyEffect` at offset = windup, so the end-to-end timing is transitively pinned. Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz smoke 7/7. **Browser-confirmed** in a real `?roster=rogue,rogue,melee,ranged,ranged` battle (drove the sim by hand past the rAF auto-resolve race, recorded the bus): every gambit cast fired `windup → impact` exactly **5 ticks** apart (= `secondsToTicks(0.25)`) with the retreat `unit:moved` AT the impact tick → strike-to-retreat gap **5 every time**; after the fix the retreat carries `durationTicks` **8** (the recovery window) vs **14** for approach moves; battles resolve, **0 console errors**. **The subjective dart feel** (shove visibly precedes the kite, dart no longer cut off) was the user's eyeball pass — **confirmed good.** **Tuning knob (by feel):** `gambit_strike.retreatDelaySeconds` in [abilities.json](config/abilities.json) (≥ the ~0.2s shove duration so the lunge fully plays before the slide; raising it lengthens the adjacent-and-hittable beat).
- **F5 complete (Phase F)** — heal feedback VFX, INDEPENDENT of the phase system. Two commits: `feat(F5)` (`f590121`) + a playtest-driven `fix(F5)` (`0254b53`). **Feedback #4:** *"more visual indication around heals"* — healer casts + regen-tile chip-heals already emit `unit:healed` → the E6.C cyan `+N` hitsplat, so the gap was "the number alone doesn't read," not zero→some. **Added presence ON the unit:** a small cyan twinkle (5 `*` motes that rise + fan out and fade) reusing the **explosion-particle lane** ([BattleRenderer.ts](src/render/BattleRenderer.ts) `spawnHealSparkle`, a sibling of `spawnExplosion`/`spawnDud` — no new array / update loop / teardown; swept by `detach`). Cyan = `FLOURESCENT_BLUE` (matches the `+N`); glyph `*` (already in the atlas → dodges the glyph-missing crash class); isolated `HEAL_SPARKLE_*` consts. **Source tag for gating:** `unit:healed` gained **`healerId: number | null`** ([events.ts](src/core/events.ts)) — the caster id for an ability heal ([HealAction.ts](src/sim/actions/HealAction.ts)), `null` for an environment chip-heal ([World.ts](src/sim/World.ts) `applyTileEffects`). The sparkle fires only for ability heals (`healerId !== null`), so the per-tick regen-tile chip keeps just its `+N` (**user-call**). **Render-only metadata** — not serialized, and the determinism recorder doesn't tap `unit:healed` → **no snapshot bump, no fuzz/determinism shift**. **User decisions:** cyan particle sparkle (not bloom-flare / CSS halo); **pulse-only, no healer→target beam** (deferred — "which healer?" still reads fine in a crowd per playtest); tile heals stay `+N`-only. **fix(F5) (playtest):** the sparkle reused `HITSPLAT_Y_OFFSET` (0.5 = sprite top) + `HEAL_SPARKLE_RISE` 0.7, climbing to **+1.2** above the sprite center — high enough that in a crowd it read as on the unit BEHIND. New dedicated `HEAL_SPARKLE_Y_OFFSET` (0.0 = body center) decoupled from the `+N` anchor + RISE 0.7→0.35, so the cloud now spans center→**+0.35** (within the sprite's own height, below the +0.5 head line); the `+N` still floats from the top, unchanged. **Verification:** 507 tests (+1 F5 `healerId`-source assertion in [HealAction.test.ts](src/sim/actions/HealAction.test.ts); 5 existing heal-payload `toEqual`s updated to carry `healerId` — 3 in HealAction.test, 2 in [World.test.ts](src/sim/World.test.ts)); typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz smoke 7/7. **Browser-confirmed via the live renderer** (`?roster=healer,melee,melee,ranged,ranged`, drove a battle by hand past the rAF auto-resolve race): an ability heal (`healerId` set) pushed **5** cyan sparkle particles (`size 0.4` / `dur 0.45` = the F5 consts) onto the `explosions` lane + the `+N`; a tile chip-heal (`healerId null`) pushed **0** (just `+N`); particles self-clean to 0 past their lifetime; post-`fix` the anchor reads +0.0 peaking at +0.35 (was +0.5→+1.2); **console clean**, no glyph crash. **The subjective look was the user's eyeball pass — confirmed "perfect" (incl. the body-anchor height).** **Tuning knobs (by feel):** `HEAL_SPARKLE_Y_OFFSET` (base height off the sprite center) + `HEAL_SPARKLE_RISE` (climb) — keep `Y_OFFSET + RISE ≲ 0.5` to stay off the head; also `_SPREAD` / `_SIZE` / `_SECONDS` / `_BLOOM` in [BattleRenderer.ts](src/render/BattleRenderer.ts).
- **F6 complete (Phase F)** — heal / utility XP, INDEPENDENT of the phase system; the **last step of Phase F**. One commit `feat(F6)` (`05fd6a9`). **Feedback #5:** *"healing (and future utility) needs to award XP"* — a pure healer was starved by the damage-only model (the E4/E7.B worry). **Shape:** new **general** `utilityDone` ledger in [World.ts](src/sim/World.ts) (mirror of `damageDealt`, keyed by the acting unit), credited via a lean new `recordHealing(healerId, amount)` called from [HealAction.start](src/sim/actions/HealAction.ts) with the **effective (clamped, non-overheal) delta** — so healing a full-HP ally earns 0 (kills the spam case). `recordHealing` is deliberately leaner than `recordDamage` (no `target`/`findUnit` team guard): heals only ever target allies, and `computeXpAwards` reads the ledger only for `playerRosterIds`, so an enemy/self entry is harmless + never paid out (also keeps it off the O(n) `findUnit` path). [computeXpAwards](src/sim/xp.ts) gained a 4th `utilityDone` param folding `LEVELING.xpPerHealing × util` into `xpGained` alongside the damage share — a heal-only healer that dealt 0 damage now levels. New `xpPerHealing` knob in [config/leveling.json](config/leveling.json) = **3 (parity with `xpPerDamage`** — user call; 1 HP healed ≈ 1 damage dealt). **Ability heals only** (user call): the per-tick regen-tile chip-heal in `applyTileEffects` does NOT call `recordHealing` (tile output, not a unit's contribution). **General axis:** a future buff/shield plugs into the same ledger + slice without another snapshot bump. **XpAward / `battle:ended` shape UNCHANGED** — the healing share folds into `xpGained`, not a separate field (matches E4's keep-the-award-shape-stable discipline; `Run.bankXpAwards` / `PromotionScene` / events.ts untouched). **WorldSnapshot v14→v15** (utilityDone round-trips so a mid-battle restore preserves heal-XP; v14 throws on load). **Verification:** 514 tests (+7, all config-derived/balance-proof: 2 xp heal-only+mixed cases, 4 HealAction ledger crediting + overheal=0 + accumulation, 1 snapshot round-trip; + a tile-heal-does-NOT-credit assertion folded into the existing World.test tile-heal case). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Fuzz baseline SHIFTS (benign) — NOT byte-identical** (the plan wrongly assumed it would be): F1 made healers **recruitable**, so a drafted healer now earns heal-XP + levels slightly differently. Confirmed via stash+diff on identical seeds — pure-random outcomes unchanged (avg ticks 1067→1070), greedy one seed flipped defeat→complete (60%→65%); **0 new hangs**, the lone greedy `endlessCorridors` hang is **pre-existing** (grown-team tick-cap, unchanged in both). **NB for future support/utility changes:** post-F1 the recruit pool holds all six archetypes, so any change touching a *recruitable* archetype's XP/behavior will shift the fuzz baseline — that's expected, not a regression; verify via stash+diff. **User-playtested:** confirmed healers now level up noticeably more often (the F6 intent). **Tuning knob (by feel):** `xpPerHealing` in [config/leveling.json](config/leveling.json) — lower it if a healer out-levels the carries.
- **G1 complete (Phase G)** — run configuration & short-run harness, the playtest-velocity unblocker pulled forward (ROADMAP §G1). Three commits (`5719b82` core, `c64e284` per-unit levels, `6484fe2` tooling) + the branch-fold/process commit. **Shape:** new `RunConfig` (`{seed?, floorCount?, startingRoster?, forcedLayoutId?, mapMaxWidth?}`) in [src/run/RunConfig.ts](src/run/RunConfig.ts), threaded into `Run` construction + `NodeMap.generate(rng, config?)` + the battle-layout seam in `Run.handleEnterNode`. One exported `parseRunConfigFromURL()` (query↔config, with a `runConfigToQueryString` inverse) is the **single source of truth** shared by the browser ([Game.ts](src/Game.ts)) and the headless paths — it **replaced** the old inline `?roster=` parser (the `DEV_ROSTER_ARCHETYPES` + `applyRosterOverride` block in Game.ts is gone). Param names: **`seed / floors / roster / layout / width`**. **startingRoster carries per-unit levels** (`?roster=rogue:3,healer:2,melee`; `:level` optional, default 1, clamped to `LEVELING.levelCap`; bad/missing level → 1) — built via `rollUnit(archetype, teamRng, level)`, the natural `simulateLevelUps` player-growth path. **Determinism (load-bearing):** the constructor's fork order (nodeMap → team → levelup) is preserved regardless of config — each override only changes a *forked child* stream's content, never how many times the **parent** is forked, so (a) the default / empty / `undefined`-config path is **byte-identical** to pre-G1 and (b) a nodeMap-shape override (floorCount/mapMaxWidth) leaves the team + levelup forks unmoved (both pinned by tests). The forced layout **rolls then overrides**: `rollLayoutId(battleRng)` still runs to advance the stream (gotcha #49 byte-continuity), then `this.forcedLayoutId ?? rolled` wins. **No snapshot bump** — RunConfig is a run *input*, fully reconstructable from the seed, deliberately NOT persisted (`Run.fromJSON` resets `forcedLayoutId` to null; revisit when save/load is real). **Tooling:** [tests/fuzz/harness.ts](tests/fuzz/harness.ts) `HarnessOptions` gained `runConfig?` (threaded into `runOne`'s `Run`; `runConfig.seed` overrides the run seed, the `seed` arg still identifies the run) so a sweep can target a short / forced-layout / leveled run; new dev CLI [tools/run-config/cli.ts](tools/run-config/cli.ts) (`npm run run-config`, run via tsx, **never in `dist/`** — `tools/` isn't a build input) maps the same flags → RunConfig, prints a browser launch URL describing the same run, and drives a headless run to completion unless `--no-run`. **Reachability:** `Game.createRun()` builds from the parsed config at construction AND on `resetRun` (a reset re-rolls the same *shape*; a pinned `?seed=` reproduces the exact run). **floorCount semantics:** total floors incl. root + terminal; **`floorCount 2` = the minimal one-battle run** (root→terminal), `floorCount 1` = a degenerate single root==terminal node with no battle (the CLI warns). **Verification:** 537 tests (+23 from 514: 13 RunConfig parser, 5 NodeMap-override DAG-validity at floorCount 1/2/3 + mapMaxWidth cap + no-config==empty, 5 integration determinism incl. forced-short-run-resolves-to-`complete` + leveled-roster-reproducible). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz:smoke 7/7 (default path unmoved). Browser-confirmed `?floors=2&layout=endlessCorridors&roster=rogue:5,healer:3,melee` applies in the live `Game` (2-node map, forced layout 8×32, per-unit levels [5,3,1]), **0 console errors**; CLI deterministic for a pinned `--seed`. **User-playtested: "working perfectly."** **Process note:** a stray `roadmap/phase-g-h` branch (a past-session one-off, not a documented process) was folded back into `main` (fast-forward) and a **work-on-`main`** rule added to *How we collaborate* below — all work stays on `main` unless the user explicitly asks for a branch.
- **G2 complete (Phase G)** — longer maps & full-screen map scene (ROADMAP §G2). Five commits: `c92dff3` planar generator, `0b3856c` full-viewport scene, `41293a9` de-bias fix, `8d669b3` centered A/B toggle, `4c3677d` centered-as-sole-construction (toggle removed). **Generator** ([NodeMap.ts](src/run/NodeMap.ts)): replaced the random-pick + orphan-backfill edge logic (crossed edges; backfill could break the out-degree cap) with a **provably non-crossing** construction. Per adjacent floor pair each parent (in x-order = `floors[f]` index, which the renderer reads directly) gets a **contiguous child interval**; consecutive intervals are monotone and overlap by **at most their shared boundary child** (`a_{p+1} ∈ {b_p, b_p+1}` — overlap-by-two-or-more *inverts*, the bug the crossing-checker caught mid-build). That single rule gives planar + fully connected (every child ≥1 parent → root-reachable; every parent ≥1 child → terminal-co-reachable) + **out-degree ≤ D by construction** (interval width ≤ D, no backfill). The interval END is biased toward the parent's **diagonal slot** (`round((p+1)/m·n)−1` ± 1 jitter, clamped to the feasible `[bMin,bMax]`) so children stay near the parent's own column → gentle per-floor lean + in-degree spread evenly (no single hub); a **50/50 per-pair horizontal mirror** then cancels the small residual lean. **`MapNode`/`NodeMap` shapes unchanged** (x-order = array index, no `xSlot` field) → **no snapshot bump**. Width loop gained a forward feasibility cap (`prevWidth·maxOutDegree`, only binds floor 1) + an `n ≤ m·D` assertion (loud on future config drift); **no in-degree cap is assumed** (would break wide→narrow feasibility — noted in-code). [config/nodemap.json](config/nodemap.json): `floorCount 5→11` (root + **10 battle floors**), `middleWidthMax 3→6`, `maxOutDegree 2→3`, `targetTotalMax 10→50` (the last is load-bearing — at 11 floors the old 10 drove the width budget negative and pinned every floor to width 2). **Scene** ([MapScreen.ts](src/ui/MapScreen.ts) + [ui.css](src/ui/ui.css)): the right-edge 280px strip over the cleared canvas (the "gray strip" the brief flagged) becomes a **full-viewport, solid-opaque (`#000`) scrollable** scene — SVG edges + %-positioned nodes lay out against an inner `.map-board` whose height = `floorCount · FLOOR_PX` (90); a board taller than the viewport scrolls with the **current node centered on show** (`offsetTop − clientHeight/2`, browser-clamped, so root sits at top + deeper nodes pull the view down). Nodes 40px / edges 1.5–2.5px for the bigger board. Per the user, the **sibling DOM scenes** (Recruit/Promotion/GameOver) also went `rgba(0,0,0,0.8x)` → opaque `#000` so every menu fully masks the canvas. **Playtest journey (decision-point, the funky part):** the first planar generator leaned **systematically rightward** (handed sweep — free-running interval end; measured mean shear +0.146, endpoints 27%/47% left/right). Fixed first with the per-pair mirror (signed shear → ~0, but each *floor* still lopsided + in-degree hubs). User flagged the per-floor lean → built a **centered** mode behind `?mapgen=centered` for live A/B, measured it (mean per-floor |lean| 0.21→0.14, avg worst-node in-degree 5.2→4.7), user picked it ("much more natural") → promoted centered to the **sole** construction + removed the toggle plumbing (`RunConfig.mapGen`/`?mapgen`/`--mapgen` gone; git history keeps the funkier look). **Baseline reset:** the generator changes shift the run RNG draw sequence (G2's one-time fuzz/determinism reset, like F1). Post-G2 wider fuzz (20 seeds × 2): pure-random **0% win / avg floor 4.05 / 0 hangs**, greedy **0% win / avg floor 5.05 / 1 hang** (the pre-existing `endlessCorridors` grown-team tick-cap hang, NOT G2). **⚠ The 0% bot win rate is expected, not a regression** — a 10-battle run under the current linear `enemyLevelPerFloor` is brutal late (bots reach ~floor 4–5 then lose); **this makes G4's difficulty rebalance load-bearing** for full-run completion. Humans can playtest depth via the G1 short-run harness (`?floors=5`) / leveled rosters. **Verification:** 540 tests (NodeMap rewritten — bounds derive from `NODE_MAP`, balance-proof; new no-crossing checker + out-degree guard across 100 seeds, folded into the override cases); fuzz:smoke 7/7; typecheck + lint clean (lone pre-existing `Renderer.ts` warning). Browser-confirmed at `?floors=11`: full-viewport opaque scrollable map, correct node/edge geometry, current node centered, frontier-click → BattleScene, all four DOM scenes opaque, 0 console errors (screenshots time out on the throttled tab per the usual — node/edge *feel* was the user's eyeball pass, confirmed). **User-playtested: maps "funky, and I like that" → centered "much more natural."** **Tuning knobs:** `config/nodemap.json` (floorCount / widths / `targetTotalMax` density / `maxOutDegree`); `FLOOR_PX` in [MapScreen.ts](src/ui/MapScreen.ts) + `.map-node` / `.map-edge` sizes in [ui.css](src/ui/ui.css).
- **G3 complete (Phase G)** — node types: rest & boss plumbing + map icons (ROADMAP §G3). One feat commit `28fac8e` + this docs commit; a playtest-driven CSS readability pass is folded into the feat. **Generator** ([NodeMap.ts](src/run/NodeMap.ts)): `NodeKind` widened `'battle'` → **`'battle' | 'rest' | 'boss'`**, assigned in a **tail pass** after the width+edge sweep — terminal → `'boss'` (deterministic, no RNG); rests scatter on the eligible band **`[2, floorCount-2]`** (never root, never floor 1 = the first battle, never the boss floor) via one `rng.next()` per eligible floor gated on **`restChance`** (0.35) + a **`restMinSpacing`** (2) floor-gap, then one node on the chosen floor picked uniformly (wide floor keeps a battle sibling → a rest is a route *choice*; width-1 floor = forced rest). **Because kinds draw at the tail, the width+edge stream — and the map structure for any seed — is byte-identical to G2; only `kind` is new.** The nodeMap RNG is a dedicated fork not reused after `generate()`, so the team/levelup forks are unmoved. **Seed-stability contract extended to widths → edges → kinds.** Edge cases handled: `floorCount ≤ 3` → empty band → no rests; `floorCount === 1` → boss-tag lands on root==terminal, inert (icon overridden by isRoot). **Rest resolution** ([Run.ts](src/run/Run.ts)): `handleEnterNode` dispatches on `kindOf(nodeId)` — a rest calls new `resolveRest()` (synthesizes a `+LEVELING.restXp` `XpAward` per roster slot, feeds the **existing `bankXpAwards`** → no parallel leveling path; promotions → `phase='promotion'` + `promotion:pending`, else `phase='map'` **silently**, per the user's call); battle/boss take the existing encounter path (boss is a regular fight, just tagged — `advancePastBattle` already routes the terminal win → `run:victory`). `handleDismissPromotion` routes by the **current node's kind**: rest → back to `map` (no recruit), battle/boss → `advancePastBattle`. **The current node's `kind` is the rest-vs-battle discriminator (already persisted via `currentNodeId`) → NO new serialized field, NO snapshot bump** (`RUN_SCHEMA_VERSION` stays 5). **Game wiring** ([Game.ts](src/Game.ts)): `enterNode` + `dismissPromotion` cases gained an `if (phase==='map') swap(new MapScene())` guard (mirrors the existing `chooseRecruit → map` pattern) so a silent rest / a rest-triggered-promotion dismiss refreshes the map. **No fuzz-harness change** — its loop is phase-driven and absorbs a rest hop (`map → (promotion →) map`) for free. **Icons** ([MapScreen.ts](src/ui/MapScreen.ts) + [ui.css](src/ui/ui.css)): node ids replaced by a `KIND_GLYPH` map — root `@` / battle `X` / rest `Z` / boss `!` — plus a `.{kind}` class. **Two playtest-driven CSS passes (the funky part):** (1) locked nodes used element `opacity:0.4`, which made the `#000` bg semi-transparent (SVG edges bled through the glyph) AND dimmed the glyph to ~`#444` (X/Z muddy) → replaced with an opaque bg + dim neutral border `#404040` + legible grey glyph `#b0b0b0` (actionable states still pop by *hue*, not brightness); (2) the kind cue was a `text-shadow` glow that read as invisible on a 16px glyph → moved the signal into the **glyph color** itself: rest = **TERMINAL_GREEN `#33ff00`**, boss = **NEON_RED `#ff3131`** (each with a matching neon halo), the kind rules sitting *after* the state rules so they override the glyph color for rest/boss **only** — battle nodes keep their state-based grey/cyan/amber. **Config:** `restChance` + `restMinSpacing` ([nodemap.json](config/nodemap.json)); `restXp` **200** ([leveling.json](config/leveling.json) — with the XP knobs since it feeds `bankXpAwards`; rest *placement* knobs stay in nodemap.json). **Verification:** 551 tests (+11 from 540: 7 generator-kind in [NodeMap.test.ts](src/run/NodeMap.test.ts) — boss=terminal, rest band/spacing/one-per-floor, reachability, empty-band at `floorCount≤3`; 4 rest-resolution in [Run.test.ts](src/run/Run.test.ts) — bank-per-slot + no-battle, promotion→dismiss→**map**, no-promotion→**silent map** via a cap roster, boss→normal-battle→complete; all balance-proof off `NODE_MAP`/`LEVELING`). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz:smoke 7/7. **Browser-confirmed** at `?floors=11&seed=7`: map renders `@`/28×`X`/3×`Z`/`!` with correct kind classes; computed colors confirmed (rest green, boss red, locked battle opaque grey, frontier cyan, root amber); **0 console errors**; plus a **live end-to-end drive through `__game`** (root→battle→rest): the battle hop swapped BattleScene→MapScene, the rest popped PromotionScene, dismissing returned to MapScene **on the map, not recruit**, all 6 units banked +200 → level 2 / xp 100. **User-playtested: rests "definitely help"; the icons "beautiful."** **Baseline note:** G3 shifts the fuzz/determinism *outcome* baseline (rests replace some battles) though the RNG streams are unchanged — like F1/G2, regenerate the untracked `tests/fuzz/output/*`, not a hard-fail. Also fixed a stale `RunConfig.floorCount = 5` doc comment (→ 11). **Tuning knobs:** `restChance` / `restMinSpacing` ([nodemap.json](config/nodemap.json)), `restXp` ([leveling.json](config/leveling.json)); kind glyph colors/halos + locked-node greys in [ui.css](src/ui/ui.css).
- **G4 complete (Phase G)** — enemy level-budget balance + recruit leveling, across 5 commits (`08e3b0c` enemy budget + seam, `4ae6d00` recruit leveling, `3c5162c` fuzz per-floor telemetry, `756377b` affine-budget + `startingLevel` model rework, `be4e272` ship preset B). **Enemy difficulty is now a *team-level budget*, not a floor-linear ramp** — new [src/run/enemyBudget.ts](src/run/enemyBudget.ts) owns it; `rollEnemyTeam` + `enemyLevelForFloor` are gone.
  - **THE SEAM — `playerTeamLevel(team)`** = SUM of roster unit levels. The single most important Phase-G extensibility point: **H5 swaps THIS ONE FUNCTION** to `avgLevel × min(rosterSize, handSize)`. Documented loudly at the def site. `buildEnemyTeam` wraps the affine + swarm logic around it; recruits use a separate `avgTeamLevel` (the seam becomes a non-average product in H5, so recruits can't route through it).
  - **Affine budget** (the *final* model — the flat delta proved too weak): `budget = max(minBudget, round(budgetOffset + budgetFactor × playerTeamLevel))`. A **proportional `factor`** holds the handicap's ratio at every depth; the flat `sum − delta` was swamped late (constant subtraction vs the player's growing sum → late battles stayed even; the fuzz showed it shifted *where* bots die, not *whether* they finish). `factor=1, offset=−5` reproduces the original flat model.
  - **Swarm count.** `cap = highestPlayerUnitLevel + unitLevelDelta`; `minCount = ceil(budget/cap)`; **`maxCount = min(swarmMaxMultiplier × size, budget)`** — bounded by the budget because a unit can't drop below level 1, so an unbounded count inflates the enemy total past the budget (at floor 1 that was "10 equal-level bodies vs 5"). `count` skews toward max via `chooseSwarmCount` (`pow(next(), 1−swarmBias)`). `distributeBudget` spreads the budget roughly equally (each ∈ [1,cap], within-team spread ≤ 1) via a partial Fisher–Yates choosing which units carry the +1 remainder. 60/40 melee/ranged by index (enemies stay melee+archer through Phase G). **Now genuinely consumes `battleRng`** (was deterministic) — it's the *last* consumer of that fork, so the variable draw count is downstream-safe.
  - **Spawn-queue transition** — a `2×`-size swarm overflows the 8-tile region onto the D5 queue (already queue-aware; `checkBattleEnd` treats a queued team as alive). Verified, no new plumbing.
  - **Recruit leveling** — recruits arrive at `round(avgTeamLevel) + recruitLevelBonus(rng, recruitBonusChance)` (was `currentFloor`). `recruitLevelBonus` is a geometric coin (`P(+k)=(1−c)·c^k`; c=0.5 → 50/25/12.5…), originally **one draw per offer** (all cards shared the level); a **post-G5 tweak made it an independent per-card draw** over a shared `round(avgTeamLevel)` base (see the post-G5 entry below). Clamped to `levelCap`. A fresh draft now scales with the *team*, not the floor.
  - **`startingLevel` knob** ([recruitment.json](config/recruitment.json), shipped **5**) — the level the canonical starting roster begins at, wired into `rollTeam`. Raising it lifts the whole run's level scale so a proportionally-weaker swarm is meaningfully weaker in *absolute* terms — the key insight from the per-floor data (at levels 1–4 a "half-level" enemy is a rounding error; at level 5–7 it's a real gap). Default 1 = byte-identical to pre-knob; shipped at 5.
  - **Shipped difficulty: preset B "swarm fodder"** — `budgetFactor 0.25, swarmMaxMultiplier 2.0, startingLevel 5`. Outnumbered by waves of lv-1 fodder you mow down; median level gap widens to ~5 deep in. **User-playtested** — preset C "mild middle" (0.35/1.5) was tried first and **rejected** (too many floor-1 deaths: the valley where enemies are numerous enough to overwhelm but not weak enough to free-kill). Presets **A "elite even" (0.5/1.2)** + **C** are documented in [difficulty.ts](src/config/difficulty.ts) to revisit after G5/Phase H. Fuzz win at B ~21%/22% (random/greedy), floor ~5.9.
  - **The conservation tradeoff (key finding, documented in difficulty.ts):** the budget is conserved, so **"enemies at half your level" XOR "a real numeric swarm" — not both, while winnable.** A half-level swarm = equal total army = an action-economy fight the (dumb) bots can't win. Spread the budget wide → weak fodder swarm; concentrate it → half-level threats at even counts. **Caveat:** the fuzz bots are dumb (no focus-fire/kiting), so their win rates *understate* human winnability and *cap the bot-provable swarm size* — a bot-skill artifact, not a true wall. **Re-tune once G5 lands smarter strategies** (the user expects a rebalance after G5 + Phase H — that's why A/C are preserved).
  - **Fuzz per-floor telemetry** — `npm run fuzz -- --per-floor` prints + writes `per-floor.csv`: mean/median unit level, within-team level spread, and team size per floor for *both* sides (the median-gap view that exposed the squashed old model). `BattleResult` gained `playerLevels`/`enemyLevels`; `perFloorStats` / `renderPerFloorAnalysis` in [reporters.ts](tests/fuzz/reporters.ts).
  - **Config:** `budgetFactor` / `budgetOffset` / `unitLevelDelta` / `minBudget` / `swarmBias` / `swarmMaxMultiplier` ([difficulty.json](config/difficulty.json), replacing the retired `enemySizeDelta` / `enemyLevelPerFloor` / `totalLevelDelta`); `startingLevel` / `recruitBonusChance` ([recruitment.json](config/recruitment.json)). **No snapshot bump** (levels/templates are existing fields). **Fuzz/determinism outcome baseline shifts** (new RNG consumption in `buildEnemyTeam` + recruit draws + the startingLevel roll) — expected; regenerate the untracked `tests/fuzz/output/*`.
  - **Verification:** 574 tests (was 540; new enemyBudget mechanic + balance-proof cases, `recruitLevelBonus` distribution, `startingLevel`, the affine formula, a spawn-queue G4 bridge test, fuzz per-floor smoke; the XP/promotion **mechanic** tests are pinned to a level-1 roster via `freshLvl1RunWithBus` so they don't ride the `startingLevel` balance dial). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz:smoke 7/7. **NOT browser-verified** — pure sim/config change, no render surface (no new glyphs/UI); headless tests + the user's playtest carry it. **Tuning knobs (all by feel):** `budgetFactor` is the dominant difficulty lever (proportional handicap); `swarmMaxMultiplier` sizes the swarm (near a cliff at low factor — tune gently); `startingLevel` sets the absolute level scale; plus `unitLevelDelta` / `minBudget` / `swarmBias` (difficulty.json) + `recruitBonusChance` (recruitment.json). A/B/C presets live in [difficulty.ts](src/config/difficulty.ts).
- **G5 complete (Phase G) — the LAST step of Phase G** (G1 ✅ → G2 ✅ → G3 ✅ → G4 ✅ → G5 ✅). Two feat commits (a playtest pause between, per the user). Pure dev-tooling — **no `src/` game-logic, snapshot, config, or balance change**; the existing fuzz/determinism baselines are unaffected (the two baseline strategies draw byte-for-byte as before).
  1. **Fuzz-strategy factory + full menu** (`6dfbbad`). Refactored the strategy layer from copy-pasted classes toward a **data-driven factory** (ROADMAP §G5 decision point: factories over subclasses). A strategy is now a `(node policy, recruit policy)` pair + a name. New [strategies/policies.ts](tests/fuzz/strategies/policies.ts) (node: `randomNode` / `maximizeKind(kind)`; recruit: `randomRecruit` / `balancedArchetype` [=greedy] / `preferArchetype(a)` / `maximizeStat(stat)`), [factory.ts](tests/fuzz/strategies/factory.ts) (`composeStrategy`), [registry.ts](tests/fuzz/strategies/registry.ts) (the **17-strategy menu** — `pure-random`, `greedy`, `recruit:<archetype>` ×6 [config-derived off `ALL_ARCHETYPES`], `stat:<stat>` ×7 [config-derived off the base-stat vocabulary], `path:battle` / `path:rest`; `power`-stat + pass-option variants deferred to H6 per ROADMAP). **Determinism-preserving:** `randomNode`/`randomRecruit`/`balancedArchetype` reproduce the pre-G5 `PureRandom`/`Greedy` draw patterns byte-for-byte (one `next()` per decision; greedy draws only on a ≥2-way tie), so their fuzz baseline is unchanged — only the new strategies add fresh draw sites. **Both CLIs now consume the single registry** ([tests/fuzz/cli.ts](tests/fuzz/cli.ts) + [tools/run-config/cli.ts](tools/run-config/cli.ts)) instead of each maintaining a duplicate STRATEGIES record. `npm run fuzz` default sweep stays the **two baselines** (fast); the full menu is opt-in via `--strategy=NAME` or `--strategy=all`. Retired `PureRandom.ts` + `Greedy.ts`. **Verification:** 574 main tests pass (unchanged — fuzz lives outside `npm test`); fuzz:smoke 22 pass (+ new [strategies.test.ts](tests/fuzz/strategies/strategies.test.ts), all config-derived/balance-proof — policy behavior + registry completeness derived off `ALL_ARCHETYPES`/`STAT_KEYS`); typecheck + lint clean. End-to-end: default sweep = 2 baselines, `all` = 17, run-config drives `path:rest` to a complete run, unknown strategy bails with the full choices list. **User-playtested: "looks great, seeing interesting strategic results."**
  2. **GUI run launcher** (`75c5d33`). A dev-only browser page ([tools/run-config/index.html](tools/run-config/index.html) + `launcher.css` + `launcher.ts`), served at **`/tools/run-config/`** (a sibling of the layout editor; never bundled into `dist/` — the production build still emits only the game's `index.html`, confirmed). Pick seed / floors / map width / layout and build a starting roster (per-unit archetype + level pickers); the page emits a launch URL for the game so an eyeball test is a click + paste, not a hand-typed query string. **Single source of truth:** the form is round-tripped through the *same* `parseRunConfig` → `runConfigToQueryString` pair the game + CLI use, so it validates / clamps / drops fields exactly as the game will (no second copy of the rules). Styling mirrors the layout editor's palette. README documents both the GUI + the CLI. **Verification:** typecheck + lint clean; build green. **Browser-verified** at `/tools/run-config/` — 0 console errors; layout dropdown = procedural + all 6 layouts; level cap 20; roster builder adds/removes/configures rows; the form produces the exact canonical query (decoded `seed=7&floors=5&roster=rogue:3,healer,mage:2&layout=endlessCorridors&width=4`), and that generated URL **drove the live game** to team `[rogue Lv3, healer Lv1, mage Lv2]` / 5 floors / forced `endlessCorridors`, 0 console errors. (Verified via the accessibility snapshot + DOM eval + the end-to-end game drive; the **JPEG screenshot times out on the throttled preview tab** per the documented issue, so the subjective pixel-level look is the user's eyeball pass.) **NB:** the launch URL is percent-encoded (`%3A`/`%2C`) — that's the canonical, game-parseable form `runConfigToQueryString` produces (same as the CLI); decodes cleanly when pasted.
- **Post-G5 tweak — independent per-card recruit bonus** (playtest-driven, user call). The G4 geometric recruit-level bonus was drawn **once per offer** (all three cards shared the level — a lucky roll boosted the whole offer together). Now it's drawn **independently per card** over a shared `round(avgTeamLevel)` base, matching the original "occasional over-leveled standout" intent: most cards sit at the team average, with the odd single card popping +1/+2 as a tempting pick. [rollOffer](src/run/Recruitment.ts)'s `level` param now accepts `number | ((rng) => number)` (flat number = back-compat/explicit; function = resolved per card off the shared `rng`); [Run.handleAdvancePastBattle](src/run/Run.ts) passes a closure `(cardRng) => min(cap, base + recruitLevelBonus(cardRng, chance))`. **Determinism:** still deterministic per seed (the recorder/`fromJSON` are unaffected — recruit level isn't a snapshot field), but the **fuzz/determinism outcome baseline shifts** (the recruit draw sequence changed — bonus now drawn post-sample + interleaved per card), benign like F1/G4 — regenerate the untracked `tests/fuzz/output/*`. Tests: Recruitment.test gains 3 cases (flat-number back-compat, per-card level-function order, per-card geometric independence — all config-free/explicit); the Run G4 recruit test dropped its now-false "all cards share one level" assertion (keeps the per-card `≥ base` / `≤ cap` invariant). **No snapshot/config change** (`recruitBonusChance` knob unchanged).
- **GP1 complete (Phase GP) — first of the 5-commit Phase-G playtest-response cluster.** Renamed the two cadence stats so they read distinctly: **`endurance → mobility`** (drives the move cooldown) and **`speed → agility`** (drives each ability's attack cadence), across `UnitStats` ([src/sim/Unit.ts](src/sim/Unit.ts)), the zod `baseStats`/`growthRates` schemas ([src/config/archetypes.ts](src/config/archetypes.ts)), [config/archetypes.json](config/archetypes.json), `STAT_KEYS` + both level-up builders ([src/sim/leveling.ts](src/sim/leveling.ts)), `ZERO_STATS` + the cooldown math ([src/sim/stats.ts](src/sim/stats.ts)), the 5 agility call sites (`unit.stats.agility` in strikes/magic/catapult/heal abilities + RecruitScreen), the PromotionScreen label map (`SPD→AGI`, `END→MOB`), and ~17 test files (no shared stat factory — each literal renamed in place). **Split the single shared cooldown curve into per-axis knobs** so the two stats can diverge: `cooldownScale(stat)` → `cooldownScale(stat, perStat, minScale)` (private; both call sites thread their own pair); `cdPerStat → mobilityCdPerStat` (0.15) `/ agilityCdPerStat` (0.05) and `minCdScale → mobilityMinCdScale / agilityMinCdScale` (both 0.4) in [config/stats.json](config/stats.json). **Dropped the per-archetype `baseMoveCooldownSeconds` override** (only the catapult's `2.5` used it) + its accessor (`baseMoveCooldownSecondsForArchetype`, gone from [src/sim/archetypes.ts](src/sim/archetypes.ts)) + the `deriveStats` param + the 3 callers (RecruitScreen + World ×2 now call `deriveStats(stats, range)`); one **universal** `STATS.baseMoveCooldownSeconds` (1.0). `mobility` is now **signed** (zod allows `min(-STAT_CAP)`): 0 = the 1.0s baseline, positive faster, negative slower — the min-scale floor caps only the **fast** side, so negative mobility (catapult −7 → ~2.05s) is unbounded-slow. **Re-tune = "preserve current feel"** (user-playtested "right where we were"): mobility melee/rogue **+2**, ranged/healer/mage **+1**, catapult **−7**; **agility = the old `speed` values + rate verbatim → attack cadence is byte-identical**; mobility growth = old `endurance` growth ÷ ~3 (melee 0.13, others 0.10, catapult 0.07) so units don't hit the 0.4s fast-cap by ~level 5 — **this is the open by-feel tunable the user will retune later** (one-line-per-archetype). **WorldSnapshot v15→v16** (v15 throws — stats round-trip as a whole object by key, so an old save would deserialize into a block missing `agility`/`mobility`; no migration, and the move-CD re-tune means old derived cadences wouldn't reproduce anyway). Because agility is behaviour-preserving, the **fuzz outcome shift is move-CD only** (ranged/healer/mage slightly slower + the mobility-growth change); baselines are untracked (`tests/fuzz/output/*` gitignored) — regenerate via `npm run fuzz`. **Verification:** **581 tests pass** (was 577; +4 in [src/sim/stats.test.ts](src/sim/stats.test.ts) + [snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts): negative-mobility-is-slower-than-baseline, the **axis-divergence split guard** [same +stat resolves to different scales through the two helpers — catches a regression that crosses the wires], catapult ~2.0s-from-mobility-alone, and snapshot v16-round-trips / stale-version-throws — all balance-proof off `STATS`/`ARCHETYPE_CONFIG`). Typecheck clean. Fuzz 40-run **0 hangs**, exit 0 (determinism intact). **Browser:** app boots clean to MapScene (the four new `stats.json` knobs + the negative catapult mobility pass zod at module load — the riskiest runtime check), and PromotionScreen renders the new **AGI** (`9 → 10 (+1)`) / **MOB** rows (verified via DOM query + module import; the JPEG screenshot times out on the throttled preview tab per the documented G5 issue). **User-playtested: "feels pretty nice, right where we were."**
- **GP2 complete (Phase GP) — second of the 5-commit playtest cluster.** The `defense` stat + a damage-application consolidation. **Two commits:**
  1. **Consolidate** (`refactor(GP2.1)`, `5c9ed54`) — the four combat-damage actions ([AttackAction](src/sim/actions/AttackAction.ts)/[GambitStrike](src/sim/actions/GambitStrikeAction.ts)/[MagicBolt](src/sim/actions/MagicBoltAction.ts)/[Catapult](src/sim/actions/CatapultShotAction.ts)) each did the same inline trailer (`currentHp -=` + `recordDamage` + `unit:attacked` emit); funnelled into one **`World.applyDamage(attackerId, target, rawDamage, {crit})`** chokepoint (next to `recordDamage`). Callers still pre-compute `rawDamage` (crit × half-cover × AoE cellMult baked in) and keep their own `rawDamage<=0` / team / radius skip guards — those decide WHICH cells are victims; applyDamage applies a confirmed hit. Transient `magic:detonated`/`catapult:fired` untouched. **PROVEN byte-identical:** the fuzz `summary.csv` (20 seeds × 2 strategies) **SHA256-matches** the pre-refactor tree (stash+diff).
  2. **Defense stat + mitigation** (`feat(GP2.2)`, `ee7e098`) — new `defense` in `UnitStats` ([Unit.ts](src/sim/Unit.ts)), mitigated in the one chokepoint: **`final = max(STATS.minDamage, raw − target.stats.defense)`** on the post-crit/post-cover number. Consumed **raw** (no derived layer, no per-stat multiplier — the locked **subtractive** design, not diminishing-returns). New **`minDamage`** knob ([stats.json](config/stats.json), default 1) keeps a high-defense target from fully negating chip/AoE. **Environmental fire/chasm damage stays UNMITIGATED** — `applyTileEffects` keeps its own `currentHp -= 1` + `unit:burned`, never routes through applyDamage. Stat plumbing mirrors the GP1 rename pattern: `defense` added to `UnitStats` / `ZERO_STATS` / the base+growth zod schemas / every archetype in [archetypes.json](config/archetypes.json) / `STAT_KEYS` (**appended LAST** so the other stats' level-up RNG draw positions are undisturbed) + both [leveling.ts](src/sim/leveling.ts) return objects / the [PromotionScreen](src/ui/PromotionScreen.ts) `STAT_LABELS` (`DEF`). **WorldSnapshot v16→v17** (new stat key; v16's defense-less stat block rejected, no migration — same rationale as GP1).
  - **Shipped defense values (base / growth):** melee **4 / 0.2**, ranged **2 / 0.1**, rogue/healer/mage/catapult **0 / 0** (user-chosen: modest + melee-tanky). The RecruitScreen/HUD card `DEF` row + ability list is **GP3**; the PromotionScreen `DEF` row ships here (it data-drives off `STAT_LABELS`).
  - **Verification:** **588 tests** (was 581; +7 balance-proof `applyDamage` tests in [World.test.ts](src/sim/World.test.ts): `final == max(minDamage, raw−def)`, floor honored, defense-0 behaviour-preserving, crit/cover-before-mitigation, XP ledger credits the *mitigated* number, fire UNMITIGATED despite defense, snapshot round-trip; test fixtures + the AbilityBehavior/sharedCooldown mechanic scenes set `defense:0` so cadence/damage assertions hold). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Fuzz 20-seed: 0 hangs, all battles resolve.** **Browser-confirmed:** clean boot (the new `defense`/`minDamage` keys pass zod at load), real spawned units carry defense (player melee 4–5 / ranged 2, enemy melee 4), a live `world.applyDamage(raw 12)` vs def 4 dealt **exactly 8** (30→22), and PromotionScene renders **"DEF 4 → 5 (+1)"**.
  - **⚠ BALANCE SIGNAL (the user is sleeping on it — DO NOT re-tune without them):** at the shipped values the fuzz-bot win rate jumps **25% → 90%** and battles run ~**2× longer** (avg ticks 1588→3445). Exactly the **subtractive caveat** the ROADMAP flagged — melee (str 6) chips an armored target (def 4) for only **2/hit**, so melee-heavy enemy teams get badly blunted while the player drafts from the wider F1 pool. **Mechanically sound (0 hangs), purely a tuning question.** A **proper balance sweep is DEFERRED** (user call — "keep the values, sweep later," folds into the post-GP+H re-tune already on the books). Tune by feel: `defense` per archetype ([archetypes.json](config/archetypes.json)) / `minDamage` ([stats.json](config/stats.json)) — no code change.
- **GP3 complete (Phase GP) — card legibility overhaul; render-only (no snapshot/config/fuzz/balance change).** The recruit card + in-battle HUD now surface what GP1/GP2 added. Two commits, playtest pause between (the GP cadence):
  1. **Card** (`feat(GP3.1)` `3d3077f` → `refactor(GP3.1)` `a5de6fe`). [RecruitScreen](src/ui/RecruitScreen.ts) `renderCard` became two sections: a stat block on top + an **Abilities** list below (one row per ability id — built as a loop so a future multi-ability unit just renders more rows: name + `N dmg/heal · rng R` + an AoE tag + the calculated cadence in seconds). First shipped with a *derived* top block (HP/DEF/CRIT/XP + a `move s · mob` line); an **independent playtester preferred the raw numbers**, so it was reworked (`a5de6fe`) to the **full raw 8-stat block** (CON/STR/RNG/MAG/LCK/AGI/MOB/DEF) — the derived HP/CRIT/move-cadence summary + the XP row were dropped (CON stands in for HP, LCK for crit, MOB for the move dial). Ability display **names are a render-only id→label map** in RecruitScreen (Strike/Shot/Gambit/Heal/Bolt/Lob) — NOT a `name` field in [abilities.json](config/abilities.json) (locked at the tee-up). Per-ability damage/heal reuses the sim's single-source-of-truth helpers (`damageStatFor` / `magic`-scaled heal) so the card can't disagree with battle; range/cadence/AoE from `abilityConfig`. The healer now reads honestly (`8 heal · rng 3`, not a misleading "DMG 0"); the mage shows `AoE 3×3`.
  2. **HUD** (`feat(GP3.2)` `67d5d1f`). [HUD](src/ui/HUD.ts) gains a second per-row sub-line beneath the kept `Lv · XP` sub showing the raw driving stats **`DEF · MOB · AGI`**, read straight off `unit.stats` (no `deriveStats`), for both rosters — a light touch on the 240px tracker (abilities stay on the card).
  - **Shared `STAT_LABELS`** extracted to [src/ui/statLabels.ts](src/ui/statLabels.ts) (out of PromotionScreen) so card / HUD / promotion read identically; PromotionScreen now imports it.
  - **Verification:** 588 tests still green (render isn't unit-tested here — no new tests, consistent with E3.6/E6); typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Browser-verified** via a synthetic all-archetype offer through the real RecruitScreen (all six cards: 8 raw stats, healer `8 heal`, mage `10 dmg · rng 5 · AoE 3×3`, catapult `MOB -7` / `rng 6`; two equal stat columns; AoE accent fluorescent-blue) AND a live battle (both rosters render `DEF d · MOB m · AGI a` reading live per-unit stats — leveled players show grown values, base enemies their archetype's); console clean both times. **NB** the JPEG screenshot intermittently times out on the throttled preview tab (documented gotcha) → DOM extraction is the authoritative proof.
  - **Follow-up logged** ([TODO.md](TODO.md)): the `RNG` stat label vs `rng` ability-reach read as ambiguous (same letters, two concepts) — cosmetic, flagged at the GP3.1 playtest. Per-archetype accent CSS for the new archetypes stays deferred to the post-H rarity overhaul (existing TODO).
- **GP4 complete (Phase GP) — acting-position pathing for ranged + catapult; sim-layer, no snapshot bump.** One commit (`feat(GP4)`). Ranged units (archers, mage) and the catapult now path to the nearest cell they can **fire** from instead of charging the target's own cell, so they hold at standoff with a clear shot / sidestep a wall that breaks LOS rather than creeping into melee.
  - **New shared helper** [src/sim/actingPosition.ts](src/sim/actingPosition.ts) `nearestActingCell(from, target, range, slack, world, losBlockers|null)` — a bounded FIFO BFS outward from the unit returning the first cell with `chebyshev(c,target) <= range` and (when `losBlockers` non-null) LOS. `null` blockers = range-only (the catapult's lob). BFS **traverses the same hard-blocker graph `findPath` uses** (in-bounds, finite tile cost, non-wall — built internally from `world.units`), so any returned cell is provably routable-to (no `pickGoalCellInRange`-style freeze). Capped at `range + SIM.actingCellSearchSlack` (new knob in [config/sim.json](config/sim.json), =2) BFS depth → a far target exceeds the cap and the caller falls back to charging.
  - **[MovementBehavior](src/sim/behaviors/MovementBehavior.ts) refactor:** the path+sidestep logic is now a `stepToward(goal)` closure used by both the firing-cell goal and the fallback. For `attackRange > 1` it tries `stepToward(firingCell)`; **falls back to `stepToward(target.position)` both when no firing cell is reachable AND when the step toward one is blocked** (a contested cell). The blocked-step fallback is the key anti-freeze guarantee — see the gotcha below. Melee (range 1) is **byte-identical** (skips the firing block). The catapult's `!ignoresLos` exclusion was **dropped** (it now repositions to hold at range, range-only); its in-range abstain still short-circuits first so the E7.D tests are untouched.
  - **Healer deferred to GP5 (acting-cell change reverted).** Investigated + confirmed a **no-op** for the healer: [SupportMovementBehavior](src/sim/behaviors/SupportMovementBehavior.ts) step 1 already idles the moment a wounded ally is within chebyshev heal range ([Targeting.ts `lowestWoundedAlly`](src/sim/Targeting.ts)), so the healer never charges to point-blank — the approach is already range-capped. The healer's real "backing off" is the panic-retreat oscillation, which is GP5's yield-rule territory.
  - **Verification:** **596 tests** (was 588; +6 [actingPosition.test.ts](src/sim/actingPosition.test.ts) helper unit tests + 2 [MovementBehavior.test.ts](src/sim/behaviors/MovementBehavior.test.ts) integration: in-range-but-LOS-blocked → repositions for a clear shot; catapult out-of-range → holds at range). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). Fuzz/determinism baseline shifts (ranged + catapult paths change — both recruitable since F1); regenerated the gitignored `tests/fuzz/output/*`. **Fuzz 20-seed: 0 hangs, 100% complete.** Sim-layer, no render change → headless is authoritative; **live feel (standoff distance, repositioning) is the user's playtest.**
  - **⚠ KNOWN RESIDUAL HANG (1/40-seed, strafingFunnel) — GP5 fixes it.** A first GP4-internal freeze WAS found + fixed (seed 12: every ranged unit's nearest firing cell was the same occupied strip tile → all jammed; the blocked-step fallback drains it). But the **40-seed** sweep surfaced a *second*, different hang (seed 30 greedy): a **healer idling on the only chokepoint cell**, blocking boxed-in melee from reaching a near-dead (hp 2) enemy while it out-heals the enemy's chip damage. **Every stuck unit uses GP4-UNCHANGED behavior** (melee target-cell pathing; the reverted healer) — it's a **pre-existing latent healer-blocks-chokepoint deadlock** that GP4's run *trajectory* exposes. **Baseline 40-seed = 0 hangs** (so GP4 is the proximate trigger). This is exactly **GP5's healer yield rule** ("healer vacates a chokepoint an ally needs to traverse") → **GP5 starts here.** User call: ship GP4 now, fix first thing in GP5.
  - **New gotcha (acting-position fallback):** the firing-cell goal must fall back to charging the target's cell **whenever it can't produce a move** — not only when `nearestActingCell` returns null, but also when the step toward a found firing cell is blocked. A contested firing cell (a strafing-funnel strip where N units share one nearest firing tile) otherwise strands the whole column. Charging the target drains the chokepoint as a pre-GP4 unit would. Do NOT abstain there.
  - **Balance note:** GP4 (ranged units fighting better at standoff) compounds GP2's defense win-rate inflation → fuzz win rate now ~95–100%. Still the deferred GP2-onward balance sweep's job; mechanically sound.
- **GP5.1 complete (Phase GP) — healer chokepoint yield via a SWAP mechanic; sim-layer, no snapshot bump.** One commit (`feat(GP5.1)`). Clears the GP4-exposed 1/40 hang (**greedy seed 30, strafingFunnel** — now resolves, full 10 floors). **The planned "step aside" vacate could NOT work** and was abandoned after tracing the live repro: the deadlock layouts are **1-wide corridors** with no lateral cell to step to, and the support can't pass its own fighters (they chase the same enemy single-file) — every forward vacate just makes the healer *lead* the column into the next bottleneck and re-block. The genuinely missing primitive is a **pass**, so #5 shipped as one.
  - **New [src/sim/actions/SwapAction.ts](src/sim/actions/SwapAction.ts)** (registered in [actions/registry.ts](src/sim/actions/registry.ts)) — an **atomic** position exchange: the boxed ally advances onto the healer's cell, the healer retreats onto the ally's, both set in one `start()` so the grid never transiently double-occupies (two separate `MoveAction`s would, since `MoveAction.start` writes `position=to` with no occupancy check). Emits `unit:moved` for **both** units (renderer lerps each — reuses the existing path, no new render code). Single-tick like a move; round-trips via the registry → **no snapshot bump**. Degrades to a plain step if the partner moved/died (snapshot-rehydrate safety).
  - **[SupportMovementBehavior](src/sim/behaviors/SupportMovementBehavior.ts) yield rule:** when the healer would otherwise idle/hold AND it sits on the **only** cell a living ally can advance through (`blockedAlly`), it proposes a `SwapAction` with that ally (score 1, so a ready `heal_ally` at 10 still wins — the swap only fires on heal-cooldown ticks). Two non-obvious pieces, both found by tracing:
    - **"Forward" is grid PATH distance, not Chebyshev.** In a funnel the route to an enemy runs *away* from it in straight-line terms (back through a gap, then around), so a Chebyshev "is this cell forward" test never fires. `blockedAlly` runs a **BFS distance field from the ally's `currentTarget`** (over the static neutral-wall topology, cached per target) — `h` blocks the ally iff `dist[h] < dist[ally]` and no *other* passable+unoccupied neighbour of the ally is closer. New helpers `distanceField` / `neutralCells` / `isNavigable` (the last two also seed GP5.2's snap). Occupancy test is **neutral-INCLUSIVE** (half-cover is a neutral *unit*, not a tile, so `tileGrid.costAt` reads finite — `passable` only rejects it via the occupied set).
    - **The yield must fire from the BLOCKED branches, not just the idle tail.** The healer is wedged exactly when an enemy is in panic range (step 2) or it's trying to trail a centroid the other side of a 1-wide row (step 4) — those branches `return`ed before the idle-tail yield. So steps 2/3/4 now fall through to `yieldChokepoint` when their own move is blocked. **A multi-healer regression was caught + fixed here:** with the *forward-vacate* version, three healers funnelling deadlocked the whole army at the spawn-row gap (a healer wanting to trail east vs. a fighter wanting the gap west — opposite directions in the 1-wide y=0 row, step 4 returned null and never swapped). The **swap** (sends the support to the rear, never leading) + the step-4 fall-through drains it.
  - **Verification:** **603 tests** (+7: 3 SupportMovement swap-yield + 4 [SwapAction.test.ts](src/sim/actions/SwapAction.test.ts) mechanic). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Fuzz: default (pure-random+greedy) 0 hangs / 100% win; 40-seed greedy 0 hangs / 95%; seed-30 strafingFunnel CLEARED.** Regenerated the gitignored `tests/fuzz/output/*`. **Residual (pre-existing, NOT a regression, OUT of GP5 scope):** the `recruit:healer` strategy still has **1 spiralFireLife hang** (baseline had it too) — it's a **degenerate all-healer team** (2 non-attacking healers + 1 archer vs 3 enemies) that's *damage-starved*, a slow grind exceeding the tick cap, not a positioning deadlock; my swap regressions in `recruit:healer`/`stat:magic` were found via the all-strategy sweep and fixed (back to baseline). **Browser-confirmed** (`?roster=healer,healer,melee,ranged,ranged`): a 2-healer battle resolves with **0 console errors**, and a forced `SwapAction` in the live `BattleScene` exchanges positions cleanly (the real renderer/audio `unit:moved` listeners handle both emissions, no throw). The **live swap *visual* (sprites crossing in a corridor) was NOT eyeballed** — the backgrounded preview throttles rAF so the sprite lerp doesn't advance under hand-driven ticks (documented constraint); it rests on the headless tests + the reused move-lerp path, and is **the user's playtest** (best on strafingFunnel / Endless Corridors).
  - **GP5.1 follow-ups (playtest-driven, 2 commits):** (1) **swap sprites lerp from their LIVE position, not the logical tile** (`fix`) — a `SwapAction` can yank a unit still mid-lerp from its own step, and `onUnitMoved` restarted the lerp from the `from` tile (a snap-then-ease *jitter* the user flagged). The move lerp now starts from `SpriteRenderer.getPosition` (the trick `startShove` already used); a normal move is provably unaffected (the unit is locked for its whole move-cooldown, so its sprite finished lerping and sits on `from`). (2) **swap promoted to a first-class `unit:swapped` event** (`feat`) — was two `unit:moved` events; now one `{unitA,unitB,cellA,cellB,durationTicks}` (the move/swap lerp factored into a shared `BattleRenderer.animateStep`). The mechanic is non-obvious, so a dedicated event leaves a clean hook for a future swap cue / VFX / telemetry **without** generalizing the pass (the healer stays the only emitter — user call). A degraded swap (partner gone post-rehydrate) still emits a plain `unit:moved`. Both browser-verified: a real `SwapAction` fires `unit:swapped`, both sprites animate, and a partner caught mid-lerp continues from its live position (dist 0.02) vs. snapping to its tile (dist 1.98).
- **GP5.2 complete (Phase GP) — healer centroid navigable-snap (#4); sim-layer, no snapshot bump.** One commit (`feat(GP5.2)`). `alliesCentroidCell` returns the rounded average ally position, which can land on an **impassable cell between allies** (a wall / half-cover, or a chasm); the step-4 trail then `findPath`s to that goal, gets `[]`, returns null, and the healer **stalls** instead of following. New `snapToNavigable` (a bounded BFS reusing GP5.1's `isNavigable` / `neutralCells`; deterministic — fixed `NEIGHBORS` order, first navigable at the min ring wins, floods through the grid so it works even if the centroid is inside a wall cluster) snaps the anchor to the nearest navigable tile before the gap check. **No-op when the centroid is already navigable** (the common case) → open-field trailing byte-unchanged. 605 tests (+2: snap-off-a-wall + navigable-no-op). Fuzz default + 40-seed greedy 0 hangs (the pre-existing `recruit:healer`/spiralFireLife residual unchanged). **This closes Phase GP.**
- **H1 complete (Phase H) — the `power` stat; first of Phase H's six (H1 → H2 → H3 → H4 → H5 → H6).** One commit (`feat(H1)`). Adds `power` to the unit stat block as the Phase-H meta-currency — a turn's surviving units will chip the *opposing* health pool by their Σ`power` (consumed in H4; **H1 is behavior-neutral** — no combat/`deriveStats`/`applyDamage` path reads it). Mirrors GP2's "add a stat" plumbing exactly. **Shipped values (roadmap-locked):** base **1** / growth **0.20**, **uniform across all 6 archetypes** (pure knob-tuning, re-balanced in H6).
  - **Plumbing** (the GP1/GP2 pattern): `power` added to `UnitStats` ([Unit.ts](src/sim/Unit.ts)) / `ZERO_STATS` ([stats.ts](src/sim/stats.ts)) / the base+growth zod schemas ([archetypes.ts](src/config/archetypes.ts)) / every archetype in [archetypes.json](config/archetypes.json) / `STAT_KEYS` (**appended LAST**, the 9th draw — each existing stat keeps its within-level draw position) + both [leveling.ts](src/sim/leveling.ts) return objects / the shared `STAT_LABELS` (`POW`, [statLabels.ts](src/ui/statLabels.ts) — auto-surfaces on the recruit + promotion cards, which iterate it) / the in-battle HUD line ([HUD.ts](src/ui/HUD.ts) `formatStats` → `DEF·MOB·AGI·POW`, user call to show it now).
  - **Roadmap correction:** the H1 card said base stats live in `config/stats.json` — stale; they're in **`config/archetypes.json`** (`stats.json` is balance scalars only).
  - **Snapshot bumps — BOTH (user call, beyond the roadmap's "WorldSnapshot bump"):** `WorldSnapshot` **v17→v18** + `RunSnapshot` **v5→v6**, because the roster `team: UnitTemplate[]` in the Run save also carries the stat block — a pre-H1 save would otherwise load a power-less roster → `NaN` power on the next level-up. Both reject stale versions outright (no migration; same stat-shape-contract rationale as GP1/GP2).
  - **Fuzz `stat:power` auto-appeared (intended):** the G5 strategy menu is config-derived off `STAT_KEYS`, so `power` auto-added a `stat:power` recruit strategy — and [strategies.test.ts](tests/fuzz/strategies/strategies.test.ts) is *also* config-derived (count + per-stat asserts off `STAT_KEYS`), so it stayed green. Updated the now-stale [registry.ts](tests/fuzz/strategies/registry.ts) comment: the genuinely-deferred-to-H6 piece is the **pass/no-recruit** strategy variant (needs new code) + the H6 balance-sweep *usage*.
  - **Verification:** **610 tests** (was 605; +balance-proof `power`-levels-per-config-growth cases in [leveling.test.ts](src/sim/leveling.test.ts) [derived from `ARCHETYPE_CONFIG`, never hardcoded] + H1 round-trip / stale-version-reject cases for World v18 AND Run v6 in [snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts); 16 `UnitStats` fixtures patched, driven off `tsc --noEmit`; the v17 pin in [spawn-overflow.test.ts](tests/integration/spawn-overflow.test.ts) bumped to v18). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); build green; fuzz:smoke 22/22. **Browser-confirmed (DOM-verified, 0 console errors):** drove a live run — HUD reads `DEF·MOB·AGI·POW` per unit, recruit cards show all 9 stats incl `POW` in canonical order, and a forced-level-up PromotionScene renders `POW 2 → 3 (+1)`. **User-playtested: "looks good."**
  - **Fuzz baseline shifts** (the extra per-level RNG draw advances the shared `levelupRng` stream → recruited rosters differ from pre-H1); expected for any stat addition, re-baselined in H6. Regenerate the gitignored `tests/fuzz/output/*`.
  - **TODOs logged** ([TODO.md](TODO.md)): the 4-stat HUD line is getting crowded (revisit presentation); `power` wants distinct visual treatment on the recruit/promotion cards (categorically different mechanical impact). Both cosmetic, out of Phase H scope.
- **H2 complete (Phase H) — spawn-region tile count relaxed to a 1–10 range; random-subset placement locked in tests. Second of Phase H's six (H1 ✅ → H2 ✅ → H3 → H4 → H5 → H6).** One commit (`feat(H2)`). **Finding that reshaped the step:** H2's specced mechanic — *"in `spawnTeam`, when units < region tiles, pick the used tiles at random instead of filling first-N (both teams)"* — was **already live since D5.B** (`9410227`). [`spawnTeam`](src/sim/battleSetup.ts) shuffles `region.tiles` (Fisher–Yates) then places `min(team, tiles)` units on the first N, which is a uniformly random subset, in random order, for BOTH teams, every battle. Regions are 8 tiles and the roster is 5, so the subset case engages in normal play. So H2 became (1) **relax the hard 8-tile-per-region constraint** and (2) **lock the property in tests**.
  - **The relaxation:** new `SPAWN_REGION_MIN_TILES=1` / `SPAWN_REGION_MAX_TILES=10` in [config/layouts.ts](src/config/layouts.ts); the zod `tiles` rule went `.length(8)` → `.min(1).max(10)` (the "8" was an old 8-card-hand relic). `SPAWN_REGION_TILE_COUNT=8` is **repurposed as the procedural/editor default** (procedural top/bottom edge bands + the layout editor's starting region template stay 8-wide); only hand-authored regions get the range. Bigger regions ⇒ more positional variance (the H2 foregone-conclusion antidote); a region *smaller* than the team trickle-spawns the remainder through the **D5.C overflow queue** (a "gate"/reinforcement lever).
  - **Sim untouched — it was already tile-count-agnostic:** every consumer reads `region.tiles.length` (`spawnTeam`'s `min(team,tiles)` + overflow, snapshot round-trip, camera anchor, centroid helpers); nothing indexes a fixed slot. No sim/snapshot change. `SpawnRegion` round-trips its tiles as-is, so **no snapshot bump.**
  - **Layout editor** ([tools/layout-editor/editor.ts](tools/layout-editor/editor.ts)): paint FIFO-cap moved to `SPAWN_REGION_MAX_TILES`, export validation flags out-of-range (only the empty/`<min` case is reachable since the cap holds the top), pill label reads `n/10`. The default-region builder still lays an 8-tile band.
  - **No fuzz baseline shift** (notable — the roadmap predicted one): the shuffle's RNG was already consumed and no shipped layout changed (all still 8, comfortably in range). This *confirms* the mechanic predated H2.
  - **Roadmap/GP5 correction:** the GP5 note "H2 randomizes spawn tiles, which defeats spawn-order bias" assumed spawning was *currently* order-based — it wasn't (D5.B already shuffled), so spawn-order bias was never an available lever. The healer-yield fix (GP5.1) was the right durable call regardless.
  - **Verification:** **620 tests** (was 610; +6 in new [spawn-placement.test.ts](tests/integration/spawn-placement.test.ts) — subset ⊆ region, every tile covered over 200 seeds, deterministic per seed, varies across seeds, both teams, exact-fill boundary; +4 schema-range accept/reject in [layouts.test.ts](src/sim/layouts.test.ts) — min/max/default accepted, out-of-range + duplicate rejected, default ∈ range; the per-layout exact-8 assertion relaxed to the range). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning); fuzz:smoke 22/22. **Headless-only — the editor UI change (paint cap / validation / label) is the user's eyeball pass** in the standalone editor tool; no game-render surface was touched.
- **H3 complete (Phase H) — per-unit deployment counter (the fatigue hook); sim/run-layer. Third of Phase H's six (H1 ✅ → H2 ✅ → H3 ✅ → H4 → H5 → H6).** One commit (`feat(H3)`). A per-roster-slot counter that bumps each time a unit is deployed into a turn and **resets per encounter** — pure bookkeeping now, shipped with a clean seam for the *future* fatigue debuff (deferred to H6 if the long-run sweep shows turns are foregone conclusions). **User decisions (both as recommended):** (1) **storage** = a **parallel `Run.deploymentCounts: number[]`** keyed by rosterIndex (NOT a `UnitTemplate` field) — keeps the template a clean unit-definition, makes the per-encounter reset a plain `fill(0)`, and avoids leaking player-only state onto enemy/live-spawn templates; (2) **wired live at the deploy boundary now** (not dormant-until-H4).
  - **Two public methods are the stable seam the H4 turn loop will drive:** `resetDeploymentCounts()` (zero all — call at encounter start) + `recordDeployment(rosterIndices)` (bump each deployed slot — call per turn with the deployed hand; out-of-range indices ignored). [Run.ts](src/run/Run.ts).
  - **Live wiring (pre-H4: an encounter == one battle):** `handleEnterNode` resets at encounter start (right after `phase='battle'`) then records one deployment of the **full roster** (pre-H5 the whole roster is the "hand") → every count reads **1** mid-battle. H4 moves the reset to the encounter-loop entry + the record to each turn's hand spawn so multi-turn encounters accumulate; H5 narrows the recorded set to the drawn hand. **Stays synced with the roster:** a recruit (`handleChooseRecruit`) appends a fresh `0`.
  - **Snapshot:** **RunSnapshot v6→v7** (`deploymentCounts: number[]` added; a v6 save is rejected outright — no counts ⇒ out-of-sync-with-roster risk). **No WorldSnapshot bump** (counts are Run-side; the future debuff reads them at deploy time, the World never needs them).
  - **No RNG / fuzz / determinism impact** — incrementing a counter draws no RNG and feeds no behavior. Fuzz:smoke 22/22 (no baseline shift).
  - **Verification:** **628 tests** (was 620; +7 in [Run.test.ts](src/run/Run.test.ts) — init zeros, deploy records 1/slot, per-encounter reset proven (a 2nd battle reads 1 not 2), recruit appends 0, accumulate-then-reset (the H4 seam), out-of-range ignored, toJSON→fromJSON round-trip; +1 in [snapshot-roundtrip.test.ts](tests/integration/snapshot-roundtrip.test.ts) — v7 carries counts parallel to roster + v6 rejected). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **Headless-only — no render surface touched** (the counter has no UI yet; any fatigue readout is H6+).
- **H4a complete (Phase H) — health pools + the multi-turn encounter loop, HEADLESS core. Fourth of Phase H's six (H1 ✅ → H2 ✅ → H3 ✅ → H4 → H5 → H6); H4 is split into H4a (this, headless + fully vitest-tested) → H4b (render + the two screens, NOT started).** The riskiest orchestration change in the plan: a "battle node" is now an **encounter** = a series of **turns**, each turn the full roster vs a **freshly-rolled** enemy wave, with two **health pools** deciding it. The tactical `World` stays a single-turn engine; the loop lives on `Run`. **Five commits** (each green; no browser pass — H4a is headless by the user's split, verified by `npm test`):
  1. **`enemyBudget` split** (`refactor`, byte-identical) — `enemyBudgetFor(team)→number` (budget, no RNG) + `rollEnemyWave(rng, team, budget)→UnitTemplate[]` (composition) out of `buildEnemyTeam` (now their composition). H4 fixes the budget once per encounter and re-rolls the wave per turn.
  2. **`battle:ended` reporting** (`feat`) — payload gains `winner: …|'draw'` + `survivorPower: {player,enemy}` (Σ `stats.power` over each team's living **on-grid** units; spawn queue excluded). `World.checkBattleEnd` routes through a shared `emitBattleEnded()` and **drops the `winner==='player'` gate on `xpAwards`** (`computeXpAwards` was already winner-agnostic) so every turn's damage banks. New **`World.resolveAsDraw()`** lets the *driver* force-end an unresolved turn as a draw; it also terminates a genuine mutual wipe (which `checkBattleEnd` still leaves un-ended to preserve the walls-only/pre-spawn invariant — gotcha: a blanket draw-on-`!playerAlive&&!enemyAlive` would trip the setup tick). Flipped the `World.test`/`Run.test` enemy-win cases. `survivorPower` is **optional** on the event so test fakes don't need it; every real emit sets it.
  3. **`config/health.json` + loader** (`feat`) — `playerHealthMax 20`, `enemyHealthMax 8`, `maxTurns 20`, `maxTurnSeconds 100` (→`secondsToTicks` at the consumer), `chipMultiplier 1`. Mirrors `difficulty.ts`.
  4. **The encounter loop + `RunSnapshot` v7→v8** (`feat`) — `Run` gains `playerHealth` (run-wide, persists), `enemyHealth`/`turnIndex`/`encounterBudget` (per-encounter), `pendingEncounterXp` (accrued, banked once at end). Structured `beginEncounter→beginTurn→resolveTurn→endTurnAndContinue→finishEncounter` **so H4b can insert the pre/post-turn screen gates without restructuring the loop** (mirrors the `promotion`-phase pause). `handleEnterNode`'s battle branch → `beginEncounter`; budget fixed via `enemyBudgetFor`, wave re-rolled via `rollEnemyWave`. Chip is symmetric off `survivorPower` (winner ignored). **Precedence: `playerHealth≤0`→defeat FIRST (run-loss terminal even on a both-zero turn), then `enemyHealth≤0`→win, then `turnIndex≥maxTurns`→resolve by remaining pool fraction (player loses ties).** XP banks once at encounter end (one PromotionScene), discarded on defeat. `handleBattleEnded`→`handleTurnEnded` (passes `survivorPower`, not `winner`). `Run.test` reworked onto `winEncounter`/`loseEncounter`/`chipTurn` helpers + 11 new loop cases.
  5. **Integration test + v8 round-trip** (`test`) — `tests/integration/encounter-loop.test.ts` drives the loop over **real Worlds** (a World per turn from `run.currentEncounter`, **polling `currentEncounter`** so there's no `battle:started` re-entrancy; the per-turn cap calls `resolveAsDraw`): terminates + ends the node (4 seeds), **resume determinism** (snapshot→restore→identical turn-for-turn encounters), 1-tick cap draws chipping **both** pools, distinct per-turn `worldSeed`s (fresh roll). `snapshot-roundtrip` gains the v8 mid-encounter pool round-trip + v7-reject.
  - **Determinism:** the per-turn `battleRng` forks at `beginTurn` (never looked ahead / stashed), so the snapshotted `this.rng` ALONE reconstructs every future turn's wave; **turn 1 is byte-identical to the pre-H4 single battle** (`enemyBudgetFor` draws no RNG). `encounterBudget` is **persisted, not recomputed on resume**.
  - **ROADMAP correction:** the H4 card predicted a **WorldSnapshot bump** — **none was needed** (driver owns the per-turn cap, not the World; there is NO World between turns — `BattleScene` rebuilds it per `battle:started` — so a mid-*encounter* save is pure Run state). Only `RunSnapshot` bumped (v7→v8).
  - **Verification:** **649 tests** (+8 new loop/round-trip; `World.test`/`Run.test`/`determinism.test` enemy-win + helper cases updated, not deleted). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **fuzz:smoke 22/22.** **Fuzz default+greedy: 0 hangs — BUT win rate jumped ~45-60% → 100%, avg floor 10.** That is the roadmap's **foregone-conclusion** risk made real: with **no within-turn attrition** (units recycle to full HP each turn) + a big player pool (20) vs small enemy pool (8), the player chips the enemy out faster than they take damage and almost never loses. **This is expected + is the H6 signal** — the **fatigue debuff** (built on H3's `deploymentCounts`) is the documented escape hatch, wired in H6 only if the balance sweep confirms it. H4a's job is the *mechanism* (correct, deterministic, hang-free); the *balance* (pool sizes, `power` growth, fatigue) is H6.
  - **H4b ✅ (render + the two screens, the user's design — see the H4b entry below):** a **pre-turn screen** + a **post-turn outcome screen**, both auto-advancing, built extensible; + the in-battle pool HUD. The async screen gates break the live `battle:ended→battle:started` re-entrancy H4a's headless tests sidestep.
- **H4b complete (Phase H) — the render half of H4: the pre/post-turn screens + the in-battle pool HUD + the live turn loop. PHASE H4 IS NOW COMPLETE (H4a headless ✅ + H4b render ✅).** Two commits.
  1. **Turn-gate wiring** (`feat(H4b)`) — `Run` gains **`pauseAtTurnGates`** (default OFF = the H4a synchronous loop, so every headless test + the fuzz harness stay byte-identical; **Game flips it ON**). When on, the loop pauses on two new phases — **`turn-intro`** before each turn (emits **`turn:starting`**) and **`turn-outcome`** after a turn resolves (emits **`turn:resolved`** with the per-side chips + the encounter `result`) — each resumed by the new **`advanceTurn`** command. These pauses also break the live `battle:ended→battle:started` re-entrancy. Refactor: `resolveTurn` now only chips/accrues; the decision (`turnResult`) + continuation (`continueAfterTurn`) are split so the post-turn screen can show the result before the loop acts. **Presentation-only — the phases + flag are NOT snapshotted; no schema bump.** +5 gated-path tests; the flag-OFF path proven byte-unchanged (existing Run/encounter suites + fuzz:smoke green).
  2. **Screens + pool HUD + Game wiring** (`feat(H4b)`) — `PreTurnScene`/`PreTurnScreen` (Turn N + both pool gauges + a deck-draw placeholder + a "Fight" skip) and `PostTurnScene`/`PostTurnScreen` (the tactical winner + the Σ`power` chips + post-chip pools + the `Encounter cleared` / `Your run ends here` / `Next: Turn N+1` status + a "Continue" skip), both **auto-advancing on a timer** (`PRETURN_AUTO_MS` 2.0s / `POSTTURN_AUTO_MS` 3.0s — user-tuned by feel) via `advanceTurn`, built **extensible** for the H5/H6 deck-draw + input. Shared `renderPoolGauge` ([poolGauge.ts](src/ui/poolGauge.ts)) drives both screens; the HUD gains a compact `Turn N · You X/20 · Foe Y/8` line. Game flips `pauseAtTurnGates` on in `createRun`, maps `turn:starting`→`PreTurnScene` + `turn:resolved`→`PostTurnScene`, forwards `advanceTurn`. The `turn:resolved` swap fires from INSIDE the ending `world.tick()` — **safe** because `BattleScene.tick` + the `Clock` callback null-guard everything `dispose()` tears down (`checkBattleEnd` is `tick`'s last step).
  - **Verification (browser, DOM + captured event payloads, 0 console errors):** drove map → pre-turn (Turn 1, 20/20 vs 8/8) → battle (HUD pools render) → post-turn across all three outcomes — **won** (green, enemy pool → 0, → recruit), **ongoing draw** (chips 2/3, pools **carry** 17/6 into Turn 2 — confirmed via the `turn:starting` payload; **NB the synchronous-eval DOM read is polluted by the ~180ms screen fade-out**, so the *events* are authoritative, not a mid-fade `querySelector`), **lost** (red, "Your run ends here." → GameOver). 654 tests; tsc + lint clean. **Subjective feel — screen timing / layout / copy — is the user's playtest;** all knobs are isolated consts + CSS. **Carries the H4a foregone-conclusion note: with the placeholder balance the fuzz wins ~100% (the H6 fatigue-debuff signal).**
- **H5 (headless mechanics) complete (Phase H) — the card-drawn hand + the `playerTeamLevel` seam swap. The deckbuilder is now mechanically live.** One commit (the **pre-turn hand DISPLAY is the H5b follow-up** — user split it "headless mechanics first," our cadence). **Deck resets per encounter** (user call — no deck state carries between encounters, matching the no-carried-wounds model).
  1. **The seam swap** ([enemyBudget.ts](src/run/enemyBudget.ts)) — `playerTeamLevel` flipped from "Σ of roster levels" → **`avgLevel × min(rosterSize, handSize)`** (the *expected hand* level, since only a `handSize` hand fights each turn). Broke the old `avgTeamLevel → playerTeamLevel` call chain via a private `rosterLevelSum` (recruits still read the plain mean; the public seam is now a non-average product). **For a roster ≤ `handSize` the new value EQUALS the old sum** (`avg × size == sum`), so the starting roster's battles + every existing balance-proof test are unchanged; the swap only diverges once the roster outgrows the hand — which is the **treadmill fix** (recruiting past `handSize` adds deck cards, NOT enemy budget; only your *average* level moves it).
  2. **The deck** ([Run.ts](src/run/Run.ts)) — `Run` gains `drawPile` / `discardPile` / `hand` (all hold `rosterIndex` values) + a dedicated **`deckRng`** stream (isolated like `levelupRng`, forked **last** in the ctor). `beginEncounter` rebuilds + Fisher–Yates-shuffles the draw pile from the **current** roster (so a fresh recruit is in the deck) and clears hand/discard — **encounter-scoped**. `beginTurn` discards the previous hand, then `drawHand()` draws up to `DECK.handSize` (popping the draw pile; reshuffling the discard back in when it empties; stopping early only when both piles are exhausted, i.e. roster < handSize fields everyone). **Only the drawn hand fights** (the per-turn `currentEncounter.playerTeam` is the hand, not the roster); `recordDeployment` now narrows to the hand, so **H3's deployment counter finally varies per turn** (the fatigue hook gets real signal). New `config/deck.json` + [src/config/deck.ts](src/config/deck.ts) (`handSize: 5`, A4 two-layer pattern).
  - **RunSnapshot v8→v9** (adds the three piles + `deckRng`; v8 rejected). **No WorldSnapshot bump** — same as H4: the World just receives the hand's templates, the deck is pure `Run` state. Round-trip + resume determinism pinned (a restored mid-encounter Run draws the identical *next* hand).
  - **The cliff (why handSize 5):** the **starting roster (5) == handSize (5)**, so `min(roster, handSize)` is pinned at 5 — the deck "turns on" only after the **first recruit** (roster 6 > 5). Before then every hand is the full (shuffled) roster; variance comes from the fresh-wave + random-spawn, not the draw.
  - **Determinism / baseline note:** the extra `deckRng` ctor fork **shifts every subsequent `this.rng.fork()`** (per-turn waves, recruit offers), so **H5 re-baselines the fuzz output** — *expected* here (unlike H2–H4's byte-identical changes), since the seam swap + the drawn-hand subset already change battle outcomes wholesale. Regenerate the gitignored `tests/fuzz/output/*`.
  - **Verification:** **664 tests** (+10 net: −2 reframed whole-roster-hand assertions, +12 H5 — seam `>handSize` cases, deck cap/overdraw/partition/coverage/per-encounter-rebuild/determinism, snapshot v9 round-trip + resume-determinism). Typecheck + lint clean (lone pre-existing `Renderer.ts` warning). **fuzz:smoke 22/22.** Live smoke (`__game`, dev build = gated path): map → `turn-intro` → `battle` spun up a World from a **shuffled 5-card hand** `[1,4,0,2,3]`, deck partition `[0..4]` intact, **0 console errors** (screenshot times out — the documented backgrounded-tab rAF throttle, not a failure). **The post-recruit dilution (a 5-of-6 hand) + the live deck feel are the user's playtest** (the headless suite pins the subset/cap/dilution logic).
  - **H5b ✅ (the render follow-up — see the entry directly below):** the drawn hand is now SHOWN on the pre-turn screen.
- **H5b complete (Phase H) — the pre-turn hand display. PHASE H5 IS NOW COMPLETE (H5 mechanics ✅ + H5b render ✅).** One commit.
  - **The draw moved before the gate** ([Run.ts](src/run/Run.ts)) — the hand was drawn in `beginTurn` (which runs on `advanceTurn`, *after* the pre-turn screen shows), so the screen had nothing to display. Split a new `drawTurnHand()` (discard previous hand + draw) out of `beginTurn` and call it at the top of `startNextTurn` (BOTH paths), so the hand exists before `turn:starting` fires. `beginTurn` now just fields the already-drawn `this.hand`. **Determinism preserved** — the lone `deckRng` draw still fires once/turn in the same order, and `this.rng` (the per-turn `battleRng` fork in `beginTurn`) is an independent stream, so moving the deck draw a few lines earlier shifts nothing: the headless path stays byte-identical (fuzz:smoke 22/22 unchanged).
  - **The payload + screen** — `turn:starting` gains **`hand: UnitTemplate[]`** (this turn's drawn cards, in draw order; [events.ts](src/core/events.ts)). `PreTurnScreen` ([PreTurnScreen.ts](src/ui/PreTurnScreen.ts)) replaces the H4b "Deploying your squad…" placeholder with a **row of compact cards** — archetype glyph (green, player units) over a `Lv N` tag, label "Your hand — N drawn". New `.preturn-hand*` / `.preturn-card*` CSS (mirrors the recruit-card palette). **No snapshot/config change** (a render + payload addition; the hand is already in `Run` state).
  - **Verification:** **665 tests** (+1: `turn:starting` carries the drawn hand, capped, matching `Run.hand`). Typecheck + lint clean; fuzz:smoke 22/22 (reorder byte-identical on the headless path). **Browser-verified** (`?roster=melee,melee,melee,ranged,ranged,mage,rogue` = 7 units): the pre-turn screen rendered a **5-of-7 hand** — DOM cards `M r a M m` exactly matched the drawn archetypes (melee/rogue/ranged/melee/mage), label "Your hand — 5 drawn", capped at handSize; `advanceTurn` → battle fielded that exact same 5-card team (displayed hand == fighting hand); **0 console errors** (only the expected dev roster-override warning). Screenshot times out (the documented backgrounded-tab rAF throttle — the three.js loop runs behind the DOM overlay), so DOM inspection is the authoritative channel; **the subjective card look/layout is the user's playtest.**
- **H6 complete (Phase H) — the Phase-H gameplay closers. PHASE H6 IS NOW COMPLETE (H6a ✅ + H6b ✅ + H6c ✅).** Three commits, playtest pause between each; **no snapshot bumps** (all touched persisted state already existed). Leaves the trial fully playable end-to-end — rough balance is H7's job.
  - **H6a — rest-node pool heal** (`1b45c65`). [Run.ts](src/run/Run.ts) `resolveRest` now heals the run-wide `playerHealth` by the new `restHealAmount` knob (default **5**, capped at `playerHealthMax`; [config/health.json](config/health.json) + [health.ts](src/config/health.ts)), unconditionally + ahead of the XP/promotion branch so a rest that ALSO levels a unit still heals. Placeholder beside the rest-XP award until the real event system. 2 balance-proof tests (heal-when-wounded + never-overfill, derived from `HEALTH.*`).
  - **H6b — pass / no-recruit option** (`e221271`). New `passRecruit` [RunCommand](src/run/Command.ts) + `handlePassRecruit` (the `handleChooseRecruit` sibling MINUS the roster/`deploymentCounts` push — drops the offer, → `phase='map'`); [Game.ts](src/Game.ts) swaps MapScene on the silent fall-through (mirrors `chooseRecruit`); a **"Pass" button** on [RecruitScreen](src/ui/RecruitScreen.ts) + `.recruit-pass` CSS (always available + free, the trial default). **Fuzz interface widened** `pickRecruit → number | null` ([Strategy.ts](tests/fuzz/Strategy.ts)/[policies.ts](tests/fuzz/strategies/policies.ts)/[harness.ts](tests/fuzz/harness.ts), `null` → `passRecruit`); existing policies still return a `number` so their draw sequences + the default-sweep baselines are byte-identical. New opt-in `pass:weak` strategy (`declineBelowPower(2)`, OUT of the default set) proves the path — 12/12 complete, 0 hangs. **Browser-verified** the button renders below the cards + click → map with roster unchanged, console clean.
  - **H6c — inert spawn-time fatigue hook** (`f4ce83d`). **⚠ RE-SCOPED with the user — the ROADMAP's §H6c describes a CHIP-time recompute; we shipped a SPAWN-time unit debuff instead.** Cleaner: the debuff rides the unit into battle and flows to the chip via the already-fatigued power, with **zero** World/event/chip change. [Run.beginTurn](src/run/Run.ts) bakes `fatigueFactor(stacks)` (new [src/run/fatigue.ts](src/run/fatigue.ts) — `1 − fatiguePerStack·stacks` clamped at 0; default rate **0** → factor 1.0 → **INERT**) into a **fresh copy** of each fielded unit's `power` at the spawn-prep seam. `stacks` = `deploymentCounts[idx]` read BEFORE the `recordDeployment` bump (so a debut unit = 0 stacks; a unit that fought 3 prior turns this encounter = 3); the fresh `stats` object keeps `this.team` canonical. **`power` is the placeholder target** — the eventual vision (user's) is a stackable "Fatigued" status owned by a future status-effect system; target stat + curve + magnitude are H7's, a localized re-wire. **No snapshot bump** — the fatigued stats bake into the already-persisted `currentEncounter.playerTeam`. Tests: `fatigueFactor` inert-default canary + linear/clamp mechanic ([fatigue.test.ts](src/run/fatigue.test.ts)); Run integration (inert at default; a redeployed unit fields weaker once the knob is flipped; roster never mutated). **Fuzz baselines unchanged — confirms inert.**
  - **Verification:** **675 tests** (main suite, +10 over H5b's 665: 2 H6a + 2 H6b-Run + 3 fatigue-unit + 3 H6c-integration); **fuzz:smoke 24** (+2 `declineBelowPower`); fuzz sweep baselines **unchanged** (H6c inert, H6a/b don't touch the default strategies); typecheck + lint clean (lone pre-existing `Renderer.ts` warning).
- **H7a ✅ — the linear scored fuzz strategy + JSON config.** First half of **H7 (search-driven balance tooling)**; H7b (the random-search driver) is next. Adds an expressive `scored` strategy — a weighted sum of normalized features → argmax expressing **path + unit + pass** preference *simultaneously* (vs the one-axis-at-a-time menu). **Zero `src/` changes** (everything in `tests/fuzz/` + `config/`), so the `pure-random`/`greedy` byte-for-byte baselines + the whole `src` suite are untouched — no fuzz re-baseline. New `config/fuzz-strategies.json` is one weight vector — BOTH the `--strategy=<file>.json` input format AND the format H7b's search will emit as its winner (ships **all-zeros = neutral**: take offer card 0, lowest-id path, never pass). Loaded by [scoredWeights.ts](tests/fuzz/strategies/scoredWeights.ts) (A4 zod pattern, but colocated with the fuzz tooling — dev-only, kept out of the production bundle; the path/archetype/stat sub-schemas are built **dynamically from `PATH_KINDS`/`ALL_ARCHETYPES`/`STAT_KEYS`** so the **~21-weight** vector auto-tracks the vocabulary, and an unknown/missing key throws). [scored.ts](tests/fuzz/strategies/scored.ts): node policy = **full-path backward DP** over `run.nodeMap` (`bestScore(n)=kindWeight(n)+max(child)`, memoized; layered DAG → max-total == max-average → no long-path bias); recruit policy = **min–max-normalized-over-{offer∪roster} argmax** (the spec's explicit anti-trap — NOT min-max-within-offer) + a **virtual pass candidate** scored on the continuous terms only (PASS iff `cont(bestCard) − cont(rosterAvg) + passBias < 0`). **Spec-staleness fix:** the ROADMAP §H7a says "8 stats (7 + power)" — GP1/GP2 since made it **9** (`speed→agility`, `endurance→mobility`, + `defense`, + `power`); derived from `STAT_KEYS` so it's self-correcting. **Selection seam (user call — built INERT):** both decisions route through `selectByScore(scores, rng, {temperature=0, tiebreak='lowest'})` which at the defaults is exactly today's argmax + lowest-index and **draws nothing** from `rng`; softmax (τ>0) / random tiebreak are reserved (throw loudly if requested) so turning them on later is a one-flag flip (the harness already threads `strategyRng` into both decisions). Optional inert `temperature` field reserved in the schema (default 0, **NOT** a search dimension this cycle). `scored` registered in the menu (joins `all`, kept OUT of `DEFAULT_STRATEGY_NAMES` like `pass:weak`); `--strategy=<file>.json` loads an arbitrary vector ([cli.ts](tests/fuzz/cli.ts)). `PATH_KINDS` hoisted to [policies.ts](tests/fuzz/strategies/policies.ts) as the single source (was a registry-local const). **Verification:** `npm test` **675/675** (src untouched); **fuzz:smoke 34** (+10 scored: seam-inert + draws-nothing, path-DP looks past the local max, pass-boundary sweep, `maximizeStat` generalization across every stat, config round-trip + unknown/missing-key reject); `npm run lint` 0 errors (lone pre-existing `Renderer.ts` warning); strict `tsc` on the new files clean (the only flagged lines are the env `node:fs` class the existing `cli.ts` already shows — the repo has no `@types/node` and never tsc's `tests/fuzz`). `--strategy=scored` + `--strategy=config/fuzz-strategies.json` both run end-to-end → **100% win / floor 10** = the documented foregone-conclusion baseline the all-zero neutral vector can't escape; **moving that band is H7b/H7c's job, not this vector's.**
 - **Next up** — **Phase G COMPLETE (G1 ✅ G2 ✅ G3 ✅ G4 ✅ G5 ✅).** A post-G5 playtest surfaced a 7-item feedback cluster (movement/attack-speed legibility, healer + ranged AI, the missing defense stat), synthesized into a **new Phase GP** ("Phase-G playtest response") in [ROADMAP.md](ROADMAP.md) §Phase GP — sequenced **before Phase H**. **Do GP first, then H.** **Phase GP** (5 commits, playtest pause between each): **GP1 `endurance→mobility` + `speed→agility` rename + universal move base + split `cdPerStat` into per-axis `mobilityCdPerStat`/`agilityCdPerStat` (WorldSnapshot bump) → GP2 defense stat + `world.applyDamage` damage-site consolidation (subtractive `max(1, raw−def)`, env damage unmitigated; WorldSnapshot bump) → GP3 card legibility overhaul (ability list + driving stats shown directly + DEF row; render-only) → GP4 ranged firing-position pathing (path to nearest in-range+LOS cell, fallback to target-cell; no snapshot bump) → GP5 healer navigable-snap + yield rule (no snapshot bump).** **User decisions LOCKED this round:** subtractive defense (NOT diminishing-returns); `speed→agility`; healer #5 fix = **yield rule** (rear-bias rejected — healers spawn at the *front* in labyrinth/corridor layouts, also partly a maps problem); per-axis cooldown-rate split (recommend splitting `minCdScale` too). **GP1 ✅ GP2 ✅ GP3 ✅ GP4 ✅ shipped** (GP1: the `endurance→mobility`/`speed→agility` rename + per-axis cooldown-knob split + universal move base, WorldSnapshot v16, user-playtested feel-preserving; GP2: `defense` stat + `world.applyDamage` damage-site consolidation, subtractive `max(1, raw−def)`, env damage UNMITIGATED, WorldSnapshot v17, melee-tanky values — **fuzz win 25%→90%, proper balance sweep DEFERRED by the user**; GP3: card legibility overhaul — recruit card → raw 8-stat block + per-ability list, HUD → `DEF·MOB·AGI` line, render-only, no snapshot bump, playtester pivoted the card's top block to raw stats; **GP4: acting-position pathing — ranged + catapult path to the nearest cell they can fire from via `nearestActingCell`, fallback to charging when no firing cell is reachable OR the step to one is blocked; healer deferred to GP5; 596 tests; 1/40 strafingFunnel hang remains, a pre-existing healer deadlock for GP5**). **Phase GP COMPLETE (GP1 ✅ GP2 ✅ GP3 ✅ GP4 ✅ GP5 ✅).** GP5 shipped as **GP5.1** (the #5 yield rule, but as a **`SwapAction` pass mechanic** rather than the planned "step aside" — the 1-wide layouts have no cell to step to; cleared the seed-30 hang) + 2 playtest follow-ups (live-position swap lerp; `unit:swapped` event) + **GP5.2** (the #4 centroid navigable-snap) — see the GP5.1/GP5.2 entries above. **Phase H IN PROGRESS — H1 ✅ + H2 ✅ + H3 ✅ + H4 ✅ shipped (the `power` stat; spawn-tile range; deployment counter; the encounter loop [H4a headless] + the pre/post-turn screens & pool HUD [H4b render] — see the H1–H4b entries above); **H5 ✅ shipped** (H5 mechanics = card-drawn hand + the `playerTeamLevel` seam swap, headless; H5b = the pre-turn hand display — see the H5/H5b entries); **H6 ✅ COMPLETE 2026-06-05 (rest-heal + pass option + inert spawn-time fatigue hook — see the H6 entry above); **H7a ✅ shipped** (the linear scored strategy + `config/fuzz-strategies.json`), next is **H7b** (the random-search driver).** Phase H (the deckbuilder battle trial — biggest + most experimental, design-locked to sub-step level): **H1 power stat ✅ → H2 randomized spawn tiles ✅ → H3 deployment counter (fatigue hook) ✅ → H4 health pools + turn/encounter loop (H4a ✅ headless + H4b ✅ render) → H5 card-drawn hand + `playerTeamLevel` seam swap (mechanics ✅ + H5b pre-turn hand display ✅) → H6 recruit pass + rest-pool-heal + balance sweep** (H6 also folds in the deferred `power`-stat / pass-option fuzz strategies + the health-pool launcher knobs + the full long-run balance sweep). G4 shipped the `playerTeamLevel()` **seam** H5 swaps (live in [enemyBudget.ts](src/run/enemyBudget.ts)); **GP2's "add a stat" plumbing + GP3's card overhaul are what H1/H6 build on** (GP de-risks H). **Hard prereqs already met:** G4 (seam) + G3 (rest nodes) + G1 (short-run harness). Full Phase H detail in [ROADMAP.md](ROADMAP.md) §Phase H. **Difficulty re-tune expected after GP+H** — the user kept presets A/C in [difficulty.ts](src/config/difficulty.ts) precisely because the dumb fuzz bots understate human winnability; revisit once defense + smarter strategies + the turn loop land.
- **Tests:** 675 passed (H6; 665 at H5b, 664 at H5, 654 at H4b, 628 pre-H4), 0 `it.todo()`. Run with `npm test`. Fuzz smoke (separate suite, opt-in): 34 pass (H7a added 10 in `scored.test.ts`) — `npm run fuzz:smoke`. **Fuzz sweep (`npm run fuzz`): post-H5 pure-random + greedy = 100% win / avg floor 10 / 0 hangs (unchanged from post-H4a — the no-attrition pools, not the budget, drive the foregone conclusion; the seam swap shrinking the budget for grown rosters + the smaller drawn hand roughly cancel for win rate). This stays the H7 balance-sweep signal (H6c shipped the *inert* spawn-time fatigue hook; H6 left the fuzz baselines unchanged — turning fatigue on is H7's data-driven call).** H7a added the `scored` strategy (`npm run fuzz -- --strategy=scored`, or `--strategy=<file>.json` for any weight vector); H7b will random-search its weights for the best-achievable win rate. Full strategy menu: `npm run fuzz -- --strategy=all` — **one pre-existing residual: `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, NOT a positioning deadlock — out of GP5 scope; see the GP5.1 entry).** Per-floor balance analysis: `npm run fuzz -- --per-floor`. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`.
- **Dev server:** not running. Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent).
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; the "Things that bit us" list below for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

Forward-looking work lives in [ROADMAP.md](ROADMAP.md) -- see the **Next up** bullet at the end of `## Current state` above for the immediate step (**Phase H IN PROGRESS — H1 ✅ (`power`) + H2 ✅ (spawn-tile range) + H3 ✅ (deployment counter) + H4 ✅ (the health-pool encounter loop [H4a headless] + the pre/post-turn screens & pool HUD [H4b render]) + H5 ✅ (the card-drawn deck/hand/discard + the `playerTeamLevel` seam swap to `avgLevel × min(roster, handSize)` + the H5b pre-turn hand display) shipped; **H6 ✅ shipped 2026-06-05** (H6a rest-pool-heal + H6b pass/no-recruit option + H6c an *inert* fatigue hook off H3's counter — **RE-SCOPED to a spawn-time `power` debuff baked in `Run.beginTurn`, not the chip-time site**; default no-effect / shape-TBD pending H7 data); **H7a ✅ shipped** (the linear scored-strategy DSL + `config/fuzz-strategies.json`), next is **H7b** = the random parameter-search driver (propose → evaluate → keep-best, train/test seed split, quick/overnight presets) that runs the long-run balance sweep + the foregone-conclusion fix (H7c). See [ROADMAP.md](ROADMAP.md) §Phase H for the H6 bullet + the `### H7` block. Smaller code-level follow-ups are in [TODO.md](TODO.md); the gameplay backlog (shop, synergies, boss variety, etc.) is under [DESIGN.md](DESIGN.md) "Out of scope".

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

The historical per-step "X landed" log for Phases A-E that used to live here is archived at [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).

## How we collaborate

The strict roadmap (one step → one commit, stop at every checkpoint) was for MVP. Post-MVP work is freer, but the underlying habits still apply:

- **Work on `main`.** All work happens directly on `main`; never create a feature branch unless the user explicitly asks for one.
- **Commit per logical change**, not per "session of work." Split commits when a step's intent grows.
- **Surface tradeoffs to the user** before making non-obvious calls (shader thresholds, refactor scope, API shape decisions).
- **Browser-verify visual work** — and verify at native resolution. The Preview MCP screenshots are unreliable for sub-pixel detail (see gotcha #15).
- **Headless-first for sim/run/core/config logic.** Write a vitest test as the first reproduction step — don't drive the browser to repro logic bugs. See [AGENTS.md](AGENTS.md) for test patterns to copy and the C1d Labyrinth backstory.
- **Keep `DESIGN.md` / `ARCHITECTURE.md` honest.** If a change reveals a documented decision is wrong, update the doc in the same commit.

See also: [TESTING.md](TESTING.md) for the testing policy (`core`/`sim`/`run` get tests, `render`/`ui` get visual verify), and [retro/](retro/) for the rolling scratchpad and the shipped-MVP review.

## Things that bit us — DON'T re-litigate

These hard-won fixes will look weird out of context. Don't "clean them up" without understanding why they exist.

1. **RETIRED at B1** (was: `scene.background` vs `setClearColor` mattered for palette-quant). Palette-quant is gone; the pattern stays but no longer load-bearing. Commit `8491daa`.
2. **`OutputPass` at the END of the composer chain.** Custom `ShaderPass`es don't auto-convert linear → sRGB at the canvas blit; `OutputPass` does. Without it, every color displays as its linear value re-interpreted as sRGB. Commit `fb5e878`.
3. **RETIRED at B1** (was: palette-quant used a color-key approach). Whole quant pass removed. Commit `2857c91`.
4. **RETIRED at B1** (was: pre-quant passes must respect the bg color-key). No quant pass → no color-key contract. Commit `3334c27`.
5. **`Math.random()` is banned in `src/sim/` and `src/run/`** (ESLint enforced). Use `RNG` from `src/core/RNG.ts`; per-battle randomness via `parentRng.fork()`.
6. **Author durations in seconds, not ticks.** Use `secondsToTicks` / `ticksToSeconds` from `src/config.ts`. Changing `TICK_RATE` shouldn't require re-tuning balance — that's the contract.
7. **Cooldown semantics are "decrement-then-check," not "check-then-decrement."** [World.tick()](src/sim/World.ts) decrements every entry in `unit.actionCooldowns` once per tick before the selector runs; behaviors set the proposal's `cooldown` to the *full* cadence value — `moveCooldownTicks` from `UnitDerived`, attack cadence from `attackCooldownTicksFor(ability.cooldownSeconds, speed)` since the pre-E5 cleanup (gotcha #101) — NOT `N-1`. The match between cooldown gap and event `durationTicks` is what keeps the sprite lerp from leaving a visible idle frame between moves. Lived in a shared `Unit.actionCooldown` field through MVP; refactored to per-action `Map<string, number>` plus `activeAction` duration lockout in A1.
8. **A1 model: per-action cooldown + activeAction duration lockout.** Each tick the selector polls every `Behavior.proposeAction`, filters proposals whose action is still on cooldown (`unit.actionCooldowns.get(id) > 0`), and picks the highest-scoring proposal. The chosen action sets its per-action cooldown and an `activeAction` with `startTick` / `finishTick` / `effectTicks`; while `activeAction != null`, the selector short-circuits (unit is busy). For single-tick actions like move/attack, `cooldown` and `duration` are equal — preserving the MVP "one action per tick" feel — but charge-ups and channels diverge them. Behaviors are stateless and produce per-proposal Action instances (see [src/sim/actions/MoveAction.ts](src/sim/actions/MoveAction.ts) and [src/sim/actions/AttackAction.ts](src/sim/actions/AttackAction.ts)). MovementBehavior scores 1; AttackBehavior scores 10. Score-tie handling is "first proposer wins."
9. **`World.tick()` iterates `this.units.slice()`, not `this.units`.** Inline death handling splices the list mid-tick (was DeathBehavior pre-A1); without the snapshot copy the loop skips the next unit after each removal. The death short-circuit at the top of the per-unit step also ensures dead-but-not-yet-removed units don't propose actions on the tick they die.
10. **`checkBattleEnd` guards on an empty world.** Without the guard, `World.tick()` fires `battle:ended` the very first tick before Game spawns anything — and also breaks any low-level World test that ticks without spawning. The guard happens to also cover the (currently impossible-with-current-stats) mutual-annihilation case.
11. **`exactOptionalPropertyTypes` gotcha.** When an object literal might assign `undefined` to a callback property, declare it as `prop: (() => void) | undefined` instead of `prop?: () => void` — the latter rejects an explicit `undefined`. Bit us in [SpriteAnimator's `ActiveFade`](src/render/animation/SpriteAnimator.ts).
12. **Subscription order matters between Run and Game.** Run is constructed before Game subscribes its own handlers, so on `battle:ended` Run advances `phase` *before* Game reads it. Game's `endBattle` and recruit/defeat/victory handlers all rely on this. If you ever construct Run after Game's subscriptions, the read-after-set ordering flips and things break. See [src/Game.ts](src/Game.ts) constructor.
13. **`Run.dispose()` unsubscribes on reset.** When the run resets (via the `resetRun` command), Game creates a new Run on the same bus. Without `dispose()` on the old Run, two Runs respond to every `battle:ended`. The unsubscribe pattern is captured-in-array; if you add a new `bus.on(...)` inside Run, push the unsub into `this.subscriptions`.
14. **Recruited units spawn via `distributeColumns`, not fixed-length arrays.** The original `MELEE_COLUMNS = [2, 6, 10]` blew up at index 3 once a recruit grew the team. Default 3M+2R formation is still preserved exactly by a fast-path; any other size uses an even-spread function. See [src/Game.ts](src/Game.ts) `spawnTeam`.
15. **Death fade gets cut short on `battle:ended` (acceptable).** When the killing blow is the final tick, `DeathBehavior.startFade` and `BattleRenderer.detach` happen in the same synchronous burst. The fade dies on the floor. The RecruitScreen / GameOverScreen modals come up immediately after and hide the cut-short visual, so it's invisible in practice. Note in [src/render/BattleRenderer.ts](src/render/BattleRenderer.ts).
16. **HUD `screen-fade` class instead of `hidden` attribute.** The HUD uses `is-visible` class toggling via [src/ui/fade.ts](src/ui/fade.ts) rather than the `hidden` HTML attribute, because `display: none` can't transition opacity. The persistent-element pattern (HUD) and the create-and-destroy pattern (Map/Recruit/GameOver) both opt into the same `.screen-fade` CSS rule. Commit `d4808f5`.
17. **Camera framing is aspect-aware.** [src/render/Renderer.ts](src/render/Renderer.ts) `fitCamera()` runs on every `handleResize` and computes camera distance from the arena AABB + pitch + FOV + aspect so the grid never clips off frame. If you change `FIT_HALF_EXTENTS` or `CAMERA_PITCH_RAD`, re-verify framing at multiple aspect ratios. Commit `f253ae5`.
18. **A2 inputs/outputs split: commands vs events.** Player intents (`enterNode`, `chooseRecruit`, `resetRun`) flow through `RunDispatcher` → `Run.dispatch` (synchronous), **not** the bus. Past-tense notifications (`run:started`, `battle:ended`, `recruit:offered`, `unit:*`, etc.) stay on the bus. Game implements `RunDispatcher` and is captured by every UI screen — that keeps the swap-Run-on-reset invisible to the UI. If you find yourself wanting to add an imperative to `GameEvents`, it's probably a `RunCommand` instead. See [src/run/Command.ts](src/run/Command.ts).
19. **A2 snapshot rehydration is two-phase for World.** Units are instantiated first (no `activeAction`), then `activeAction` references are resolved once every unit exists — an in-flight `AttackAction` may reference another unit by id, and that target has to be present first. `World.fromJSON` enforces this order; if you add a new Action whose `fromData` looks up world state, follow the same pattern. Same applies if multi-unit links (e.g. a "guard ally X" behavior) ever exist.
20. **Behavior/Action registry. `kind` is the contract.** New Behavior implementations declare `static readonly kind = '...'` AND `readonly kind = X.kind`, plus a factory in [src/sim/behaviors/registry.ts](src/sim/behaviors/registry.ts). New Action implementations declare an `id`, a `toData()`, a `static fromData(data, world)`, and register in [src/sim/actions/registry.ts](src/sim/actions/registry.ts). Skipping either side breaks `World.fromJSON` only for snapshots that mention the new kind — easy to miss until someone tries to save mid-battle.
21. **`Run.fromJSON` uses `Object.create` to bypass the constructor.** A fresh `new Run(seed, bus)` regenerates the NodeMap and emits `run:started`; neither is what we want when restoring a snapshot. The factory uses `Object.create(Run.prototype)` plus a mutable cast to set the readonly fields, then calls `subscribe()` to wire up `battle:ended`. Don't replace this with constructor surgery without preserving both behaviors.
22. **A3 fuzz CLI uses tsx, smoke uses a separate vitest config.** Node ESM can't resolve extensionless `.ts` imports natively, so `npm run fuzz` runs through `tsx` (added as devDep). `npm run fuzz:smoke` uses [vitest.fuzz.config.ts](vitest.fuzz.config.ts) — the default `vite.config.ts` *excludes* `tests/fuzz/**` from `npm test` to keep pre-commit fast, so the smoke needs its own config to flip the include. If you ever wire fuzz into CI, call `npm run fuzz:smoke` not `npm test`.
23. **Battle-setup logic lives in [src/sim/battleSetup.ts](src/sim/battleSetup.ts), not Game.** `Game.beginBattle` and the fuzz harness both call `spawnTeam` / `spawnEncounter` from this module. If formation columns ever change (or new behaviors are added to default-spawned units), it has to land there — both call sites pick it up automatically. Don't reintroduce a local copy in either consumer.
24. **A4 config split — JSON source of truth, TS validator.** Balance numbers live in `config/*.json`; each one has a zod schema + parsed export in `src/config/*.ts`. Validation runs at module load, so a malformed JSON crashes the app at boot with a readable zod trace — that's the intended failure mode. To add a new tunable: drop it in the JSON file, extend the schema in the matching TS module, import the parsed value at the call site. The `Archetype` TS union in [src/sim/archetypes.ts](src/sim/archetypes.ts) stays the canonical list of archetype keys; the JSON keys must match it. C2 (mage/rogue/healer) will add to both in lockstep.
25. **A4 shader split — `.glsl` files + `?raw` imports.** Shader sources live in `src/render/shaders/*.glsl`. Two post-process passes (sat-clamp, scanlines) share `fullscreen-pass.vert.glsl`. The `__PALETTE_SIZE__` / `__BLACK_INDEX__` placeholder + `substituteShaderConstants` machinery from MVP is gone — palette quant retired at B1 (gotchas #1/#3/#4) and nothing else needed the substitution.
26. **A5 Scene swap is single-active + bus-driven.** `Game.bus.on('battle:started' | 'recruit:offered' | 'run:victory' | 'run:defeated', …)` does the entire scene routing — Run emits the event from inside its own `battle:ended` handler so phase is already updated by the time the swap fires. The one transition not driven by a bus event is recruit→map (Run.handleChooseRecruit doesn't emit anything), so Game.dispatch checks `run.phase === 'map'` after `chooseRecruit` and swaps explicitly. Don't add an event for that single case; the explicit branch is cheaper than a one-consumer event.
27. **HUD has a real `dispose()` post-A5.** Pre-A5 HUD was a Game-lifetime singleton — subscriptions were never torn down because the object lived forever. A5 made it per-battle (BattleScene owns it), so HUD captures its subscriptions into `this.subscriptions[]` and `dispose()` unsubscribes them + `fadeOutAndRemove`s the root. Any new long-lived bus subscription added to HUD must be pushed onto that array, same pattern as BattleRenderer + Run.
28. **`SceneContext` is rebuilt per swap.** `Game.buildContext()` returns a fresh bundle on every `swap()` call so `ctx.run` reflects the current Run instance — important because `resetRun` replaces `this.run`, and a stale closure-captured ctx would still point at the disposed Run. Scenes that need scene-specific args (recruit offer, gameover variant) take them via constructor, not via ctx, because ctx is supposed to be stable across all scenes.
29. **B1 bloom high-pass uses `max(R,G,B)`, not Rec.709.** UnrealBloomPass ships with a perception-weighted luminance high-pass (`0.299·R + 0.587·G + 0.114·B`), which makes pure red pixels measure as dim — NEON_RED enemies wouldn't cross the bloom threshold while TERMINAL_GREEN allies would. Physically correct for HDR scenes; actively wrong for stylized glyphs. `createBloomPass` in [src/render/PostProcess.ts](src/render/PostProcess.ts) swaps in a max-channel shader (`MAX_CHANNEL_HIGH_PASS_FRAG`) so any saturated channel triggers bloom equally. Don't "fix" this by reverting to LuminosityHighPassShader.
30. **B1.1 selective bloom: two composers + per-sprite `bloomIntensity` decoupled from visible color.** SpriteRenderer owns two meshes sharing the same `InstancedBufferGeometry` (and therefore the same per-instance buffers): `mesh` on layer 0 renders the *visible* sprite at its natural color via [sprite.frag.glsl](src/render/shaders/sprite.frag.glsl) (no intensity math); `bloomMesh` on `BLOOM_LAYER` (=1) renders `color * bloomIntensity` via [sprite-bloom.frag.glsl](src/render/shaders/sprite-bloom.frag.glsl). [Renderer.ts](src/render/Renderer.ts) drives two `EffectComposer`s per frame — `bloomComposer` (camera at BLOOM_LAYER, scene.background→null, explicit (0,0,0) clear, then UnrealBloomPass) and `mainComposer` (camera at layer 0, scene.background restored, SatClamp → MixPass(adds bloomComposer's renderTarget2.texture) → Scanlines → OutputPass). `bloomIntensity` semantics: `0` = no halo (sprite visible at natural color), `1` = natural halo (blooms iff color crosses threshold), `>1` = forced strong glow. Lerping 0↔1 fades the halo while visible color is constant. Why this shape and not the simpler "additive emissive" multiplier we tried first: with one pass, "visible color" and "bloom trigger" are the same fragment; sprite intensity can only push values up, not suppress natural bloom on saturated colors (NEON_RED). Two passes decouple them. Don't try to collapse back to one composer — the suppression case (B3 dim HP bars, C2 charge-down, etc.) needs it.

    Two gotchas the selective-bloom wiring picked up that bit during implementation:

    - **bloom RenderPass clearColor must be explicit `(0,0,0,0)`.** Default GL clear color is TERMINAL_BLACK `#282828` sRGB, which lands as raw 0.157 in the HalfFloat linear target. UnrealBloomPass *additively* composites its bloom result onto its un-cleared input target, so any non-zero ground floor leaks gray across the whole bloom buffer. Pass `new RenderPass(scene, camera, null, new THREE.Color(0x000000), 0)`.
    - **UnrealBloomPass's `blendMaterial` must be patched to NormalBlending + transparent=false** (property name in three.js r184+; was `materialCopy` pre-r163). The pass's last step copies the bloom result onto its input target using AdditiveBlending by default — correct for single-composer chains where the bloom should smear glow on top of the rendered scene, wrong for selective bloom where the visible sprite already lives in mainComposer and we want bloomComposer's output to be JUST the halo. NormalBlending makes the final copy *replace* the input; transparent=false ensures dark pixels (alpha=0) write zero instead of bleeding through.
    - **Bloom threshold is 0, not the more-typical 0.6+.** A non-zero high-pass threshold turns `bloomIntensity` into a *step function* (off below ~threshold/maxChannel, full above) rather than the smooth linear knob B3 HP-bar fade and C2 charge-up ramps need. The threshold normally exists to filter out dim background pixels before bloom; in our selective-bloom setup the bloom layer renders only sprite contributions onto an explicit (0,0,0) clear, so there are no dim pixels to filter. Setting threshold=0 makes the bloom contribution scale linearly with `color × bloomIntensity` all the way down, which is what makes `bloomIntensity = 0.25` and `0.75` look visibly different.

31. **C1a: walls are neutral-team `Unit`s, not a `TileKind`.** Original design instinct was a `wall` tile variant. User flagged: making walls Units reuses the existing Unit pipeline (id allocation, snapshot rehydration, pathfinding blocker list, sprite rendering) and leaves the door open for destructible walls + healing shrines + other "static entity with optional behaviour" without a new abstraction. The split is now **TileGrid = surface properties** (movement cost, eventually combat modifier / LOS) and **neutral-team Units = solid objects sitting on top**. Don't try to collapse them — they answer different questions.

32. **C1a: `Team` extended to `'player' | 'enemy' | 'neutral'`.** Every team-comparing call site has to handle the third case. The audit at C1a landing covered: [Targeting.findTarget](src/sim/Targeting.ts) (skip neutrals as targets), [World.checkBattleEnd](src/sim/World.ts) (don't count neutrals toward either side, and *don't synthesize a winner when both player+enemy are dead but walls remain* — same "no combatants left" pattern as the pre-spawn empty-world guard), [HUD.addUnit](src/ui/HUD.ts) (skip neutrals from rosters), [BattleRenderer.colorForTeam + onUnitSpawned](src/render/BattleRenderer.ts) (TERMINAL_STONE color, suppressed bloom, no HP/progress bars for neutrals). New consumers MUST audit the same way — `team === 'player' ? a : b` is now wrong when neutrals can show up.

33. **C1a: every glyph used for a sprite needs an entry in [FontAtlas.ts](src/render/FontAtlas.ts) `GLYPHS`.** Browser-verify caught the wall `#` glyph missing — `addSprite` throws `FontAtlas: no UV for glyph "#"` deep inside the unit:spawned handler, which silently aborts BattleScene mount mid-applyTerrain. The HUD shows "Floor N" with empty rosters and there's no obvious error in the UI; the throw only surfaces via `window.onerror`. Append new glyphs to the end of the array — order is stable, existing UV lookups stay valid.

34. **C1a: `Pathfinding.findPath` accepts an optional `CostFn`; Chebyshev heuristic stays admissible only because every cost is `>= 1`.** Default unit-cost preserves pre-C1a behavior. If you ever add a tile cost `< 1` (e.g., a "speed boost" tile), the Chebyshev heuristic stops being a lower bound on min-cost and A* loses its optimality guarantee. Either keep all costs `>= 1` or swap the heuristic to `min_cost * Chebyshev`. `Infinity` cost = data-driven block (equivalent to a blocker-list entry).

35. **C1a: `World.spawnEnvironment` is the spawn path for non-combatants; UnitTemplate is combatant-only.** Walls don't fit the `archetype: 'melee' | 'ranged'` mold (no rolled stats, fixed glyph). [`spawnEnvironment`](src/sim/World.ts) takes `{ glyph, position, maxHp?, team? }` and produces a Unit with degenerate inert stats + no behaviors; [`spawnWall`](src/sim/environment.ts) is the convenience wrapper. Both reuse the same private `addUnit` helper as `spawnUnit` so id allocation + bus emission stays consistent. Don't extend UnitTemplate with `glyph` overrides to fit walls in — the split spawn paths are clearer.

36. **C1a: `WorldSnapshot` schema bumped to version 2 (added `tileGrid`).** Old version-1 snapshots throw on load — the version check guards against silent acceptance of stale data. The default constructor still produces a floor-everywhere TileGrid so existing tests (which build World without an explicit grid) keep passing; only the snapshot path enforces strict versioning.

37. **C1a: `applyTerrain` MUST run before `spawnTeam`.** The terrain generator guarantees walls + water never land on `config.spawnRowsClear`, so spawning teams after applyTerrain means units never land on a wall. Mounting in the opposite order would put units on walls (or worse, fail in pathfinding when the unit's own cell is blocked). Both call sites — `spawnEncounter` (fuzz harness) and `BattleScene.mount` — already follow this order; if you add a third, mirror it.

38. **C1b: ranged LOS uses Bresenham, NOT supercover.** [`hasLineOfSight`](src/sim/LineOfSight.ts) walks integer cells along the Bresenham line from attacker to target and rejects on any cell in `blockers`. Supercover (every cell the geometric line touches) was the alternative and would block diagonal "squeezes" between corner-touching walls — geometrically purer but tactically inconsistent with 8-dir Chebyshev movement, where a unit *can* step diagonally between such walls. The line walk skips both endpoints: the attacker's own cell isn't a self-block, and the target's cell may appear in the blocker list (typically it doesn't, but the guard is there) and shouldn't block a shot at the target itself. If a future feature needs strict diagonal-squeeze prevention, swap the loop body — not the call sites.

39. **C1b: `unit:died` carries `team`.** By the time the event fires, `World.tick` has already spliced the unit out of `world.units`, so `world.findUnit(unitId)` returns undefined. Subscribers that need to branch on combatant vs neutral death (BattleScene's audio handler, future wall-destruction VFX) read `team` from the payload. New `unit:died` subscribers MUST include `team` if they care about that distinction — there's no other source of truth post-removal.

40. **C1b: `AttackBehavior.collectWalls` filters by `team === 'neutral'`.** Today neutrals == walls, but the moment we add other neutral entities (healing shrines, hazards) the filter starts overcollecting. The right fix at that point is a per-Unit `blocksLineOfSight` flag (or similar) — not a glyph check. Don't paper over by inspecting the glyph; that ties LOS semantics to a rendering detail.

41. **C1b: hand-authored layouts assume `gridSize = 12`.** [`generateTerrain`](src/sim/terrainGen.ts)'s layout path throws on any other size rather than silently scaling. Layouts are design artifacts authored against a known canvas; resizing the arena (future C1c work or beyond) means re-authoring them. The procedural path stays grid-size-agnostic for the fuzz harness and any future scaling experiments.

42. **C1b: `Run.handleEnterNode` now does 3 RNG draws on `battleRng` before `rollEnemyTeam`.** Order is `worldSeed → terrainSeed → layoutId → rollEnemyTeam`. The third draw (introduced by `rollLayoutId`) means enemy compositions shifted for existing seeds vs. the C1a baseline. Existing tests that assert enemy stat ranges still pass (the ranges are permissive), but the byte-identical replay tests in `tests/integration/` reflect the new stream — old fuzz output CSVs are stale on this dimension and shouldn't be compared across the C1b cut.

43. **C1c: `TerrainRenderer.heightAt(cx, cy, kind)` is the canonical height function.** Both the prism geometry AND `BattleRenderer.tileWorldPos` read from it. Don't compute heights in a second place — keep `heightAt` as the single source of truth. Heights are deterministic functions of `(cx, cy, kind)` over a fixed-seed simplex; per-battle terrain seed is intentionally NOT used here so the visual character of the world stays canonical across battles. If a future feature wants per-encounter terrain variety, introduce a separate seed plumbed through `setTiles` rather than reaching into the noise instance.

44. **C1c: Sprite Y is per-tile via `BattleRenderer.tileWorldPos`, not the pre-C1c fixed `SPRITE_Y`.** `gridToWorld` still exists but only computes XZ + a default Y for callers without terrain context; `tileWorldPos(coord)` is what `onUnitSpawned` and `onUnitMoved` use. Both spawn AND lerp endpoints go through it, so a unit moving across a height seam gets a smooth Y-interpolated path via the existing SpriteAnimator lerp — no extra animation wiring. If you add a new spawn point or lerp endpoint, use `tileWorldPos`, not raw `gridToWorld`.

45. **C1c: Walls inherit per-tile Y via the unit-spawn pipeline.** Walls are neutral-team Units (see #31) and spawn through the same `onUnitSpawned` path as combatants, so `tileWorldPos` automatically places them on their tile top. If a future feature wants walls at a Y different from their tile top (e.g. ceiling-mounted hazards, floor traps that flush below tile level), the wall path needs its own branch — but for the default "obstacle on floor" case it just works.

46. **C1c: No scene lights — terrain shades from a baked light direction in `terrain.frag.glsl`.** `LIGHT_DIR` + `AMBIENT` + per-face normal go directly into a Lambert term in the shader; there is no `THREE.DirectionalLight` in the scene. This is deliberate: sprites use a custom unlit ShaderMaterial, and adding a real scene light would mean either tagging it to a layer the sprites don't read (fragile) or rewriting the sprite shader to ignore lights (pointless). If you add a new material that *does* want lighting, mirror the same baked-direction pattern rather than adding a `Light`.

47. **D3: `World.gridSize` is GONE — use `world.gridW` + `world.gridH`.** Every sim/render call that used to take a single `gridSize` now takes both dimensions independently (`Pathfinding.findPath`, `generateTerrain`, `gridToWorld`, `TerrainRenderer.setTiles`, `World` constructor). The reason is correctness: passing one value where two are wanted would silently squash one axis to match the other. WorldSnapshot bumped to v3 — v2 snapshots throw at load via the existing schema-version guard. If you add a new function that consumes board dimensions, take them as two parameters, not one.

48. **D3: reserved spawn rows are computed per-`gridH`, not configured.** `config/terrain.json` no longer has `spawnRowsClear`; the canonical formula is `reservedSpawnRows(gridH) = [1, 2, gridH-3, gridH-2]`, exported from `src/sim/terrainGen.ts`. The formula mirrors what `battleSetup.spawnTeam` actually puts there (player melee/ranged on rows 2/1; enemy on `gridH-3`/`gridH-2`). The editor's diagonal-stripe overlay, the procedural generator's reservation, and the layout validator all call this same function. D5 retires it in favor of explicit `SpawnRegion`s — until then, don't fork the formula. The function returns `[]` for `gridH < 4` (no usable arena); the procedural path treats that as "no reservation," so degenerate sizes surface as out-of-bounds spawn writes rather than silent corruption.

49. **D3: `Run.handleEnterNode` now does FOUR `battleRng` draws before `rollEnemyTeam`.** Order is `worldSeed → terrainSeed → layoutId → proceduralSide → rollEnemyTeam`. `rollProceduralSide` always runs (even when `layoutId !== null` so the resulting `proceduralSide` is discarded) — same invariant `rollLayoutId` already maintained. This is what keeps enemy-team byte continuity stable when the procedural-size band is later retuned. Note this means D2-era seeds shift enemy compositions (the new draw lands between `layoutId` and `rollEnemyTeam`); permissive range assertions in `Run.test.ts` still pass.

50. **D3: `TerrainRenderer` allocates buffers at the largest D3 grid up front.** Constructor takes no arguments; the vertex buffers are sized for `LAYOUT_MAX_SIDE × LAYOUT_MAX_SIDE` (32×32 = 1024 tiles × 30 verts × 32 bytes ≈ 1 MB). `setTiles(tileGrid, gridW, gridH)` rewrites cells in place and calls `geometry.setDrawRange(0, gridW * gridH * VERTS_PER_TILE)` so only the active region is drawn. Trade-off: a flat ~1 MB reserved at boot vs. a frame stall every time the board size changes. Don't add a per-encounter buffer realloc path — the cap was picked so the upfront cost is small enough to skip that complexity.

51. **D3: `Renderer.fitToBoard(gridW, gridH)` is the per-battle hook.** `BattleScene.mount` calls it after `applyTerrain` so the camera frame matches the rolled / declared arena. Non-battle scenes (Map / Recruit / GameOver) don't call it; they inherit whatever the last battle (or boot default of `GRID_SIZE × GRID_SIZE`) set. The fit math uses board-relative X/Z half-extents (`boardW/2 + XZ_PADDING`, same for Z) and a fixed Y headroom (`Y_HALF_EXTENT = 1.0`); pre-D3 these were a single `FIT_HALF_EXTENTS` constant.

52. **D4: `Renderer` owns two camera modes; `fitCamera()` dispatches on `this.cameraMode`.** Mode `fit` calls `fitCameraFit()` (board AABB centered at origin); mode `scroll` calls `fitCameraScroll()` (fixed 12-tile window centered at `cameraTarget`). Shared per-corner FOV math lives in `computeCameraDistance(hx, hy, hz)` so changes to fit logic land in both modes — don't duplicate it inline. The `DEV_DEFAULT_MODE` constant pins the default to `fit` through D4; D5 will flip it to `scroll` once spawn regions give a better anchor than "rows 1-2 midpoint". The backtick (`` ` `` / `~`, `e.code === 'Backquote'`) toggles for dev verification.

53. **D4: scroll-mode target is always re-clamped to the current board.** `clampCameraTarget()` runs inside both `setCameraTarget` and `fitCameraScroll`, so external code can call `setCameraTarget` with any value (e.g. the player-spawn anchor from BattleScene) and trust it'll get capped to `±max(0, boardDim/2 - 6)`. Boards where a dim is ≤ `SCROLL_WINDOW_TILES` (12) snap to 0 in that dim — there's no pan room. This is why WASD does nothing on a 12×12 board even in scroll mode; it's intentional, not a bug. The clamp also re-runs from `fitToBoard`, so per-encounter size changes never leave the target outside the new bounds.

54. **D4: pan-key + edge-scroll listeners are page-lifetime, gated by mode in the loop.** Listeners are attached once in `Renderer`'s constructor (`keydown`/`keyup` on `window` so the user doesn't need to focus the canvas; `mousemove`/`mouseleave` on the canvas so HUD hover doesn't pan) and torn down in `stop()`. The render loop calls `updateScrollFromInput(dt)` ONLY when `cameraMode === 'scroll'` — so fit mode pays nothing per frame, and a mouse parked near the edge during fit mode doesn't queue up panning that'd jump the moment you toggle. `mouseleave` clears `mouseX`/`mouseY` to `null` so edge-scroll stops when the cursor moves over the HUD or off the canvas entirely. Both WASD and arrow keys are recognized as pan keys (`PAN_KEY_CODES` set); they're OR'd per axis in `updateScrollFromInput` so holding W + ArrowUp together still only contributes -1 to dz, not -2. Uses `e.code` (layout-independent) throughout so non-QWERTY keyboards still pan with the physical W/A/S/D and arrow keys.

55. **D5.A: `LayoutDef.spawns` is REQUIRED; zod's "valid pair" rule subsumes the simpler checks.** Every layout in `config/layouts.json` must declare `spawns: SpawnRegion[]` (≥2 regions, each exactly 8 in-bounds tiles, no duplicates within a region, no overlap with walls/water). The cross-region rule is **"there must exist at least one `(player, enemy)` pair where the two regions differ"** — this single rule captures (a) "≥1 player-available region," (b) "≥1 enemy-available region," AND (c) "if the only player-available region is the only enemy-available region, the enemy pool would be empty after the player draws." Don't loosen it to the simpler "≥1 player + ≥1 enemy" — you'd accept the one-`both`-region degenerate case that the sequential picker rejects at runtime.

56. **D5.B: walls + water mask against spawn-region TILES now, not `reservedSpawnRows`.** The procedural generator builds its candidate pool by excluding every tile that appears in any spawn region (union across regions). `reservedSpawnRows` is retired entirely as of D5.D.A — the editor's diagonal-stripe overlay (its last consumer) went away with the layer system, and the export from `src/sim/terrainGen.ts` was removed in the same commit. Don't reintroduce a row-band check inside the generator — the spawn-region union is the only correct reservation now (rectangular bands of arbitrary shape live anywhere on the board).

57. **D5.B: setup RNG is `new RNG(encounter.terrainSeed).fork()` — `fork()` is load-bearing.** Both `BattleScene.mount` (via the `setupRngFor(encounter)` helper in `battleSetup.ts`) and the headless `spawnEncounter` derive the spawn-pick + intra-region shuffle RNG from a fork of the terrain seed. `fork()` advances the ephemeral parent once and returns an independent child, so the child stream is decoupled from terrain-gen's RNG state — changing wall density or grid size doesn't shift spawn picks for the same `terrainSeed`. Don't replace this with `new RNG(encounter.terrainSeed)` directly: that aliases the generator's first-step draw, coupling terrain-config changes to spawn outcomes. Don't add a new draw on `Run.battleRng` either — gotcha #49's 4-draw byte continuity stays as-is.

58. **D5.B: spawn formation flattens — no more "melee front rank / ranged back rank."** `spawnTeam(world, team, templates, region, rng)` shuffles `region.tiles.slice()` deterministically and places `templates[0..N-1]` one per tile, where N = `min(templates.length, region.tiles.length)`. Templates are NOT sorted by archetype, and the region typically lives on a single row. Ranged units may end up in front of melee (between melee and the enemy) and vice versa. This is the intended D5 outcome — layout authors who want the rank split back must define two `player`-only regions (or two `enemy`-only) so each archetype has its own band. Don't bring back the archetype split inside `spawnTeam`; that'd ignore the layout author's region intent and defeat the schema.

59. **D5.B: `pickSpawnRegions` throws as a runtime safety net; zod is the canonical static check.** The picker filters player-available regions, draws one, then filters enemy-available regions excluding the player's pick. If either pool is empty it throws. zod's "valid pair" rule (gotcha #55) should make this impossible for any layout that loaded successfully, so the throw is the "this should never fire" guard — if it does, the bug is in the schema validator, not the picker.

60. **D5.C: overflow-queue spawns are seated with `activeAction` directly, NOT through the selector.** `World.spawnFromQueue` sets `unit.activeAction = { action: new SpawnAction(), startTick, finishTick, effectTicks: [] }` immediately after addUnit. The Behavior/Proposal pipeline never sees SpawnAction — no behavior proposes it, no Action.start() runs at instantiate time. This breaks the usual "selector picks proposal → set activeAction → call start()" lifecycle, but only this one path; everything else still goes through the selector. If you add a new direct-set activeAction case, mirror the SpawnAction shape: pure-lockout, no Behavior, registry-rehydratable.

61. **D5.C: spawning units are targetable + the SpawnAction lockout is the ONLY thing keeping them inert.** Targeting.findTarget doesn't filter by activeAction.action.id — a freshly-spawned (mid-fade) unit is a valid attack target for an adjacent enemy, can take damage, and can die before its fade finishes. This is deliberate scope: untargetable-while-spawning is a tactical lever that should be a conscious design choice, not a side-effect of a placeholder fade duration. If playtesting flags it, add a `targetable: boolean` derived from `unit.activeAction?.action?.id !== 'spawn'` inside Targeting — don't paper over it inside SpawnAction itself.

62. **D5.C: `checkBattleEnd` widens "team alive" to include `queueLength > 0`.** Otherwise killing a team's last on-board unit declares the other side winner even when reinforcements are queued. The empty-world short-circuit also widens to `units.length === 0 && spawnQueues.size === 0` so a World that's been told to expect overflow stays in "battle hasn't started" mode if nothing has spawned yet. Edge case: a team with units in queue but all spawn-region tiles permanently occupied (e.g. enemy parks on the player spawn region) stalemates the battle. That's a layout-design bug, not a sim bug — in practice the enemy will engage and die, freeing tiles.

63. **D5.C: BattleRenderer fade-in routes through `SpriteAnimator.fades` (same map as fade-out) via the generalized `fromAlpha`/`toAlpha` fields, but bars get a separate `barFadeIns` lane.** Don't try to fold bar fade-in into `barFades` — the fade-out lane removes the bar pair when complete, the fade-in lane doesn't. The bar progress fill is also explicitly hidden during SpawnAction lockout (filtered alongside MoveAction in `updateProgressFill`) so the fade-in visual isn't fighting a progress bar drawing over a half-faded sprite.

64. **D5.C: `unit:spawned` payload now `{ unitId, instant }`.** `instant: true` = setup-time spawn (initial battle layout); `false` = D5.C overflow-queue spawn. All existing emit sites pass `true`; only `spawnFromQueue` passes `false`. Subscribers that branch on the visual lifecycle (BattleRenderer, possibly future HUD enrichments) key off this flag. If you add a new spawn-path, decide based on whether the spawn happens at world-construction time (`true`) or mid-battle (`false`).

65. **D5.D.A: layered erase only touches the ACTIVE layer's content.** Right-click on a wall while the terrain layer is active is a NO-OP; right-click on water while neutral-units is active is also a no-op. Each stroke kind (`erase-wall` / `erase-water`) guards on the current cell value and only sets `floor` if the cell already holds the corresponding layer's content. This is deliberate — the layer radio is the kind picker AND the erase scope. If you find a "right-click should clear everything" muscle memory bug-report, the fix is a new "all" erase layer or a meta-modifier, NOT relaxing the per-layer guards in `applyTerrainStroke`. Cell content stays mutex (one of `floor`/`wall`/`water` per cell — the layer system is a UX overlay, not a multi-layer per-cell data model).

66. **D5.D.B: spawn-region painting has a FIFO 8-tile cap; the 9th paint evicts the OLDEST tile in the region (not the newest).** `applyPaintRegion` pushes to `region.tiles.push(...)` then `shift()`s if length > `SPAWN_REGION_TILE_COUNT`. During drag-paint, this means the region trails the cursor: paint 12 cells, the first 4 evict. Re-painting a tile already in the region is a no-op (it doesn't refresh FIFO position). `applyEraseRegion` removes by `(x, y)` equality. Region pills disable `Delete region` when at `MIN_SPAWN_REGIONS = 2` (mirrors zod's lower bound — deleting below would produce a JSON that fails to parse).

    Two sub-points worth flagging:

    - **Multi-region tile overlap renders as stacked corner badges**, one per region membership. Position is by region index mod 4 (top-left / top-right / bottom-right / bottom-left); region index 4+ wraps back to the same corners (acceptable — D5 layouts in practice have ≤4 regions). Color is also indexed mod 4 via `--region-color-N` CSS custom properties. The active region's tag is fully opaque + outlines its tiles with `.cell.active-region-N`; inactive regions render at `opacity: 0.55` (and dim further to 0.3 when the spawn-regions layer isn't active, via the `[data-active-layer]` rules). Tags are `<span class="region-tag">` children of cells — `refreshCell` tears them down and rebuilds every time. If you add a new cell-rebuild path, mirror the teardown or the tags duplicate.
    - **Mid-stroke region switches** (clicking another region pill while a paint stroke is in progress) call `endStroke()` synchronously before swapping `activeRegionIdx`. Cleaner than letting the stroke leak onto the new region; matches the "Can't switch mid-stroke" answer the user gave on the D5.D scope.

67. **D5.D follow-up: editor renders y=0 at the BOTTOM of the CSS grid, not the top.** `gridToWorld` in BattleRenderer maps cell.y=0 to `+Z` (the near edge of the camera frame, i.e. the visual bottom of the game view). Pre-fix the editor used the standard top-left CSS convention, so asymmetric layouts (river, anything off-center vertically) appeared vertically mirrored between editor and game. `buildGrid` now iterates `y` in reverse when appending DOM children — the y=0 row gets appended last and lands in the bottom CSS-grid row under `grid-auto-flow: row`. `cellEls[y][x]` stays indexed normally so click handlers and cell lookups don't have to know about the DOM order. If you ever rebuild the grid construction, KEEP the reverse iteration — it's the only knob that aligns editor and game vertical orientation.

68. **D5.E: scroll-camera anchor is the player region's TILE centroid passed through `gridToWorld`, NOT a hand-rolled XZ.** `BattleScene.mount` computes `(meanX, meanY)` over `playerRegion.tiles` in grid coords (fractional is fine), feeds it through the canonical `gridToWorld({x:meanX,y:meanY}, gridW, gridH)`, and passes `.x` / `.z` to `setCameraTarget`. Don't open-code the world-coord conversion — `gridToWorld` is the single source of truth for cell↔world mapping (same function spawn placement uses), and reproducing its math inline is how the editor↔game y-axis mirror bug (#67) was originally introduced. Also: `setCameraTarget` must run AFTER `pickSpawnRegions` (the player region is its input); the pre-D5.E ordering had it before, with a placeholder XZ. Keep it after.

69. **D5.E: on boards ≤ SCROLL_WINDOW_TILES (12) in a dim, the centroid anchor is invisible in scroll mode.** `clampCameraTarget` mutates `cameraTargetX/Z` toward 0 when there's no pan room (gotcha #53 documents this); on a 12×12 board both old and new heuristics clamp to 0. The centroid IS preserved in fit mode (fit doesn't call clamp), so the *toggle-back* path picks up the right anchor on a board >12. Don't add a "preserve unclamped target" code path to make 12×12 look different — there's no useful behavior to expose on a board that already fits entirely in the visible window.

70. **D6: `blocksLineOfSight` defaults to TRUE — the explicit `false` only lives on half-cover.** UnitInit's field is `?: boolean`; Unit constructor resolves via `init.blocksLineOfSight ?? true`. Same default applies on the addUnit / spawnEnvironment plumbing. If a future neutral entity (shrine, hazard) needs to transmit LOS, pass `blocksLineOfSight: false` explicitly — and don't fall into the trap of inferring it from glyph or team (gotcha #40 explicitly retired in favor of the flag). The `?? true` resolution at the `new Unit(...)` site is gotcha #11 in action: `?: boolean` rejects `boolean | undefined` under `exactOptionalPropertyTypes`, so we narrow at the construction site rather than the field declaration.

71. **D6: `MovementBehavior` collects neutrals into TWO separate lists (`pathBlockers` + `losBlockers`).** Pre-D6 a single `walls` list served both pathfinding and the LOS-gated in-range abstain. With half-cover that's broken: half-cover blocks pathing (it's a unit, units occupy cells) but is LOS-transparent (the `blocksLineOfSight: false` flag exempts it from the AttackBehavior pool). MovementBehavior must mirror AttackBehavior's LOS view, OR a ranged unit in range with half-cover between it and target keeps stepping (LOS reads broken via the old wall pool → in-range abstain fails) while AttackBehavior would fire. Two lists is the fix: every neutral lands in `pathBlockers`; only `blocksLineOfSight: true` neutrals land in `losBlockers`. Symptom if you collapse it back to one list is unit "wobble" near a half-cover at range.

72. **D6: half-cover is hand-authored-only in D6.** `GeneratedTerrain.halfCovers` is non-empty only for the layout-library path; procedural emits `[]`. There's no `config/terrain.json` knob (yet). When C6 (longer runs) wants procedural half-cover density tuning, add a `halfCoverDensity` field to the terrain config and roll it like walls; the masking-against-spawn-regions step already considers union of all reservations and doesn't need to know about half-cover specifically.

73. **D7.A: chasm relies entirely on Infinity-cost path blocking + tile-not-in-LOS-blocker pipeline.** No Pathfinding code change, no LineOfSight code change. The two contracts that make this work: (a) `Pathfinding.findPath`'s `if (!isFinite(stepCost)) continue;` short-circuit (gotcha #34 documents this for general data-driven blocks), and (b) `hasLineOfSight` only consumes a `blockers: GridCoord[]` of unit cells — tiles never enter that pipeline, so a chasm cell on the Bresenham line is invisible to LOS. If a future feature ever wants chasm to break LOS (e.g. a chasm so deep it occludes shots), that's a behavior change requiring a new "tile LOS blocker" predicate — don't try to retrofit by stuffing chasm coords into AttackBehavior's blocker list; that'd recreate the gotcha #40 anti-pattern.

74. **D7.A: chasm is hand-authored-only in D7 (same scope as half-cover in D6).** `GeneratedTerrain.chasms` is non-empty only for the layout path; procedural emits `[]`. No `config/terrain.json` density knob. When future work wants procedural chasm density, mirror the half-cover punt (gotcha #72): add a `chasmDensity` field and a separate reservation-aware roll, since chasm tiles need the same "no overlap with walls / water / spawn regions" guarantee that walls and water already get via the candidate pool. Note that procedural's internal `hasPath` BFS only blocks on walls today — if procedural chasm is ever introduced, the BFS needs to also block on chasm tiles via the tileGrid.

75. **D7.A: TileKind expansion doesn't bump snapshot schema version.** The TileGrid stores tile kinds as discriminator strings (`'floor'` / `'shallow_water'` / `'chasm'`), so a new kind value just expands the set of legal strings — v5 snapshots written before chasm existed simply contain no chasm cells. Contrast with adding a new FIELD to UnitSnapshot or WorldSnapshot, which DOES require a version bump (D6 `blocksLineOfSight` was 4→5). The TileGrid.ts comment "adding a new TileKind doesn't invalidate existing snapshots" calls this out explicitly. If a future tile kind needs auxiliary per-cell data (e.g. fire intensity, healing remaining), THAT'S the change that bumps the version.

76. **D7.A: no exhaustive `switch (kind: TileKind)` exists in the codebase.** `TerrainRenderer.heightAt` and `topColorFor` use a `if (kind === 'shallow_water') return ...; fall-through to floor` shape. Adding chasm fell through to the floor branch without compile errors AND without runtime crashes — chasm renders as floor visually in D7.A. The D7.C visual pass will explicitly branch chasm into a sunken-pit visual. If you introduce an exhaustive switch later (e.g. via a `assertNever(kind)` helper), audit ALL existing call sites — there are currently zero, and the implicit fall-through is what made the D7.A stopgap a no-op for the renderer.

77. **D7.B: tile effects use GLOBAL cadence, not per-unit accumulators.** `applyTileEffects` checks `currentTick % FIRE_TICKS_PER_DAMAGE === 0` (and the healing equivalent) and applies 1 HP delta per matching cell, to every live combatant on a matching tile, in `this.units` insertion order. Zero per-unit state, no snapshot bump. Known edge case: a fast-moving unit can theoretically dance onto fire between cadence ticks and dance off before the next one, skipping damage entirely. With default rates (5 ticks/fire, MoveCooldown ~5 ticks), the unit is almost always on the tile during exactly one cadence tick → 1 damage per visit, which is what we want. The "fast-unit-skips-fire" exploit is mild and intentional for D7.B; if playtest reveals it feels broken, switch to a per-unit "ticks-on-current-effect-tile" accumulator (resets on tile-kind change) — that's the migration path documented in the D7.B "Tile cadence" decision answer.

78. **D7.B: tile-effect pass runs AFTER per-unit selector, AFTER overflow scan, BEFORE checkBattleEnd, AND immediately followed by `reapDead()`.** The full tick() order is now: (1) drain commands, (2) per-unit loop (death-reap + cooldown decrement + selector + action execute), (3) `runOverflowScan` (D5.C reinforcement), (4) `applyTileEffects` (D7.B chip damage/heal), (5) `reapDead` (clean up any units killed in step 4), (6) `checkBattleEnd`. The `reapDead` pass is what makes a fire-kill end the battle on the same tick — without it, `checkBattleEnd` counts the dead-but-not-yet-spliced unit as alive and the battle ends one tick late. Don't fold `reapDead` into the per-unit loop's step-1 death check — they handle different deaths (step 1 = combat kills + lingering 0-HP from prior ticks; step 5 = fire kills that happened in step 4). Both emit `unit:died`.

79. **D7.B: fire damage uses a separate `unit:burned` event, NOT an extended `unit:attacked`.** The original ROADMAP entry said "extend unit:attacked with reason," but the D7.B design call landed on a separate event because (a) fire has no attacker, so reusing the existing shape would require `attackerId: number | null` and a null-guard at every subscriber, and (b) `unit:healed` is its own event by necessity, and asymmetry between burn and heal would read worse than two parallel events. `unit:attacked` shape is unchanged for back-compat. Subscribers that need to track "any HP change" should subscribe to all three (`unit:attacked` / `unit:burned` / `unit:healed`) — BattleRenderer is the canonical example, refreshing the HP bar on all three. Audio currently fires for `unit:attacked` only; fire-burn audio is queued for D7.C alongside the visual flicker.

80. **D7.B: healing emits `amount: 0` when clamped at maxHp.** A unit at maxHp still gets `unit:healed` fired on each heal-cadence tick, with `amount: 0`. Subscribers that want to debounce (e.g. avoid a no-op refresh of the HP bar) should check `amount > 0` themselves. The sim emits unconditionally because it's a deterministic side effect of the cadence — observers like the determinism replay test would otherwise see different event streams depending on which unit happened to be at full HP. Don't "optimize" by suppressing the emit when amount is 0; that introduces a hidden branch in the event log.

81. **D7.B: tile-effect pass skips already-dead units.** `if (unit.currentHp <= 0) continue;` guards against re-burning a corpse that died in step 2 (combat kill) but is still in `this.units` waiting for step 5's reap. Without the guard, a unit on fire that was just slain by an adjacent enemy would emit a posthumous `unit:burned` event with `unitId` pointing at a unit that the next tick's per-unit loop is about to reap. The combination of "skip dead in step 4" + "reap in step 5" gives clean death semantics: one `unit:died` per kill, never doubled, never delayed.

82. **D7.C: `BOTTOM_Y` is `-1.5` globally (was `-0.7`), not per-tile.** Lowering globally is simpler than threading a per-cell bottom Y through `writeVert` calls. Non-chasm tiles' visible sides only differ where the deeper bottom is geometrically exposed — in the middle of an all-floor board, side faces are occluded by neighboring tile tops at the same X/Z plane, so the change is invisible. The two places it IS visible: (a) the chasm boundary (a floor cell next to a chasm cell shows its full side face from `topY` down to -1.5, which is the cliff into the pit — exactly what we want), and (b) the literal board edge, where the camera angle barely sees the depth anyway. Don't add a per-tile bottom-Y path; the global lowering is intentional and the visible visual character is preserved at the floor-floor interior.

83. **D7.C: per-tile animation is encoded as a `vec2 aAnim` vertex attribute, NOT a uniform array.** `aAnim.x` is the anim-type discriminator (0 = none, 1 = fire, 2 = healing); `aAnim.y` is a per-tile phase offset hashed from `(cx * 13 + cy * 7) * 0.43`. All `VERTS_PER_TILE = 30` verts of a tile share the same (type, phase). Encoding per-tile via a uniform array would mean either a fixed-size cap (annoying with `LAYOUT_MAX_SIDE × LAYOUT_MAX_SIDE = 1024` tiles potentially) or a per-tile-kind switch in the shader. The attribute approach is the same pattern `aColor` + `aTopUV` already use. The fragment shader gates on `vAnim.x > 0.5` and `vAnim.x > 1.5` to branch type; phase keeps neighboring fire tiles from pulsing in unison (visually crucial — synchronized pulses read as "one big fire," uncorrelated phases read as "many small fires").

84. **D7.C: `uTime` is driven from `BattleScene.tick(dt)`, NOT the `Renderer` rAF loop.** Animation only matters during battle — non-battle scenes call `terrain.clear()` and have no animated tile kinds. Putting `advanceTime(dt)` on the scene tick (which is gated on the active scene being BattleScene) means Map / Recruit / GameOver scenes pay zero animation cost. The uniform is a clean fp32 accumulator — no wrap, no modular reduction; the precision holds second-grain time for many hours of continuous play. If you ever add an animated tile kind to a non-battle scene, move the call to Game's onFrame instead.

85. **D7.C: `unit:healed` audio gate is `amount <= 0`, not `!amount` or `amount === 0`.** Gotcha #80 documents that the sim emits `unit:healed` with `amount: 0` on every cadence tick for a unit already at maxHp — the no-op heal is part of the deterministic event log. The audio handler skips with `if (amount <= 0) return;` to also handle a hypothetical future negative-amount path (e.g. if a healing-tile-with-debuff archetype ever lands). `!amount` would falsely skip on `amount === 0n` or `null` if the bus payload typing ever loosens; `=== 0` is too narrow if a negative-amount path lands. `<= 0` is the right predicate for "this heal didn't help the unit; suppress the audio cue."

86. **D7.C: terrain animation runs BEFORE the grid-line stamp in the fragment shader.** The fragment computes `vec3 base = vColor * shading`, then applies the per-tile flicker/pulse multiplication, THEN stamps the grid line via `mix(base, uGridLineColor, lineAlpha * 0.6)`. Reversing this order would make the grid line itself flicker on fire tiles — visually fragile (the grid is an architectural feature of the board, it should read as stable). The animation also applies uniformly across the prism (top + sides), not just the top face — sides flicker too, but `SIDE_SHADE = 0.7` tapers the visual amplitude naturally, so fire "leaks" heat down its walls without looking glaring.

87. **D7.C: editor's `#terrain-row` mirrors the D6 `#neutral-row` SHAPE.** Same row markup (.neutral-row class, even on the terrain-row element — the class is the row family, not the layer name), same hidden-when-layer-inactive pattern, same mid-stroke commit rule (clicking another sub-tool calls `endStroke()` synchronously before swapping `activeTerrainKind`), same per-sub-tool erase scope (gotcha #65 — `erase-fire` only clears `'fire'` cells, leaves `'water'` / `'chasm'` / `'healing'` untouched). The legend gained entries for each new kind. If you add a fourth terrain kind later, the only files to touch are `editor.ts` (Cell union, TerrainKind union, applyTerrainStroke + refreshCell branches, collect/export hooks, validate's blockedSet), `editor.css` (new `.cell.NEW` block), and `index.html` (the radio `<input>` + a legend `<li>`).

88. **D8: themes re-color FLOOR tops only.** Water / chasm / fire / healing keep their fixed D7 palettes regardless of theme. They're functional indicators (which tile is impassable, which damages, which heals) and recoloring them per theme would push parsing-load onto the player. `topColorFor(topY, kind, theme, out)` consults `FLOOR_PALETTE[theme]` ONLY when `kind === 'floor'` (the fall-through branch); every other kind ignores the theme parameter. If a future feature wants per-theme water tinting (e.g. lava-water in volcanic), extend the table — but think hard first about whether the affordance loss is worth the flavor gain.

89. **D8: `Run.handleEnterNode` now does FIVE `battleRng` draws before `rollEnemyTeam`.** Order is `worldSeed → terrainSeed → layoutId → proceduralSide → theme → rollEnemyTeam`. `rollTheme` always runs (even when discarded on hand-authored encounters that pin `layout.theme`) — same invariant `rollLayoutId` / `rollProceduralSide` already maintain (gotcha #49 generalizes here). **Behavior shift:** existing seeds shift enemy compositions by one draw vs pre-D8 (acceptable; permissive existing range assertions in `Run.test.ts` still pass). When C6 lands multi-map runs, the plan is to move `rollTheme` up to nodeMap construction (each map carries a theme; procedural battles within it inherit) — at that point this draw exits `handleEnterNode` and the order shortens. Do NOT add new draws here in between without the same "always run" guard.

90. **D8: `RUN_SCHEMA_VERSION` bumped 1 → 2.** `BattleEncounter.theme` is a new required field on persisted state. v1 snapshots throw at load. The TileGrid / UnitSnapshot schema is unchanged (no new TileKind values, no new Unit fields), so World snapshots stay at v5. If a future change touches BOTH snapshot families, bump both independently — they version separately.

91. **D8: editor floor preview is a CSS variable (`--floor-bg`), NOT a direct background override.** `.grid[data-theme='rock'] { --floor-bg: var(--terminal-stone); }` and the `.cell { background: var(--floor-bg); }` rule. Doing `.grid[data-theme='rock'] .cell { background: ... }` directly would override the per-kind background classes (`.cell.wall`, `.cell.water`, etc.) due to specificity — a wall on a rock-themed grid would render gray-stone instead of TERMINAL_STONE-with-`#`-glyph. The CSS-variable approach only fires on naked floor cells. If you add a new tile kind that needs its own background, the existing `.cell.NEW { background: ... }` rule wins via specificity. The editor's floor preview shows ONE flat color per theme (the renderer's palette "high" endpoint) — the in-game faceted-prism lerp can't be CSS-replicated without overengineering.

92. **D8: banner suffix is suppressed for `theme === 'default'`.** `bannerText = encounter.theme === 'default' ? locationName : `${locationName} — ${titleCaseTheme(theme)}``. Reason: `default` IS the baseline visual look, so writing "Corridor — Default" everywhere reads as boilerplate noise. Non-default themes get the suffix because they're flavor variants worth surfacing. If you add a fourth theme that's also meant to be "the new baseline," update this conditional alongside whatever scope decision drove the change — don't accumulate special-cases (this would be a smell pointing at "the theme list grew, time to revisit the suffix rule").

93. **E1: `UnitStats` is the new vocabulary; battle-time numbers live on `UnitDerived`.** Old `{maxHp, attackDamage, attackRange, attackCooldownTicks, moveCooldownTicks}` is gone. UnitStats now carries `constitution / strength / ranged / magic / luck / speed / endurance`; everything the sim or renderer reads at battle time (`maxHp` for HP bars, `attackCooldownTicks` / `moveCooldownTicks` for selector cadence, `attackRange` for in-range checks, `critChance` for AttackAction's roll) lives on `derived: UnitDerived`, computed once at construction via `deriveStats(stats, attackRange)` in [src/sim/stats.ts](src/sim/stats.ts). The two reads to fix on the day stats start mutating mid-battle (status effects, future buffs): (a) recompute `derived` whenever `stats` changes; (b) audit `Unit.currentHp` clamping — `World.applyTileEffects` already uses `derived.maxHp`, but other future damage / heal sources need the same. Don't put battle-time numbers on `UnitStats` to "shortcut" the derive — the split is what lets E3's level-up dial stats cleanly.

94. **E1: `Unit.archetype: 'melee' | 'ranged' | 'environment'`.** Combatants carry the same archetype tag as their template; environment entities (walls + half-cover, spawned via `World.spawnEnvironment`) get `'environment'` + `ZERO_STATS` + `inertDerived(maxHp)`. The tag is what `basicAttackDamage(unit)` switches on (melee → strength, ranged → ranged stat, environment → 0). Don't infer archetype from glyph (gotcha #40 retired that pattern with `blocksLineOfSight`; same anti-pattern applies here). E7 will add `'mage'` / `'rogue'` / `'healer'` to the union; the basicAttackDamage switch is the one place to extend. Tests that hand-build `Unit` need `archetype` in the init bundle — same way `team` is required.

95. **E1: `world.combatRng` is a dedicated RNG channel, forked from `world.rng` at construction.** AttackAction's start-time crit roll consumes it; the spawn-setup `setupRngFor` stream (anchored on `encounter.terrainSeed`) and World.rng (anchored on `encounter.worldSeed`) stay independent of crit noise. Default fork inside the constructor preserves naked-test ergonomics (`new World(bus, rng)` still works). Snapshotted alongside `rng` in WorldSnapshot v6 so replays produce byte-identical crit sequences. Don't reach into `world.rng` for any combat roll — that'd re-tangle pathfinding tests with combat outcomes. If E2's Ability resolvers add more RNG draws (e.g. AoE target selection), route them through `combatRng` too.

96. **E1: `rollUnit` does NO RNG draws today.** Returns `{ archetype, stats: { ...CONFIGS[archetype].baseStats } }` verbatim — every melee unit on a given run is the exact same template. The `rng` argument stays on the signature because callers (`rollOffer`, `rollTeam`, `rollEnemyTeam`) already thread one; preserving the param keeps the E3 transition painless (E3's `simulateLevelUps(baseStats, growthRates, n, rng)` will start consuming it). Tests that asserted "different seeds → different stat rolls" had to flip to "different seeds → IDENTICAL templates (no roll today)" — the [src/sim/archetypes.test.ts](src/sim/archetypes.test.ts) check documents this contract explicitly so the next refactor doesn't accidentally restore non-determinism.

97. **E1: `AttackAction` snapshot shape + `unit:attacked.crit` flag.** `AttackActionData` is now `{ targetId, baseDamage, critChance }` (was `{ targetId, damage }`). Crit resolves at action start, not propose, so the stored data is the pre-crit base; the rolled `damage` lands on the event. `unit:attacked` carries `crit: boolean` — every emit site has to set it (today just AttackAction.start and the multiTick test fixture). E6's hitsplats will key off it to colour-code crits. If you add a future Action that emits `unit:attacked` (channel cast, AoE pulse), pass `crit` even if it's always `false`; missing-field is a type error.

98. **E1: difficulty scaling moved from `maxHp` to `constitution`.** `Run.scaleConstitution` (was `scaleMaxHp`) bumps `template.stats.constitution` by the per-floor multiplier; `deriveStats` then produces the scaled maxHp downstream. Reason: maxHp is now a derived quantity — scaling it directly would create two sources of truth that drift when E3 changes the derive formula. The same rule applies for any future "make this unit tougher" hook: bump the *stat*, never the derived. E3 retires this scaling entirely in favor of `enemyLevelPerFloor` driving a full `scaleStats` pass per the ROADMAP, so the constitution-only path is intentionally narrow.

99. **E3.5: tests asserting tick counts should derive them, not hardcode them.** `src/config.test.ts` originally pinned `secondsToTicks(0.5) === 5` (matching TICK_RATE=10); the E3.5 bump to TICK_RATE=20 broke the assertion. Rewritten to compute boundaries from `TICK_RATE` itself (`const halfTickSeconds = 0.5 / TICK_RATE`) so future tick-rate changes don't churn this test again. Apply the same pattern when writing new tests that need to express "N seconds of behavior" in tick terms: `secondsToTicks(N)` over a hardcoded number. Tests that pin behavior at a specific TICK_RATE are a smell — gotcha #6 says "durations are authored in seconds, not ticks," and tests should mirror that contract.

100. **E3.5: `RNG.toJSON()` must normalize to uint32 to match `fromJSON()`.** The mulberry32 step uses `(this.state + 0x6d2b79f5) | 0` (signed cast); `RNG.constructor` and `RNG.fromJSON` both normalize via `>>> 0` (unsigned). Pre-E3.5 `toJSON()` returned the raw signed state, so a snapshot-then-restore round-trip produced two RNGs with bit-identical internal state but different `.toJSON().state` values (differing by exactly 2^32) when the sign bit happened to be set. Functionally invisible — the next `| 0` step on either side collapses them — but `.toEqual()` is strict and fails. E3.5 added `>>> 0` to `toJSON()` so the serialized form matches the constructor's normalization. If you ever stringify-then-parse the snapshot (currently we never do; `World.fromJSON` consumes the in-memory object), this fix also makes that round-trip clean. Latent for all of A2 through E3; surfaced only because E3.5's TICK_RATE doubling caused a different RNG state to land at the 50-tick snapshot point.
101. **Pre-E5: attack cadence lives on the Ability, NOT `UnitDerived`.** If you go looking for `attackCooldownTicks` on a unit's derived block, it's gone — `deriveStats` no longer produces it and the global `STATS.baseAttackCooldownSeconds` is retired. Each ability authors its own `cooldownSeconds` in [config/abilities.json](config/abilities.json); `proposeBasicStrike` resolves ticks at propose time via `attackCooldownTicksFor(cooldownSeconds, speed)` in [src/sim/stats.ts](src/sim/stats.ts) (same `cooldownScale(speed)` curve as move CD). The reason a single per-unit value couldn't stay: a multi-ability unit (E7 mage/rogue) carries several timings. Two consequences that look weird without context: (a) [src/sim/abilities/registry.ts](src/sim/abilities/registry.ts) has a module-load IIFE that throws if the factory id-set and abilities.json id-set diverge — that's intentional loud-failure, not dead code; **no silent global fallback** by design. (b) WorldSnapshot bumped **v11→v12** purely because `UnitSnapshot.derived` (stored verbatim) lost the field. **`range` deliberately stayed on the archetype** (`derived.attackRange`) — MovementBehavior's in-range abstain reads a single value, so moving range per-ability needs an answer for "which ability's range" and was deferred to E5's pathfinding work. Test seam: don't reach for a `derived.attackCooldownTicks` override anymore — use a synthetic explicit-cadence `Ability` (see `ConfigurableStrike` in `AbilityBehavior.test.ts`) or derive the expected cadence from config (see `sharedCooldown.test.ts`).

102. **E5: `derived.attackRange` is now the unit's MAX ability range, not an archetype primitive.** The range migration moved range from `config/archetypes.json#attackRange` (removed) to a per-ability `range` in `config/abilities.json`. `rangeForArchetype` (renamed from `attackRangeForArchetype`) computes `max(abilities.map(abilityConfig(id).range))` and feeds it into `deriveStats` unchanged, so `derived.attackRange` keeps working as the "effective engagement range" that MovementBehavior's in-range abstain + the BattleScene audio melee/shoot pick + the RecruitScreen RNG row read. **But per-ability gating is different from the engagement range:** `proposeBasicStrike` gates on `abilityConfig(abilityId).range` (THIS strike's reach), NOT `derived.attackRange` — so a future multi-ability unit's short-range swing correctly abstains when only its long-range bolt can reach. Don't "simplify" the strike gate back to `derived.attackRange`; the `AbilityBehavior.test` guard "a strike gates on its ability config range, not the unit engagement range" pins this (it force-sets `derived.attackRange = 5` and asserts a range-3 ability still abstains at chebyshev 4). No snapshot bump — `derived.attackRange` stayed in `UnitDerived` (kept as a cached max rather than deleted, which avoided a third E5 snapshot bump).

103. **E5.A: `updateTarget` runs ONCE per free unit in the selector, never inside a behavior.** It mutates `unit.targetId` + `unit.outOfLosTicks`, so calling it from both MovementBehavior and AbilityBehavior (both polled per tick) would double-advance the LOS counter and could re-decide mid-tick. The single call site is in `World.tick`, right before the `// 4. Selector.` behavior loop — reached only when `activeAction === null` (a free unit), so a busy/locked unit's target doesn't drift mid-action. Behaviors are READ-ONLY consumers via `currentTarget(unit, world)`, which returns the sticky `targetId` if it still resolves to a living enemy, else falls back to `findTarget` (nearest). The nearest fallback is load-bearing for tests: unit tests that call `behavior.proposeAction` directly (without going through `World.tick`→`updateTarget`) would otherwise see `targetId === null` and abstain. If you add a new targeting consumer, call `currentTarget`, not `findTarget`, and never call `updateTarget` outside the selector.

104. **E5.B: how often the boids sidestep fires depends on `occupiedCellPenalty` (gotcha #107) — at the original 100 it was near-dormant; at the shipped 4 it's a real contributor.** The sidestep only triggers when MovementBehavior's A* `path[1]` is occupied by another unit. With a HIGH soft-block penalty A* routes AROUND an occupied cell whenever any cheaper detour exists (so `path[1]` is rarely occupied → sidestep dormant); that was the case at the initial +100, which is why E5.A's target stickiness, not the sidestep, is what killed the *visible* corridor backpedal. Lowering the penalty to 4 (gotcha #107) flips this: A* now routes *through* occupied cells for any non-trivial detour, so `path[1]`-occupied is common → the sidestep + step-collision queue do real work. The `MovementBehavior.test` sidestep cases construct an explicit forced chokepoint (only route runs through the occupied cell) to exercise it regardless of penalty. Also in E5.B: `popLowestF` breaks f-ties toward the goal (lower h, then lex key) — this is a *pure ordering of equal-f nodes*, so it does NOT touch f-values and keeps the Chebyshev heuristic admissible (gotcha #34 still holds); it only changes WHICH min-cost path is returned (straighter, no leftward crab). Do NOT add an h-weighted nudge to the f-value itself — that would break admissibility.

105. **E5 test-helper trap: hand-assigned unit ids in `scene()` helpers collide with `spawnWall`/`spawnEnvironment` ids.** The `MovementBehavior.test` + `Targeting.test` `scene()` helpers push units to `world.units` directly with a local `nextId` counter (1, 2, 3…), bypassing `World`'s own `nextUnitId`. So calling `spawnWall(world, ...)` afterward pulls ids from World's counter (0, 1, 2…) and **collides** — and MovementBehavior's `if (u.id === unit.id) continue` then silently drops a wall from `pathBlockers`, so A* routes through it (a unit "walks onto" a wall in the test). Single-obstacle tests dodge it (one `spawnWall` → id 0, no collision); multi-wall tests don't. Fix: spawn walls as `team: 'neutral'` specs THROUGH the `scene()` helper's id counter (the helper now maps neutral specs to `archetype: 'environment'` + `inertDerived` + glyph `#`), not via `spawnWall`. If a future test needs hard blockers, use neutral specs, not `spawnWall`-after-`scene`.

106. **E5: fuzz `DEFAULT_MAX_TICKS` derives from TICK_RATE — don't hardcode it.** A per-battle tick cap authored as a raw `1000` went stale when E3.5 doubled TICK_RATE (the cap's "≈100s" intent silently became ~50s), which surfaced as false `hang` outcomes on the long (32-tall) endlessCorridors board with grown teams. It's now `secondsToTicks(100)`. When you see fuzz "hangs by layout" concentrated on a big board, **read the trace before assuming a deadlock**: a real deadlock shows ~0 deaths and a frozen tick count; a cap artifact shows deaths progressing on both sides right up to the cap (the seed-20 trace had 4 deaths each side at exactly the cap). Same gotcha #6 lesson as #99: express "N seconds of behavior" via `secondsToTicks(N)`, never a hardcoded tick count.

107. **E5: `SIM.occupiedCellPenalty` is the soft-block dial — it ONLY affects unit-vs-unit detours, never walls.** The extra A\* cost for routing through a cell occupied by an ally / non-target enemy (`config/sim.json`, default **4**, was a hardcoded 100). It's the trade between "flank around allies" (high) and "hold the line / queue / sidestep" (low): A\* detours around an occupied cell only when the detour costs LESS than the penalty. At 100 that meant detours up to ~99 cells — units peeled off the packed spawn band into long backward flanks before engaging (the symptom the user reported as "all units, very early"). A 24-seed endlessCorridors sweep put the knee at 4 (early-backward 6.2%→3.4%, ~911→~707 avg ticks); user-playtested as much improved. **Safe to retune across the whole range:** the penalty stays finite, so `findPath` never returns `[]` and the gotcha #71/Labyrinth deadlock can't come back (0 hangs at every swept value incl. 1 and 100) — only the detour-vs-through *preference* moves, never reachability. Must stay ≥ 0 so total cost stays ≥ 1 (Chebyshev admissible, gotcha #34). **Walls + half-cover are hard `blockers` in `findPath`, not penalised cells — the knob can never make a unit phase through one at any value, including 0.** The target itself is excluded from the penalty set so A\* always has a reachable goal. If a future board feels too eager to queue, raise it (6–8 loosens step-arounds); too much flanking, lower it. The flip side worth knowing: this value also governs how often the E5.B sidestep fires (gotcha #104).

108. **SpriteRenderer has a per-instance `instanceSize` attribute — set it via `updateSprite({ size })`, default 1.** Sprites used to share one `uSpriteSize` uniform with no per-instance scale, so the E6.B `*` tracer rendered at full unit-glyph size; playtest flagged it a touch large, so `aSize` was plumbed (buffer + `writeSize` + a `size?` on `updateSprite` + `copyInstance`) and the billboard vertex shader now does `position.xy * uSpriteSize * instanceSize`. The projectile spawns at `PROJECTILE_SIZE` (0.6, a BattleRenderer const). **The load-bearing detail: `addSprite` MUST write size 1 for every sprite** — the `Float32Array` defaults to 0 and size 0 = invisible, so a missed write makes a sprite silently vanish (verify a new add path writes all attrs). The visible mesh + the bloom mesh share this shader and these buffers, so a size change scales the halo to match for free — no second SpriteRenderer / bloomComposer wiring needed (that path would also have to re-register `bloomMesh` or projectiles wouldn't glow). The `·`-glyph swap is the alternative if you ever want zero-code sizing. The tracer lives in `BattleRenderer.projectiles` (a Set), NOT `handles` — `detach` sweeps it separately because `animator.clear()` drops the lerp without firing the despawn `onComplete`.

109. **F4: the gambit retreat's `unit:moved` `durationTicks` is the REMAINING busy window, NOT `moveCooldownTicks` — don't "simplify" it back.** `GambitStrikeAction.applyEffect` emits the retreat with `durationTicks = max(1, min(moveCooldownTicks, activeAction.finishTick − world.currentTick))`. The naive `moveCooldownTicks` (what a normal move uses) is WRONG here: the gambit defers its reposition to the `impact` boundary, so only `recovery = D − windup` ticks remain in the action's busy window. At the rogue's high speed the gambit cadence `D` is short (~13 ticks), so `recovery` (~8) is SHORTER than `moveCooldownTicks` (~14) — a full-cooldown lerp is still in flight when the unit frees up and starts its next action, whose `startLerp` snaps the sprite and visibly cuts the dart's tail off (the exact bug the `fix(F4)` follow-up addressed). Capping at the remaining window lands the dart exactly as the rogue frees up, the way a normal move's lerp fills its cooldown. The `activeAction`-null fallback to `moveCooldownTicks` is only for direct-construction unit tests; at runtime `applyEffect` always fires from `World.tick` with the action live. This is render-only (`durationTicks` is a renderer signal — no sim/RNG consumer), so it never shifts fuzz/determinism. Pinned by `GambitStrikeAction.test`'s "fills the recovery window" + fallback cases.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**

## Project shape

[ARCHITECTURE.md](ARCHITECTURE.md) carries the canonical tree. The summary
below highlights post-MVP additions and their phase tags; refer to
ARCHITECTURE for the full layout.

```
src/
  main.ts              # entry; top-level await on FontAtlas.create(); dev-only window.__game (D5.C)
  Game.ts              # top-level orchestrator: owns Renderer/Bus/Run; scene swapper (A5)
                       # builds Run from parseRunConfigFromURL() (G1)
  config.ts            # TICK_RATE=20 (E3.5), GRID_SIZE=12, secondsToTicks/ticksToSeconds
                       # (engine knobs; balance lives in config/*.json -- see src/config/)
  config/              # A4: zod-validated wrappers around config/*.json
    archetypes.ts      # glyph + baseStats + growthRates (E1/E3); attackRange moved to abilities (E5)
    abilities.ts       # E2+: per-ability cooldownSeconds/range/aoe/travelSeconds/retreatDelaySeconds
    difficulty.ts      # G4: enemy level-budget knobs (budgetFactor/offset, swarm) + A/B/C presets
    recruitment.ts     # starting team + offer size + startingLevel + recruitBonusChance (G4)
    leveling.ts        # E4: xp curve + half-cover mult + restXp (G3) + xpPerHealing (F6)
    nodemap.ts         # floor count + width bands + degree cap + restChance/restMinSpacing (G2/G3)
    stats.ts           # hpPerConstitution, crit, base move CD; per-axis mobility/agility CdPerStat+MinCdScale (GP1)
    sim.ts             # E5: targeting + pathfinding knobs (retarget, occupiedCellPenalty, healer*)
    terrain.ts         # C1a: wall + water density
    layouts.ts         # C1d.A: hand-authored layout array
    tiles.ts           # D7.B: fire/healing chip rates
    spawn.ts           # D5.C: SpawnAction lockout duration
    schemas.ts         # shared zod helpers (A4)
  core/
    RNG.ts             # mulberry32, fork(), pick(), int(); toJSON normalizes (E3.5, gotcha #100)
    EventBus.ts        # typed pub/sub; on() returns unsub
    Clock.ts           # fixed-timestep accumulator
    events.ts          # GameEvents catalog (typed event payloads)
    types.ts           # GridCoord, Vec2
  sim/
    World.ts           # tick(): selector + phase timeline (F2) + overflow scan (D5.C)
                       #   + tile-effect pass (D7.B) + reapDead + checkBattleEnd
                       # combatRng (E1) + damageDealt/utilityDone XP ledgers (E4/F6)
                       # + playerRosterIds; toJSON/fromJSON -- WorldSnapshot v16 (GP1)
    Unit.ts            # Unit + UnitTemplate + UnitStats (GP1 vocab) + UnitDerived + level (E3)
                       # + xp/rosterIndex (E4) + actionCooldowns/activeAction (A1)
                       # + blocksLineOfSight (D6); Archetype = 6 combat kinds + environment
                       # Team: 'player' | 'enemy' | 'neutral' (C1a)
    stats.ts           # deriveStats/inertDerived/ZERO_STATS + damage/heal/range/cooldown helpers
    leveling.ts        # E3: simulateLevelUps (player rolls) + scaleStats (enemies, deterministic)
    xp.ts              # E4: xpToNext curve + computeXpAwards + displayLevel
    TileGrid.ts        # floor|shallow_water|chasm|fire|healing (D7); per-cell cost
    LineOfSight.ts     # Bresenham line walk for ranged LOS (C1b)
    Action.ts          # Action / ActionProposal / phase timeline (F2) + OrphanPolicy
    Command.ts         # WorldCommand union -- drained at tick boundary (A2)
    Pathfinding.ts     # A* king's-move, Chebyshev heuristic + CostFn (C1a)
    Targeting.ts       # updateTarget/currentTarget stickiness (E5) + lowestWoundedAlly (E7.B)
    archetypes.ts      # ALL_ARCHETYPES pool (F1), rollUnit, glyphForArchetype
    environment.ts     # spawnWall (C1a) + spawnHalfCover (D6) -- neutral-team factories
    terrainGen.ts      # procedural tiles/walls (C1a) + layout-library dispatch (C1b)
    layouts.ts         # re-export of validated config; LAYOUT_IDS picklist (C1b/C1d.A)
    battleSetup.ts     # shared applyTerrain/spawnTeam/spawnEncounter (A3 + C1a)
    actions/
      MoveAction.ts          # logical position update + unit:moved
      AttackAction.ts        # melee/ranged damage + crit + half-cover mult (E1/E4)
      GambitStrikeAction.ts  # E7.A: rogue strike + deferred reposition (F4)
      HealAction.ts          # E7.B: HP restore + heal-XP ledger (F6)
      MagicBoltAction.ts     # E7.C: multi-tick ground-target AoE
      CatapultShotAction.ts  # E7.D: multi-tick homing heavy hit
      SpawnAction.ts         # D5.C: pure-lockout action for overflow-queue spawns
      registry.ts            # action factories keyed by Action.id (A2)
    abilities/               # E2: generic Ability layer (retired AttackBehavior)
      Ability.ts             #   Ability interface + ignoresLineOfSight flag (E7.D)
      strikes.ts             #   MeleeStrike/RangedShot/gambit via shared proposeBasicStrike
      heal.ts                #   E7.B: HealAlly (lowest-HP wounded ally, no LOS)
      magic.ts               #   E7.C: MagicBolt (AoE ground-target)
      catapult.ts            #   E7.D: CatapultShot (homing, lobs over walls)
      registry.ts            #   ability factories; boot-asserts vs abilities.json (E5)
    behaviors/
      MovementBehavior.ts    # A* approach; boids sidestep (E5.B); LOS-optional abstain (E7.D)
      AbilityBehavior.ts     # E2: walks per-unit Ability[] (replaced AttackBehavior)
      SupportMovementBehavior.ts  # E7.B: healer idle/panic/approach/centroid-trail
      registry.ts            # createMovementBehavior + behavior factories (A2)
  run/
    Run.ts             # state machine: map|turn-intro|battle|turn-outcome|recruit|promotion|defeat|complete
                       # (E4.4/H4b); H4 encounter loop + H5 card deck (draw/hand/discard + deckRng);
                       # rest/boss node resolution (G3); XP banking; toJSON/fromJSON -- Run v9
    RunConfig.ts       # G1: RunConfig + parseRunConfigFromURL (shared by browser/CLI/GUI)
    enemyBudget.ts     # G4 SEAM playerTeamLevel -- H5 swapped to avgLevel x min(roster, handSize)
    Command.ts         # RunCommand union + RunDispatcher interface (A2)
    NodeMap.ts         # planar non-crossing DAG (G2) + NodeKind battle|rest|boss (G3)
    Recruitment.ts     # rollOffer: distinct archetypes from full pool (F1); per-card level (post-G5)
  render/
    Renderer.ts        # WebGLRenderer + two EffectComposers (selective bloom, B1.1)
                       # + RAF loop + fit/scroll camera modes (D4)
    FontAtlas.ts       # canvas2d glyph atlas -> THREE.CanvasTexture (glyph set from glyphs.ts)
    glyphs.ts          # E7.A: THREE-free GLYPHS set (FontAtlas.test asserts archetype coverage)
    SpriteRenderer.ts  # InstancedBufferGeometry + shaders; dual mesh (visible/bloom)
                       # + per-instance bloomIntensity (B1.1) + instanceSize (E6.B)
    UnitOverlayLayer.ts # E3.6: DOM HP bar + level badge + action-progress + hitsplats (E6.C)
                       #   (replaced the retired BarRenderer + bar.*.glsl)
    TerrainRenderer.ts # C1c faceted prism-per-tile; D7.C flicker/sink; D8 theme palettes
    BattleRenderer.ts  # sim/render seam: attach/detach; shove/projectile/explosion VFX (E6/E7)
    PostProcess.ts     # SatClamp + Bloom + BloomMix factories (B1.1)
    palette.ts         # COLORS table (+ TERMINAL_STONE neutrals, C1a)
    shaders/           # .glsl via Vite ?raw (A4): sprite/billboard/terrain/bloom-mix/sat-clamp
    animation/SpriteAnimator.ts  # startLerp (arc/targetProvider, F3) + startShove (E6.A) + startFade
  scenes/              # A5: Scene system -- single-active swap driven from Game
    Scene.ts           #   Scene interface + SceneContext (bars -> overlays, E3.6)
    BattleScene.ts     #   wraps World + Clock + BattleRenderer + HUD
    MapScene.ts        #   DOM-only, wraps MapScreen
    RecruitScene.ts    #   DOM-only, wraps RecruitScreen
    PromotionScene.ts  #   E4.4: DOM-only level-up summary, shown before recruit
    GameOverScene.ts   #   DOM-only, defeat / complete
  ui/
    ui.css
    fade.ts            # fadeIn / fadeOutAndRemove -- shared screen transitions
    MapScreen.ts       # full-viewport node map (G2) + kind icons (G3); frontier click -> enterNode
    RecruitScreen.ts   # recruit offer cards -> dispatch chooseRecruit; raw stat block + ability list (GP3)
    PromotionScreen.ts # E4.4: per-unit level-up rows
    GameOverScreen.ts  # defeat / complete variants -> dispatch resetRun
    HUD.ts             # in-battle HUD: floor, rosters, Lv/XP (E4.5) + DEF·MOB·AGI (GP3), banner
    statLabels.ts      # GP3: shared STAT_LABELS map (card + HUD + PromotionScreen)
  audio/
    AudioPlayer.ts     # B6: clone-ring per sound; per-key volume + pitch jitter; + magicboom (E7.C)

tests/
  smoke.test.ts                              # vitest + module resolution
  integration/determinism.test.ts            # deterministic replay (taps action:phase, F2)
  integration/snapshot-roundtrip.test.ts     # A2: World/Run JSON round-trip
  fuzz/                                      # A3: opt-in headless balance harness
    Strategy.ts                              #   pickNextNode / pickRecruit interface
    strategies/policies.ts                   # G5: node + recruit decision policies
    strategies/factory.ts                    # G5: composeStrategy(name, node, recruit)
    strategies/registry.ts                   # G5: 17-strategy menu (shared by both CLIs)
    harness.ts                               #   runOne / runMany; RunResult shape
    reporters.ts                             #   CSV summary + per-floor (G4) + markdown traces
    cli.ts                                   #   `npm run fuzz` entry point
    output/                                  #   gitignored -- regenerated each fuzz run

retro/
  scratchpad.md           # rolling scratchpad of process notes / gotchas
  post-mvp-review.md      # CHECKPOINT 7 retrospective written after MVP shipped

config/                              # A4: balance JSON source-of-truth (zod-checked by src/config/)
  archetypes.json                    # glyph + baseStats + growthRates per archetype (E1/E3)
  abilities.json                     # per-ability cooldownSeconds/range/aoe/travel/retreatDelay (E2+)
  difficulty.json                    # G4: enemy level-budget knobs + A/B/C presets
  recruitment.json                   # starting team + offer size + startingLevel + recruitBonusChance (G4)
  leveling.json                      # E4: xp curve + half-cover mult + restXp (G3) + xpPerHealing (F6)
  health.json                        # H4: player/enemy health pools + maxTurns/maxTurnSeconds + chipMultiplier
  deck.json                          # H5: handSize (card-drawn hand; 2nd half of the playerTeamLevel seam)
  nodemap.json                       # floor count + width bands + degree cap + rest knobs (G2/G3)
  stats.json                         # hpPerConstitution, crit, base move CD, per-axis mobility/agility knobs (GP1)
  sim.json                           # E5: retarget + occupiedCellPenalty + healer knobs
  terrain.json                       # C1a: wall + water density
  layouts.json                       # C1d.A: hand-authored layouts (walls/water/halfCovers/chasms/fires/healings/spawns/theme)
  tiles.json                         # D7.B: fire/healing chip rates -> tick cadences
  spawn.json                         # D5.C: SpawnAction lockout duration

public/audio/                        # B6: preloaded .wav files (+ magicboom, E7.C)

tools/                                       # dev-only, never in dist/
  layout-editor/                             # C1d.B: 12x12 grid painter (/tools/layout-editor/)
  run-config/                                # G1/G5: short-run CLI + GUI launcher (/tools/run-config/)
```

Co-located `*.test.ts` next to source for unit tests. Integration tests under `tests/`.

## Pre-flight (run when picking up a session)

```bash
git log --oneline -5    # confirm latest commit
npm test                # 463 passed, 0 todo
npm run typecheck       # tsc --noEmit; should be clean (E3.5 added this script)
npm run fuzz:smoke      # 7 passed — confirms the harness still runs
npm run dev             # opens at :5173 — verify the full run flow plays
```

## Pre-commit checklist

Before every commit (especially the AI-driven ones — `npm test` doesn't
catch type errors that vitest happens to tolerate):

```bash
npm test                # 0 failures, 0 it.todo() regressions
npm run typecheck       # tsc --noEmit clean
# only if touching sim/run/core logic that could affect long-run behavior:
npm run fuzz:smoke      # 7 passed
```

`npm test` and `npm run typecheck` are non-overlapping: vitest runs
the test files through esbuild's loose-mode transformer, which accepts
some constructs strict tsc rejects (readonly mutation, unused locals,
exact-optional-property mismatches). Catching those before the commit
lands keeps `npm run build` from breaking on push.

In the browser you should see: dark terrain (smooth blue→green→amber gradient) with 4px-thick scanlines, neon-glowing sprites (green allies + red enemies bloom equally on attack), map screen on load (right panel), click a frontier → battle plays out with in-battle HUD on the left → recruit modal (3 cards, at least one M + one a) → click a card → map screen at new node with visited trail. Win 4 in a row → green "Run Complete" screen. Lose → red "Defeat" screen. Button on either resets to a fresh map. All screen transitions fade over 180ms.

C1a adds: per-encounter terrain with gray `#` walls scattered on the arena (~9 per battle), shallow_water tiles rendered as flat blue patches (~6 per battle, units move through them at half speed). Walls + water never overlap the spawn rows; the arena always has a path between teams.

C1b adds: about half the battles use a hand-authored layout (corridor with two horizontal wall bands, or diamond with a center block) instead of scattered procedural walls; ranged units stop firing when a wall lands on the line between them and their target (you'll see them path-step around the wall rather than shoot through it).

C1c adds: the arena is now a faceted low-poly tile mesh — each cell is its own short prism with a hard-edged top facing up, height varies subtly per tile, water tiles drop into a visible sunken pool, grid lines bake into the top face of every prism. Sprites and walls stand on the actual tile they occupy (no more floating plane) and Y-lerp across height seams when they move.

## Toolchain versions

- Node 25.5, npm 11.8
- TypeScript 6.0.3, Vite 8.0.13
- three.js 0.184.0, simplex-noise 4.0.3, @fontsource/jetbrains-mono 5.2.8
- vitest 4.1.6, ESLint 10.4.0, typescript-eslint 8.59.3, prettier 3.8.3

## Existing memories

- `project_asciibattler.md` — points here. Update at major phase boundaries.
- `feedback_context_estimates.md` — don't fabricate context-window %s; use qualitative terms or trust the user's actual number.

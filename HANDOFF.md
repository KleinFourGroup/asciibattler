# HANDOFF — Post-MVP pickup

A fresh-session orientation for ASCIIbattler. Read this first; then dive into the docs only where they're called out.

## 🧭 Cursor

**The ONE home for live status.** Other docs (and the agent memory) point here instead of restating it; when state changes, this section is the single place that updates.

| | |
|---|---|
| **NEXT** | **ROUND 6 (INSTRUMENTS) — SPEC LOCKED 2026-08-22** ([round-6-spec.md](round-6-spec.md) §Kickoff resolutions, user-signed; findings WORKLOG §Kickoff; plan [ROADMAP.md](ROADMAP.md) §84–88). The road to ship is [META-ROADMAP.md](META-ROADMAP.md) v2 (seven rounds 6→12, locked 2026-08-21; Cluster 5 ✅ condensed in Closed rounds below). **Round 6 as locked: §84 the long-horizon SHADOW instrument** (the §71c `shadowTier` generalized to a run-end horizon on the acquisition sites + a shadow-only recruit site, sampled 1-in-m, own arm; the `--grant` bridge on ~3 items; the per-remaining-hop prior table committed with provenance) → **§85 the FOLD** (`scoreTerminal` + `priorBonus = λ_prior × Σ table × hopsRemaining` over the holdings delta — daemons · packets · units; λ_prior a board arm {0, 0.5, 1}; the ε re-read + an event-page context; riders: campRaid as a RUN-LAYER preTurn site · the 55pre `--search` re-derive) → **§86 perf** (profile-first, byte-identity oracle, expectation = tick-level wins or a documented no-op) → **§87 roster realism** (`playerArchetypes` capture + `--roster=sampled:<hop>` whole rows → the X3 re-read) → **§88 the rarity read** (§84's unit rows; targeted `--grant` for thin rows) + the close + the ONE amendment. ⭐ The audit's catches: decisions.csv is the convergence MONITOR, not the prior's source (within-horizon, double-counts); the battle evaluator can't see a camp raid's payout (two evaluators, one fold). **NEXT ACT = the §84 kickoff** (code-reality audit of `driver.ts`/`walker.ts`/`reporters.ts` + the recruit seam → the sub-step cut → shape-lock). Predictions: World v35 / Run v44 hold all round. Standing tools: `--set=group.key=value` (flat numeric registry) + `--event-chance=0` + the queue-file box driver (one HEAD per cohort). **Hook: docs/UI ~45s, sim-touching ~7 min.** |
| **Last phase** | **§84 (the long-horizon shadow instrument) ✅ CLOSED 2026-08-24, user-signed** — the run-end shadow + the **v1 prior table** committed (17 units signable, 9+9 directional); **§85-pre rode along** (15 findings, F1–F5 fix-first; the F3A wall-refs PENDING closed at the 85f re-pin). Detail: WORKLOG §84*/§85-pre · BALANCE 2026-08-23/24. Before it: **§83 ✅ 2026-08-21** (the signed sheet ⭑ + Closed rounds below; the `archive/post-72-*` pair). |
| **Before it** | **§82** (the reward & weapon feel round — single-type tables, daemons@1 on elite/boss, `rewardOverride` ref list **Run v42→v43**, the catapult RELEASE GATE) · **§81** (procedural parity: per-theme tiles + camps, ⭐ dose-independence) · **§80** (the five C6 planning docs; ⚠ user review OPEN) · **§79** (glyph anchor rework, drift 9.1px→0; self-hosted JBM ⚠ live at next deploy) · **§78** (UI/UX batch — ⭐ DESIGN §Input accessibility) · **§77** (braid gen rework — **Run v41→v42**, gotcha #125) · **§76** (Unit mechanics — gotcha #124) · **§75** (Camps — **v34→v35**, gotchas #122–123) · **§74** (Events — **v40→v41**) · **§73** (hook speedup). Per-phase detail: the `archive/post-72-*` pair + git. |
| **In flight** | **§85 — the fold + the ε re-read + the two riders: 85a–85f + 85h ✅ LANDED 2026-08-25, all user-signed. 85g5 ✅ RAN 2026-08-26 (overnight + 4-arm arbitrated selection, box-drive maiden clean): **argmax = finalist-56 18/30** vs 16/15/14 — refined winner THIRD (the hybrid-light inversion); fixtures `tests/fuzz/fixtures/85g5-*.json`; BALANCE 2026-08-26 + WORKLOG §85g5. DEPLOY SIGNED 2026-08-26 (finalist-56, argmax; the K=4/n=30/λ=0 calls ratified). NEXT ACT = the 85g5 close-out (board re-anchor: 55pre → the deployed vector, `pre55ReachRef` retires, full board re-run) → the 85g6 λ-cohort staging**: the §59f recipe on the TRAIN bank (`--search --refine --searcher --audition --preset=heavy --vectors=96 --seeds=32 --sampler-seed=85 --seed-offset=1000 --jobs=8`, ~9 h — the 8.85 h §59f anchor) → the 3 finalists' ARBITRATED full-length selection batches (argmax deploys — the signed HYBRID-LIGHT staging) → 85g6 the λ cohort on the CHOOSE bank (2001+) + the campRaid causal arm → the one-use SIGN bank (3001+) → λ signs, `pre55ReachRef` retires, the 0.438 wall re-reads. **85g1–85g4 ✅ LANDED 2026-08-25/26, user-signed** (detail WORKLOG §85g-kickoff/§85g1–4): 85g1 the candidate-delta de-fold (per-site `priorItemKeys` on the walk-terminal diff — ⚠ NOT apply-time, the reward null arm accepts IN-WALK; campRaid = the explicit `'all'` by 85d design) · 85g2 prior v2 (load-time shrinkage SHRINK_K=80 · machine provenance · ⭐ TABLE v2 at `measured @fbcb363`: 17 units signable, miner ALIVE −40.65 [v1's structural zero broken], rioter sign-flip — BALANCE 2026-08-26) · 85g3 the `--arbitrate`+`--search` compat (the generic `wrapStrategy` seam; ⭐ the relocation ORACLE: summary+decisions sha-IDENTICAL with the fold engaged; ONE resolver `arbitratedWrapFromArgs` across run/serial/shards; instruments REFUSED with --search) · 85g4 HYBRID-LIGHT signed (~10 h total at the §59f anchor; full-arb ≈ 40 h+ waits on §86). **85h ✅** all twelve amendments as proposed (WORKLOG §85h = the canonical record; λ rerun MANDATORY · λ=0.5 PRE-REGISTERED · 55pre regenerates · board split + perf riders → §86 · ⭐ fold-makes-arb-pay STANDING · gotchas #127/#128 + the reader-class guards). **85f ✅** 30 arms at two HEADs, pooling PROVEN; ⭐ 55pre λ=0.5 paired Δwin +0.142 p=0.008 EXPLORATORY; wall refs RE-PINNED 0.775/0.675 (n=120, sheet amended); campRaid alive+selective; gotcha #126 (WORKLOG §85f · BALANCE 2026-08-25). Then the phase close after 85g. **85a–85d ✅** the table fix · the walk-fidelity batch · the FOLD (`--prior-lambda`, λ=0 byte-identical) · the campRaid preTurn site (WORKLOG §85a–d + git `51e026f`…`5d0c846`). **Standing:** World v35 / Run v44 HOLD (predicted+observed) · λ_prior≠0 loads `tests/fuzz/board/prior-table.json` at launch (missing = loud throw) · the doctrine arm still runs λ_prior=0 until the post-85g-rerun signing (λ=0.5 pre-registered) · ⚠ the wall-refs WARN pair expected until the 85f board · ⚠ the csv `decision` index counts shadow records. Ops: fetches land in `output/box-batches/<id>/`; the sanctioned overnight driver is `scripts/box-drive.sh` (✅ promoted 2026-08-26 — AGENTS §Box-driver ops). Probe cost facts: WORKLOG §84d. |
| **Branch / tree** | `main`, working tree clean at the 85f close commit; zero boxes billing (7 created + 7 destroyed through 2026-08-25 — the 85f box destroyed by driver v2's artifact-verified stand-down at 14:03:54Z) |
| **Snapshots** | WorldSnapshot **v35** (75b; the 77d3 + §82 + §83b no-bump predictions all HELD) · RunSnapshot **v44** (83d — `currentEncounter` gains `kind`, the pull's boss gate; the 47f reject class; before it v43, the 82c override ref list) |
| **Tests** | **2693 main (0 skipped) + 487 fuzz:smoke** green (re-pinned 2026-08-26 at the 85g2b commit — the 85h/85g1/85g2a/85g3 suites: walkDepth/sites · de-fold · shrinkage/provenance · search-compat) · typecheck clean · the pre-commit fuzz trigger watches `src/bot/`+`tests/fuzz/` since the 2026-07-21 sweep · permanent gates: `tests/nodemap-metrics.test.ts` (the signed 77c sheet at n=500 — the drift.test.ts analog, NEVER relax) · the §78d EMPOWER_DISPLAY coverage pins (statusDisplay.test.ts — a new buff key fails until it picks a color) · the §81a per-theme presence + dose-independence pins (proceduralMap/terrainGen tests, config-derived) |
| **Last closed** | **Cluster 5 — Map Content (Phases 73→83, 2026-08-05 → 2026-08-21) ✅ COMPLETE, every phase user-signed** — see Closed rounds below · before it, the rollout-arbitration interstitial (69→72, ✅ 2026-08-04), Cluster 4 (61→68, ✅ 2026-07-29) and the micro/balance-realism round (53→60, ✅ 2026-07-21) |

## Current state

- **MVP shipped** at CHECKPOINT 7 (Phases 0–5 complete; deployed to GitHub Pages, full loop playable).
- **Phases A–D complete** — the pre-combat foundation (selectors/commands/fuzz/config/scenes · palette+bloom/bars/scanlines/audio/root `@` · terrain/walls/LOS/prisms/layout-editor · renderer-cap/drag-paint/variable-sizes/fit-scroll-camera/spawn-overflow/half-cover/chasm+fire+healing/theming). Detail: [archive/phase-a-e-worklog.md](archive/phase-a-e-worklog.md).
- **Phases E · F · G · GP complete** — combat depth + run progression + the early playtest responses (the stat vocabulary/`deriveStats`/ability layer/leveling/`TICK_RATE` 20/XP-promotion/the six archetypes; the action-phase timeline + gambit/heal VFX; `RunConfig`/rest+boss nodes/level-*budget*/fuzz GUI; the `mobility`/`speed` renames + `defense` + `World.applyDamage` + healer chokepoint yield). Full per-step detail: [archive/phase-e-gp-worklog.md](archive/phase-e-gp-worklog.md).
- **Phases H · I · J · K complete** — the deckbuilder battle trial + combat-identity + objectives + the status-effect foundation: the `power` stat / health-pool encounter→turn loop + pool HUD / card-drawn hand / search-balance tooling (H); the `precision`/`evasion` dodge stats + subtractive hit/miss + per-ability weapon profiles (sword/club/katana/whip/bow) + the schema-driven archetype editor (I); the in-battle objective MODEL + the [movement.ts](src/sim/movement.ts) pathing seam + rebindable [Keybindings](src/ui/Keybindings.ts) (J); the generic `StatusEffect`/`foldEffects`/trigger system + roster/hand decouple + redraw + pre-turn empower (K). Carried World v21→v24 / Run v11→v15. Full per-step detail + the snapshot-version trail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §§I–K + [BALANCE.md](BALANCE.md).
- **Phase L complete** — L1 daemons (relics): the four-idol catalog (Mars/Minerva/Mercury/Janus) + DAEMON-ONLY gates + a run-start roll + `chance` as a first-class gate + the PreTurnScreen surface + fuzz `--daemon`; **Run v15→v16** (Minerva +4→+2 nerf; `battleTrigger` daemons + multi-daemon economy deferred to the shop round). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase L + [BALANCE.md](BALANCE.md) §L1c3.
- **Phase M complete** — progression & presentation (M1–M6): the per-turn promotion cadence (**Run v16→v17**, band-neutral), the staggered level-up screen + LOCKED curve `50/1.1`, the battle backdrop, layout-editor save-to-config, + the water mechanic + procedural-map rework (`?layout=procedural`). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase M.
- **Phase N complete (bar N4)** — rogue mobility & balance closure: N1 the rogue DASH (the ability `kind` union + an aggressive-close `DashAbility` on J2's movement-intent seam + the `unit:dashed` event), N2 the band re-sweep → **LOCKED `1.25 × 1.5`** (a mercenary+ranged duopoly), N3 the leveling check (GREEN at `50/1.1`), + the turn-cap unification (`resolveAsDraw`-at-cap across harness/arena/live). **N4 (overnight out-of-sample verify) DEFERRED INDEFINITELY to a VPS** (the dwm leak). Detail: [archive/post-h-roadmap.md](archive/post-h-roadmap.md) §Phase N + [BALANCE.md](BALANCE.md).
- **Phase O complete** — the Post-N agency round's typed objective refactor (O1–O5). O1 (always-an-objective TYPED per-team model `atWill`/`engage`/`hold`, enemy inert-but-symmetric; **WorldSnapshot v24→v25**), O2 (`hold`), O3 (`focus` + 3 switchable tile-resolutions), O4 (ranged `minRange` kiting — bow 2 / mage 2 / catapult 4), O5 (dev-only `--objective=coverage` churn bot, kept separate from the measurement proclivity). Band **re-confirmed `1.25 × 1.5`** (no retune). Detail: git log + [BALANCE.md](BALANCE.md) §O5.
- **Phase P complete** — the shared `UnitCard` component ([src/ui/UnitCard.ts](src/ui/UnitCard.ts)). P1 (extract + recruit/promotion parity + the rarity-accent seam), P2 (full-variant XP-to-next bar, config-derived `xpProgress`), P3 (PreTurnScreen adopts the full card; the screen scrolls for a tall hand), + the POW-display rework (its own accented meta row). **UI-only — no snapshot/fuzz change** (WorldSnapshot v25 / RunSnapshot v17 hold). Detail: git log + [archive/post-n-roadmap.md](archive/post-n-roadmap.md) §Phase P.
- **Phase Q COMPLETE (Q1–Q6)** — the in-battle HUD overhaul + pacing: the `PlaybackSpeed` set-by-value model (0.5×–3× + pause), the pre-battle countdown (replacing the spawn-anim), the four HUD panes (speed TR / objective BR / player-cards BC / enemy-cards top, on P's `compact`/`full` UnitCard + a `team`-tinted enemy mirror), and dismantling the old monolithic HUD (net −325 lines). **Presentation/UI/input — no snapshot/fuzz change** (v25/v17 hold). Detail: git log + [archive/post-n-roadmap.md](archive/post-n-roadmap.md) §Phase Q.
- **Phase R COMPLETE (R1–R3)** — auxiliary card surfaces + cleanup; closes the Post-N O→R round. R1 (the shared roster-view modal), R2 (the pre-turn draw-pile + discard-pile views on a shared `CardListModal`), R3 (the procedural-map label unify + cleanup chores: `.gitattributes`, inline-SVG favicon, three.js vendor-chunk split). **UI-only — no snapshot/fuzz change** (v25/v17 hold). User-confirmed in the native browser. Detail: git log + [archive/post-n-roadmap.md](archive/post-n-roadmap.md) §Phase R; deferred bugs in [TODO.md](TODO.md) §Phase Qb.
- **Phase Qb COMPLETE (#2 + #3)** — the two deferred bug investigations (the billboard depth-sort; the corridor kite-pin same-cell overlap), both user-confirmed in the native browser. No snapshot/fuzz change (v25/v17 hold). Detail: git (`0d9413e`, `b6da01f`) + [TODO.md](TODO.md) §Phase Qb.
- **Phase S COMPLETE (S1 + S2)** — encounter-system groundwork: **S1** (`deceb6a`) the **Floor→Hop** rename (node-map progression concept + dev surface; **RunSnapshot v17→v18**; battlefield-floor tile / `Math.floor` left alone); **S2** (`f25fc8f` + glyph follow-up `e927308`) the **selectable root** via a pre-root sentinel (`PRE_ROOT_NODE_ID = -1`, [NodeMap.ts](src/run/NodeMap.ts); **v18→v19**; adds one battle/run). **GOTCHAS #110** — don't "fix" `currentNodeId = -1` back to `rootId` (`currentHop`/`hopOf` throw at pre-root by design). Both user-confirmed. Detail: git log + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §Phase S.
- **Phase T COMPLETE (T1–T3 + follow-ups)** — the run-container round: **T1** (`e3a03eb`) the **Sector schema** ([sectors.ts](src/config/sectors.ts) + `config/sectors.json`, zod — hop-gated layout pool; procedural = reserved sentinel literal-matching `RunConfig.FORCE_PROCEDURAL`); **T2** (`6295d66` + `33b4fd5` weight-seam + `eb9b2ab` banner) **the run = a sequence of sectors** (`sector-map.json` DAG + [sectorWalk.ts](src/run/sectorWalk.ts); `Run` → a current-sector model, **RunSnapshot v19→v20**; weighted board roll; player pool + roster carry across; ships one sector **"The Start"**, source == sink; **GOTCHAS #111** — `pickOne` zero-draws a singleton); **T3** (`cf773ea` + `46207e6` + `84bf7ea`) the dev tooling (sector editor + `/tools/` index + the layout-editor "add to sector" toggle). Detail: git log + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §Phase T + [TODO.md](TODO.md).
- **Phase U COMPLETE (U1–U3b)** — the keystone: `rollEnemyWave` RETIRED from `Run`, replaced by an authored **Encounter** model. **U1** (`89957c0`) the pure wave resolver [wave.ts](src/run/encounters/wave.ts) (`resolveWave` — budget/count/weight; `distributeWeightedLevels`; per-instance level weighting [USER-LOCKED]); **U2** (`010d35e`) the wave-list grammar + sequencer [sequencer.ts](src/run/encounters/sequencer.ts) (`waveForTurn` — wave | weighted-pick | loop{N|forever} | condition-gated **stages**; nests to any depth; resumable cursor; terminal = last-wave-repeats [USER-LOCKED]); **U3a** (`2b4f733`) the Encounter schema [encounters.ts](src/config/encounters.ts) (`kind` enum normal|elite|boss + `rewards?` seam); **U3b** (`252b7ea`) the Run wiring (`beginTurn` resolves via `waveForTurn`→`resolveWave`; **RunSnapshot v20→v21**; per-encounter `enemyHealthPoolMax`; encounter name → HUD). `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow still call them). User-confirmed. Detail: git log + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §Phase U.
- **Phase V COMPLETE (V0–V2c).** The `sector-owns-both` flip (`9b8d818`); the keyed `selectEncounter` resolver + `config/selection.json` (`498745d`+`c647ebf`, **RunSnapshot v21→v22**); the **encounter editor** + visual wave-grammar builder + byte-faithful `formatEncountersJson` (`f92a63a`+`18a59d5`); placement on both editors via `addEncounterToSectorPools` (`8ddbdc7`); the 6-encounter catalog in "The Start" (`c932593`); editor tab-restore (`e061c8e`). No snapshot bump; user-confirmed. Detail: git + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §"Phase V".
- **Phase W COMPLETE (W1 boss + W2 elites; scope EXPANDED; both user-confirmed).** **W1** (`5046960`) **"The Bandit King"** (`kind:'boss'`/pool 20) — the first to exercise the condition-gated **`stages`** grammar (3-phase escalation at pool ≤0.66/≤0.33). **W2** (`d08a696`) **elite map-nodes** (pulled forward): a new `'elite'` `NodeKind` + a `NodeMap.generate` scatter pass (structure+rest byte-identical; `eliteChance 0.25`/`eliteMinSpacing 2`; always a non-elite sibling = optional detour); `*` glyph NEON_PURPLE; two elites (pool 12/budget 1.35×); no Run dispatch change (reuses the battle path); **RunSnapshot v22→v23**; fuzz `PATH_KINDS += 'elite'`. Boss/elite numbers UNTUNED (X targets). Detail: git + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §"Phase W".
- **Phase Wb COMPLETE (QoL interstitial; Wb1–Wb4; pre-X polish).** The pre-turn encounter NAME (`turn:starting` gains `encounter:{name,kind}`, elite/boss badged; `9ea338e`); two editor fixes (`427d57d`, `51fd160`); and the sector fight pool split BY KIND `encounters:{normal,elite,boss}` (exhaustive `Record<EncounterKind,…>` + a kind-consistency guard + a kinded `encounterPoolAtHop`; both editors reworked; `c99ab3f`). No snapshot bump. Detail: git + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §"Phase Wb".
- **Phase X COMPLETE (X1 + X2 + X3) — the band RE-DERIVATION** (closes the S→X encounter round). `levelCap` (per-wave, opt-in) + **X1** per-run difficulty multipliers (`waveSizeMultiplier`/`levelBudgetMultiplier`; `difficulty.json` default + `RunConfig` seam) + **X2** the per-encounter pool-damage harness (`--per-encounter` / `--encounter=<id>` / `--seed-offset`; dev-only + a one-line `RunConfig.forcedEncounterId`) + **X3** the 5-step sweep: per-kind bands **normal≈3 / elite≈6 / boss≈10** pool-damage/instance, **win rate 0%→~37% optimal, +24pt gradient**, bands holding out-of-sample. Config + tests only — **no snapshot bump**. Detail: [BALANCE.md](BALANCE.md) run log + [archive/post-r-roadmap.md](archive/post-r-roadmap.md) §X + git.
- **Cluster 1 (Combat Depth) — Phases Y→34 COMPLETE & user-confirmed.** Full as-built archived → [archive/post-x-roadmap.md](archive/post-x-roadmap.md); the load-bearing facts are condensed in **Closed rounds** below. Per-phase detail in git + [BALANCE.md](BALANCE.md) + the project memory.
- **Cluster 2 (Spatial & Movement) — Phases 35→41 COMPLETE & user-confirmed.** Full as-built archived → [archive/post-34-roadmap.md](archive/post-34-roadmap.md); the load-bearing facts are condensed in **Closed rounds** below. Per-phase detail in git + [BALANCE.md](BALANCE.md) + the project memory.
- **The Pathfinding Audit round (Phases 42→46) COMPLETE & user-confirmed (2026-07-06).** Run-log in [PATHING.md](PATHING.md); condensed in **Closed rounds** below; archives → `archive/post-41-roadmap.md` at the Cluster-3 kickoff.
- **Cluster 3 (Economy) — Phases 47→52 COMPLETE & user-signed-off (2026-07-11).** Condensed in **Closed rounds** below; per-phase detail in [archive/post-46-roadmap.md](archive/post-46-roadmap.md) + [archive/post-46-worklog.md](archive/post-46-worklog.md) + git + [BALANCE.md](BALANCE.md).
- **The micro/balance-realism round (Phases 53→60) + Cluster 4 (Drafting & Identity, Phases 61→68) COMPLETE & user-signed (2026-07-21 / 2026-07-29).** Both condensed in **Closed rounds** below; per-phase detail in [archive/post-52-roadmap.md](archive/post-52-roadmap.md)+[-worklog](archive/post-52-worklog.md) and [archive/post-60-roadmap.md](archive/post-60-roadmap.md)+[-worklog](archive/post-60-worklog.md) + git + [BALANCE.md](BALANCE.md).
- **The rollout-arbitration interstitial (Phases 69→72) COMPLETE & user-signed (2026-08-04).** The arbitrated default shipped; condensed in **Closed rounds** below; per-phase detail in [archive/post-68-roadmap.md](archive/post-68-roadmap.md)+[-worklog](archive/post-68-worklog.md) + git + [BALANCE.md](BALANCE.md) §§69–72f.
- **Cluster 5 (Map Content) — Phases 73→83 COMPLETE & user-signed (2026-08-21).** Events · camps · unit mechanics · the braid map · the UI/UX batch · the glyph fix · the C6 planning docs · procedural parity · the feel round · the closing rebalance; condensed in **Closed rounds** below; per-phase detail in [archive/post-72-roadmap.md](archive/post-72-roadmap.md) + [archive/post-72-worklog.md](archive/post-72-worklog.md) + [archive/cluster-5-spec.md](archive/cluster-5-spec.md) + git + [BALANCE.md](BALANCE.md) 2026-08-05 → 08-21.
- **Tests:** live counts are pinned in the 🧭 Cursor (top of this file); 0 `it.todo()`. One known residual — `recruit:healer` has a single spiralFireLife hang (a damage-starved all-healer team, out of GP5 scope). Fuzz sweep `npm run fuzz` (20 seeds default; `--per-hop` for per-hop analysis; `--layout=procedural` to isolate the M6 maps); the `scored` strategy + `--search` (→ `output/best-strategy.json`) drive the balance tuning. Short / forced / leveled runs: `npm run run-config -- --help` or the GUI at `/tools/run-config/`. **READ [BALANCE.md](BALANCE.md) FIRST for any balance work; [PATHING.md](PATHING.md) for movement-quality work (regenerate its tables with `npm run pathing`).** The per-phase test-count history lives in git log + [archive/post-h-roadmap.md](archive/post-h-roadmap.md).
- **Dev server:** Start with `npm run dev` → http://localhost:5173/ (port 5174 if 5173 is held by a stale process — check with `Get-NetTCPConnection -LocalPort 5173`; Vite spawns child Node processes that survive `taskkill` on the parent). **The user usually has their own `npm run dev` on 5173 during a session**, and the preview MCP refuses to attach to a server it didn't start ("not a preview server"). For agent browser-verify, use the **`dev-preview` launch config** ([.claude/launch.json](.claude/launch.json)) — `npm run dev -- --port 5191 --strictPort` binds a dedicated port that never collides with 5173, so `preview_start name=dev-preview` always works. **Dev handle:** `window.__game` (DEV-only) exposes `bus`/`renderer`/`run`/`activeScene`/`keybindings`/`sprites`; drive a battle headlessly with `__game.dispatch({kind:'enterNode',nodeId})` then `{kind:'advanceTurn'}` (past the PreTurnScene gate), and freeze the auto-advancing run with `scene.clock.advance = () => {}` for a stable screenshot.
- **Build:** `npm run build`. `vite.config.ts` uses `base: './'` so the same `dist/` works at any subpath.
- **Canonical references:** [ARCHITECTURE.md](ARCHITECTURE.md) for the project tree + event/command catalogs; [ROADMAP.md](ROADMAP.md) for forward-looking steps; [GOTCHAS.md](GOTCHAS.md) for the hard-won fixes that look weird without context — don't refactor those without understanding why.

## What's next

**The road to ship is [META-ROADMAP.md](META-ROADMAP.md) v2 (locked 2026-08-21) — seven ordered rounds, 6 → 12.** Clusters 1–5 (the v1 plan, archived at [archive/post-x-meta-roadmap.md](archive/post-x-meta-roadmap.md)) are complete & user-signed (see Closed rounds below); **the live NEXT is pinned in the 🧭 Cursor at the top of this file.** Smaller follow-ups live in [TODO.md](TODO.md); each round's charter, dependencies, decision points, and scope guards live in META-ROADMAP, and its Coverage map says where every carried item landed.

Balance numbers live in `config/*.json` (zod-validated by `src/config/*.ts`) -- edit the JSON and refresh for a Vite hot-reload, no recompile.

## Closed rounds — condensed history + the still-load-bearing facts

> **🗄️ Cluster 5 — Map Content (Phases 73→83, 2026-08-05 → 2026-08-21): ✅ COMPLETE, every phase user-signed.** The content cluster end to end: §73 the hook speedup (`isolate:false`, docs ~45s) · **§74 EVENTS** (page-map grammar → Run phase [**v40→v41**, `eventRng`] → executor → dense placement + every run opens on the starting boon → EventScene → the `eventChoice` arbitration site → the editor → ten user-authored events + prodigy; per-run no-repeat) · **§75 CAMPS** (the neutral third faction: registry [**World v34→v35**] · portal drip · the ~40-site active-neutral widening · leash wander · damage-sourced hostility + kill credit · rewards · TERMINAL_AMBER · both editors · the per-turn enemy PULL; gotchas #122–123) · §76 unit mechanics (gotcha #124) · **§77 the BRAID map** (**v41→v42**; keyed `deriveRng` streams — gotcha #125; the n=500 executable sheet `nodemap-metrics.test.ts`, NEVER relax) · §78 UI/UX (⭐ DESIGN §Input accessibility) · §79 the glyph anchor fix (drift 9.1px→0; self-hosted JBM) · §80 the five C6 `plans/` docs (⭐ ONE store/FOUR consumers) · §81 procedural parity (per-theme tiles + camps; ⭐ dose-independence) · §82 the feel round (single-type reward tables, daemons@1, `rewardOverride` ref list **v42→v43**, ⭐ the catapult RELEASE GATE) · **§83 the closing rebalance** (⭐ `statusImmunities` + healer panic immunity — parity regen INSIDE ±5 · the camp pull's BOSS EXEMPTION **v43→v44** · dose ×1.075 · the §82 flood ACCEPTED · ladder TABULAR · ⭐ the camps probe `sim.campsStartHostile` [forced engagement decisively net-negative; documented] · ⭐ `pre55ReachRef` · the closing board 0 FAIL / 6 WARN at the ⭑ 2026-08-21 sheet). **Load-bearing:** ⭐⭐ the signed sheet now reads §60e→§68d/f→§72b/f→**§83f** (seam 15–18 · wall 30–35 held [floor-hugging 0.265/0.292 ACCEPTED] · reach 40–50 regen / 0.542 ref 55pre · win DERIVED · act-1 refs at n=120 · bank ~111/~90 · fires ~1.7 · fire channel +0.10; parity: the 55pre-shape breaches ride the vector re-derive) · ⭐ the n=120 protocol (decision-feeding arms extend via `--seed-offset=40 --count=80`, SAME HEAD only) · the queue-file box driver + ONE HEAD per cohort · the standing probe tools (`--set` flat-numeric registry + `--event-chance=0`) · ⭐ the two fold riders (campRaid nominator · 55pre re-derive) and the ε-floor re-read wait for the interstitial's fold · the horizon-blindness doctrine now has its sharpest exhibit (K=2 can't tell a daemon from 25 bits). Archives (landed 2026-08-21): [post-72-roadmap](archive/post-72-roadmap.md) + [post-72-worklog](archive/post-72-worklog.md) + [cluster-5-spec](archive/cluster-5-spec.md); the sweep: [retro-scratchpad-cluster-5](archive/retro-scratchpad-cluster-5.md); numbers BALANCE 2026-08-05 → 08-21.
>
> **🗄️ The rollout-arbitration interstitial (Phases 69→72, 2026-07-29 → 2026-08-04): ✅ COMPLETE, all four user-signed.** §69 the arbitration substrate (clone seam · walker · five-site cost model · resolution-4 evaluator · the amended A/A ε methodology) · §70 ALL FIVE DECISION SITES live (`--arbitrate`; ε floors pooled-per-class flat) · §71 decision-grade telemetry end to end (the decisions.csv 18-col sidecar · per-item aggregates + the n=80 floor · the flip instrument · ⭐⭐ THE PARITY TABLE: the arb arm inside paired noise of doctrine — the "crater" was baseline-anchoring · the cheap inner tier validated) · §72 the balance agenda (72a the board twins + run-alongside cycle · 72b ⭐⭐ the wall correction [gotcha #120] + the unified band architecture [the run = a pool-HP budget flow; win DERIVED reach×(1−wall)] · 72c the 2× value buffs [the channels convert] · 72d the cheap tier holds · 72d2 the act-1 ×1.15 one-round hit · 72e draw DISSOLVED under the cap [A/A sha-identical] + elites negative-EV at decision grade + packet cuts · **72f THE SIGNING SESSION: ⭐⭐ the ARBITRATED DEFAULT** [ARM gains `--arbitrate`; the board = 10 arb primaries + 5 doctrine controls; the searcher-tier direct test paired-Δ EXACTLY 0.000 — the cheap tier locked by test, not inference] · the deep-end boss pool split at the ×1.25 dose [walls 0.313/0.348 in the held 30–35; ×1.5 craters the finale — the two-dose bracket caught it] · seam re-signed 15–18 at arb reality · the fire channel re-signed +0.10 [the 72c buffs repaired it; ≈0-by-substitution on the arb arm] · maxHandSize 10 + Surge-3 · closing board GREEN, 0 FAIL / 3 pre-registered WARNs). **Load-bearing:** ⭐⭐ the DEFAULT realistic-bot arm = **`--searcher --audition --redraw=level:2 --empower=level:hi --arbitrate`** (board.ts ARM is the canon) · decisions.csv rides every default-arm batch (per-item decision value is a standing instrument) · the horizon-blindness doctrine (a knob must land INSIDE the rollout horizon to move a decision-grade needle; run-long passives judge at run grade — BALANCE caveat) · probe-shape win rates are SHAPE artifacts · the dose-bracket protocol (unmeasured elasticity → two-point bracket before a magnitude signs) · the §72b/§72f signed sheet supersedes §68d/f (seam 15–18 · reach 40–50 · wall 30–35 · win derived) · gotchas #120 (sector-aware hop reads) + #121 (the sectorAdvanceConfig slice). Archives: [post-68-roadmap](archive/post-68-roadmap.md) + [post-68-worklog](archive/post-68-worklog.md) + [rollout-arbitration-spec](archive/rollout-arbitration-spec.md) + [retro-scratchpad-rollout-arbitration](archive/retro-scratchpad-rollout-arbitration.md); numbers in BALANCE §§69–72f.
>
> **🗄️ Cluster 4 — Drafting & Identity (Phases 61→68, 2026-07-21 → 2026-07-29): ✅ COMPLETE, all eight user-signed.** The C4 chain + the absorbed riders, end to end: §61 rarity (four tiers legendary-top, the 2-draw weighted sampler, the USER-SIGNED 5/3/3/2 assignment, background-tint accents, the 1/1.5/2/3 price seam via `unitPriceFor`, `ranged`→`archer`) · §62 the hcloud box launcher (ON-DEMAND; box-batch.sh the only driver) · §63 the three starting characters (Soldier/Priest/Gambler; **Run v37→v38**) · §64 the three drafting daemons · §65 the draw/packet dials (`--draw-add`; the +2-vs-+4 non-monotonicity flagged → the interstitial) · §66 boss forewarning (**v38→v39**, pre-rolled identity+layout) · §67 the second sector **"The Deep End"** (**v39→v40**, the `sectorCleared` gate; world identity LOCKED: "dark fantasy on a haunted terminal") · **§68 protocol v2 + the balance pass** (the executable board vs signed-sheet.json · ⭐ the §68d/f SIGNED SHEET supersedes §60e · ⭐ character parity ±5 ACHIEVED — a signed design principle, defect repaired live via four buffs · the two-act cliff decomposed into two defective entries → minHop gates 6/3 + trims · prices dispositioned · the fire channel re-signed ≈0, repair → the interstitial · the instrument registry stamps the kit). **Load-bearing:** per-batch character isolation, Soldier = the continuity anchor · the consumption contract + the new-mechanic shipping checklist · `--grant` paired same-seed = realized value, price against it · the n=80 floor for per-item signals · elite reads are censoring-aware (the arrival-pin caveat) · the walk-wall/two-act re-read WAITS for the interstitial's ceiling move (the board's four two-act WARNs = the deliberate marker). Archives: [post-60-roadmap](archive/post-60-roadmap.md) + [post-60-worklog](archive/post-60-worklog.md) + [cluster-4-spec](archive/cluster-4-spec.md); numbers in BALANCE (Protocol v2 + the dated 2026-07-27→29 run-log).
>
> **🗄️ The micro/balance-realism round (Phases 53→60, 2026-07-11 → 2026-07-21): ✅ COMPLETE, all eight user-signed.** The interstitial between Clusters 3 and 4 that closed the §52 calibration gap: §53 the trace recorder + human gauntlet (the ~80% human baseline, §53g) · §54 traffic scripts + the spot-check board · §55 the cell-Goodhart doctrine (probes arbitrate, cells attribute — now a BALANCE caveat) · §56 the generalized swap engine · **§57 the AUDITION SEARCHER** (candidate supply, not arbitration, was the bottleneck; K=2 locked; bot ~60 vs human ~80 — the gap HALVED; VERDICT YES ×3, pre-registered) · §58 the earned no-op (searcher-ate-it) · §59 the economy strategy layer (WASH — expressive but outcome-neutral) · **§60 the REAL balance pass**: fire-ablation (fires real, +5/−0) · the port-toll ladder (the toll IS the policy) · ⭐⭐ the grant-consumer catch → **the EXTENDED realistic-bot arm** (`--searcher --audition --redraw=level:2 --empower=level:hi`, doctrine-grade) · daemon reprice + drop probe · the per-boss split (the King 33.3% IS the wall; the Queen reads softer) · ⭐ **the SIGNED RE-ANCHOR SHEET** (bot 60–67/62 held-out · wall band 30–35 · channels +5/+5 non-stacking · bank idle-high accepted; BALANCE §60e). Held-out verify: both instrument vectors at exactly 62.5%. **Load-bearing:** the extended arm is the default for every skill-anchored read · the §60e sheet = the design anchors until §68 re-anchors · force-isolate rolled encounters before tuning · price against REALIZED value · two mid-round crash recoveries cost zero work (determinism + commit discipline). Archives: [post-52-roadmap](archive/post-52-roadmap.md) + [post-52-worklog](archive/post-52-worklog.md) + [micro-round-spec](archive/micro-round-spec.md); numbers BALANCE §§53g–60e.
>
> **🗄️ Cluster 3 — Economy (Phases 47→52, 2026-07-07 → 2026-07-11): ✅ COMPLETE & user-signed-off.** The earn-store-use-spend loop end to end: §47 the **Rule vocabulary keystone** (`Rule = modifier | hook` over run-stat folds; battleRules compile-in + serialized-tally settle — the two seam crossings; uncapped multi-daemon by id; the bits substrate) · §48 rewards (tables + the serialized reward phase + the bits overlay + `bitsMultiplier`) · §49 packets & cache (the guided fire strip, `passIsFinal` TRUE; cache size DERIVED via fold, never flagged) · §50 ports (`$` node + stock + five transactions; the `removeRosterUnit`/`appendRosterUnit` chokepoint pair — gotcha #118) · §51 cohesion (NOT a no-op; source-labeled declinable tally portions) · §52 the re-scoped docs-only close. Carried **Run v24→v37 / World v32→v34**; gotchas #116–118. **Load-bearing:** sell/refund = raw `addBits` (the mint guard vs Moneta-style `bitsGain` folds — fold at EARN sites only); injected rules carry `{rule, sourceId}` provenance; the reward phase interposes at ANY turn gate; ⚠ the §49 shrink flow has no shipped trigger content. **The §52 calibration finding: bot ~30% vs human ~80%** — economy numbers ship LAUNCH-ROUGH BY DESIGN, the whole tuning agenda + the boss-wall rider live in the micro interstitial round (BALANCE §52 + worklog §52). Riders: boss-wall RE-SCOPED there; renderer "queued"-stance stays in [TODO.md](TODO.md). Archived 2026-07-11: [post-46-roadmap](archive/post-46-roadmap.md) + [post-46-worklog](archive/post-46-worklog.md) + [cluster-3-spec](archive/cluster-3-spec.md).
>
> **🗄️ The Pathfinding Audit round (Phases 42→46, 2026-07-04 → 2026-07-06): ✅ COMPLETE & USER-CONFIRMED.** The interstitial movement-intelligence audit between Clusters 2 and 3 (the Post-N O→R precedent). All four playtest symptoms addressed (§43 bias fixes — "no drift I can ID at all" · §44 the decision protocol: [positioning.ts](src/sim/positioning.ts) + the instantaneous, deliberately-unregistered [WaitAction](src/sim/actions/WaitAction.ts) · §45 cooperation — "dithering WAY down"/👍 · §46a the verdict); **the School-2/3 gate = NO — well-tuned School 1 is enough**, WHCA\*-lite/flow-field seams documented not built; §46b balance = **ACCEPT + RE-BASELINE, no config change** (the ceiling genuinely moved — symmetric cooperation, asymmetric benefit at the boss wall). The whole round is serialized-shape-neutral (no snapshot bump). **Canonical detail: [PATHING.md](PATHING.md)** (permanent run-log — append at any future movement change) + [BALANCE.md](BALANCE.md) §46b; blow-by-blow in git + the archived round doc.
>
> **Standing movement rules (permanent):** the [drift.test.ts](tests/pathing/drift.test.ts) QUALITY GATES survive every re-baseline — **NEVER relax a gate** (drift bounds · dx sign-mixed · riverFork osc ≤ 0.5 · corridor throughput floors · river ttfc · riverFork move mass); `baseline.test.ts` exact pins re-pin on every DELIBERATE movement change; **the round's doctrine: DERIVE-DON'T-CACHE** (vacancy ETAs + route stability derived from `activeAction`/serialized state, never serialized themselves; the §45 dials live in `config/sim.json`); ⚠ gotcha #114 recurrence: `positioning.ts` takes `minRange` as a PARAM — never import archetypes.ts there; the FIXED-VECTOR probe (re-run `best-strategy.json` around engine rounds) = the cheapest ceiling-drift read; **NO RNG in movement — symmetric rules only**; ⚠️ labyrinth stays an intentional slow maze.

> **🗄️ Cluster 2 — Spatial & Movement (Phases 35→41): COMPLETE & user-confirmed.** Full as-built archived → [archive/post-34-roadmap.md](archive/post-34-roadmap.md); per-phase detail in git + [BALANCE.md](BALANCE.md). In one breath: the §35 occupancy core ([occupancy.ts](src/sim/occupancy.ts) — every spatial query routes through it; `cellsOccupiedBy` footprint + `OccupancyPlane` seams; `World.shove` the de-overlap/knockback primitive) · §36 non-instant moves (destination CLAIMED on move-start; logical position flips at the 50% mark; claims block pathing — **gotcha #113**) · §37 terrain/palettes (the `TileDef` cost+mod table, [TileGrid.ts](src/sim/TileGrid.ts)) · the §38 data-driven `UnitDef` keystone ([units.json](config/units.json), catalog reads CALL-time — **gotcha #114**) · §39 N×N footprints (`position` stays the canonical corner) · §40 destructibles (HP-presence ⇒ destructible; auto-target rubble; **WorldSnapshot v31→v32**) + auto-target-aware connectivity ([layoutConnectivity.ts](src/sim/layoutConnectivity.ts)) · §41 the closing balance pass = **a documented NO-OP** (the §33 caster-summoner equilibrium HOLDS; run-log in BALANCE.md §41). Flight design LOCKED / build deferred (seams planted §38). ⚠️ `rollEnemyWave`/`buildEnemyTeam`/`enemyBudgetFor` STAY exported (fuzz arena + spawn-overflow).

> **🗄️ Cluster 1 — Combat Depth (Phases Y→34): COMPLETE & user-confirmed.** Full as-built archived → [archive/post-x-roadmap.md](archive/post-x-roadmap.md); per-phase detail in git + [BALANCE.md](BALANCE.md) + the project memory. In one breath: the data-driven `EffectAbility`/`EffectAction` attack/effect keystone (legacy attack classes + the determinism oracle DELETED) · non-stat statuses (burn/bleed/poison/blind/confusion/panic/frozen, periodic + behavior axes) · new ops (status-on-hit / chain / summon + the 9-archetype demo roster) · the attack & archetype-create editors · effect-scaling (`ScaledValue`) · FX·SFX·status-viz · the §33 balance re-derive (a caster-summoner equilibrium; merc+ranged duopoly dissolved) · §34 polish (double-KO soft-lock + §29 ability-detail rows). Carried World → v30 / Run → v23. Load-bearing Cluster-1 facts still in play: every combat verb is one `AbilityDef` (`config/abilities.json`, [src/sim/effects/](src/sim/effects/)); statuses are DEF-RESOLVED by key ([statusEffects.ts](src/sim/statusEffects.ts) — a runtime `StatusEffect` carries only `nextTickAt`/`sourceUnitId`/`magnitude`); the renderer-owned FX registry ([fxRegistry.ts](src/render/fxRegistry.ts), one `FxKey` = visual+SFX, boot-asserted); `applyDamage`/`dealDamage` ([World.ts](src/sim/World.ts)) is the single damage chokepoint (`bypassDefense`/`evadable`); ⚠ the action registry holds NON-verb ids only (a verb's `EffectAction.id` == its `AbilityDef` id — registering a colliding id would shadow the snapshot-decode fallback).

## How we collaborate

The collaboration norms (work on `main`; commit per logical change; surface
tradeoffs before non-obvious calls; claim only what a tool result proves; native
Read/Grep/Glob over shell for file inspection; headless-first for sim/run/core
logic; keep DESIGN.md / ARCHITECTURE.md honest in the same commit), the
load-bearing invariants, and the "where to add things" / "things to avoid"
guidance are the fuller, authoritative copy in **[AGENTS.md](AGENTS.md)**. See
also [TESTING.md](TESTING.md) (testing policy) and [retro/](retro/).

## Things that bit us — DON'T re-litigate

The hard-won fixes that look weird out of context now live in their own file:
**[GOTCHAS.md](GOTCHAS.md)**. Referenced across the docs as "gotcha #N" (numbering
is permanent; retired ones stay as tombstones). Don't "clean up" anything on that
list without understanding why it exists.

## Browser-verify tips (learned the hard way)

- **Preview MCP screenshots are unreliable for sub-pixel detail.** JPEG compression smears 1-2px features (scanlines, dither stipple) into uniform tints; resize timing can produce thumbnail-sized images that look like rendering bugs. **If a screenshot contradicts intuition, sample canvas pixels via `getImageData` first** — they're more reliable than the screenshot tool for detecting real issues. Even better: ask the user to look in their native browser.
- **`preview_click` selector dispatch sometimes lands outside the clickable region** in a narrow preview viewport. When click "succeeds" but no event fires, fall back to `preview_eval` with `element.click()`.
- **A mid-fade DOM read can grab the OLD screen.** `fadeOutAndRemove` keeps a disposed screen's DOM in the document for the ~180ms fade, so a `querySelector` right after a scene swap can return the lingering outgoing screen (it once made a correct Turn-1→Turn-2 transition read as "still Turn 1"). When verifying scene-swap UIs, capture **bus-event payloads** (subscribe → drive → read the array) — they're authoritative; don't trust a mid-fade DOM read.
- **The preview MCP duplicates console output 6×** for reasons not yet understood. Log de-duplication is purely cosmetic; the events are still firing once per emit.
- **Force-verifying defeat/victory paths** when natural runs keep winning/losing the wrong way: temporarily add `(window as unknown as { __bus: typeof this.bus }).__bus = this.bus;` to Game constructor, then `window.__bus.emit('battle:ended', { winner: 'enemy' })` or `window.__bus.emit('run:victory', {})` from eval. Remove the hook before committing.
- **`getImageData` on a WebGL canvas can return cleared buffers** if `preserveDrawingBuffer` is false (Three.js default). Read pixels synchronously inside the same frame the render happened, or temporarily set `preserveDrawingBuffer: true` for debugging.
- **Verifying short-duration sprite fades (D5.C-style) is hard via the preview MCP.** Each `preview_eval` synchronously blocks the JS main thread, stalling `requestAnimationFrame`. When the next frame fires, `dt` is the full eval+sleep time — easily 100-200ms per frame instead of 16ms — and the renderer's animator drains a 500ms fade in 2-3 frames. The fade IS correctly wired (you can prove it by inspecting `br.animator.fades.size` immediately after the spawn fires), it just visually completes faster than you can screenshot. To capture a clean mid-progress visual: trigger the fade, freeze `br.animator.update` to a no-op, then manually call `sprites.updateSprite(handle, { alpha: 0.5 })` to lock the alpha — that survives across screenshots and evals. Bumping `SPAWN.durationSeconds` to a longer value in `config/spawn.json` is the alternative (and works for any short fade) but requires a rebuild.
- **A backgrounded preview tab throttles `requestAnimationFrame` to ~0, so the Clock-driven sim freezes at `tick 0` and `preview_screenshot` times out (the WebGL render loop never goes idle for the capture).** This bit E6's eyeball verification hard. Two workarounds, both via the dev `window.__game` handle (DEV-only, `src/main.ts`): (1) **drive the loop by hand** — `const br = __game.activeScene.battleRenderer; for (let i=0;i<600 && !ended;i++){ br.world.tick(); br.update(1/20); }` advances sim + animations deterministically without rAF (TS `private` fields are runtime-accessible, so `br.world` / `br.animator` / `br.handles` / `br.projectiles` are all reachable). (2) **inspect synchronously** — subscribe to `__game.bus.on('unit:attacked', …)` and read DOM/positions in the handler; the BattleRenderer's handler runs first (subscription order) so the shove/hitsplat/projectile already exists when yours fires. The tab sometimes un-throttles mid-session (becomes active), at which point combat auto-runs to completion on the real loop — don't be surprised if `activeScene` has advanced to PromotionScene between evals; persist probes on `window.__x` so they survive the scene swap. Net: **functional correctness is fully eval-verifiable; subjective feel still needs the user's native browser.**
- **An UNDISPLAYED Browser pane composites nothing — no rAF, the battle clock sits at tick 0, screenshots time out (§82e2, worse than the §79 aspect-NaN case).** The answer that verified a sim+render feature end to end without one screenshot: drive the REAL production loop synchronously — `world.tick()` + `battleRenderer.update(0.05)` in lockstep via `__game.activeScene` — pin the fixture with URL dials (`?roster=catapult:2,…`), and assert on DOM state. (`resize_window` + a `resize` event first if the camera booted with aspect NaN.)
- **The user's tab and your probe tab are different page instances, and yours can't see theirs (§79g).** A days-old tab accumulating Vite HMR patches ran pre-change renderer code against new CSS — a bug no fresh `preview_start` tab could reproduce. When a user-visible symptom contradicts a clean measurement, ask for a hard reload BEFORE hunting the code; a precise symptom fingerprint usually names the superseded code path.
- **Default-seed sampling bias is real (§77e2b): the viz opened on seed 1 — the single worst drifter in a 500-seed corpus.** When an eyeball impression contradicts an instrument, check what the default view happened to show before trusting either.

## Project shape

The canonical annotated source tree is in **[ARCHITECTURE.md](ARCHITECTURE.md)**
("Top-level structure") — one copy, kept current there instead of
hand-mirrored across three docs (which is how the snapshot-version + retired-file
drift crept in). ARCHITECTURE also carries the event + command catalogs.

## Pre-flight, pre-commit & toolchain

Session pre-flight, the pre-commit checklist, and the toolchain versions are in
**[AGENTS.md](AGENTS.md)** (`## Pre-flight` / `## Pre-commit checklist` /
`## Toolchain`). The short version: before every commit run `npm test` (green, 0
todo) + `npm run typecheck` (clean), add `npm run fuzz:smoke` when touching
sim/run/core, and confirm your edits actually landed first.

## Existing memories

The agent memory index (auto-loaded each session) points back here —
`project_asciibattler.md` treats this file's 🧭 Cursor as the authoritative live
status and carries only durable orientation. Update it at major phase
boundaries, not per-step.
